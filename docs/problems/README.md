# Problem Backlog

> Last reviewed: 2026-05-17 **P087 Phase 2c shipped via `/wr-itil:work-problems` AFK iter 5 session 3** — performance-reassessment iter prescribed by ADR-058 §Reassessment Triggers (line 201). `wr-itil-skill-invocations --window-days=30` warm-cache median 7.12s across three runs (Apple M1 Pro, Darwin 25.3.0, Python 3.10.6) against 5155 jsonl / 1.13 GB / 380,898 lines — confirms iter-2's 7.8s observation; ~42% above the 5s reassessment threshold. `wr-itil-plugin-exercise-index` healthy at 0.39s median (well within ADR-058 line 128 ≤1.0s budget); iter-3's 1.07s observation appears transient. Profile breakdown: Python startup + json import 0.10s; `Path.rglob("*.jsonl")` enumeration alone 0.30s; rglob + per-file stat() 0.17s; read every file no-parse 5.91s; read + JSON parse every line 8.80s — **file I/O dominates** (~6s); enumeration negligible; mtime-gate filter takes 8.80s parse-all to 7.12s observed. **Decision**: defer transcript-axis optimization to a new Phase 2d Investigation Task within P087 (sibling-phase pattern 2a → 2b → 2c → 2d, WSJF-anchored to P087's L effort) rather than split to a separate ticket or amend ADR-058 ahead of optimization. Trigger language at ADR-058 line 201 ("open a performance-tuning iter AND re-quantify") makes re-quantification the optimization iter's exit deliverable, not a separate pre-amendment. Optimization candidates surfaced for Phase 2d: binary-search-to-first-in-window-line (JSONL append-only — older lines = older invocations; reduces parse population); larger-window-tail file filter before parse; in-process mtime cache (per ADR-058 line 131 carve-out; disk cache remains prohibited per ADR-053 §Confirmation #1). Investigation-only iter; no script changes; no ADR amendment; no test changes. Ticket effort and WSJF unchanged at L / 3.0; no conditional README-refresh trigger fires per Step 6's contract — README refresh proceeds anyway per the mechanical P165 ticket-edit gate. Prior line-3 fragment (P132 Phase 2b shipped) rotated to `docs/problems/README-history.md` per P134.
> Run `/wr-itil:review-problems` to refresh WSJF rankings.

## WSJF Rankings

Dev-work queue only. Verification Pending (`.verifying.md`, WSJF multiplier 0) and Parked (`.parked.md`, multiplier 0) tickets are excluded per ADR-022 — surfaced in their own sections below. Rows sort by `(WSJF desc, Known-Error-first, Effort-divisor asc, Reported-date asc, ID asc)` so top-to-bottom row order matches `/wr-itil:work-problems` Step 3 tie-break selection 1:1 (P138). The `Reported` column makes the third tie-break input visible — without it, users cannot reconcile the rendered order against the orchestrator's selection.

| WSJF | ID | Title | Severity | Status | Effort | Reported |
|------|-----|-------|----------|--------|--------|----------|
| 6.0 | P162 | Codify dogfood-graduation criteria for held changesets — symmetric risk assessment (release-risk vs delay-risk) drives the reinstate decision, not arbitrary calendar guards | 12 High | Open | M | 2026-05-04 |
| 4.5 | P132 | Agents over-ask in interactive sessions — conflating mechanical-stages with user-interactive-stages (inverse-P078; re-reopened 2026-05-17 after orchestrator main-turn mid-loop AskUserQuestion violated P130 during AFK overnight — Phase 2a 4-surface fix did NOT close the orchestrator-main-turn surface; Phase 2b structural-enforcement hook load-bearing) | 9 Med | Known Error | M | 2026-04-27 |
| 3.0 | P087 | No maturity / battle-hardening signal on plugins, skills, agents, or hooks — READMEs don't tell users which features are stable vs experimental | 12 High | Known Error | L | 2026-04-21 |
| 3.0 | P097 | SKILL.md files mix runtime-necessary steps with maintainer-facing rationale, bloating every skill invocation | 12 High | Known Error | L | 2026-04-22 |
| 3.0 | P182 | `measure-context-budget.sh` flat-glob misses per-state-subdir problem tickets — sibling fix to RFC-002 T4 dual-tolerant `reconcile-readme.sh` | 3 Med | Open | S | 2026-05-11 |
| 3.0 | P161 | Advisory-then-escalate may be over-applied as the default for drift-class detectors generally; load-bearing-from-the-start may be the better default — observation-only meta-question ticket surfaced by P159 | 6 Mod | Open | M | 2026-05-04 |
| 2.25 | P033 | No persistent risk register for ISO 31000 / ISO 27001 compliance — sibling-project survey shows 99% miss rate; re-rated L → XL 2026-04-28 (Phase 1a ADR-047 design landed) | 9 Med | Known Error | XL | 2026-04-17 |
| 2.25 | P015 | TDD enforcement does not flag vague Gherkin outcome steps | 9 Med | Open | L | 2026-04-16 |
| 2.25 | P136 | ADR-044 alignment audit — sweep all unaudited skills/hooks/agents/ADRs/JTBDs/READMEs against the framework-resolution boundary (master ticket) | 9 Med | Open | L | 2026-04-27 |
| 2.0 | P018 | TDD enforce BDD + Example Mapping principles | 16 High | Open | XL | 2026-04-16 |
| 2.0 | P022 | Agents must not fabricate time estimates | 16 High | Open | XL | 2026-04-16 |
| 2.0 | P039 | Autonomous loops conflate diagnose with implement | 16 High | Open | XL | 2026-04-17 |
| 1.875 | P069 | docs/problems/ flat layout is unskimmable — migrate to per-state subdirs + auto-migrate adopter repos (re-rated L → XL 2026-04-20 after auto-migration scope add) | 15 High | Open | XL | 2026-04-20 |
| 1.875 | P091 | Session-wide context budget — Claude Code consumes substantial context before and during every session across all contributor surfaces (meta) | 15 High | Open | XL | 2026-04-22 |
| 1.5 | P080 | No bidirectional update of upstream-reported problems — local lifecycle transitions never propagate back to the reporter (re-rated 6.0 → 1.5 transitive 2026-04-26 — blocked by P079/P038/P064) | 12 High | Open | M | 2026-04-21 |
| 1.5 | P169 | Build recogniser-shape catalogue into @windyroad/risk-scorer plugin (automatic bootstrap + ongoing maintenance) — Phase 1 scorer-side, Phase 2 bootstrap-side | 3 Low | Open | M | 2026-05-04 |
| 1.5 | P171 | drain-register-queue.sh and tests reference obsolete pre-wipe R-file shape | 3 Med | Open | M | 2026-05-05 |
| 1.5 | P172 | Skill contract "interactive vs AFK" commit-gating anti-pattern contradicts ADR-014 | 3 Med | Open | M | 2026-05-05 |
| 1.5 | P173 | BYPASS_*_GATE env vars do not propagate from Bash subshell to PreToolUse hook context | 3 Med | Open | M | 2026-05-06 |
| 1.5 | P174 | Topic-file rotation contract requires `first-written` HTML metadata that doesn't exist on most briefing entries — Step 3 Branch A unenforceable in practice | 3 Med | Open | M | 2026-05-06 |
| 1.5 | P175 | Agent over-narrows scope-pin words ("just", "only", "first") into count constraints — halts AFK loop on agent-inferred scope rather than framework-prescribed stop conditions | 3 Med | Open | M | 2026-05-06 |
| 1.5 | P176 | Agent-side I2 (no type-branching) coverage gap — SKILL.md type-branching invariant not behaviourally testable until skill-invocation harness lands (P012-descendant; surfaced by P170 Slice 4 iter 2) | 6 Med | Open | M | 2026-05-06 |
| 1.5 | P177 | P141 changeset-discipline gate doesn't recognise `docs/changesets-holding/` — forces 2-commit pattern when work belongs to a held window | 3 Med | Open | M | 2026-05-07 |
| 1.5 | P178 | Agent skips ITIL state-machine gates on architecture-driven problems — treats architect-PASS as substitute for empirical RCA + skips Open → Known Error transition | 3 Med | Open | M | 2026-05-10 |
| 1.5 | P179 | Agent defers requested work into untracked phases — phases are fine, but unticketed phases never get implemented | 3 Med | Open | M | 2026-05-10 |
| 1.5 | P180 | Agent defers mitigation selection to user during active incident — surfaces mitigation choice as user-authority when SKILL contract empowers agent-driven reversible mitigations | 3 Med | Open | M | 2026-05-10 |
| 1.5 | P181 | PostToolUse `architect-mark-reviewed.sh` verdict-grep is fragile — `"ISSUES FOUND"` substring anywhere blocks marker even when proposed-change verdict is PASS | 3 Med | Open | M | 2026-05-11 |
| 1.5 | P045 | Auto plugin install after governance release (deferred install on next session start per 2026-04-20 direction) | 6 Med | Open | L | 2026-04-19 |
| 1.5 | P079 | No inbound sync of upstream-reported problems — reports filed via the intake templates never surface in the local backlog (re-rated 6.0 → 1.5 transitive 2026-04-26 — assessment-pipeline scope blocked by P038/P064) | 12 High | Open | L | 2026-04-21 |
| 1.5 | P014 | No lightweight aside invocation for governance skills (master tracker for ADR-032 — capture-* sibling skills + pending-questions hook + supersession cleanup) | 12 High | Open | XL | 2026-04-16 |
| 1.5 | P073 | No voice-and-tone or risk gate on changeset authoring (which populate CHANGELOG.md, Release PRs, GitHub Releases, and npm release notes) | 12 High | Open | XL | 2026-04-20 |
| 1.5 | P082 | No voice-and-tone or content-risk-scoring gate on commit messages (transitive XL via P038 per P076; marginal M) | 12 High | Open | XL | 2026-04-21 |
| 1.5 | P129 | P079 inbound assessment pipeline lacks version-aware classification — already-fixed-in-newer / recurred / still-active branches | 12 High | Open | XL | 2026-04-26 |
| 1.25 | P160 | Ship quota-pacing surface to prevent weekly-quota exhaustion — advisory/blocking nudge when burn rate exceeds sustainable pace, so users retain Claude tokens for non-Claude-Code surfaces (chat, cowork) for the full week | 10 High | Open | XL | 2026-05-03 |
| 1.0 | P170 | Problem tickets strain as fixes decompose into multiple coordinated changes — need RFC framework that ties all changes back to problems and unifies technical with user/business problems (Phase 1 + Phase 2 shipped 2026-05-12; ticket re-opened from 2026-05-12 premature Verifying transition per user direction 2026-05-13 — Phase 3 + Phase 4 in-scope; sibling [[P189]] captures the "deferred without user direction" class-of-behaviour) | 8 Med | Known Error | XL | 2026-05-04 |
| 1.0 | P193 | `risk-gate.bats:163` "Band B with no hash file" test fails non-deterministically at the 5s/5s TTL boundary | 3 Med | Open | S | 2026-05-15 |
| 1.0 | P183 | AFK orchestrator halts on transient API stream-idle-timeout — should classify is_error reason and retry transient classes instead of halting the whole loop (captured 2026-05-11 via /wr-itil:capture-problem; deferred re-rate) | 3 Med | Open | M | 2026-05-11 |
| 1.0 | P184 | Agent treats conditionally-deferred work (deferred-pending-X-graduation) as permanently out of scope — prematurely transitions parent ticket when X graduates (captured 2026-05-12 via /wr-itil:capture-problem after the P170 premature-Verifying transition revert; sibling to P179 defer-discipline at a different decision surface; would have silently lost Phase 2 work if user hadn't asked about story maps; deferred re-rate) | 3 Med | Open | M | 2026-05-12 |
| 1.0 | P187 | AFK orchestrator detects `/wr-itil:review-problems` would unblock its preflight but halts with a "recommended next step" instead of auto-dispatching the unblock (captured 2026-05-12 via /wr-itil:capture-problem; deferred re-rate) | 3 Med | Open | M | 2026-05-12 |
| 1.0 | P188 | AFK orchestrator Step 2.5b AskUserQuestion option-sets assume AFK-only paths even when the user's answering the AskUserQuestion is itself proof of interactivity — should include "do it interactively now" options when user-presence is proven by the surface firing | 3 Med | Open | M | 2026-05-13 |
| 1.0 | P189 | Agent invents "deferred" framing on tracked phases without user-deferral direction — projects fictional out-of-scope onto in-scope work (captured 2026-05-13 via /wr-itil:capture-problem after P170 second-attempt premature-Verifying revert; sibling to P184 transition-surface + P179 untracked-phase-defer; deferred re-rate) | 3 Med | Open | M | 2026-05-13 |
| 1.0 | P190 | Agent designs schemas with user-asked classification fields when the framework should derive silently OR eliminate the classification entirely — deeper generalisation of P185 derive-first-don't-ask (captured 2026-05-13; deferred re-rate; user correction 2026-05-13: JTBD auto-create is in-the-moment collaborative not framework-derived) | 3 Med | Open | M | 2026-05-13 |
| 1.0 | P191 | JTBD edit gate misfires "no JTBD documentation exists" branch on bats fixture edits despite `docs/jtbd/` being present in session CWD | 6 Med | Open | M | 2026-05-15 |
| 1.0 | P192 | Risk-pipeline gate forces repeat rescoring round-trips when the working tree changes between scorer invocation and `git commit` | 6 Med | Open | M | 2026-05-15 |
| 1.0 | P195 | Briefing Tier 3 rotation repeat-deferral — 13 of 14 topic files over ADR-040 budget with 2 in MUST_SPLIT (≥2× ceiling) branch | 6 Med | Open | M | 2026-05-15 |
| 1.0 | P196 | Agent reports RFC-document completion as fix-shipped — premature-completion on multi-slice RFCs | 3 Med | Open | M | 2026-05-15 |
| 1.0 | P194 | ADRs accumulate forward-chronology evidence inline (Phase 2 dogfood evidence, amendment history, cross-iter cross-references) — `decisions` bucket dominates context at 41% / 1.3 MiB | 6 Med | Open | L | 2026-05-15 |
| 0.75 | P012 | Skill testing harness scope undefined | 6 Med | Open | XL | 2026-04-16 |
| 0.75 | P034 | Centralise risk reports for cross-project skill improvement | 6 Med | Open | XL | 2026-04-17 |
| 0.75 | P197 | Agent proposes bypassing documented processes (SKILL contracts, classifier pipelines, changeset discipline) when work feels large/expensive — contract-bypass-reflex class-of-behaviour (sibling of P078/P119/P196; captured via /wr-itil:capture-problem post-correction "DONT skip using the capture-problem skill") | 3 Med | Open | M | 2026-05-15 |
| 0.75 | P198 | external-comms gate marker key cannot be computed by the reviewer agent (Read/Glob/Grep tool surface — no shasum); hash-scope ambiguity adds three+ recurrences (upstream-mirror of inbound #125 from downstream bbstats P205; sibling P163/P166) | 3 Med | Open | M | 2026-05-15 |
| 0.75 | P199 | capture-problem → manage-problem same-session halts at Step 0 reconcile (HALT_ROUTE_RECONCILE on deferred-refresh seam; upstream-mirror of #126 from bbstats P221) | 3 Med | Open | M | 2026-05-15 |
| 0.75 | P200 | wr-voice-tone:agent returns blanket FAIL when docs/VOICE-AND-TONE.md is missing — **safe-high-fix-risk flag** (upstream-mirror of #124 from dry-aged-deps P005) | 3 Med | Open | M | 2026-05-15 |
| 0.75 | P201 | @windyroad/tdd hook only recognises same-dir or __tests__/ test associations (upstream-mirror of #123) | 3 Med | Open | M | 2026-05-15 |
| 0.75 | P202 | wr-risk-scorer:pipeline treats changesets as queued when introducing commits are already on origin (upstream-mirror of #121) | 3 Med | Open | M | 2026-05-15 |
| 0.75 | P203 | architect-enforce-edit + jtbd-enforce-edit hooks should add docs/retros/ to exclusion paths (upstream-mirror of #120) | 3 Med | Open | M | 2026-05-15 |
| 0.75 | P204 | No /wr-retrospective:migrate-briefing skill — legacy docs/BRIEFING.md → docs/briefing/ tree migration is manual (upstream-mirror of #117; **new-jtbd-flag**) | 3 Med | Open | M | 2026-05-15 |
| 0.75 | P205 | wr-risk-scorer:assess-release SKILL.md step 5 prose-vs-parameter mismatch (upstream-mirror of #110) | 3 Med | Open | M | 2026-05-15 |
| 0.75 | P206 | work-problems iter workers don't add changesets — fix commits accumulate without release (upstream-mirror of #98 / bbstats P195) | 3 Med | Open | M | 2026-05-15 |
| 0.75 | P207 | report-upstream SKILL.md Step 5 example uses --label flag that fails when upstream repo has no labels (upstream-mirror of #87) | 3 Med | Open | M | 2026-05-15 |
| 0.75 | P208 | git-push-gate.sh does not check CI status on push/release before scoring risk — **safe-high-fix-risk** (load-bearing gate hardening) (upstream-mirror of #86) | 3 Med | Open | M | 2026-05-15 |
| 0.75 | P209 | manage-problem Step 0 reconcile-readme.sh hits exit 127 on marketplace consumers; script only resolves via repo-relative path (upstream-mirror of #85) | 3 Med | Open | M | 2026-05-15 |
| 0.75 | P210 | work-problems SKILL.md AFK-fallback marker uses em-dash, forces consumer whitespace surgery (upstream-mirror of #84) | 3 Med | Open | M | 2026-05-15 |
| 0.75 | P211 | work-problems orchestrator carries prior-ticket Fix Strategy verbatim into iter dispatch without re-grounding (upstream-mirror of #97 / bbstats P194) | 3 Med | Open | M | 2026-05-15 |
| 0.75 | P212 | work-problems iter boundary leaves run-retro BRIEFING.md edits uncommitted (upstream-mirror of #83) | 3 Med | Open | M | 2026-05-15 |
| 0.75 | P213 | risk-scorer 30-min TTL expires during long orchestrator turns (upstream-mirror of #82) | 3 Med | Open | M | 2026-05-15 |
| 0.75 | P214 | work-problems Step 5 exit-code rule doesn't handle is_error:true transient API failures 529 Overloaded (upstream-mirror of #81) | 3 Med | Open | M | 2026-05-15 |
| 0.75 | P215 | architect-gate drift detection rm's marker without offering recovery path (upstream-mirror of #80) | 3 Med | Open | M | 2026-05-15 |
| 0.75 | P216 | architect-refresh-hash only refreshes on docs/decisions/ writes, leaves cross-session drift on other gated paths (upstream-mirror of #79) | 3 Med | Open | M | 2026-05-15 |
| 0.75 | P217 | architect-mark-reviewed strict-verdict-string parsing under-counts affirmative ISSUES FOUND verdicts as FAIL (upstream-mirror of #78) | 3 Med | Open | M | 2026-05-15 |
| 0.75 | P218 | manage-problem SKILL.md doesn't explain how to obtain SESSION_ID for P119 marker (upstream-mirror of #77; in-session repro this monorepo) | 3 Med | Open | M | 2026-05-15 |
| 0.75 | P219 | manage-problem SKILL.md uses repo-relative script path that fails for plugin-installed users (upstream-mirror of #76; sibling P209) | 3 Med | Open | M | 2026-05-15 |
| 0.75 | P220 | manage-problem has no cadence for checking upstream-bound tickets (upstream-mirror of #63; composes P063+ADR-062) | 3 Med | Open | M | 2026-05-15 |
| 0.75 | P221 | work-problems Step 6.5 lacks baseline CI health check before drain — **safe-high-fix-risk** (load-bearing AFK drain; sibling P208/#86) (upstream-mirror of #62) | 3 Med | Open | M | 2026-05-15 |
| 0.75 | P222 | manage-problem skill should auto-commit ticket file changes — likely resolved by ADR-014 (upstream-mirror of #61) | 3 Med | Open | M | 2026-05-15 |
| 0.75 | P223 | Risk scorer ignores release-risk accumulation across commits (upstream-mirror of #60) | 3 Med | Open | M | 2026-05-15 |
| 0.75 | P224 | Risk-scorer agent does not write numeric score to gate files (upstream-mirror of #59) | 3 Med | Open | M | 2026-05-15 |
| 0.75 | P225 | Docs-only changes should not invoke risk scorer or trigger drift detection — **safe-high-fix-risk** (load-bearing allowlist scope) (upstream-mirror of #58) | 3 Med | Open | M | 2026-05-15 |
| 0.75 | P226 | Review-marker TTL forces repeated re-review cycles on multi-file work (upstream-mirror of #57; sibling P213) | 3 Med | Open | M | 2026-05-15 |
| 0.75 | P227 | Risk scorer credits monitoring/post-release activities as residual-risk reducers (upstream-mirror of #56) | 3 Med | Open | M | 2026-05-15 |
| 0.75 | P228 | ADR-022 .known-error.md → .verifying.md transition not happening consistently at release time (upstream-mirror of #42) | 3 Med | Open | M | 2026-05-15 |
| 0.75 | P229 | Inbound-discovery ack comments are bureaucratic, not verdict-shaped — JTBD-301 violation; 31 comments posted with framework-vocab boilerplate this session | 3 Med | Open | M | 2026-05-15 |

## Verification Queue

Fix released, awaiting user verification (driven off `docs/problems/*.verifying.md` via glob per ADR-022). Sorted by `Released date ASC` (oldest at row 1; same-day releases tiebreak by ID ASC). <!-- VQ-SORT-DIRECTION: oldest-first per ADR-022 --> `Likely verified?` column marks tickets ≥14 days old (P048 Candidate 4 default).

| ID | Title | Released | Likely verified? |
|----|-------|----------|------------------|
| P019 | Deprecate single-file JTBD fallback (ADR-008 Option 3) | @windyroad/jtbd@0.6.0 (commit 6dd6a77) — breaking change | no — not observed |
| P020 | No on-demand assessment skills | v0.3.2 | no — not observed |
| P021 | Governance skill structured prompts | v0.3.2 | no — not observed |
| P035 | manage-problem commit-gate no subagent delegation fallback | pending — fallback path never fired this session | no — not observed |
| P037 | jtbd-reviewer returns bare verdict without reason | commit 6e7c2e4 | no — not observed |
| P044 | run-retro does not recommend new skills when it should | @windyroad/retrospective@0.1.6 (commit 6510b29) | no — not observed |
| P046 | wr-architect agent misses runtime-path performance implications | commit b2f1646 | no — not observed |
| P049 | Verification Pending `.verifying.md` status — SKILL.md contract + migration | @windyroad/itil@0.6.0 (5b9aa96) | no — not observed |
| P050 | run-retro generalises codification branch from skill-only to 12 shapes | @windyroad/retrospective@0.2.0 (b401c7b) | no — not observed |
| P051 | run-retro extended with improvement axis (12 create + 6 improve shapes) | @windyroad/retrospective@0.3.0 (4a107a3) | no — not observed |
| P061 | install-updates Step 6 grouping fallback for siblings > 3 | commit b6ba3bd (AFK iter 6 iter 6 — repo-local skill) | no — not observed |
| P066 | Intake templates problem-first (bug-report + feature-request replaced by problem-report) | commit ed36f69 (AFK iter 6 iter 1) | no — not observed |
| P068 | run-retro Verification-close housekeeping (Step 4a) | @windyroad/retrospective@0.4.0 (commit c268327) | no — not observed |
| P072 | plugin-user persona + JTBD-301 ship; closes external-reporter JTBD gap | commit pending (post-AFK interactive) | no — not observed |
| P076 | WSJF scoring models transitive dependencies (methodology gap — rule now inline in manage-problem SKILL.md + Step 2.5 traversal in review-problems) | commit pending (this AFK iter 7 iter 2) | no — not observed |
| P083 | work-problems Step 5 iteration-worker prompt forbids `ScheduleWakeup` / time-deferring primitives — synchronous-handoff contract | `@windyroad/itil@0.13.0` commit `260768f` (forbidding clause rode the P084 subprocess-dispatch rewrite; bats regression assertions added this iter — retroactive transition) | no — not observed |
| P024 | Risk-scorer WIP flag uncommitted completed work | 2026-04-17 | no — not observed |
| P048 | manage-problem Verification Queue detection: fast-path 9d + `Likely verified?` column | 2026-04-19 (pending commit) | no — not observed |
| P071 | Argument-based skill subcommands are not discoverable in Claude Code autocomplete | 2026-04-21 through 2026-04-22 (6 phased-landing slices in commits `412443f` / `d8ab4c5` / `ffa85a7` / `91da109` / `38756a8` / `248edad` / `4a25a60` across @windyroad/itil@0.10.0–0.17.0; 9 new skills + 9 thin-router forwarders + `deprecated-arguments: true` frontmatter on both host skills + ADR-010 P071 amendment; each new skill carries contract bats and each forwarder carries a forwarder-bats test) | no — not observed |
| P093 | transition-problem ↔ manage-problem circular delegation for `<NNN> <status>` args | 2026-04-22 (AFK iter 2) | no — not observed |
| P098 | Project-owned and user-owned context contributors — global `~/CLAUDE.md`, local `.claude/skills/`, and memory index | 2026-04-22 (progressive-disclosure project CLAUDE.md + install-updates SKILL+REFERENCE split + stale memory pruning; ~/CLAUDE.md follow-up for user; amendment trimmed redundant CLAUDE.md pointers 2026-04-22) | no — not observed |
| P102 | No invocation surface creates risk register entries — `docs/risks/` stays empty after P033 scaffolding | 2026-04-22 (fix landed — passive trigger partial; P110 tracks remaining gap) | no — not observed |
| P104 | `/wr-itil:work-problems` partial-progress iteration outcome can paint the release queue into a mid-state corner — slice-1-without-slice-2 hazard | 2026-04-22 (ADR-041 landing — Rule 7 blesses docs/changesets-holding/) | no — not observed |
| P105 | `/wr-retrospective:run-retro` needs a signal-vs-noise pass on briefing entries to drive session-start curation | 2026-04-22 (`@windyroad/retrospective` — Step 1.5 + ADR-040 curation-feedback amendment + bats contract assertion) | no — not observed |
| P116 | `push:watch` preflight does not count local-only commits before batch-push — CI regressions in intermediate commits stay invisible | 2026-04-24 (AFK iter; warn-and-proceed preflight extracted to `scripts/push-watch.sh`; 18/18 push-watch bats green) | no — not observed |
| P070 | `/wr-itil:report-upstream` does not check for existing upstream issues before filing | 2026-04-25 (AFK iter; `@windyroad/itil` patch — Step 4b dedup check (own re-run grep + third-party `gh issue list --search` + inline LLM semantic match) + Step 5c comment path (`gh issue comment` with cross-reference body) + new `commented-on-existing-issue` disclosure-path enumeration in Step 7 back-write + AFK behaviour summary table dedup-halt row + ADR-024 amendment (Decision Outcome adds Step 4b + 5c; Out of scope dedup narrowed to residual `update-mode`; Confirmation criterion extended); maintainer-annoyance risk evaluator DEFERRED to compose with `wr-risk-scorer:external-comms` per ADR-028 line 117 (architect verdict — keeps P070 at Effort M); AFK auto-comment branch on interim static heuristic until P064 / ADR-028 evaluator lands; 9 new behavioural bats; file 24/24 green) | no — not observed |
| P078 | Assistant does not offer problem ticket on strong-signal user correction | 2026-04-25 (AFK iter; `@windyroad/itil` patch — new `itil-correction-detect.sh` UserPromptSubmit hook + `CORRECTION_SIGNAL_PATTERNS` extension to `lib/detectors.sh` registry + new user-memory + CLAUDE.md pointer; 14 new behavioural bats; full itil hook suite 52/52) | no — not observed |
| P090 | risk-scorer commit-gate bypass-marker TTL expires mid-session forcing manual rescore round-trip | 2026-04-25 (AFK iter; `@windyroad/risk-scorer` patch — three-band TTL in `check_risk_gate`: Band A pass / Band B hash-invariant slide w/ 2×TTL hard-cap via `<action>-born` siblings / Band C halt; push-gate refactored to inherit band logic via `check_risk_gate "push"`; ADR-009 footnote appended; 9 new behavioural bats; full suite 846/846 green) | no — not observed |
| P110 | Risk register has no passive trigger — `/wr-risk-scorer:create-risk` alone partially satisfies JTBD-001 | 2026-04-25 (pending release — fix landed alongside P102 but tracked here as the JTBD-001 passive-trigger gap; reconciled into VQ via P118 self-bootstrap demonstration) | no — not observed |
| P111 | Agent/Task subprocess tool calls do not refresh parent session's gate markers — TTL inflation is a symptom-treatment, not the fix | 2026-04-25 (AFK iter 4; multi-package patch — `@windyroad/architect`, `@windyroad/jtbd`, `@windyroad/style-guide`, `@windyroad/voice-tone`, `@windyroad/risk-scorer` — Option 1 subprocess-completion refresh per architect verdict; new shared `slide_marker_on_subprocess_return` helper in 5 byte-identical `gate-helpers.sh` copies + 5 new `*-slide-marker.sh` PostToolUse:Agent\|Bash hooks; risk-scorer slides only `${RDIR}/{commit,push,release}` (born markers deliberately not slid — preserves P090 2×TTL hard-cap invariant); ADR-009 amended with "Subprocess-boundary refresh (P111, 2026-04-25)" subsection citing ADR-032 line 123 cross-process isolation invariant; 36 new behavioural bats; full suite green) | no — not observed |
| P088 | run-retro cannot see the full session context when invoked as a subagent, subprocess, or from any non-parent-session surface — P086's fix is partially blind | 2026-04-26 (AFK iter; `@windyroad/retrospective` patch — settles user-direction-scoped (a)-(c): ADR-032 amendment defers `/wr-retrospective:capture-retro` sibling at both enumeration sites (lines 26, 80) + Related-list cross-ref to P088, citing the context-marshalling problem (background subagents have isolated context at spawn; retro's input is whole-session tool-call history not a self-contained aside payload, distinct from `capture-problem` and `capture-adr` which retain self-contained payload shape); `packages/retrospective/skills/run-retro/SKILL.md` gains "When to use" preamble enumerating supported invocation surfaces (foreground `/wr-retrospective:run-retro` + `claude -p` subprocess per P086) plus explicit "Never invoke as a background agent" anti-pattern clause forbidding `Agent(run_in_background: true)` invocation, placed before Step 1 so the agent encounters it before committing to an invocation shape; P086 ticket gains settlement note clarifying retro-inside-`claude -p`-subprocess is correct and distinct from the deferred background-agent surface (no P086 status change — it stays Closed); new behavioural-contract bats fixture `packages/retrospective/skills/run-retro/test/run-retro-anti-pattern-clause.bats` (six structural assertions on clause presence + P088 driver citation + supported-surface enumeration + deferred-surface explicit naming + preamble placement + ADR-032 cross-reference); architect verdict: structural-with-fallback-note per ADR-037 permitted exception + minimal ADR-032 amendment scope (P081 follow-up tracks behavioural-test infrastructure that would replace structural assertions once a synthetic-subagent-surface mock harness exists; retro-context-layer taxonomy ADR deliberately deferred — landing taxonomy prose without that ADR pre-empts a design decision the ticket's Investigation Tasks explicitly leave open); JTBD-001 preserved (deferring `capture-retro` is the JTBD-001-preserving move since shipping context-blind capture would violate "without slowing down" outcome) + JTBD-006 preserved (P086's per-iteration retro-inside-`claude -p` path explicitly clarified) + JTBD-101 preserved (P086 vs P088 surface distinction reduces re-attempt risk); item (d) (session-log parser to reach into `~/.claude/projects/*/*.jsonl`) OUT OF SCOPE per ticket "potentially" hedge — deferred under the same context-marshalling-problem framing the deferral itself names; full retrospective run-retro suite 85/85 green including the 6 new anti-pattern-clause assertions) | no — not observed |
| P099 | `docs/BRIEFING.md` grows unbounded via run-retro appends — violates progressive disclosure | 2026-04-26 (AFK iter; `@windyroad/retrospective` minor — new `packages/retrospective/scripts/check-briefing-budgets.sh` advisory diagnostic + 14 behavioural bats covering threshold / boundary / README-excluded / env-var-override / sort-stability / missing-dir-exit-2; `run-retro` SKILL.md Step 3 gains Tier 3 budget rotation pass as final action with `AskUserQuestion` four-option interactive path (split-by-subtopic / split-by-date / trim-noise / defer per ADR-013 Rule 1) + AFK fallback to retro summary's new Topic File Rotation Candidates table per ADR-013 Rule 6; Step 5 summary gains the matching table; ADR-040 amended Tier 3 informational → advisory enforcement with new Reassessment trigger ≥3 files exceed 2× ceiling for ≥2 cycles → revisit / promote-to-fail-closed; reusable-pattern note (JTBD-101) names the advisory-script + bats + ADR-tier-budget-amendment triplet for future accumulator surfaces; smoke-test confirms 6/6 current topic files surface as overflow candidates; full retrospective suite 93/93 green; architect amendment to ADR-040 confirmed not sibling; per-plugin scripts/ placement mirrors itil reconcile-readme.sh) | no — not observed |
| P117 | No batch-transition surface for multiple problem tickets in a single run-retro / review cycle | 2026-04-26 (AFK iter; `@windyroad/itil` minor — new plural sibling skill `/wr-itil:transition-problems` accepting space-separated `<NNN> <status>` pairs (no `P` prefix, no `=`/`:` separator, no flag-style); inline per-ticket mechanic per ADR-010 amended "copy, not move" — pre-flight + P063 detection + `git mv` + Edit + P057 re-stage per pair, NO Skill-tool re-invocation back to singular (would reintroduce the N×SKILL.md-reload cost the ticket targets); ONE README refresh at end (P062 at batch grain) + ONE commit at end per ADR-014 batch-grain unit-of-work; partial-failure semantics = skip-and-surface (succeeded pairs commit at end; failed pairs surfaced in summary; zero-success means no commit + failure summary); 20 behavioural contract assertions in `packages/itil/skills/transition-problems/test/transition-problems-contract.bats` covering frontmatter / allowed-tools / citations (P117 + ADR-010 amended + P057 + P062 + P063 + ADR-022 + ADR-013 Rule 6 + ADR-037) / inline-mechanic positives / no-Skill-tool-delegation negative / single-commit-at-end positive + no-per-pair negative / single-README-refresh-at-end / partial-failure skip-and-surface / argument-shape positive + flag-style negative / no `deprecated-arguments` flag / cross-file drift-detection asserting the staging-trap `git add docs/problems/` phrase appears in BOTH this skill's SKILL.md and the singular's SKILL.md; full singular + work-problems sibling test suites re-run green confirming no drift introduced; architect verdict PASS — clean application of multiple existing decisions at a new batch grain (no new ADR); JTBD-001 primary (eliminates N×SKILL.md reload tax + ownership-boundary violation at batch closures); JTBD-006 composes (work-problems may delegate batch closures here during AFK orchestration); JTBD-101 (singular/plural split mirrors the established work-problem/work-problems precedent); AFK iter, single commit per ADR-014) | no — not observed |
| P125 | P057 staging-trap recurs despite documentation — hook-level enforcement candidate | 2026-04-26 (AFK iter; `@windyroad/itil` patch — new `packages/itil/hooks/p057-staging-trap-detect.sh` PreToolUse:Bash hook matching `git commit` invocations; new `packages/itil/hooks/lib/staging-detect.sh::detect_p057_trap` shared helper running `git diff --staged --name-status` + `git diff --name-only` and detecting staged-rename + working-tree-modify shape; fail-open on non-git tree / parse error per `lib/create-gate.sh` precedent (P119); 10 behavioural bats per ADR-005 + P081 covering trap detection / re-stage allow / pure rename allow / modify-only allow / empty-batch allow / non-Bash bypass / non-commit Bash bypass / parse-incomplete fail-open / deny-message contract / deny-message <400-byte budget; deny message ~348 bytes well under ADR-038 progressive-disclosure cap; architect APPROVED-WITH-AMENDMENTS verdict applied — ADR-005 cited in bats header / fail-open contract / cost-discoverability docstring; JTBD-001 primary fit, JTBD-006 composes; voice-tone draft suggestion applied) | no — not observed |
| P154 | P137 namespace-prefix detector must run against npm pack output not source tree — source-tree advisory misses publish-manifest drift class | pending `@windyroad/retrospective` minor (P154 commit, release deferred to AFK orchestrator Step 6.5 drain) — ships `check-tarball-shipped-shims.sh` advisory + `wr-retrospective-check-tarball-shipped-shims` bin shim per ADR-049 + 15-test ADR-052 behavioural bats fixture + dogfood-fix `scripts/` in `packages/retrospective/package.json#files`; pre-fix detector reported 5 broken shims under `@windyroad/retrospective`; post-fix silent-on-pass | no — not observed |
| P159 | JTBD currency detector should be a load-bearing commit-hook with auto-fix, not a retro-time advisory; section structure should integrate organically not bolt on a `## Jobs to be Done` block | 2026-05-04 (this commit; pending `@windyroad/retrospective` minor — ships PreToolUse:Bash commit-hook `retrospective-readme-jtbd-currency.sh` denying `git commit` on `drift_instances > 0`; ADR-051 amended in-place with load-bearing-from-the-start direction + Recommended Section Structure rewrite rejecting bolt-on `## Jobs to be Done` as anti-pattern + prose-weaving target guidance + persona-primacy preservation; 19 hook bats green; JTBD-302 + JTBD-007 outcome refresh; 2 tactical bootstrap drift fixes (architect/itil READMEs); Phase 2-3 deferred (12-README prose-weaving refresh + auto-fix grant-Edit decision); user verifies on next `git commit` invocation in this monorepo: tactical drift → deny with redirect; restored README → commit allowed. Recovery path if rollback needed: `BYPASS_JTBD_CURRENCY=1 git commit ...` for one-time bypass, or `/wr-itil:transition-problem 159 known-error` for full rollback) | no — not observed |
| P065 | No skill scaffolds intake files in downstream projects (re-rated M → L per architect direction) | 2026-04-27 (`@windyroad/itil@0.21.0`; commit `8653541` fix → released by AFK iter 1 drain; new `/wr-itil:scaffold-intake` skill + pre-publish PreToolUse gate `pre-publish-intake-gate.sh` with `INTAKE_BYPASS=1` override + first-run trigger wiring in manage-problem and work-problems with marker-suppression at `.claude/.intake-scaffold-{done,declined}` per ADR-009 + ADR-036 shipped + bats coverage per ADR-036 Confirmation; P127 verifies together as the post-release CI-vs-local idempotency-fixture gap) | no — not observed |
| P101 | `wr-retrospective` has no context-usage analysis — opaque where session tokens are consumed; no guidance on what to trim | 2026-04-27 (`@windyroad/retrospective@0.10.0`; commit `75238fb` fix → release commit `4387824`, merge `12c24d8`; two-layer context-usage analyzer per ADR-043; cheap layer integrated into `run-retro` Step 2c; deep layer new `/wr-retrospective:analyze-context` skill for on-demand analysis; per-surface breakdown + top-N offenders + actionable suggestions; ADR-026 + ADR-014 amended) | no — not observed |
| P126 | `/wr-itil:work-problems` failure-handling halt paths bypass Step 2.5's interactive-default routing for accumulated user-answerable design questions | 2026-04-27 (`@windyroad/itil@0.21.1`; commit `6c46694` fix → release commit `4387824`, merge `12c24d8`; Step 2.5b reusable surfacing routine extracted from Step 2.5; cross-references added to Step 0 / Step 6.5 / Step 6.75 halt paths; architect FLAG guard under Rule 5 separating prior-iter accumulated skips from halt-causing scorer-gap; behavioural second-source `work-problems-step-2-5b-cross-halt-routing.bats` 15/15 green; full work-problems suite 136/136 green) | no — not observed |
| P127 | scaffold-intake idempotency bats fixture (test 645) fails in CI but passes locally — local-vs-CI test divergence on the same commit | 2026-04-27 (`@windyroad/itil@0.21.1`; commit `482b54a` fix → release commit `4387824`, merge `12c24d8`; snapshot dir moved outside `$TEST_DIR` so the snapshot itself isn't included in the second-pass diff; test 645 idempotency assertion now stable across local + CI environments) | no — not observed |
| P085 | Assistant asks for input when next step is obvious, AND uses prose asks instead of AskUserQuestion when input is actually needed | 2026-04-28 (AFK iter; pending `@windyroad/itil` patch — `PROSE_ASK_PATTERNS` extended in `packages/itil/hooks/lib/detectors.sh` with `Awaiting your (direction\|input\|decision\|response\|confirmation\|answer\|reply)`, `Pending your (direction\|input\|decision\|response\|confirmation\|answer\|reply)`, `Once you confirm`, and the more-specific `Awaiting your direction on whether` (kept alongside broader pattern for first-match observability); empirical pre-fix verification: piping the Citation 1 verbatim text through `detect_prose_ask` returned exit-code 1 (no match), post-fix returns exit-code 0 with matched phrase reported on stdout; 5 new behavioural bats per ADR-037 + P081 in `packages/itil/hooks/test/itil-assistant-output-review.bats` covering Citation 1 verbatim shape + four adjacent phrasings — 17/17 review-hook tests green and full 85-test itil hooks suite green; Citation 2 (over-ask when framework prescribes the answer) deferred to P132 — class-of-behaviour overlap, framework-knowability detection requires reading SKILL.md decision tables which is substantially harder than phrasing-list extension; architect verdict ALIGNED / PASS no new ADR + ADR-013 Rule 1 enforcement strengthened + ADR-044 R6 covers broader over-ask family without amendment; JTBD verdict ALIGNED / PASS JTBD-001 + JTBD-006 axes preserved; user verifies on next session where any agent emits "Awaiting your direction" / "Pending your decision" / "Once you confirm" prose-ask — Stop hook should now emit `stopReason` nudge) | no — not observed |
| P096 | PreToolUse / PostToolUse hook injection volume across windyroad plugins — Phase 3 ADR landed | 2026-04-28 (AFK iter; ADR-045 "Hook injection budget policy for PreToolUse and PostToolUse hooks" landed as sibling to ADR-038; codifies five reusable patterns from P096 Phase 2: silent-on-pass default, side-effect-only silent, silent-on-unchanged-state, hash-dedupe of repeated body content, once-per-session gating reuse; per-band byte budget table codifies Phase 2 measurements as policy; bats coverage already in place from Phase 2 — `tdd-post-write-phase2.bats`, `plan-risk-guidance-once-per-session.bats`, `session-marker.bats`, `sync-session-marker.bats`; architect verdict PASS sibling-not-extension confirmed per ADR-040 + ADR-043 precedent; JTBD verdict PASS JTBD-001/002/003/006/101 cited; release pending Step 6.5 drain) | no — not observed |
| P123 | Blocked-user list mechanism for inbound report management — refuse future tickets from clearly-malicious reporters | 2026-04-28 (AFK iter; pending `@windyroad/itil` patch — ADR-046's v1 audit-log-only slice landed: `packages/itil/hooks/lib/block-list.sh` with `is_blocked` / `add_block` / `remove_block` / `list_blocks` (caller-supplied SHA-256 hex hashes; helper validates hex shape, does not compute hashes — keeping helper GitHub-agnostic per ADR-046 §Reassessment); `docs/blocked-reporters.json` per-repo persistent block list (empty array on creation); `docs/blocked-reporters.audit.jsonl` sibling append-only audit log with five-field shape per ADR-046 Q2 (`type, reporter_id_hash, evidence_ticket, timestamp, author`); 10 behavioural bats per ADR-005 + ADR-037 + P081 in `packages/itil/hooks/test/block-list.bats` covering round-trip, idempotent add, remove path, audit-log presence (block + unblock), `list_blocks` shape, and hex-shape validation rejections (non-hex + wrong-length) — 10/10 green and full 95-test itil hooks suite green (no regression); ADR-046 transitioned `proposed → accepted` in same commit per ADR-022 fold-fix convention with Q1 (flat list) + Q2 (five-field shape) + Q3 (agent-monitored review-cycle direction) all marked Adopted (Q3 implementation specifics deferred to a future iter — direction is settled, monitor mechanism + surface format + response-handling implementation rides a sibling ticket); P079 inbound-discovery filter integration and `/wr-itil:report-upstream` outbound pre-check explicitly out of scope per P123 line 78 — those land when P079 + P070 close; architect verdict ALIGNED-with-notes / PASS no new ADR — ADR-046 governs, helper-doesn't-hash separation locked in, JSONL audit-log shape obvious local choice; JTBD verdict ALIGNED / PASS — JTBD-101 primary (foundation-only slice fits encode-once discipline) + JTBD-001 + JTBD-202 compose, no regression vs zero-defence today; user verifies by sourcing `block-list.sh` and exercising round-trip → `docs/blocked-reporters.json` + `docs/blocked-reporters.audit.jsonl` updated as expected) | no — not observed |
| P124 | `/wr-itil:manage-problem` Step 2 substep 7 session-id discovery — Phase 3 mtime-selection fix shipped (within-system selection switched from first-glob-match alphabetical to most-recent-mtime via `ls -t \| head -1`) | 2026-04-28 (AFK iter; pending `@windyroad/itil` patch — within-system selection switched from first-glob-match alphabetical to most-recent-mtime; outer system priority loop preserved; Phase 2 portability fix preserved; `-announced-` markers per ADR-038 are write-once-per-session so mtime IS the announcing session's first-prompt timestamp — newest mtime within a system glob unambiguously identifies the live session; closes Phase 2 regression where 103 stale `architect-announced-*` markers caused the alphabetical-first stale UUID to win and the create-gate hook P119 to deny ticket Writes; new bats `session-id.bats` test 8 asserts within-system mtime selection — 8/8 green; architect verdict PASS-WITH-NOTES per ADR-038 line 65 write-once contract justifying mtime; JTBD-001 primary fit / JTBD-006 composes; user verifies by next fresh session's first manage-problem invocation completing Step 2 substep 7's marker write without brute-force recovery) | no — not observed |
| P131 | Agents write project-generated artefacts under `.claude/` (user-controlled config space) — gate exclusions are read tolerance, not write permission | 2026-04-28 (AFK iter; pending `@windyroad/itil` minor — Phase 2 load-bearing enforcement hook shipped: `packages/itil/hooks/itil-claude-space-protection.sh` PreToolUse:Write|Edit hook denies agent writes to project-scoped `.claude/` paths NOT in user-space allow-list (`settings.json`, `settings.local.json`, `MEMORY.md`, `.install-updates-consent`, `scheduled_tasks.lock`, `*.local.json` root-only, plus `skills/`, `commands/`, `agents/`, `hooks/`, `projects/`, `worktrees/` subtrees, plus `.agent-write-approved-*` markers themselves) unless approval marker `.claude/.agent-write-approved-<sha256-of-rel-path>` is present; new shared helper `packages/itil/hooks/lib/claude-space-gate.sh` exporting `is_protected_claude_path` / `has_approval_marker` / `claude_space_deny` — semantically distinct from session-scoped `review-gate.sh` (TTL+drift) and `create-gate.sh` (per-session grep marker), this is the new persistent path-keyed marker class precedent-shaped on `.claude/.install-updates-consent` (ADR-030 / P120); deny message <500 bytes per ADR-038 (~440 bytes), names P131 + suggests `docs/plans/` / `docs/audits/` / inline ticket alternatives + names approval-marker bypass + references project CLAUDE.md MANDATORY rule; silent-on-pass per ADR-045 Pattern 1 (zero bytes on allow path); fail-open on parse failure per ADR-013 Rule 6; 34/34 behavioural bats per ADR-037 + P081 in `packages/itil/hooks/test/itil-claude-space-protection.bats` covering deny path / user-space allow-list / outside-.claude paths / approval-marker bypass / Read|Glob|Bash unaffected / allow-list anchor depth (deeper-than-root `*.local.json` denied) / deny-message contract / byte budget / silent-on-pass; full 129-test itil hooks suite green (no regression); architect verdict PASS-WITH-NOTES (allow-list amended per architect note to add `MEMORY.md`, `commands/`, `agents/`, `hooks/` subtrees + anchor `*.local.json` to root depth; defer formalising-ADR to Phase 3 per ticket Fix Strategy line 113); JTBD verdict ALIGNED PASS (JTBD-001 primary, JTBD-006 strong fit on originating P131 incident, JTBD-101 no conflict, JTBD-202 indirect); style-guide PASS out-of-scope; voice-tone PASS advisory-only (`docs/VOICE-AND-TONE.md` not yet authored); user verifies on next session by attempting Write to `.claude/plans/foo.md` → expect canonical `BLOCKED ... (P131)` deny; bypass verifies by creating `.claude/.agent-write-approved-<hash>` marker → expect Write to succeed) | no — not observed |
| P133 | zsh-portability gap in shell-snippet examples — Phase 1 immediate fix at `/install-updates` SKILL.md L167 (bash-array iteration) + defensive rename in reconcile-readme.sh | 2026-04-28 (AFK iter; `@windyroad/itil` patch pending — install-updates SKILL.md L167 array form + reconcile-readme.sh `status` → `ticket_status` rename + new bats P133 regression case; Phase 2 repo-wide audit + Phase 3 lint deferred to compose with P136) | no — not observed |
| P135 | Decision-delegation contract — agents over-apply ADR-013 Rule 1 to framework-resolved decisions; ADR-044 + 5-phase implementation plan (master ticket) | 2026-04-28 (AFK iter; phases shipped earlier 2026-04-27 across 5 commits — `e703656` Phase 1 Anchor (ADR-044 + ADR-013 Rule 1 amendment + master ticket), `5d414fc` Phase 5 Measurement (`run-retro` Step 2d "Ask Hygiene Pass" + `packages/retrospective/scripts/check-ask-hygiene.sh` + `run-retro-step-2d-r6-auto-flag.bats` + `check-ask-hygiene.bats`), `fae42aa` Phase 2 Skill amendments (run-retro Step 3 silent removals / Tier 3 silent rotation / Step 4a close-on-evidence / Step 4b Stage 2 silent fix-shape; work-problems Step 5 NEVER-AskUserQuestion-mid-loop + Step 2.5 batch-as-default; manage-problem Step 9d evidence-grounded close + recovery path; transition-problem Step 5 P063 silent-default), `328f92a` Phase 3 AFK loop redesign (ITERATION_SUMMARY `outstanding_questions` schema + deviation-candidate shape + `.afk-run-state/outstanding-questions.jsonl` between-iter aggregation + mid-loop UserPromptSubmit handler does NOT abort iter + `work-problems-mid-loop-userpromptsubmit-handler.bats` + `work-problems-deviation-candidate-shape.bats`), `258ac25` R6 auto-flag automation (Step 2d substep 8 invokes `check-ask-hygiene.sh` and auto-queues a deviation-candidate when R6 numeric gate fires); released as `@windyroad/retrospective` minor (Phase 5 + Phase 2 retro surfaces + R6 auto-flag) and `@windyroad/itil` patch (Phase 2 itil surfaces + Phase 3 AFK loop redesign); 2026-04-28 AFK iter audit confirmed all 9 shipped surfaces in code + 6 named bats files present + 5 cited commits exist + 2 ask-hygiene trail files (lazy=3 baseline 2026-04-27 includes pre-Phase-2 calls; lazy=0 2026-04-28 AFK iter — denominator-1 caveat per trail-file note); Phase 4 enforcement hook deferred and R6-gated (lazy AskUserQuestion count ≥2 across 3 consecutive retros — gate has NOT fired) per ADR-040 Tier 3 declarative-first precedent; user verifies on lazy-count metric trend across consecutive retros via `docs/retros/<YYYY-MM-DD>-ask-hygiene.md` trail — declarative-only path closes P135 if metric trends to 0 across 3+ retros; R6-fire path ships Phase 4 hook and verifies on that release) | no — not observed |
| P138 | `docs/problems/README.md` WSJF Rankings table row order doesn't match `/wr-itil:work-problems`'s tie-break selection — users assume the orchestrator is broken | 2026-04-28 (AFK iter; pending @windyroad/itil patch — fold-fix commit transitions Open → Verifying per ADR-022; multi-key sort `(WSJF desc, Known-Error-first, Effort-divisor asc, Reported-date asc, ID asc)` + Reported column documented at all three render-block sites in manage-problem SKILL.md + review-problems SKILL.md + work-problems SKILL.md Step 1; stable greppable marker `<!-- TIE-BREAK-LADDER-SOURCE: /wr-itil:work-problems SKILL.md Step 3 -->` at each render site; new bats `manage-problem-readme-tie-break-order.bats` 13/13 green covering marker + sort spec + Reported column + drift-warning + behavioural fixture sort with 4 same-WSJF tickets matching tie-break ladder result; this README is the FIRST rendering with the new sort applied — WSJF 6.0 tier now shows P123 → P135 → P082 instead of the prior P135 → P123 → P082 misorder; user verifies by reading the rankings + running the bats + confirming next AFK iter's `work-problems` Step 1 fast-path top-row matches Step 3 selection) | no — not observed |
| P139 | Repo-local skills under `.claude/skills/` pay architect-gate overhead on every edit — relocated source-of-truth outside `.claude/` and symlink | 2026-04-28 (AFK iter 7; source-of-truth `git mv`'d from `.claude/skills/install-updates/` to `scripts/repo-local-skills/install-updates/` per architect Option B verdict — `packages/` semantic preserved for publishable plugins (ADR-002/ADR-003); `.claude/skills/install-updates/{SKILL.md,REFERENCE.md,test/}` now relative symlinks pointing into the source-of-truth so `/install-updates` autocomplete + invocation continue unchanged; ADR-030 amended in same commit — Decision Outcome contract point 1 rewritten with source-of-truth + resolution-layer split, new "Symlink contract" section (relative-only / per-file for SKILL+REFERENCE / directory-level allowed for test/ / Windows degradation documented), Confirmation gains four assertions, Reassessment Criteria gains "symlink resolution breaks" trigger; gate-exclusion audit confirmed no `scripts/` carve-out in architect or JTBD hooks (assertion baked into bats); 11 new behavioural bats `install-updates-symlink-contract.bats` 11/11 green covering source-files-are-regular / symlinks-are-relative / symlinks-resolve-to-source-content / gate-exclusion-audit; full install-updates test suite 47 — 46/47 green (1 pre-existing P120 doc-lint failure on stale "skip Step 6" pattern, unrelated, out of scope); architect verdict APPROVED with amendments folded in (location: `scripts/repo-local-skills/`); JTBD verdict APPROVED — JTBD-006 primary fit (AFK loop edits to repo-local skills no longer pay per-edit gate overhead) + JTBD-001 + JTBD-101 compose; user verifies on next interactive session by running `/install-updates` and confirming the skill loads + by editing `scripts/repo-local-skills/install-updates/SKILL.md` once and observing the architect/JTBD gate fires through normal review (not blocked-on-`.claude/`-path)) | no — not observed |
| P140 | `/wr-itil:work-problems` Step 6.5 halt-on-CI-failure direction should be fix-and-continue when failure is mechanically fixable (P081-class stale assertions) | 2026-04-28 (AFK iter; pending `@windyroad/itil` patch — Phase 1 declarative SKILL.md amendment: `packages/itil/skills/work-problems/SKILL.md` Step 6.5 "Failure handling" subsection rewritten with diagnose-then-classify routing — `gh run view --log-failed` diagnostic preamble per ADR-026 grounding + closed fixable-in-iter allow-list (P081-class stale-grep-string / hook stub mismatch / test ID drift / environmental flake) + ambiguous-defaults-to-halt guard + fix-and-continue branch (each retry through standard ADR-014 commit gate flow — architect / JTBD / risk-scorer per retry — per ADR-042 Rule 3 commit-gate-per-retry precedent) + 3-retry cap per iteration (not per failure-class) + halt branch preserved for genuinely-unrecoverable (auth failure / npm publish rejection / semantic test requiring user judgment / repeated transient / anything outside the closed allow-list) + Step 2.5b cross-reference (P126) preserved on halt path; Decision Making table gains "CI failure during Step 6.5 drain (within-appetite branch)" row routing fix-and-continue + 3-retry cap; Mid-loop ask discipline (P130) Step 6.5 CI-failure halt-point bullet narrowed to outside-allow-list / cap-reached scope (failures inside the allow-list route to fix-and-continue, not this halt point); new behavioural-contract bats `work-problems-step-6-5-fix-and-continue.bats` 28/28 green per ADR-037 + P081 covering Failure handling self-identification + diagnostic preamble citation + ADR-026 grounding cross-reference + each fixable class named + closed-list framing + ADR-044 deviation-candidate framing + ambiguous-defaults-to-halt + fix-and-continue branch identity + per-retry ADR-014 gates + ADR-042 Rule 3 cross-reference + 3-retry cap + per-iteration cap clarification + halt branch preservation + halt branch unrecoverable enumeration + Step 2.5b P126 preservation + ADR-013 Rule 5 policy authorisation + P081/P135/P130/P132 cross-references + Mid-loop ask discipline halt-point narrowing + Decision Making table row presence + closed-allow-list citation + 3-retry-cap citation; full work-problems suite 203/203 green confirming no regression; architect verdict PASS (Phase 1-only scope correct per ticket Fix Strategy / Phase 2 advisory classifier `packages/itil/scripts/diagnose-ci-failure.sh` deferred to observe declarative discipline over 30 days; ADR-014 one-commit-per-iter invariant preserved — ADR-042 Rule 3 already establishes that retries each ride own commit through full gate flow, the 3-retry cap is an attempt counter not a commit-count cap; fix-and-continue branch belongs inside Failure handling subsection sibling to halt branch — readers look there for "what happens when CI fails"; no new ADR needed — ADR-013 Rule 5 + ADR-044 framework-resolution boundary + in-skill prose suffice; advisory closed-allow-list scope-creep guard added per architect FLAG: extending the allow-list is itself a deviation-candidate per ADR-044 — surface to user via Step 2.5b's AskUserQuestion-default branch, do NOT auto-extend at agent discretion; reassessment-window check: ADR-018 reassessment 2026-07-18 within window, ADR-014 reassessment 2026-10-16 within window, no staleness flags fire); JTBD verdict PASS (JTBD-006 primary — restores "progress continues without me being present" while preserving "stops gracefully on a blocker" guarantee; JTBD-001 + JTBD-002 compose intact via per-retry gates preserving governance — no governance bypass; persona-misread risk that "fix-and-continue" could be read as "auto-fix anything" addressed via four guard-rails: closed-list framing + ambiguous-defaults-to-halt + per-iteration cap clarification + halt-on-anything-outside-allow-list); user verifies on next CI failure during a `/wr-itil:work-problems` AFK drain — fixable-class failures should trigger fix-and-continue branch (single Edit, ADR-014 gates per retry, push, re-watch, wall-clock recovery on the order of minutes vs prior ~45min halt-and-wait), unrecoverable failures should halt cleanly through the halt branch with Step 2.5b surfacing routine intact; 30-day observation window watches for retry-cap drift / allow-list drift — if drift observed, surface for Phase 2 classifier or further allow-list narrowing) | no — not observed |
| P143 | `release-watch.sh` race condition — `gh pr list` queries before changesets/action GitHub workflow has created the release PR | 2026-04-29 (AFK iter; pending `@windyroad/itil` patch — `find_release_pr` shell function added to `scripts/release-watch.sh` wrapping `gh pr list --head changeset-release/main --base main --state open` in a 12-attempt × 10s = 120s poll loop so callers absorb the ~30-120s changesets/action workflow latency between `git push` and PR creation/update; empty-poll exit message points at Actions tab; `RELEASE_WATCH_VERBOSE=1` env var gates per-iteration "Polling for release PR (attempt N/12)..." stderr lines (default off — `npm run release:watch \| tee` orchestrator pipe shape preserved); 7 behavioural bats per ADR-037 + P081 in `packages/itil/scripts/test/release-watch-poll-loop.bats` — awk-extract `find_release_pr` + source + PATH-shadow `gh` mock with comma-delimited iteration sequence + stub `sleep` to count without burning wall-clock — covering fast-path one-call no-sleep on immediate PR / three-call two-sleep on iteration-3 PR / 12-call 11-sleep exit-1 on full-empty / verbose-mode-on stderr / verbose-mode-default-off / tab-separated parseable output; 7/7 green; ADR-022 amended in same commit endorsing fold-fix Open → Verifying when Open ticket already documents root cause + fix strategy + workaround inline (precedents: cc79ae2 P140 + this commit) — destination-reachability validation preserved as the canonical default, fold-fix is the explicit shortcut for inline-pre-flight cases; architect verdict ISSUES-FOUND with concrete recommendations all applied (test placement → `packages/itil/scripts/test/`; same-commit ADR-022 amendment; `RELEASE_WATCH_VERBOSE` documented in script header) then PASS on re-confirm; JTBD verdict PASS — JTBD-006 progress-the-backlog-while-AFK direct fit (orchestrator's first `release:watch` after `push:watch` no longer races the changesets/action workflow PR-creation window); user verifies on next AFK Step 6.5 release drain or manual `npm run release:watch` invocation immediately after `npm run push:watch` — the empty-PR exit on race window should disappear) | no — not observed |
| P144 | P119 hook deny on `manage-problem` Step 2 marker has no documented agent-side recovery; agent attempts brute-force bypass instead of using prescribed surface | 2026-04-29 (interactive iter; pending `@windyroad/itil` patch — `packages/itil/skills/manage-problem/SKILL.md` Step 2 substep 7 grew a "Recovery if hook denial persists" sub-block documenting the gate-misfire signal (active manage-problem flow + helper-succeeded + Write-denied conjunction) and a two-tier recovery: first-tier scrapes `/tmp/itil-assistant-gate-announced-*` for the orchestrator SID and touches `/tmp/manage-problem-grep-<SID>`, second-tier uses `python3 -c 'Path(p).write_text(...)'` invoked through the `Bash` tool when the runtime hook stdin SID isn't in any announce-marker class (2026-04-29 evidence); `packages/itil/hooks/manage-problem-enforce-create.sh` deny message gained a conditional recovery hint `(Helper succeeded but SID mismatch detected — see manage-problem SKILL.md Step 2 substep 7.)` appended only when `compgen -G '/tmp/manage-problem-grep-*'` matches at least one marker for SOME SID — routine first-creation deny is unchanged; ADR-048 (`docs/decisions/048-gate-misfire-recovery-procedure.proposed.md`) sanctions and scopes the procedure with explicit P142-supersession criteria, an audit-trail-preservation test that rules out the P131 "any-marker-anywhere" anti-pattern, and ADR-044 framework-mediated surface catalog extension for the mechanical-decision rationale; new behavioural bats `packages/itil/skills/manage-problem/test/manage-problem-p119-recovery-path.bats` 14/14 green per ADR-037 + P081 covering recovery sub-block presence + gate-misfire signal + two-tier procedure naming + audit-trail-preservation test + anti-pattern bound (`THIS ticket` wording) + DO-NOT-brute-force durable wording + ADR-048/P124/P142 cross-references + supersession HTML comment + mechanical-decision rationale; extended `packages/itil/hooks/test/manage-problem-enforce-create.bats` with 3 new behavioural assertions covering deny-without-any-marker omits hint + deny-with-other-SID-marker includes "SID mismatch" + "Step 2 substep 7" pointer + recovery hint avoids ADR-038 jargon (no `P124-Phase-3-regression` wording) — full hook suite 19/19 green; SKILL.md sub-block carries `<!-- supersedes-when: P142 ships -->` HTML comment + bats supersession assertion so cleanup becomes a CI-fail signal once P142's resolution ADR is `accepted`; architect verdict ALIGNED-with-four-advisory-items all incorporated (anti-pattern bound naming `THIS ticket` rules out the P131 any-marker-anywhere generalisation surface; CI-enforced supersession invariant via bats; ADR-044 framework-mediated extension framing in Decision Drivers + Related; Confirmation bats listing); JTBD verdict PASS — JTBD-001 primary (closes the JTBD-001 outage today without waiting for P142; documented recovery > silent bypass; gate authority preserved by bounded scope), JTBD-101 directly served (transforms undocumented dead-end into documented procedure for plugin developers), JTBD-201 audit-trail integrity preserved (Step 2 grep evidence intact; only marker-check enforcement layer is sanctioned for skip in audit-trail-preserved branch); user verifies on next orchestrator session that triggers the P124 helper bug — recovery hint should appear in deny message + SKILL.md substep 7 should walk the agent through the two-tier procedure without invoking the brute-force-marker anti-pattern) | no — not observed |
| P141 | AFK iter `packages/<plugin>/` commits without changesets — orchestrator-main-turn back-fill is fragile recovery, hook-level enforcement preferable | 2026-05-02 (AFK iter; pending `@windyroad/itil` patch — new `packages/itil/hooks/itil-changeset-discipline.sh` PreToolUse:Bash hook denies `git commit` when staged set includes `packages/<plugin>/` source files but no `.changeset/*.md` is staged; new `packages/itil/hooks/lib/changeset-detect.sh::detect_changeset_required` helper categorises staged paths and returns 1 when publishable source lacks a changeset; allow-list per architect verdict 2026-05-02 — test paths (`test/`, `hooks/test/`, `scripts/test/`), package `README.md`, `*.md` under package `docs/`, and any non-`packages/` path; `SKILL.md` deliberately NOT in allow-list per architect amendment (it IS the publishable contract per ADR-037 framing); `BYPASS_CHANGESET_GATE=1` env var override for legitimate non-publishable commits; deny message ≤300 bytes per ADR-045 Pattern 1 silent-on-pass / deny-band cap names plugin slug + `bun run changeset` recovery + bypass env var + P141 cite; fail-open on parse failure / outside-git-tree / non-Bash tool / non-`git commit` Bash invocation per `lib/staging-detect.sh` precedent (P125); registered in `packages/itil/hooks/hooks.json` as third PreToolUse:Bash matcher alongside p057-staging-trap-detect and pre-publish-intake-gate; 21 behavioural bats per ADR-005 + P081 in `packages/itil/hooks/test/itil-changeset-discipline.bats` covering deny on staged source without changeset (3 shapes — SKILL.md / hook .sh / plugin.json) + deny message contract + ≤300-byte band + allow with valid changeset + allow test-only paths (3 shapes) + allow doc-only paths (2 shapes — package README and docs/) + deny SKILL.md (allow-list amendment) + allow .github/ + allow top-level docs/ + allow BYPASS env var + ADR-045 Pattern 1 silent-on-pass + non-Bash bypass + non-`git commit` Bash bypass + `.changeset/README.md` not counted as real changeset + mixed source+test set still requires changeset + fail-open on parse error + fail-open outside git tree; 21/21 green and full itil hooks suite 153/153 green confirming no regression; architect verdict APPROVED-WITH-AMENDMENTS (ADR-005 cited not ADR-037 — skill-scoped; ≤300-byte deny band per ADR-045 — tighter than ADR-038 400-byte ceiling; SKILL.md NOT in allow-list per ADR-037 framing of skill contract as publishable; ADR-014 + ADR-018 + ADR-038 + ADR-045 + ADR-009 in scope; no new ADR needed — same enforcement-layer pattern as P125 staging-trap hook); JTBD-006 primary fit (40% iter miss rate observed in 2026-04-28 AFK loop session — hook makes the requirement unmissable without adding to iter context budget); JTBD-001 composes (deterministic deny adds audit value; ~10ms detection cost per `git commit` invocation negligible); composes-with P073 (changeset author-time gate at Write/Edit on `.changeset/*.md` — different surface, layered defence-in-depth); user verifies on next AFK loop session by attempting a `git commit` with staged `packages/<plugin>/` source but no changeset → expect deny with directive; allow paths verified by staged test-only set + staged README.md alone + `BYPASS_CHANGESET_GATE=1` set; hooks load from marketplace cache per session-start briefing — verification path requires `@windyroad/itil` release + reinstall before adopter agents see the new hook) | no — not observed |
| P148 | Agent defers ticket creation to retro summary "Tickets Deferred" section instead of immediately invoking `/wr-itil:manage-problem` — observations could be lost if user is in a rush | 2026-05-02 (`@windyroad/retrospective` patch — fix work landed 2026-04-29 AFK iter, commit deferred to 2026-05-02 user session; architect-picked Fix 1+2 hybrid: `packages/retrospective/skills/run-retro/SKILL.md` Step 4b Stage 1 AFK-branch rewritten to (a) name `cause: skill_unavailable` as the only valid fallback gate, (b) require every Tickets Deferred entry carry an explicit `cause:` field, (c) enumerate four named anti-pattern rationalisations the agent must NOT use (session-length pressure, lifecycle weight, retro-summary-defer preference, fabricated subcommands), (d) cite the user's verbatim correction phrase *"observations could have very easily been lost if I was in a rush"*, (e) cite ADR-044 framework-mediated surface + P145 sibling pattern; Step 5 retro summary template gains `### Tickets Deferred` section with `Observation | Cause | Citation` columns; new advisory script `packages/retrospective/scripts/check-tickets-deferred-cause.sh` walks `docs/retros/*.md` retro summaries, parses the `### Tickets Deferred` table in each, emits `RETRO <date> file=<basename> deferred=<N> with_valid_cause=<M> violations=<K>` per file plus trailing TOTAL summary, exit 0 always per ADR-040 declarative-first / ADR-013 Rule 6 (advisory contract); Cause allowlist single-source `{skill_unavailable}` — extending requires matching SKILL.md AFK-branch update; tolerates Cause values wrapped in backticks/bold, missing/empty Cause cells (treated as violation), out-of-allowlist values; 23 behavioural bats per ADR-037 + P081 — 20 in `packages/retrospective/scripts/test/check-tickets-deferred-cause.bats` (empty dir / no-defer steady state / good fixture / bad fixture / legacy fixture / mixed fixture / empty Cause / format tolerance / placeholder skip / multi-file ordering / file-type filtering / portability / read-only contract) + 3 in `packages/retrospective/skills/run-retro/test/run-retro-stage-1-fallback-gating.bats` (prose names cause:skill_unavailable / prose references script path / prose cites P148 driver) — 23/23 green; full retrospective suite 127/127 green confirming no regressions; architect verdict 1+2 hybrid; JTBD verdict PASS aligned with JTBD-001 / JTBD-006 / JTBD-201; advisory-only initial mode preserves JTBD-006 line 32 (extended AFK safety) per Rule 6 graceful-stop contract; user verifies on next retrospective with two distinct Tickets Deferred shapes (one entry with `cause: skill_unavailable`, one with `cause: session_pressure`) — `bash packages/retrospective/scripts/check-tickets-deferred-cause.sh docs/retros` should report `with_valid_cause=1 violations=1` for that retro; behavioural confirmation that the rationalisation loop is closed comes from observing whether the agent stops broadening the fallback gate in subsequent retros — measurable via the script's TOTAL violations count tracking down toward 0 across 3 consecutive retros; forward-looking deviation-candidate: if advisory script surfaces ≥2 Stage 1 violations across 3 consecutive retros, declarative layer is insufficient and enforcement-hook escalation per P135 R6 trajectory becomes warranted) | no — not observed |
| P151 | Published skills reference repo-relative script paths — adopter `bash` invocations hard-fail at Step 0 (5 invocation sites in `@windyroad/itil` + `@windyroad/retrospective`; sibling concern to P137 semantic-ID-leak but distinct failure mode — hard-fail executable-path-leak) | 2026-05-02 (AFK iter 3; pending `@windyroad/itil` + `@windyroad/retrospective` patches — single fold-fix commit per ADR-014 / ADR-022 ships ADR-049 codifying `bin/`-on-`$PATH` resolution rule (sibling to ADR-002 / ADR-003 / ADR-017 plugin-boundary class); naming grammar `wr-<plugin>-<kebab-script-name>` fixed; rename-cost trade-off named explicitly per JTBD-101 review; 3 new shim wrappers (`packages/itil/bin/wr-itil-reconcile-readme`, `packages/itil/bin/wr-itil-check-problems-readme-budget`, `packages/retrospective/bin/wr-retrospective-measure-context-budget` — 3-line `exec "$(dirname "$0")/../scripts/<name>.sh" "$@"` body, executable, no `.sh` extension since they are `$PATH`-resolved CLI commands not internally-invoked scripts); 5 SKILL.md invocation sites updated (manage-problem Step 0 L189, work-problems Step 0 L89, reconcile-readme Step 1 L44, run-retro Step 2c L179, analyze-context Step 2 L45) + 2 documentation references at manage-problem L465/L477 rewritten to name the bin-wrapper while preserving maintainer-side canonical-path hint; cross-plugin grep-as-lint bats `packages/shared/test/no-repo-relative-script-paths-in-skills.bats` 8/8 green covering both regex (script invocations + hook invocations) + shim presence + executable bit + smoke-test exec relay; architect verdict APPROVED-with-one-CONCERN (full plugin name `wr-retrospective-` not abbreviated; shim no-`.sh` extension correct; bats placement at `packages/shared/test/` over itil-scoped per cross-cutting precedent — sibling to `external-comms-gate-canonical.bats` and `plugin-manifest-sync.bats`); JTBD verdict PASS-with-two-non-blocking-CONCERNS (rename-cost surface named in ADR; naming-grammar fixed) — both concerns folded into ADR-049 before commit; sibling concern P153 opened for `for d in packages/*/hooks` glob loops in analyze-context SKILL.md L56-67 (silent zero-byte degradation, distinct failure mode); user verifies on next adopter session running `/wr-itil:work-problems` or `/wr-retrospective:analyze-context` from outside the source repo — Step 0 should succeed via `wr-itil-reconcile-readme` / `wr-retrospective-measure-context-budget` bin-resolved commands instead of hard-failing at exit 127 `No such file or directory`) | no — not observed |
| P153 | Published skills enumerate repo-relative directories — `analyze-context` SKILL.md Step 2 inline `packages/*/hooks` + `packages/*/skills` glob loops silently emit zero-byte attribution rows in adopter sessions (sibling to P151 / ADR-049 reassessment clause 3, distinct failure mode — silent zero-byte degradation vs P151's hard-fail) | 2026-05-02 (AFK iter 4; pending `@windyroad/retrospective` patch — single fold-fix commit per ADR-014 / ADR-022 ships new helper `packages/retrospective/scripts/list-plugin-attribution.sh` (source-tree first / `$PATH`-derived plugin-cache walk fallback / `PLUGIN-ATTRIBUTION not-measured reason=no-plugin-source-resolvable` ADR-026 sentinel when neither resolves; sorted output for stable diffs; advisory exit-0 contract matching `measure-context-budget.sh`) + new 3-line shim `packages/retrospective/bin/wr-retrospective-list-plugin-attribution` per ADR-049 naming grammar (`wr-<plugin>-<kebab-script-name>`, no `.sh` extension); SKILL.md Step 2 prose at `analyze-context` SKILL.md replaces inline `for plugin_dir in packages/*/hooks; do ... done` + `for plugin_dir in packages/*/skills; do ... done` loops with single `wr-retrospective-list-plugin-attribution "${CLAUDE_PROJECT_DIR:-.}"` invocation + resolution-order documentation inlined for adopter-agent context; cross-plugin grep-as-lint `packages/shared/test/no-repo-relative-script-paths-in-skills.bats` extended with new `@test` matching `for +<var>+ in +packages/<plugin-or-glob>/(hooks\|skills\|scripts\|bin)` pattern + new shim smoke test — 11/11 green; new behavioural-contract bats `packages/retrospective/scripts/test/list-plugin-attribution.bats` 10/10 green covering existence + executable + advisory exit-0 + source-tree single-plugin PLUGIN-HOOKS + source-tree single-plugin PLUGIN-SKILLS + nonzero hook bytes + multi-plugin enumeration + cache-fallback resolution from synthetic `~/.claude/plugins/cache/<owner>/<plugin>/<version>/` layout + not-measured sentinel branch + ADR-038 ≤150-byte per-row budget; architect verdict APPROVED — proposal fully covered by ADR-049 reassessment-criteria clause 3, no new ADR required, naming grammar correct, shim shape correct, lint extension at correct location with correct mechanism, `bin/` already declared in `files` array of `packages/retrospective/package.json`, ADR-026 sentinel honoured for unresolvable cases; JTBD verdict ALIGNED — JTBD-301 plugin-user persona served (preserves adopter-session diagnostic value; sentinel makes failure visible rather than silent), JTBD-101 plugin-developer persona served (helper-script + bin/-shim pattern + lint extension matches "clear patterns, not reverse-engineering" outcome — class-of-behaviour discipline P132/P151 enforced); user verifies on next adopter session running `/wr-retrospective:analyze-context` from outside the source repo — Step 2 should emit populated `PLUGIN-HOOKS <plugin> bytes=<N>` / `PLUGIN-SKILLS <plugin> bytes=<N>` rows for each installed `@windyroad/*` plugin via the cache-fallback resolution path, instead of silent zero rows from the previous inline glob expansion) | no — not observed |
| P142 | P124 Phase 4 — `get_current_session_id` helper system-priority bug; subprocess SIDs win mtime selection over orchestrator SID | 2026-05-03 (AFK iter 5; pending `@windyroad/itil` patch — single fold-fix commit per ADR-014 / ADR-022 ships ADR-050 codifying runtime-SID instrumentation via new `PreToolUse:Bash|Write|Edit|Read` hook (`packages/itil/hooks/itil-runtime-sid-marker.sh`) writing runtime stdin `session_id` to per-machine, per-user, per-project marker (`/tmp/itil-runtime-sid-${USER}-${cksum_PWD}.current`); shared `packages/itil/hooks/lib/runtime-sid.sh` exports `runtime_sid_path()` so producer (hook) and consumer (helper) agree on path; `packages/itil/hooks/lib/session-id.sh` `get_current_session_id` now reads runtime marker FIRST (env var > runtime marker > Phase 3 announce-marker priority cold-path); ADR-048 superseded in same commit (`.proposed.md` → `.superseded.md` rename + frontmatter flip + supersession callout); SKILL.md Step 2 substep 7 recovery prose removed (replaced with single "Phase 4" pointer paragraph); `manage-problem-enforce-create.sh` conditional `RECOVERY_HINT` removed (deny stays terse regardless of `/tmp/manage-problem-grep-*` state); obsolete `manage-problem-p119-recovery-path.bats` deleted (structural-grep on removed prose); behavioural coverage migrated to new `runtime-sid-marker.bats` 7/7 green (write / silence / exit code / overwrite / empty input / malformed JSON / fail-open) + 4 new `session-id.bats` tests (runtime-marker priority / empty-marker fallback / cold-path fallback / env-var precedence) + 2 reframed `manage-problem-enforce-create.bats` tests (post-ADR-050 deny-message-invariance); full itil hooks suite 163/163 green + manage-problem skill suite 139/139 green; architect verdict PASS Option (d) chosen over ticket-listed (a)/(b)/(c) — pure-helper algorithms cannot disambiguate orchestrator-vs-subprocess context from filesystem state alone, would shift misfire between contexts; ADR-045 Pattern 1 silent-on-pass binding + ADR-009 marker lifecycle preserved + ADR-023 perf review noted (~0.5-2ms per tool call, ≤1s wall-clock + ≤100KB tmpfs writes per session, ungoverned-risk accepted in ADR-050 Consequences); JTBD verdict ALIGNED — JTBD-001 primary (P119 create-gate reliability foundation), JTBD-006 (eliminates recurring AFK loop friction class — 4× recurrence in 2026-04-29 session per ADR-048 Decision Drivers), JTBD-101 (pattern reusable by future plugins via shared lib source-and-consume); user verifies on next AFK orchestration session by confirming `/tmp/itil-runtime-sid-${USER}-*.current` is OVERWRITTEN as orchestrator's main-turn tool calls fire after each iter exit + zero `BLOCKED` denials on legitimate first-creation paths in either context + no `Helper succeeded but SID mismatch detected` recovery-hint text in any deny message) | no — not observed |
| P145 | `/wr-retrospective:run-retro` Tier 3 budget rotation prompt accumulates "defer" answers across retros — topic files exceed budget recurringly without action | 2026-05-03 (AFK iter 6; pending `@windyroad/retrospective` patch — fold-fix Open → Verification Pending per ADR-022 amendment when Open ticket documents root cause + fix strategy + workaround inline; Option H per architect review chosen over ticket-listed options 1/2/3 — single-script + single-SKILL.md amendment + bats triplet, lightest hybrid; `packages/retrospective/scripts/check-briefing-budgets.sh` extended to emit `MUST_SPLIT <basename> reason=ratio-exceeds-2x` for files at ratio ≥ 2.0× threshold (in addition to existing `OVER` line) — promoting ADR-040's "≥ 2× ceiling for ≥ 2 consecutive retro cycles" reassessment trigger from policy-revisit-time to per-cycle script enforcement (single-sourced, no policy duplication); `packages/retrospective/skills/run-retro/SKILL.md` Step 3 Tier 3 heuristic gains a Branch A that forecloses the `trim-noise` / `leave-as-is` defer escape hatches for `MUST_SPLIT` files — agent picks `split-by-subtopic` if a coherent boundary exists, else `split-by-date` as the safe default (mtime-sort + median-age archive, mechanical and AFK-safe per JTBD-006); Branch B (only `OVER`, no `MUST_SPLIT`) retains the original four-option heuristic with defer permitted inside the reassessment-trigger envelope — files in 1.0×-2.0× range get one or two more retros to escalate to MUST_SPLIT; ADR-040 Confirmation criterion + Reassessment-trigger More-Information section updated to reference P145 as the per-cycle enforcement layer of the existing 2× threshold; 6 new behavioural bats per ADR-005 + ADR-037 + P081 in `packages/retrospective/scripts/test/check-briefing-budgets.bats` covering MUST_SPLIT-at-2x-boundary / MUST_SPLIT-not-under-2x / MUST_SPLIT-with-OVER / MUST_SPLIT-not-without-OVER / MUST_SPLIT-respects-env-override / mixed-block-sort-determinism — RED→GREEN cycle confirmed (4 tests failed before script edit; all 20 tests pass after); architect verdict APPROVE Option H with edge-case additions all incorporated (under-threshold-emits-neither + sort-stability across mixed blocks + integer-arithmetic 2.0× boundary + env-override flow-through); architect-recommended SKILL.md modification incorporated (split-by-date as safe-default explicitly documented to protect against weak-boundary split-by-subtopic picks); JTBD verdict PASS — JTBD-001 (silent-agent stage; user not slowed down; 4× ticking accumulator removed), JTBD-006 (split-by-date is mechanical mtime-sort, AFK-safe, no human judgment required), JTBD-101 (extends advisory triplet `MUST_<action> <id> reason=<code>` shape cleanly for future accumulator surfaces — problems README, risk register); rejected options: ticket option 1/2 (per-file HTML comment defer-count trailer — fights git-blame-as-history; the byte-ratio signal is a derived measure of the same accumulation), ticket option 3 (sibling `/wr-retrospective:rotate-briefing` skill — overweight; introduces new skill surface for the same outcome); user verifies on next `/wr-retrospective:run-retro` invocation against any repo with topic files at ≥ 2× ceiling — Step 3 Tier 3 silent-agent rotation must follow Branch A for any file whose script-output line has a corresponding `MUST_SPLIT` line, picking `split-by-date` as the safe default; behavioural confirmation that the recurring-defer loop is closed comes from observing topic-file byte counts shrink toward the Tier 3 envelope across the next 2-3 retro cycles — measurable via `bash packages/retrospective/scripts/check-briefing-budgets.sh docs/briefing` output trending MUST_SPLIT lines down toward 0; live exercise this iter against `docs/briefing/` reproduces the symptoms exactly — 6 OVER lines + 3 MUST_SPLIT lines for afk-subprocess at 4.0× / agent-interaction-patterns at 2.2× / governance-workflow at 3.4×, exactly the recurring-defer victims the ticket called out) | no — not observed |
| P150 | docs/problems/README.md Verification Queue rendered newest-first contradicts section header "Ranked by release age, oldest first" | 2026-05-03 (AFK iter 9; pending `@windyroad/itil` patch — fold-fix Open → Verification Pending per ADR-022 amendment when Open ticket documents root cause + fix strategy + workaround inline; encodes canonical Verification Queue sort direction `Released date ASC` (oldest at row 1; same-day releases tiebreak by ID ASC) at all six SKILL.md render sites — `manage-problem` Step 5 P094 + Step 7 P062 + Step 9c presentation + Step 9e template, `review-problems` Step 3 + Step 5 README template, `transition-problem` Step 7, `transition-problems` Step 4a batch render, `reconcile-readme` Step 4 row-insertion, `list-problems` cache-stale render — alongside the canonical greppable HTML-comment marker `<!-- VQ-SORT-DIRECTION: oldest-first per ADR-022 -->` analogous to P138's `<!-- TIE-BREAK-LADDER-SOURCE -->` marker so future ladder changes know to update every render block; new behavioural-contract bats `packages/itil/skills/manage-problem/test/manage-problem-readme-vq-sort-order.bats` 13/13 green covering marker presence at every render site (6 contract assertions) + Released-date-ASC direction prose presence (2 assertions) + drift-re-opens-P150 warning prose presence (2 assertions) + behavioural fixture sort with 4 .verifying.md fixtures of known dates asserting row 1 = oldest entry + same-day-Released ID-ASC tiebreaker + P048-aligned likely-verified-first ordering (3 behavioural assertions); README VQ table re-rendered top-to-bottom = oldest → newest with refreshed `Likely verified?` markers based on today_date - released_date (was rendering stale `no (0 days)` for entries multiple days old); architect verdict PASS — no new ADR required (ADR-022 already authorises VQ ordering; ADR-014 already covers single-commit grain; P138 fix-shape established as in-repo precedent), marker grammar matches `TIE-BREAK-LADDER-SOURCE` shape, `oldest first` is the in-spec choice per ADR-022 Decision Outcome line 63 + P048 user-task semantics (older entries surface as actionable closure candidates first), inline fold-fix Open → Verifying endorsed per ADR-022 P143 amendment when pre-flight criteria met inline; JTBD verdict PASS — JTBD-006 primary (AFK loop continuity — actionable closure candidates surface at top of queue, not bottom), JTBD-001 secondary (governance enforced via greppable marker + bats fixture, sibling P138 pattern), no persona regression (no documented need for newest-first VQ framing — that need is served by git log / changelog, not the queue); `@windyroad/itil` patch changeset queued (`.changeset/p150-vq-sort-direction.md`); P150 row removed from WSJF Rankings, inserted at top-of-fresh in Verification Queue per ADR-022 fold-fix mechanic; user verifies on next session by reading `docs/problems/README.md` Verification Queue table — row 1 must be the oldest-released entry (2026-04-22 P093) and row N must be the most-recent (today's P149/P147/P145/P142/P153/P150 cohort); behavioural confirmation that the recurring-drift loop is closed comes from observing future render sites (Step 7 transitions, Step 9e review re-emits, reconcile-readme repairs) preserve oldest-first ordering across 2-3 retro cycles — measurable via `bats packages/itil/skills/manage-problem/test/manage-problem-readme-vq-sort-order.bats` exit 0 across the cycles) | no — not observed |
| P152 | No pressure or nudge for keeping documentation up to date — JTBD-anchored README drift advisory shipped as Phase 1 (sibling family with P137 / P151 / P087) | 2026-05-03 (AFK iter 11; pending `@windyroad/retrospective` minor — fold-fix Open → Verification Pending per ADR-022 amendment when Open ticket documents root cause + fix strategy + workaround inline; ships ADR-051 codifying the JTBD-anchored README rule (every plugin README MUST cite at least one current `JTBD-\d{3}` ID resolving under `docs/jtbd/<persona>/JTBD-NNN-*.md` any status suffix) + recommended `## Jobs to be Done` section structure with persona-grouped subsections (D2 chosen over D1/D3/D4); JTBD-302 newly filed (`Trust That the README Describes the Plugin I Just Installed`, plugin-user persona); JTBD-007 amended to extend currency scope from code-currency to doc-content-currency + cite ADR-051; new advisory script `packages/retrospective/scripts/check-readme-jtbd-currency.sh` walks `packages/*/README.md`, greps `JTBD-\d{3}` distinct citations, resolves against `docs/jtbd/`, emits per-package `README package=<name> has_jtbd_anchor=<yes|no> cited_jobs=<N> known_jobs=<M> drift_hints=<csv>` lines + `TOTAL packages=<N> with_jtbd=<M> drift_instances=<K>` summary; drift hints `missing-jtbd-section` / `stale-jtbd-citation` / `deprecated-jtbd-citation` / `skill-inventory-drift`; exit 0 always per ADR-013 Rule 6 / ADR-040 / ADR-051 Phase 1; new bin shim `packages/retrospective/bin/wr-retrospective-check-readme-jtbd-currency` per ADR-049 naming grammar; 12 behavioural bats `packages/retrospective/scripts/test/check-readme-jtbd-currency.bats` 12/12 green per ADR-005 + P081 (drift / clean / stale-ID / deprecated-only / inventory-drift / multi-package / no-readme cases); empirical first-run baseline confirms 12/12 plugins flagged with `drift_instances=12` (only `itil` has any anchor; even that one has skill-inventory-drift); architect verdict PASS-no-blockers (five issues addressed inline during ADR authoring — primary drivers refined to plugin-user persona + JTBD-302 + JTBD-007; D1/D2/D3/D4 considered with D2 chosen; Confirmation §1 names drift/clean/stale/deprecated bats cases; script naming retained; bin shim per ADR-049); JTBD verdict PASS (co-primary JTBD-302 + JTBD-007; section heading `## Jobs to be Done` aligned; persona-grouped substructure surfaces all four personas; JTBD-007 cheap edit incorporated); out of scope for Phase 1 — retroactive refresh of 12 plugin READMEs (filed as follow-on; mechanism-first / retroactive-after pattern); wiring into `/wr-retrospective:run-retro` Step 2b (deferred until empirical validation); adopter-surface generalisation; `.github/ISSUE_TEMPLATE/*.yml` walking; Phase 2 R6-gated hook (escalates if `drift_instances ≥ 2` across 3 consecutive `chore: version packages` releases); user verifies on next adopter session running `wr-retrospective-check-readme-jtbd-currency` from a marketplace-installed cache outside the source repo — bin shim should resolve on `$PATH` and emit the same `README package=...` + `TOTAL` lines; behavioural confirmation that the asymmetric pressure-stack is closing comes from observing `drift_instances` count trend down across the next 2-3 retro cycles as the retroactive README refresh follow-on iter lands) | no — not observed |
| P128 | /wr-itil:report-upstream report body lacks consolidated Versions section | 2026-05-03 (AFK iter 16; pending `@windyroad/itil` minor — fold-fix Open → Verification Pending per ADR-022 when Open ticket documents fix strategy inline; ships ADR-033 amendment 2026-05-03 (`amended-date` frontmatter + `## Amendments` section) reshaping Step 5 structured default body — labelled `## Versions` section replaces freeform `## Environment`; five-field schema `Local plugin / Upstream package / Claude Code CLI / Node / OS`; missing fields render as `not detected` (normative MUST); auto-population sources documented inline (`package.json` / `claude plugin list` / `gh api releases` / `npm view` / `claude --version` / `node --version` / `uname -srm`); applies to problem-shaped default AND bug-shaped fallback default body (feature-shaped + question-shaped fallbacks unchanged — not version-bound); ADR-024 `## Amendments` gains forward-pointer to the ADR-033 amendment; SKILL.md gains new `#### Versions schema (ADR-033 amendment 2026-05-03, P128)` block under Step 5 field-mapping table + the `## Versions` block replaces `## Environment` in both default body templates; `.github/ISSUE_TEMPLATE/problem-report.yml` replaces freeform `environment` textarea with five structured `input` fields matching the schema (Local plugin version / Upstream package version / Claude Code CLI version / Node version / Operating system); `packages/itil/skills/scaffold-intake/templates/problem-report.yml.tmpl` symmetrically reshaped so downstream-scaffolded intakes inherit the schema per ADR-036; bats coverage extended — `packages/itil/skills/report-upstream/test/report-upstream-contract.bats` adds 6 new assertions (Versions section header / five field labels / `not detected` rule / ADR-033 amendment authority cite / auto-population sources / freeform `## Environment` absence) + updates problem-shaped section-order test from `Environment` to `Versions`; both bats files extended with `tdd-review: structural-permitted (justification: P012 …)` whole-file annotations per ADR-052 line 198 Migration clause (touched-bats-must-retrofit-or-justify; P012 cited as canonical harness-gap); `packages/shared/test/intake-templates.bats` adds new `intake: problem-report.yml carries the consolidated Versions schema` test asserting all five `input` field labels + drops `Environment` from existing problem-ticket-sections iteration; full report-upstream contract bats 30/30 green + intake-templates 12/12 green + scaffold-intake 29/29 green confirming no regression; architect verdict APPROVED — no new ADR required (Step 5's body-shape sits inside what ADR-033 already governs; ADR-022 partial-supersession does NOT apply because nothing is being superseded — this is intra-ADR amendment, parallel to ADR-028's `amended-date` pattern), `not detected` normative MUST approved (JTBD-301 round-trip-reduction served), structured `input` fields approved over textarea (symmetry with output schema is load-bearing per ADR-036 seed-source invariant), version strings confirmed low-leak (factual identifiers, not prose) so safe to land before P064/P038 verify, ADR-014 batch grain confirmed (one ADR amendment + one skill prose change + two intake-template changes + one bats extension + one changeset = same coherent feature batch matching ADR-052 precedent); JTBD verdict PASS — JTBD-301 (plugin-user) primary fit (signals collected at intake map 1:1 into maintainers' workflow with no re-shaping; explicit `not detected` reduces upstream maintainer round-trips), JTBD-101 (plugin-developer) extended (every plugin follows the same problem-report shape — inbound/outbound symmetry serves the consistent-pattern outcome), JTBD-201 (tech-lead) aligned (audit-trail clarity across version transitions — historical `## Reported Upstream` links carry consistent version provenance going forward), JTBD-001 (solo-developer) preserved (single mechanical render step within 60-second flow budget; no human decision required); composes-with P129 (companion inbound version-aware classification — depends on this schema being stable; now unblocked); `@windyroad/itil` minor changeset queued (`.changeset/p128-consolidated-versions-schema.md`); user verifies on first real `/wr-itil:report-upstream` invocation post-ship — body should carry the labelled `## Versions` section with all five fields populated or `not detected` per the rule; downstream `/wr-itil:scaffold-intake` invocation should propagate the structured five-input intake template; behavioural confirmation that maintainer round-trip extends are closing comes from observing comment counts before first triage label trend down across the next 2-3 reports) | no — not observed |
| P137 | Plugin-published artefacts reference internal ADR/JTBD/P-IDs that adopter projects can't resolve — Phase 1 advisory detector + ADR-055 namespace-prefixed-permalinks strategy shipped (sibling family with P151 / P152 / P087 / P097) | 2026-05-03 (AFK iter 17; pending `@windyroad/retrospective` minor — fold-fix Open → Verification Pending per ADR-022 amendment when Open ticket documents root cause + fix strategy + workaround inline; ships ADR-055 codifying namespace-prefix `WR-ADR-NNN` / `WR-JTBD-NNN` / `WR-PNNN` as the structural rule + GitHub permalinks as progressive enhancement; rejects Option A strip (lossy), Option D disclaimer (brittle — fades from agent working memory by tool-result expansion), Option E build-step (premature — adds publish-pipeline coupling); rationale — only namespace-prefix closes failure mode 3 (adopter agent resolves to UNRELATED ADR-014 in their own tree) because adopter agents pattern-match on the visible token not the URL host; advisory detector `packages/retrospective/scripts/check-internal-id-leaks.sh` walks `packages/<plugin>/skills/<skill>/SKILL.md` + `agents/*.md` + `hooks/*.sh` + `CHANGELOG.md`, emits `OVER <plugin>/<file> bare_count=<N>` lines + `TOTAL packages=<N> with_leaks=<M> drift_instances=<K>` summary, silent-on-pass per ADR-045, exit 0 always per ADR-013 Rule 6 / ADR-051 Phase 1 precedent, exit 2 only on root-dir parse error; excludes REFERENCE.md siblings per ADR-054 (lazy-loaded maintainer surface) and `# @adr|@jtbd|@problem` docstring annotation lines (maintainer-source comments never expanded into adopter agent context); new bin shim `packages/retrospective/bin/wr-retrospective-check-internal-id-leaks` per ADR-049 grammar; 23 behavioural bats `packages/retrospective/scripts/test/check-internal-id-leaks.bats` 23/23 green per ADR-005 + ADR-052 (bare-ID detection across all 4 surfaces / WR-prefix exclusion / docstring-annotation exclusion / REFERENCE.md exclusion / count accuracy / TOTAL aggregation / deterministic ordering / boundary tokens P3-too-few-digits + lowercase-case-sensitive + WR-mid-sentence / error path); empirical baseline 2026-05-03 confirms `TOTAL packages=13 with_leaks=81 drift_instances=2880` against the source repo; architect verdict PASS-with-adjustments (5 issues addressed inline: namespace-prefix-as-primary confirmed over permalink-only, detector siting in retrospective package confirmed, hybrid framing in ADR-055 with explicit A/D/E rejection criteria, CHANGELOG/changeset deferred to P082 family, docstring annotations explicitly out-of-scope); JTBD verdict PASS (primary anchor JTBD-302 — Trust That the README Describes the Plugin I Just Installed — semantic correctness axis already cited by JTBD-302 line 40; no new JTBD needed; JTBD-101 secondary fit for Phase 4 convention guardrail only); out of scope for Phase 1 — mechanical sweep across 2880 baseline drift instances (Phase 2 opportunistic-as-touched per ADR-052 migration shape; no big-bang rewrite); per-plugin slices may be carved out as separate problem tickets if/when an adopter incident forces prioritisation; promotion to blocking PreToolUse hook (Phase 3) when `drift_instances ≤ 100` and three consecutive monthly retros confirm no regression; retro Step 2b wiring deferred to Phase 2; CONTRIBUTING.md guardrail deferred to Phase 4; historical npm CHANGELOG entries (already published) acknowledged out-of-scope; user verifies on next adopter session running `wr-retrospective-check-internal-id-leaks` from a marketplace-installed cache outside the source repo — bin shim should resolve on `$PATH` and emit `OVER` + `TOTAL` lines against the adopter's checked-out windyroad source if any; behavioural confirmation that the cluster is closing comes from observing `drift_instances` count trend down across the next 2-3 retro cycles as opportunistic-touched files land WR-prefixed) | no — not observed |
| P156 | Ship `/wr-architect:capture-adr` skill — sibling for ADR capture during foreground work (ADR-032 child of P014; skeleton MADR with deferred-flagged sections) | 2026-05-03 (AFK iter 3 of `/wr-itil:work-problems` post-restart, commit `d28bd51`; backfilled into Verification Queue by iter 4 because iter 3's commit missed the P094 README refresh — sibling-finding regression class; pending `@windyroad/architect` minor — ships new skill `packages/architect/skills/capture-adr/{SKILL.md, REFERENCE.md, test/capture-adr.bats}` per ADR-038 + ADR-052; ~3-4 turn flow vs create-adr's ~10-15; skeleton-fills MADR template at status proposed with sentinel decision-makers + default 3-month reassessment-date + numbered-options placeholder + deferred-flag literal pointer string for canonical-expansion detection; ADR-032 amended with `### Foreground-lightweight-capture variant — capture-adr (P156 amendment, 2026-05-03)` section symmetric with P155 amendment; 12 behavioural bats green; architect verdict PASS-WITH-NOTES (Q1/Q2/Q5 refinements applied); JTBD verdict PASS; external-comms verdict PASS; pipeline risk commit=2 push=2 release=2 Very Low; user verifies on next interactive session by invoking `/wr-architect:capture-adr <description>` — ~3-4 turn skeleton-filled ADR lands at `docs/decisions/<NNN>-<kebab-title>.proposed.md`; recovery path: `/wr-itil:transition-problem 156 known-error` if rollback needed) | no — not observed |
| P157 | Ship pending-questions-surface SessionStart hook — auto-surface accumulated outstanding_questions from .afk-run-state queue when user returns interactive (ADR-032 child of P014; closes queue-file lifecycle gap) | 2026-05-03 (AFK iter 4 of `/wr-itil:work-problems` post-restart; pending `@windyroad/itil` minor — fold-fix Open → Verification Pending per ADR-022 amendment when Open ticket documents fix strategy inline; ships new SessionStart hook `packages/itil/hooks/itil-pending-questions-surface.sh` parsing JSONL queue via `jq -e .` with malformed-line silent skip + dedupe on `(rank, category, ticket_id, question)` tuple + ranking per ADR-044 6-class taxonomy precedence (deviation-approval > direction > one-time-override > silent-framework > taste > correction-followup) + cleanup directive for agent to rewrite queue file removing resolved entries after each AskUserQuestion batch + batching note for entry counts > 4 citing ADR-013 Rule 1 `<=4 per call` cap + AFK-iter cross-context-leak prevention via `WR_SUPPRESS_PENDING_QUESTIONS=1` env-var self-suppress per ADR-032 line 127 implementation choice (a); silent-on-no-content per ADR-040 Mechanism step 1 (missing/empty/whitespace-only/all-malformed → exit 0 zero stdout); wired into `packages/itil/hooks/hooks.json` as second SessionStart entry with matcher `"startup"` mirroring `wr-retrospective` `session-start-briefing.sh` ADR-040 Option A precedent; orchestrator `/wr-itil:work-problems` Step 5 dispatch block exports the env var immediately before each `claude -p` subprocess spawn so accumulated session-level queue does not leak into iter subprocess contexts; ADR-032 amended with new section `### Pending-questions-surface variant — JSONL queue at SessionStart (P157 amendment, 2026-05-03)` between P156 amendment and Scope — disambiguates the two pending-questions surfaces (markdown variant `pending-questions-surface.sh` UserPromptSubmit per ADR-032 line 169 for paused-background-subagent-state tokens; JSONL variant `itil-pending-questions-surface.sh` SessionStart for AFK-loop-accumulated queue), names ADR-040 Option A precedent + ADR-044 6-class precedence, documents the two-hook split + the env-var self-suppress contract; variant-selection precedence pinned (SessionStart-JSONL is LEAD post-P157 for AFK-loop direction-question surfacing across session boundaries; markdown UserPromptSubmit remains LEAD for paused-subagent-state tokens); 19 behavioural bats per ADR-052 — silent-on-no-content (missing / empty / whitespace-only) × 3, surfacing × 2, full 6-class precedence ranking × 2, dedup × 2, batching × 2, cleanup directive × 1, env-var self-suppress (=1 suppresses / =0 does not) × 2, hooks.json wiring × 1, work-problems Step 5 export ordering × 1, malformed-JSON skip × 2, exists × 1; 19/19 green; dogfood end-to-end against this session's real 9-entry queue surfaces 1× deviation-approval (P154) first, 6× direction (BRIEFING_TIER3 / P014 / P156×3 / P160) next, 2× silent-framework (P154×2) last with batching note + cleanup directive present; architect verdict PASS-WITH-NOTES (8 actionable items folded in: ADR-040 cited over ADR-045 for SessionStart-specific silent-on-no-content shape; env-var self-suppress per ADR-032 line 127 implementation choice (a) over orchestrator-side queue drain/restore; ADR-032 amendment over new ADR; explicit cleanup directive in additionalContext text; work-problems Step 5 export of the env var alongside the hook ship; behavioural bats includes WR_SUPPRESS_PENDING_QUESTIONS=1 case; ADR-040 plain-stdout shape over additionalContext-keyed JSON; cross-reference ADR-040 Mechanism step 1 not ADR-045 Pattern 1); JTBD verdict PASS (JTBD-006 primary — Progress the Backlog While I'm Away — closes the documented "queued for my return, not guessed at" desired-outcome gap by making the queue surface deterministically on return rather than only when Step 2.5 fires; JTBD-001 secondary — direction-class observations resolve before user begins foreground work preserving 60-second-flow promise; JTBD-101 tertiary — extends the suite via reusable SessionStart-JSONL pattern); composes-with P155 (sibling capture-problem) + P156 (sibling capture-adr) — closes the ADR-032 child trio (P014 master tracker now has all three children fix-released this AFK loop); P130 (transient-user discipline — P157 is the structural form of "treat user as transient — surface on return"); P135 (master schema for outstanding_questions — P157 closes the queue-file lifecycle); empirical evidence in P157 body — this very session sat 16 hours with 2 entries that only surfaced because user explicitly asked, now would surface deterministically on next session start; user verifies on next AFK loop end-to-end — orchestrator's iter subprocesses must NOT see queue (env-var suppresses); orchestrator's main turn on session resume must see it via SessionStart hook; behavioural confirmation that the cross-context leak is prevented comes from observing zero queue surface inside iter subprocess transcripts vs full surface on orchestrator main-turn session start; recovery path: `/wr-itil:transition-problem 157 known-error` if rollback needed) | no — not observed |
| P081 | Structural source-content tests are wasteful — TDD agent should reject them and require behavioural tests (+ framework / stub enhancements) | 2026-05-03 (AFK iter 13; pending `@windyroad/tdd` minor — fold-fix Open → Verification Pending per ADR-022 amendment when Open ticket documents root cause + fix strategy + workaround inline; Layer A only (Layer B harness primitives + Layer D ~50-bats retrofit out of iter scope per Migration section); ships ADR-052 (`docs/decisions/052-behavioural-tests-default-for-skill-testing.proposed.md`) supersedes ADR-037 codifying behavioural-as-default with documented-justification escape hatches (env-var `WR_TDD_REVIEW_TEST=skip` per ADR-044 category 3 + in-file `tdd-review: structural-permitted (justification: …)` comment per ADR-044 category 2 with cited harness-gap ticket per ADR-026 grounding); ADR-037 renamed to `.superseded.md` with banner-block + `superseded-by: [052-...]` frontmatter mirroring ADR-027 supersession precedent; ADR-005 amended with additive sub-clause inside Permitted-Exceptions narrowing scope to exclude prose-document content greps (SKILL.md / agent.md / *.proposed.md / RISK-POLICY.md) while preserving hooks.json / file-existence / safety-construct exceptions, plus `[Reassessment Triggered 2026-05-03 per ADR-052]` flag in Reassessment Criteria parallel to existing 2026-04-21/ADR-037 flag; new `packages/tdd/agents/review-test.md` semantic test classifier (frontmatter mirrors `architect/agents/agent.md`; tools `[Read, Glob, Grep]`; multi-framework recognition for bats/vitest/jest/mocha/cucumber/.feature/pytest with paired structural/behavioural exemplars; verdict-shape JSON-in-fenced-block `{verdict, evidence, suggestion, harness_gap}` with `harness_gap` MUST cite ticket ID or be null per ADR-026; mechanical/silent classification stage per project CLAUDE.md P132 — never calls AskUserQuestion); new `packages/tdd/hooks/tdd-review-test.sh` PostToolUse Edit|Write advisory hook silent on non-test files / outside-PWD / env-skip / justification-comment / non-existent file, emits additionalContext directing assistant to invoke review-test agent on test-file writes; `packages/tdd/hooks/hooks.json` extended with new entry composing alongside `tdd-post-write.sh` (separation of concerns: post-write owns RGR state, review-test owns kind-of-test classification); 15 behavioural bats `packages/tdd/hooks/test/tdd-review-test.bats` 15/15 green per ADR-005 + P081 dogfood (NO source greps — bats files run the hook with mock JSON tool input and assert on emitted output / silence) covering all six advisory paths (test-file → directive; non-test → silent; env-skip → silent; bash-comment → silent; ts-comment → silent; outside-PWD → silent; non-existent-file → silent; advisory text mentions ADR-052 + both escape hatches; exit always 0); full 71-test TDD hook suite green confirming no regression on existing tdd-enforce-edit / tdd-post-write / tdd-inject / tdd-setup-marker / tdd-reset behaviour; `@windyroad/tdd` minor changeset queued (`.changeset/p081-review-test-agent-and-hook.md` 0.3.1 → 0.4.0); architect verdict PASS — both review rounds (initial + re-confirm) returned PASS with all 6 prior issues incorporated (ADR numbering correction 044→052 since 044 is taken by `decision-delegation-contract`; supersession-banner mirroring ADR-027 with `superseded-date` + `superseded-by`; Migration section in ADR-052 explicit on ~50 existing structural bats accepted-until-touched; ADR-044 escape-hatch category citations 2/3; ADR-026 grounding for `harness_gap` cite-or-null; ADR-005 narrowing as additive sub-clause + parallel `[Reassessment Triggered]` flag; ADR-035 verdict-store explicit scope-out with future opt-in clause); JTBD verdict PASS — JTBD-001/101/201 anchors confirmed (test-green-≡-safe-to-commit signal restored; JTBD-101 plugin-developer copy-paste propagation chain reversed; JTBD-201 audit trail integrity restored), JTBD-006 AFK-loop friction respected via mechanical-stage carve-out (no AskUserQuestion + ADR-013 Rule 6 fail-safe), JTBD-302/ADR-013 Rule 6 Phase-1-advisory shape matches the README-drift-advisory precedent set by ADR-051; out of scope for Layer A (deferred to follow-on tickets): Layer B framework primitives (Skill-tool interceptor / AskUserQuestion stub / filesystem sandbox / subagent return stub — descend from P012); Layer D retrofit of ~50 existing structural bats (incremental-as-touched per ADR-052 Migration); Phase-2 PreToolUse blocking promotion (named in ADR-052 Reassessment Criteria — fires when un-annotated structural count drops to 0 OR advisory-skip rate exceeds 20%); user verifies on next test-file edit in any plugin — PostToolUse hook should emit advisory directive naming the file path + invocation guidance + both escape hatches; future structural test surfacing demonstrates classifier verdict shape; behavioural confirmation that the propagation chain is reversing comes from observing the count of new-plugin contract.bats files written behaviourally instead of structurally across the next 2-3 plugin additions) | no — not observed |
| P167 | Risk register aggregate reads as "don't ship" — sparse coverage + undercredited controls | 2026-05-04 (commit `9e339d0` — `RISK-POLICY.md` `## Risk Catalog` + `## Control Composition` sections landed; substantive structural successor P168 wipe-and-bootstrap composes; AFK transition iter 2026-05-11 by `/wr-itil:work-problems`) | no — not observed |
| P168 | Risk-scorer doesn't consume `docs/risks/` catalog or bootstrap from `.risk-reports/` | 2026-05-04 (commits ab73328 + af5447c + 8edaf7b; @windyroad/risk-scorer minor bump in `.changeset/wr-risk-scorer-p168-consume-catalog-and-bootstrap.md` reinstated from held area) | no — not observed |
| P164 | Latent octal-eval bug in next-ID formula across all 4 ticket-creator skills — `$(( $local_max + 1 ))` fails with "value too great for base" when local_max reaches 099 | 2026-05-11 (AFK iter of `/wr-itil:work-problems`; pending `@windyroad/itil` + `@windyroad/architect` + `@windyroad/risk-scorer` patch — applies `10#` base-10 prefix across all 6 affected SKILL.md (`manage-problem`, `capture-problem`, `capture-rfc`, `create-adr`, `capture-adr`, `create-risk` — scope expanded from the originally-named 4 after Investigation-Task grep verification) eliminating the `bash: 099: value too great for base` error when local_max reaches the `099 → 100` ID transition; 2 new regression bats fixtures in `capture-adr.bats` test 6 + `capture-problem.bats` test 21 exercise the `098-foo` / `099-bar` boundary and assert `next=100`; sanity check confirms unfixed formula fires the documented octal-eval error and fixed formula returns `100`; existing 26 bats updated in-place with `10#` prefix; full 28-test contract bats green; architect + JTBD verdicts PASS (no new ADR; aligned with ADR-014 single-commit / ADR-019 preflight / ADR-044 framework-resolution boundary / ADR-052 behavioural-tests-default — the new tests assert formula output not SKILL.md prose; ADR-031 per-state subdir unaffected; ADR-055 namespace-prefix unaffected); shared-helper `lib/next-id.sh` deferred (Investigation Task optional bullet) — DRY benefit small vs sourcing-order regression risk across 6 currently-independent skills; user verifies on next ticket-creation invocation that crosses the 099 → 100 boundary in any of the six ticket-creator surfaces — skill should compute `next=100` cleanly without bash error; recovery path if rollback needed: `/wr-itil:transition-problem 164 known-error` after reverting the 8-file commit) | no — not observed |
| P165 | P094 README-refresh enforcement gap — iter subprocess commits can land a `.verifying.md` rename without staging the corresponding Verification Queue row in `docs/problems/README.md` | 2026-05-12 (AFK iter of `/wr-itil:work-problems`; pending `@windyroad/itil` patch — ships new PreToolUse:Bash commit-gate hook `itil-readme-refresh-discipline.sh` denying `git commit` whose staged set contains a `docs/problems/<state>/NNN-*.md` ticket change but no `docs/problems/README.md` refresh; new shared helper `lib/readme-refresh-detect.sh`; 22-test ADR-052 behavioural bats fixture (22/22 green; full itil hook suite 231/231 green — pre-fix 209); registered in `packages/itil/hooks/hooks.json` PreToolUse:Bash array alongside P125 + P141 siblings; deny ≤300 bytes per ADR-045 deny-band naming the offending ticket ID `P<NNN>` (extracted from leading digits to fit budget across all ticket-name lengths) + literal recovery `git add docs/problems/README.md` + bypass env `BYPASS_README_REFRESH_GATE=1`; silent-on-pass per ADR-045 Pattern 1; fail-open outside git tree; covers new state-directory layout (ADR-031) `docs/problems/(open\|verifying\|closed\|known-error\|parked)/NNN-*.md` + legacy flat `docs/problems/NNN-*.md`; allow-list paths — README-only / retro-only / ADR-only / source-only / README-history.md / `BYPASS_README_REFRESH_GATE=1` / non-Bash tool / non-git-commit Bash command / outside git tree; architect verdict PASS (Shape #2 PreToolUse:Bash; sibling to P125 + P141; no new ADR — ADR-009 no-marker per-invocation + ADR-013 Rule 1 deny+recovery + ADR-014 single-commit grain + ADR-022 .verifying.md lifecycle + ADR-038 terseness + ADR-045 Pattern 1 + ≤300-byte deny band all apply unchanged); JTBD verdict PASS (JTBD-001 primary — strengthens existing commit-time gate band; JTBD-302 composes — load-bearing-from-the-start drift detection at closest surface; in-scope; no JTBD doc update); user verifies on next `git commit` in this monorepo: staged `docs/problems/<state>/NNN-*.md` without README → deny with redirect (e.g. `BLOCKED: P165. P<NNN> needs docs/problems/README.md refresh. Run: git add docs/problems/README.md. Bypass: BYPASS_README_REFRESH_GATE=1.`); staged ticket+README → commit allowed; recovery path if rollback needed: `BYPASS_README_REFRESH_GATE=1 git commit ...` for one-time bypass, or remove the `itil-readme-refresh-discipline.sh` entry from `hooks.json` for full rollback) | no — not observed |
| P185 | `/wr-itil:capture-problem` asks a classification question it can answer itself from the description's observable evidence — inverse-P078 / P132 trap at a SKILL contract surface | 2026-05-13 (AFK iter 2 of `/wr-itil:work-problems`; pending `@windyroad/itil` patch — Step 1.5 derive-first refactor + lexical-signal classifier + stderr-advisory contract in `packages/itil/skills/capture-problem/SKILL.md`; dispatch order is `--type=<value>` → `--no-prompt` → 5-class lexical classifier (camelCase / kebab-case / snake_case identifiers + file paths + command patterns + mechanism words + error patterns ↔ persona names + journey words + JTBD-shaped need words) → AskUserQuestion fallback on ambiguous; unambiguous one-sided signal classifies silently with stderr advisory `"capture-problem: classified type=<value> from description signals: <s1>, <s2>; re-invoke with --type=<other> to override"`; I2-isomorphic advisory shape; 17 new behavioural bats in `capture-problem.bats` (32/32 green) covering pure-technical / pure-user-business / mixed / no-signal classifier outcomes + 3 advisory-shape isomorphism tests + 5 dispatch-precedence tests + meta-recursive corpus test; SKILL.md Rule 6 audit table + Composition table + Related section updated; architect verdict APPROVED with riders (taxonomy recategorisation cat-5 → cat-4 silent-framework with cat-5 fallback on ambiguity, all 4 architect-rider edits applied, no ADR amendment needed per ADR-044 Reassessment Criteria); JTBD verdict APPROVED (JTBD-001 primary friction reduction; JTBD-006 AFK contract preserved; JTBD-301 plugin-user firewall preserved + extended scope guard naming manage-problem ingestion path); memory `feedback_derive_classification_dont_ask.md` already captures the class-of-behaviour rule; user verifies on next interactive `/wr-itil:capture-problem "..."` — pure-technical description should classify silently with stderr advisory (no AskUserQuestion); pure-user-business same; mixed-signal or no-signal description should still fire AskUserQuestion; `--type=<value>` and `--no-prompt` short-circuits preserved; recovery path: `/wr-itil:transition-problem 185 known-error` if rollback needed) | no — not observed |
| P186 | VQ `Likely verified?` column uses age-based heuristic (≥14 days = yes) instead of session-observed evidence — sibling proxy-for-evidence anti-pattern to P185 at the review-problems Step 3/5 surface | 2026-05-15 (AFK iter of `/wr-itil:work-problems`; pending `@windyroad/itil` patch — fold-fix Open → Verification Pending per ADR-022 P143 amendment; ships evidence-first `Likely verified?` cell shape across every VQ render site (three canonical values `yes — observed: <evidence>` / `no — not observed` / `no — observed regression`) supersedes original P048 Candidate 4 14-day age-based heuristic; greppable `<!-- LIKELY-VERIFIED-CELL-SHAPE: evidence-based per P186 -->` marker at every render site (analogous to P138 / P150 marker precedent); render-site sweep across review-problems Step 3 + Step 5, list-problems Step 2 + Step 3, manage-problem Step 5 P094 + Step 7 P062 + Step 9c + Step 9e, transition-problem Step 7, transition-problems Step 4a, reconcile-readme Step 3 + Step 4; drift-tripwire prose ("drift re-opens P186") at primary owners; Step 4 verification-prompt path updated to surface `yes — observed: …` rows first for batch-close and flag `no — observed regression` rows for `.verifying.md` → `.known-error.md` flip-back; docs/problems/README.md Verification Queue re-rendered — 69 age-marker rows collapsed to `no — not observed` (P024 + P048 prose-evidence rows likewise canonicalised, prose preserved in git history); new behavioural bats `packages/itil/skills/review-problems/test/review-problems-likely-verified-cell-shape.bats` 17/17 green; full sibling suite re-run green 158/158 manage-problem + 150/150 review-problems/list-problems/transition-problem/transition-problems/reconcile-readme; architect + JTBD verdicts PASS (clean composition of ADR-022 + ADR-014 + ADR-026 + ADR-052 + P138/P150 fix-shape precedent, no new ADR; JTBD-001 primary, JTBD-006 composes); Task 7 (P048 design-intent forensics) deferred — orthogonal to the cell-shape fix; user verifies on next `/wr-itil:review-problems` invocation that VQ rows use new vocabulary; recovery path if rollback needed: `/wr-itil:transition-problem 186 known-error` after reverting the iter commit) | no — not observed |
| P163 | `wr-risk-scorer:external-comms` agent emits placeholder marker key on first invocation when prompt doesn't direct shasum computation | 2026-05-16 (`/wr-itil:work-problems` AFK iter 5 — folded into P166 fix commit per ADR-014 + ADR-022 P143 amendment; original 2026-05-13 user direction (option a — grant agent narrow Bash) reversed in favour of P166's option b (hook-side compute) per architect + JTBD review; reversal closes both P163 and P166 simultaneously while preserving ADR-013 Rule 2's "agents stay [Read, Glob] only" discipline; shared helper `packages/shared/hooks/lib/external-comms-key.sh` derives `sha256(DRAFT + '\n' + SURFACE)` from agent prompt's mandatory `SURFACE: <name>` + `<draft>...</draft>` structure — agent no longer emits or computes the key, single fire per gate cycle; ADR-028 amendment 2026-05-16 documents the contract change + fix-strategy reversal; backward-compat fallback to agent-emitted KEY preserved one release cycle for cached-old-prompt rollover; recovery path if rollback needed: `/wr-itil:transition-problem 163 known-error` after reverting the iter commit) | no — not observed |
| P166 | Precomputed-sha256 helper for `wr-risk-scorer:external-comms` agent invocations to eliminate double-invocation cost | 2026-05-16 (`/wr-itil:work-problems` AFK iter 5 — architect + JTBD verdicts PROCEED + ALIGNED; Option 2 hook-side compute chosen via prompt-parsing (not sidecar — sidecar variant cannot identify which pending key the agent reviewed under interleaved surfaces); shared helper `packages/shared/hooks/lib/external-comms-key.sh` synced into both consumer packages via extended `scripts/sync-external-comms-gate.sh` per ADR-017; both PostToolUse mark hooks (`risk-scorer/hooks/risk-score-mark.sh` external-comms branch + `voice-tone/hooks/external-comms-mark-reviewed.sh`) source the helper + read agent's tool_input.prompt + derive key + fall back to agent-emitted KEY when prompt lacks structure; both agent prompts updated to drop `EXTERNAL_COMMS_<EVAL>_KEY` from MANDATORY verdict format; both `assess-external-comms` SKILL.md files updated with structured-prompt requirement (`SURFACE:` line + `<draft>...</draft>` block); canonical gate denial message updated to instruct orchestrator on the structured prompt shape; ADR-028 amendment 2026-05-16 documents the contract change + P163 fix-strategy reversal + ADR-013 Rule 2 strengthening; new behavioural bats coverage: `packages/shared/test/external-comms-key-helper.bats` 9 tests, `packages/voice-tone/hooks/test/external-comms-mark-prompt-parse.bats` 6 tests, `packages/risk-scorer/hooks/test/risk-score-mark-external-comms-prompt-parse.bats` 5 tests = 20 new tests total; existing sync-fixture tests updated to copy the new helper; sync verification `bash scripts/sync-external-comms-gate.sh --check` OK across all 6 canonical/copy pairs; performance impact monotonically improving — ~$685/year saved across user base, ~225-450s/day wall-clock saved; user verifies on next changeset commit-gate cycle that the external-comms agent fires exactly once per gate; recovery path if rollback needed: `/wr-itil:transition-problem 166 known-error` after reverting the iter commit) | no — not observed |
| P232 | bash until-loop with `pgrep -f 'bats --recursive'` self-references the polling loop's own command line — new variant of P146 stuck-before-emit deadlock; SKILL.md prompt warning insufficient | 2026-05-16 (`/wr-itil:work-problems` AFK iter 1 — fold-fix Open → Verification Pending per ADR-022 P143 amendment; ships Option A SKILL.md prompt extension + Option B structural PreToolUse:Bash hook in one commit per architect APPROVE 2026-05-16; new hook `packages/itil/hooks/itil-bash-polling-antipattern-detect.sh` denies polling-loop shape regex `(until\|while)[[:space:]]+!?[[:space:]]*(pgrep\|pkill[[:space:]]+-0)` with deny message citing P232 + naming both recovery alternatives (`wait $bg_pid` shell-native; Bash-tool `run_in_background=true` + `BashOutput` harness-native); 17 behavioural bats `packages/itil/hooks/test/itil-bash-polling-antipattern-detect.bats` 17/17 green covering 6 positive deny shapes (until/while pgrep, until/while pkill -0, heredoc body, P232 deadlock witness multi-line shape) + 7 allow negatives (one-shot pgrep, non-`-0` pkill, `wait $!`, unrelated until/while, commit-message prose mentioning pgrep) + tool-name filter + 2 parse-incomplete fail-open + advisory-content cite + ADR-045 deny-budget; registered in `packages/itil/hooks/hooks.json` PreToolUse:Bash array alongside `p057-staging-trap-detect.sh` precedent; `packages/itil/skills/work-problems/SKILL.md` Step 5 iter prompt extended with second polling-discipline clause naming the self-reference failure mode + worked-example syntax + 2026-05-16 deadlock-witness context (45 min wall-clock waste + $20-30 cost); SKILL.md Related section cites P232 between P147 and P146; architect verdict APPROVE (no new ADR — sibling to p057-staging-trap-detect.sh; ADR-013 Rule 1 deny+recovery, ADR-045 deny-path band 200-700 bytes, ADR-009 no-marker per-invocation, ADR-014 single-commit grain, ADR-026 grounding all apply unchanged); JTBD verdict ALIGN (JTBD-006 primary AFK-orchestrator beneficiary; JTBD-001 secondary governance-without-slowing — sub-millisecond regex case-match; JTBD-101 plugin-developer auto-adopts on install with no opt-in); user verifies on next AFK iter that backgrounds a `bats` run — any `until ! pgrep -f` polling shape attempted by an iter-spawned bash call should be denied at PreToolUse with the P232 message; recovery path: `/wr-itil:transition-problem 232 known-error` after reverting the iter commit) | no — not observed |

## Inbound Upstream Reports

Reports discovered by `/wr-itil:review-problems` Step 4.5 inbound-discovery pipeline (per ADR-062 § Step 9e renderer). Section is rendered from `docs/problems/.upstream-cache.json`; lazy-empty discipline applies (empty table body = discovery has run with zero reports awaiting triage; advisory row = no discovery pass has run yet).

| # | Source | Title | Author | Created | Classification | Matched local ticket |
|---|--------|-------|--------|---------|----------------|----------------------|

## Closed

Recently closed this session (2026-04-19/20, against direct in-session evidence):

| ID | Title | Closed via |
|----|-------|-----------|
| P230 | README-refresh-discipline hook misfires on narrative-only ticket edits when no ranking-bearing field changed AND `reconcile-readme.sh` exit=0 | Closed 2026-05-16 (`/wr-itil:work-problems` AFK iter 2 fold-fix). Option Y implementation in `packages/itil/hooks/lib/readme-refresh-detect.sh` — `_readme_refresh_staged_is_ranking_bearing` helper detects field-line diffs (`**(Priority\|Effort\|Status\|WSJF\|Type)**:`, title `# Problem `) and rename / creation / deletion via `git diff --staged --name-status -M`; narrative-only edits short-circuit through `_readme_refresh_reconcile_clean` consulting `packages/itil/scripts/reconcile-readme.sh` against `docs/problems`. Ranking-bearing edits still fall through to existing deny logic per ADR-014 single-commit grain (architect rejected Option X "race → allow" framing; reconcile is robustness layer, not supersession). 29/29 hook bats green including 7 new behavioural cases. Folded with [[P231]] Option A deny-message correction in same commit. |
| P231 | README-refresh-discipline hook deny message advertises BYPASS_README_REFRESH_GATE=1 inline-prefix that does not propagate to the PreToolUse hook (recurrence of P173) | Closed 2026-05-16 (`/wr-itil:work-problems` AFK iter 2 fold-fix). Option A correction in `packages/itil/hooks/itil-readme-refresh-discipline.sh` REASON string — replaces `Bypass: BYPASS_README_REFRESH_GATE=1.` with `Bypass: BYPASS_README_REFRESH_GATE=1 via .claude/settings.json env (P173).`; under ADR-045 deny-band ≤300 bytes. Architect rejected Option B (remove) — legitimate one-off escape cases remain (force-amend after rebase, partial-progress hand-off). Bats coverage: `P231 deny message advertises .claude/settings.json bypass path + P173 reference` green. Folded with [[P230]] in same commit per ADR-014. |
| P064 | No risk-scoring gate on external communications | Closed 2026-05-14 (transition-problem skill, verification close-on-evidence). Fix Released 2026-04-26 (commit `a0713f3` "fix(risk-scorer): P064 external-comms risk-leak gate (gh issue/pr/api, npm publish, .changeset) — known error"). Verified in this session by 30/30 bats green: 12 in `packages/risk-scorer/hooks/test/external-comms-gate.bats` (surface match / hard-fail leak deny / marker permit / BYPASS short-circuit / advisory-only / changeset author-time / non-changeset path / gh api security-advisories / npm publish), 11 in `packages/shared/test/external-comms-gate-canonical.bats` (canonical hook surface + lib coverage), 7 in `packages/shared/test/sync-external-comms-gate.bats` (ADR-017 drift coverage). All artefacts present: canonical `packages/shared/hooks/external-comms-gate.sh` + `lib/leak-detect.sh`; per-package synced copy `packages/risk-scorer/hooks/external-comms-gate.sh` + `lib/leak-detect.sh`; subagent `packages/risk-scorer/agents/external-comms.md`; on-demand skill `packages/risk-scorer/skills/assess-external-comms/`. Closure unblocks the risk-gate side of P079's pushback + acknowledgement comment paths (assessment-pipeline). The two Deferred Investigation Tasks (`/wr-itil:report-upstream` SKILL.md cross-reference, P038 voice-tone composite-marker upgrade) remain non-blocking per the ticket — `report-upstream` cross-ref is documentation polish (the gate fires from the hook regardless); composite-marker upgrade is owned by P038's iter (the next ticket in this session's chain). |
| P155 | Ship `/wr-itil:capture-problem` skill — lightweight aside-invocation surface for problem capture during foreground work | Closed 2026-05-05 (run-retro Step 4a close-on-evidence). `/wr-itil:capture-problem` ran end-to-end this session for P170 capture: Step 0 reconcile preflight halted on P169 cross-session drift (correctly routed to /wr-itil:reconcile-readme — exercises P149 fix), Step 2 marker write + duplicate grep, Step 3 next-ID compute (170), Step 4 skeleton-fill, Step 5 Write succeeded under P119 create-gate, Step 6 wr-risk-scorer:pipeline gate + commit `2bb0800` landed, Step 7 trailing pointer rendered. Skill contract held; SID-marker discovery friction encountered (P124 territory) was a surrounding-context issue not a P155 regression. |
| P134 | `docs/problems/README.md` line 3 narrative-blob accumulator bloat — sibling to P099 (briefing tier 3) on a different surface | Closed 2026-05-05 (run-retro Step 4a close-on-evidence). Truncation discipline applied during this session's P118 reconcile flow: prior line-3 fragment rotated to `docs/problems/README-history.md` under `## YYYY-MM-DD (...)` heading; new fragment ≤1024-byte soft cap; no `Prior:` accumulator pattern. Reconcile commit `6f4fce4` landed with both files in one ADR-014-grain commit. |
| P149 | `/wr-itil:manage-problem` Step 0 reconcile halt-on-drift directive doesn't distinguish uncommitted-rename-rooted drift from committed cross-session drift | Closed 2026-05-05 (run-retro Step 4a close-on-evidence). `wr-itil-classify-readme-drift` fired correctly on this session's P169 README drift (committed cross-session, not staged-rename). Bash output: `HALT_ROUTE_RECONCILE uncovered=1` with `classify_exit=1`; capture-problem Step 0 correctly routed to `/wr-itil:reconcile-readme` per the documented HALT branch. The INLINE_REFRESH branch was not exercised this session (no staged renames in working tree), but the HALT branch's specific evidence shows the classifier distinguishes the two cases. |
| P158 | ADR-051 Phase 1 detector shipped but not wired into run-retro Step 2b | Closed 2026-05-04 (P159 Phase 1 commit migrated the primary consumption surface to PreToolUse:Bash hook; retro Step 2b wiring shipped under `df47ad1` survives as a backup advisory per ADR-051 amended Confirmation criterion 5. The mechanism this ticket asserted is in place and operational; the architectural direction has shifted to load-bearing-from-the-start at commit time, but the retro wiring is still live as a backup signal so the design intent for which P158 was filed is met. Architect verdict endorsed Closed over re-Open — see ADR-051 amendment 2026-05-04). |
| P058 | `install-updates` SKILL.md under-reports plugins whose names contain digits (regex `[a-z-]+` skips `wr-c4`) | Closed 2026-04-28. Verified via 1325/1325 bats green in this session including `Step 2 and Step 3 discovery regex uses [a-z0-9-]+ character class (P058)` + `SKILL.md's discovery regex matches wr-c4@windyroad against a synthetic fixture (P058)` — the regex contract held under the install-updates legacy-JTBD + rename-mapping trim renumbering. |
| P112 | `/install-updates` Step 6 (was Step 7) `uninstall && install` chain has an unrecoverable failure window — plugin silently lost if install fails after uninstall succeeds | Closed 2026-04-28. Verified via 5 bats `install-updates Step 6 P112: ...` tests passing under the install-updates trim renumbering — retry budget (3× backoff), rollback path (marketplace-cache refresh + one additional install), and Status vocabulary (`installed` / `restored` / `lost`) all exercised. |
| P119 | Agent bypasses `/wr-itil:manage-problem` Step 2 duplicate-check by writing tickets directly | Closed 2026-04-28. Verified by direct in-session exercise: tried to Write `139-...open.md` without the create-gate marker; `manage-problem-enforce-create.sh` denied with the canonical `BLOCKED: ... (P119)` message; subsequent successful write after marker placement confirmed the gate operates as designed. The brittleness exposed by P124 in this same session is captured separately as P124-regression evidence (Verifying → Known Error). |
| P122 | `/wr-itil:work-problems` stop-condition #2 defaults to the AFK Outstanding Design Questions table when AskUserQuestion is available | Closed 2026-04-26 (post-restart). Cached `~/.claude/plugins/cache/windyroad/wr-itil/0.19.6/skills/work-problems/SKILL.md` Step 2.5 verified loaded with new "Default branch — call AskUserQuestion when available" prose; legacy "non-interactive path the default for this skill" prose absent (grep count = 0); `work-problems-step-2-5-routing.bats` 8/8 green at commit time as the architect-mandated second source. Same-session evidence: this very turn used AskUserQuestion to surface the "what next?" question (the spirit of the fix is operational from the assistant side; the orchestrator-side stop-condition #2 will exercise on next /wr-itil:work-problems run that hits the surface). |
| P121 | AFK orchestrator should SIGTERM stuck `claude -p` subprocesses after idle-timeout | Closed 2026-04-26 (post-restart). Cached `~/.claude/plugins/cache/windyroad/wr-itil/0.19.6/skills/work-problems/SKILL.md` Step 5 verified loaded with `IDLE_TIMEOUT_S="${WORK_PROBLEMS_IDLE_TIMEOUT_S:-3600}"`, `SIGTERM_SENT=0` tracker, `LAST_ACTIVITY_MARK = max(DISPATCH_START_EPOCH, last commit timestamp)` semantics. `work-problems-step-5-idle-timeout-sigterm.bats` 10/10 green at commit time as the architect-mandated second source for the SIGTERM-flushes-JSON evidence (single-source production observation 2026-04-25 P118 iter 5 + bats fixture with fake `claude -p` shim covering SIGTERM-fires/JSON-clean-flush/env-override/no-fire-within-threshold). Production exercise of a real stuck-iter event will happen organically; the contract is loaded and tested. |
| P120 | `/install-updates` consent gate fires on every invocation despite the answer being invariably "all confirmed siblings" | Closed 2026-04-26 (post-restart). End-to-end verified this turn: ran `/install-updates`; Step 6a cache-check fired; cached scope (`addressr addressr-mcp addressr-react bbstats windyroad`) matched detected sibling set via set-equality; gate skipped per ADR-013 Rule 5 (policy-authorised silent proceed); NO AskUserQuestion fired. Exact behaviour the user asked for in the originating ticket prose ("Yes it should install in all 6 projects. Don't ask me again"). |
| P026 | install-utils duplicated across packages | CI `check:install-utils` passed on every push this session |
| P031 | Stale cache detection in manage-problem work | Returned correct output at AFK loop start and after subsequent commits |
| P040 | work-problems does not fetch origin before starting | `git fetch origin` ran at AFK loop start per orchestrator spec |
| P041 | work-problems does not enforce release cadence | `assess-release` ran after each iteration with correct scores |
| P042 | changesets does not sync plugin manifest version | End-to-end validated in the 2026-04-19 release (PR #30 contained paired plugin.json entries after P052 fix) |
| P043 | Next-ID collision guard in ticket-creator skills | `max(local, origin)+1` used for 10 new IDs this session (8 problems + 2 ADRs) |
| P052 | ADR-021 release.yml missing `version:` input | End-to-end validated in the 2026-04-19 release; paired with P042's closure |
| P028 | Governance skills should auto-release (non-AFK) | Verified end-to-end in the 2026-04-19 AFK loop iter 2: push of `f0de540` triggered Release workflow → PR #31 auto-created with version bumps and plugin.json manifest syncs → `release:watch` merged and published both `@windyroad/itil@0.4.5` and `@windyroad/retrospective@0.2.0` to npm (run `24619740990`, merge commit `b401c7b`). No manual intervention within the AFK loop. |
| P055 | No standard problem-reporting channel for plugin users | Closed 2026-04-20 (commit 038c3de). Part A (OSS intake scaffolding) + Part B (`/wr-itil:report-upstream` skill) both shipped; @windyroad/itil@0.8.0 released to npm. |
| P029 | Edit gate overhead for governance docs | Closed 2026-04-22 (run-retro Step 4a). Every UserPromptSubmit hook message this session carried the governance-docs exclusion line (docs/problems/, docs/BRIEFING.md, RISK-POLICY.md, .changeset/); edits to docs/problems/091-*.open.md, docs/problems/092-*.open.md, and docs/BRIEFING.md all proceeded without demanding architect/JTBD review. Fix contract (ac9d453, 2026-04-17) held end-to-end. |
| P059 | install-updates no plugin rename handling | Closed 2026-04-22 (run-retro Step 4a). This session's `/install-updates` Read rename-mapping.json, scanned all 6 projects for enabled keys matching `renames[].from` (zero stale — no sibling has `wr-problem`), and emitted the transparency line "No rename migrations applied this run." per the ADR-030 Confirmation amendment. |
| P057 | git mv + Edit + git add staging-ordering trap drops content edits | Closed 2026-04-22 (run-retro Step 4a). Rule exercised 4 times this session (P098 .open→.known-error, P098 .known-error→.verifying, P099 creation, P100 creation); explicit re-stage after Edit held contract — no content leaked to subsequent commits. |
| P062 | manage-problem README refresh on transitions | Already closed in a prior session (verified in AFK-iter-7); this run-retro removed a stale Verification-Queue row that had not been cleaned up at the prior closure. |
| P084 | work-problems iteration worker has no Agent tool — `claude -p` subprocess dispatch closes the tool-surface gap | Closed 2026-04-22 (run-retro Step 4a). Exercised end-to-end 4× across AFK iter 1–4: Agent tool present in each subprocess surface; architect + JTBD + risk-scorer delegations fired inside each subprocess; `/tmp/{architect,jtbd}-reviewed-<SID>` markers set natively; commits a1b9ee0, 0fae309, 8d28266, 5d367e9 landed from inside subprocess sessions. ADR-032 subprocess-boundary sub-pattern proven. |
| P036 | work-problems commit-gate: inter-iteration verification | Closed 2026-04-22 (run-retro Step 4a). Step 6.75 `git status --porcelain` fired between every iteration (pre-iter-2, pre-iter-3, pre-iter-4, pre-slice-2); classified correctly (clean or dirty-for-known-reason); never produced a false dirty halt. |
| P060 | push:watch anchors on HEAD sha + loops all runs + propagates exit code | Closed 2026-04-22 (run-retro Step 4a). push:watch executed 4× this session (post-P102 push, slice-1 drain, slice-1-fix drain, slice-2 drain); each CI run matched the pushed HEAD SHA (`gh run list --commit=$(git rev-parse HEAD)`); no stale-workflow success observed. |
| P054 | release:watch requires stable drift hash across push | Closed 2026-04-22 (run-retro Step 4a). release:watch drained 3 release PRs this session (itil 0.17.1, itil 0.17.2 + architect + jtbd patches, retrospective 0.8.0); auto-reducing-bypass fired on each closing-P100 / closing-P094 commit; drift-hash contract held. |
| P095 | UserPromptSubmit hooks re-emit full MANDATORY prose on every prompt | Closed 2026-04-22 (run-retro Step 4a). ADR-038 once-per-session + terse-reminder contract observed across ~15 user prompts this session: turn 1 emitted full ~4.2KB MANDATORY blocks for architect/JTBD/TDD; turns 2+ emitted ≤150-byte terse reminders. wr-tdd continued emitting dynamic state per-prompt per the ADR-038 carve-out. |
| P063 | manage-problem trigger-surface wired to /wr-itil:report-upstream (external root cause detection) | Closed 2026-04-24 (run-retro Step 4a). Exercised end-to-end: `/wr-itil:report-upstream P113 https://github.com/anthropics/claude-code` fired this session via P063's external-RCA trigger contract, filed anthropics/claude-code#52831, back-write to P113's closed ticket (commit 539e952). |
| P067 | /wr-itil:report-upstream classifier is problem-first (ADR-033 partial supersession of ADR-024 Steps 3 + 5) | Closed 2026-04-24 (run-retro Step 4a). For P113: classifier matched primary-problem tokens; template discovery fell through to anthropics/claude-code's `bug_report.yml` (ADR-033 backward-compat branch — upstream has no `problem-report.yml`); filed issue uses structured problem-shape content under the bug template — exactly the mixed primary-and-fallback path ADR-033 designs. |
| P092 | install-updates Step 4 `<plugin-short-name>` placeholder is ambiguous about the `wr-` prefix | Closed 2026-04-24 (run-retro Step 4a). `/install-updates` this session resolved `npm_name="@windyroad/${plugin_key#wr-}"` for all 11 plugin keys; `npm view` returned valid version for every one; only wr-itil flagged (0.18.0 → 0.18.1). No empty-version false-negative reproduced. |
| P094 | manage-problem does not refresh README.md on ticket creation | Closed 2026-04-24 (run-retro Step 4a). Every manage-problem-class commit this session kept docs/problems/README.md in-sync: 2be1bfa (P113 open), e14040b (P115 open + P113 RCA update), 245e09c (P113 closure), 25e06e5 (P114 closure), 539e952 (P113 upstream back-write). ADR-014 single-commit-transaction rule held across 5 commits. |
| P114 | `risk-scorer-structured-remediations.bats` bare-word `action_class` grep collides with "No structured action_class column" prose | Closed 2026-04-24. Verified in-session via observed green CI on every commit since the fix (97c976e, 38a0f7e, 8f6e89e, e14040b, 245e09c); release PR #64 auto-merged; `@windyroad/itil@0.18.1` published. Tests 699/700/701 flipped from `not ok` to `ok`. |
| P113 | `/wr-itil:report-upstream` installed and enabled but missing from Claude Code slash-command autocomplete | Closed 2026-04-24. User restarted + re-tested; `/wr-itil:report-upstream` now appears in autocomplete (screenshot confirmed). Actual RCA was NOT the shipped `Skill, Agent` allowed-tools fix (kept as a valid semantic improvement) — it was a stale `wr-itil@0.1.0` install pinned to abandoned git worktree `.claude/worktrees/upbeat-keller`; 0.1.0 had only 3 skills (no report-upstream); TUI autocomplete read first-matching-install while agent enumerator took union (explained the agent-sees-it / TUI-blind asymmetry). Per-machine fix: `(cd .claude/worktrees/upbeat-keller && claude plugin uninstall wr-itil@windyroad --scope project)` + `git worktree remove --force`. P115 tracks the systemic install-updates gap. |
| P107 | Architect + JTBD edit-gate markers expire during long multi-file edit batches | Closed 2026-04-23. Default TTL extended 1800s → 3600s across architect, jtbd, style-guide, voice-tone, risk-scorer gates + READMEs + ADR-009 + briefing files. Chosen fix: Option 1 (extend TTL) after investigation showed PreToolUse already implements sliding-window refresh; 1800s simply insufficient for 40+ minute orchestrator batches. |
| P108 | Scorer remediation action-class vocabulary — scorer writes prose; agent decides (ADR-042 Rule 2a) | Closed 2026-04-24 — design decision: the scorer's free-form `RISK_REMEDIATIONS:` prose + agent judgment is the intended contract; ADR-042 Rule 2a already defines the semantics; no code change needed. The ticket's own Fix Strategy section records this conclusion — this closure records it in the lifecycle. |
| P053 | work-problems surfaces outstanding design questions at stop-condition #2 | Closed 2026-04-24 (run-retro Step 4a). Orchestrator emitted `### Outstanding Design Questions` table with 6 user-answerable Qs (P070, P078, P082, P111, P110 follow-ups, P089 Gap 2 follow-up) before ALL_DONE sentinel — contract held end-to-end. |
| P077 | work-problems Step 5 delegates iterations via subprocess (subprocess-boundary variant of ADR-032) | Closed 2026-04-24 (run-retro Step 4a). 10/10 iteration `claude -p` subprocesses dispatched cleanly across iter1-iter10 — exit 0, valid JSON, ITERATION_SUMMARY block, governance gates ran inside subprocess at full depth, commits landed natively from subprocess sessions. |
| P086 | AFK iteration subprocess runs `/wr-retrospective:run-retro` before emitting ITERATION_SUMMARY (ADR-032 subprocess-boundary retro-on-exit) | Closed 2026-04-24 (run-retro Step 4a). 10/10 iteration `.result` fields contained `## Session Retrospective` section before ITERATION_SUMMARY. Iter 6 retro opened P118; iter 4 retro appended evidence to P111. Contract held end-to-end. |
| P103 | work-problems auto-applies above-appetite remediations instead of escalating (ADR-041 invariant) | Closed 2026-04-24 (run-retro Step 4a). First real-world exercise of ADR-042 auto-apply path: iter 3 hit residual 6/25, orchestrator did NOT call AskUserQuestion as escalation — auto-applied move-to-holding for wr-itil-p085 changeset, re-scored 6/6/6 → 3/3/0, drain proceeded. No user prompts during above-appetite resolution. |

## Parked

| ID | Title | Reason | Parked since |
|----|-------|--------|-------------|
| P005 | Connect setup skill doesn't match Discord plugin | Upstream: same --channels bug as P007/P008 — all connect work suspended | 2026-04-16 |
| P007 | Discord inbound reactions not delivered | Upstream: Discord channel plugin doesn't forward reaction events | 2026-04-16 |
| P008 | AskUserQuestion unavailable with --channels | Upstream: Anthropic `--channels` flag removes AskUserQuestion | 2026-04-16 |
| P115 | `/install-updates` does not detect stale plugin installs pinned to git worktrees under the current project | User direction 2026-04-26: "we don't use worktrees here" (twice). Project workflow does not use worktrees; systemic concern stays valid for adopters who do. | 2026-04-26 |
