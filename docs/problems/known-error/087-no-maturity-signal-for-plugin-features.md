# Problem 087: No maturity / battle-hardening signal on plugins, skills, agents, or hooks — READMEs don't tell users which features are stable vs experimental

**Status**: Known Error
**Reported**: 2026-04-21 (AFK iter-7 post-run, user observation)
**Priority**: 12 (High) — Impact: Significant (3) x Likelihood: Almost Certain (4)
**Effort**: L — requires: (a) an ADR defining the maturity taxonomy (Experimental / Alpha / Beta / Stable / Deprecated, or similar), promotion/demotion criteria, and where the signal lives (README badge, plugin.json frontmatter, manifest field, or runtime); (b) a measurement mechanism that actually computes the signal from observable evidence (not self-report); (c) surfacing the signal in every plugin's README, `claude plugin list` output, and the marketplace listing; (d) retroactive assessment for the 11 existing `@windyroad/*` plugins and every skill/agent/hook within them. Cross-cutting — touches every plugin, the marketplace manifest, and likely a new shared telemetry/metrics convention.
**WSJF**: 3.0 — (12 × 1.0) / 4 — High severity (users currently cannot distinguish `manage-incident` which has never fired in anger from `manage-problem` which has shipped hundreds of tickets this repo alone — adoption decisions are made blind); L effort because it's ADR-level cross-cutting + retroactive assessment across 11 plugins.
**Type**: technical

## Description

Observed 2026-04-21 (user direction, verbatim): *"the readme files give no indication of how robust or battle hardened each skill plugin feature is. For instance the mitigate-incident skill has never been used in anger, but the run-retro skill and manage-problem skill and the architect controls have been used hundreds if not thousands of times. How might we determine this?"*

Every `@windyroad/*` plugin's README today reads as if the plugin were uniformly mature. `manage-incident`'s newly-split `mitigate-incident` / `restore-incident` / `close-incident` / `link-incident` skills — shipped this session in `@windyroad/itil@0.15.0`, with 600+/600+ bats green but **zero real-world invocations** — look identical to `manage-problem` (several hundred real invocations), `run-retro` (dozens of real retros across this session alone), and the architect / JTBD / risk-scorer hooks (fired on every edit in every project for weeks).

A user evaluating which plugin to adopt — or which skill to invoke — has no signal to calibrate trust against. The existing `@windyroad/*` suite ships:

- **High-usage, well-exercised**: `manage-problem`, `run-retro`, `create-adr`, `wr-architect:agent`, `wr-jtbd:agent`, `wr-risk-scorer:pipeline`, `/install-updates`, `/push:watch`, `/release:watch`, architect + JTBD + TDD + risk-scorer PreToolUse/PostToolUse gate hooks.
- **Shipped but low-usage**: `mitigate-incident`, `restore-incident`, `close-incident`, `link-incident` (all just split this session, never fired in practice), `list-incidents`, `/wr-itil:report-upstream`, `/wr-itil:capture-problem` (not yet shipped per ADR-032 but on roadmap), `/wr-retrospective:capture-retro`, `/wr-architect:capture-adr`.
- **Shipped but niche**: `/discord:configure`, `/wr-connect:setup`, `/wr-c4:generate`, `/wr-wardley:generate`.
- **Shipped but near-zero-signal**: ADRs (11 plugins × multiple ADRs, all `status: proposed`, none `accepted` despite heavy exercise).

Users cannot distinguish any of these categories today.

## Symptoms

- Plugin READMEs uniformly present features in feature-list shape. No "Stability: Stable / Beta / Experimental" indicator. No "Invocations logged this release: N" indicator. No "Known issues in this skill's area: M" indicator.
- `claude plugin list` output is version-only. No maturity column.
- The marketplace manifest (`marketplace.json`) has no maturity field. Adopters pick plugins by title alone.
- `docs/problems/` backlog has ticket IDs but no aggregation per skill — a user scanning "how buggy is `manage-incident`?" has to eyeball titles.
- ADR `status: proposed` is binary and stale — everything is proposed; nothing gets promoted to `accepted` even when heavy exercise would warrant it.
- Newly-shipped skills (e.g. slice 6b/c/d shipped 20 minutes ago at the time of this ticket) look identical to skills that have been bullet-proofed over weeks.
- Users reading a 648/648 bats-green signal may mistake **structural doc-lint PASS** for **behavioural robustness**. The test count increases look impressive but don't distinguish "the skill has been exercised 1000× without bug" from "the skill has a well-formed SKILL.md".

## Workaround

User manually inspects git log / commit history / problem ticket citations to build a mental model of which features are mature. Defeats the signal-on-the-tin promise.

## Impact Assessment

- **Who is affected**:
  - **Plugin adopters (JTBD-101 Extend the Suite with Clear Patterns)** — "clear patterns" includes clarity about which features are safe to bet on. Without maturity signal, every plugin looks equally mature/unproven.
  - **Solo-developer persona (JTBD-003 Compose Only the Guardrails I Need)** — composing the suite requires knowing which features are stable enough to depend on. No signal = defensive over-compose or accidental under-compose.
  - **Contributors (JTBD-101)** — no signal for where to invest hardening effort. Low-usage features accumulate surface-level quality (tests pass) without real-world exercise.
  - **Tech-lead persona (JTBD-201 Restore Service Fast with an Audit Trail)** — audit trail requires knowing the runtime confidence level of every guardrail the audit depends on. Uniform presentation undermines audit confidence.
- **Frequency**: every plugin installation, every skill invocation decision, every architectural decision about which skill to rely on. Compounds as the suite grows.
- **Severity**: High. The plugin suite's credibility rests on users trusting the guardrails — and trust requires signal.

## Root Cause Analysis

### Structural

Plugin READMEs are authored from a "feature inventory" stance. There's no convention (no ADR, no shared template, no CI assertion) that requires a maturity indicator, a usage count, or an exercise-evidence cite. The marketplace manifest has no such field either. There's no telemetry layer — neither local nor opt-in — that would enable the signal to be computed rather than self-reported.

This is a **design-level gap**, not a single-plugin bug. The whole suite ships without a convention for "how battle-tested is this?" — so every new plugin inherits the gap and every existing plugin masks its own maturity.

### Measurement candidates

The core question from the user ("How might we determine this?") decomposes into (a) what to measure, (b) how to collect it, (c) how to surface it. Six candidate approaches, ranked by signal fidelity × implementation cost:

1. **Claude Code session-transcript analysis (observational, high-fidelity)** — parse `~/.claude/projects/*/sessions/*.jsonl` for `Skill` / `Agent` / `Bash npm run ...` tool invocations, grouped by skill name. Aggregate across all user sessions on the host. Pros: retrospective (works for already-shipped skills), real usage (not self-report), opt-in by definition (user owns the logs), deterministic counts. Cons: host-local only (won't aggregate across the plugin-user community), privacy-sensitive (transcripts contain user content), transcript format may change upstream. **Best fit for THIS repo's internal signal** — each adopter can run the analysis on their own history.

2. **Commit-history + problem-ticket heuristic** — count commits touching each plugin/skill's files (source + SKILL.md + bats + changesets) over the last N weeks; count closed problem tickets citing the skill; count days since first release. Emit a composite "exercise index". Pros: deterministic from git, no new infrastructure. Cons: commits-touching-the-file is a proxy for activity, not invocation; may over-credit skills that get churned vs skills that just work. **Good complement to option 1.**

3. **Explicit maturity badge in plugin.json frontmatter / README header** — per-plugin + per-skill "maturity: experimental | alpha | beta | stable | deprecated" with promotion criteria spelled out in an ADR. Each plugin owner maintains the badge. Pros: simple, immediately visible in `claude plugin list` if we extend the output. Cons: self-report is subjective; owners drift (everything becomes "beta" forever or everything becomes "stable" to avoid friction).

4. **Opt-in local telemetry hook** — PostToolUse hook on `Skill` invocations that writes a timestamped line to `~/.claude/metrics/windyroad-skill-invocations.jsonl`. A `/wr-itil:usage-report` skill aggregates. Pros: deterministic counts going forward, privacy-local. Cons: only forward-looking (doesn't help for already-shipped skills; option 1 does that), requires a new hook file per plugin that emits.

5. **GitHub Actions / CI-assertion signal** — a per-plugin CI job that asserts "this plugin has N% test coverage and at least M commits touching it in the last N days". Emits a badge in the README (shields.io-style). Pros: objective, automated, visible. Cons: CI coverage doesn't equate to battle-hardening; easy to game with test-churn.

6. **Community-usage signal** — count downloads from npm registry (weekly/monthly), stars on the repo, install-updates triggers per release. Pros: cross-community signal. Cons: npm download counts are noisy (CI runs, mirror traffic); stars aren't about usage. **Weakest on its own, useful as a multiplier.**

Recommended: **(1) + (2) + (3) combined**. Session-transcript analysis + commit-history heuristic gives retroactive and forward-looking deterministic signal; an explicit maturity badge (with promotion criteria tied to the objective signals) gives the user-facing presentation. Option (4) as a forward-looking complement once the infrastructure is drafted. Options (5) and (6) are optional multipliers, not primary.

### Promotion criteria (strawman, for the ADR)

An ADR defining the taxonomy should probably pin:

- **Experimental** — shipped within last 14 days; < 10 invocations observed; < 3 resolved problem tickets involving the skill's files.
- **Alpha** — 14-60 days shipped; 10-100 invocations; 3-10 resolved problem tickets; any breaking changes in the window.
- **Beta** — 60-180 days; 100-1000 invocations; 10+ resolved tickets; no breaking changes in last 30 days.
- **Stable** — 180+ days; 1000+ invocations; sustained low rate of new ticket creation (indicator of diminishing novelty); no breaking changes in last 90 days.
- **Deprecated** — author-declared; supersede-by path exists.

Thresholds tunable per plugin surface (e.g. AFK orchestrators accumulate invocations fast but ADRs accumulate slowly).

## Direction decision (2026-04-21, user — interactive AskUserQuestion)

**User question**: *"can we use /insights?"*

**Answer** (per `claude-code-guide` delegation 2026-04-21): `/insights` IS a Claude Code built-in slash command (documented at `code.claude.com/docs/en/commands.md` line 53 — *"Generate a report analyzing your Claude Code sessions, including project areas, interaction patterns, and friction points"*), BUT it does **not** report per-skill invocation counts. The docs are silent on per-skill breakdown, invocation counts by skill, and JSON/CSV export. `/insights` provides session-level friction-type data (Wrong Approach / Buggy Code / etc.) — a valuable but orthogonal signal to the "how many times has skill X been invoked" question.

**Direction**: combined approach leveraging `/insights` for what it gives plus a complementary mechanism for what it doesn't:

1. **`/insights` as session-friction signal** — use the existing command's output directly. Plugin authors can cite `/insights` output in their README ("this plugin surfaces in friction reports X% of sessions"). Zero new infrastructure; ships today.
2. **Session-transcript parser for invocation counts** — a new `/wr-itil:usage-report` (or `/wr-itil:skill-metrics`) skill parses `~/.claude/projects/*/sessions/*.jsonl` for `Skill` / `Agent` / `Bash npm run ...` tool invocations grouped by skill. Retrospective. Host-local. Output: per-skill counts over a windowed timeframe, sortable.
3. **Commit-history heuristic** — per-plugin composite: days-since-first-release, commits touching the plugin's files in last N weeks, resolved problem tickets citing the plugin, breaking-change-free window. Deterministic from git.
4. **Explicit maturity badge on each plugin README** — its value is **derived** from (1) + (2) + (3), NOT author self-report. ADR defines promotion criteria mapping the objective signals to bands (Experimental / Alpha / Beta / Stable / Deprecated).

**Not needed short-term**: opt-in telemetry hook (option 4 from RCA). Session-transcript parsing (item 2 above) is retrospective AND forward-looking — it reads the same jsonl files a telemetry hook would write to. No need to write to a second store.

**Follow-up for upstream**: open a `/feedback` or GitHub issue on `anthropics/claude-code` requesting per-skill invocation analytics either inside `/insights` output or as a machine-readable export from session JSONL. If that ships, item 2's custom parser becomes redundant. Tracked here as an investigation task; doesn't block P087's in-repo implementation path.

## Related

- **JTBD-003** (Compose Only the Guardrails I Need) — composing safely requires maturity signal.
- **JTBD-101** (Extend the Suite with Clear Patterns) — "clear patterns" includes clarity about stability.
- **JTBD-201** (Restore Service Fast with an Audit Trail) — audit confidence depends on knowing runtime-exercise level of each guardrail.
- **P082** (no voice-tone or content-risk gate on commit messages) — adjacent plugin-suite gap on the commit surface.
- **ADR-010** (skill-split naming / deprecation-window) — deprecation has a lifecycle entry in the maturity taxonomy.
- **ADR-022** (problem verification pending) — parallels "status lifecycle" concept this ticket proposes for skills.
- `README.md` files under every `packages/*/` — targets for the retroactive assessment.
- `marketplace.json` — target for a potential manifest field.
- `~/.claude/projects/*/sessions/*.jsonl` — evidence source for option 1.
- `docs/decisions/` — needs a new ADR (candidate title: "Plugin / skill / agent maturity taxonomy") before implementation.

### Investigation Tasks

- [x] Architect review on the fix shape: option taxonomy (which of the six to pursue), ADR-level vs SKILL.md-level assertions, retroactive assessment scope. (Phase 1 ADR-053 landed 2026-05-03; Phase 2 ADR-058 landed 2026-05-04 with architect YELLOW-with-adjustments verdict.)
- [x] Draft ADR defining the maturity taxonomy, promotion/demotion criteria, and the measurement mechanism. (Phase 1 ADR-053 covers taxonomy + promotion criteria; Phase 2 ADR-058 covers measurement mechanism.)
- [x] Prototype option 1 (session-transcript analysis) on this session's history to confirm signal fidelity. Produce a per-skill invocation count for the last 30 days and sanity-check against intuition ("manage-problem should be near the top; mitigate-incident should be near zero"). (Done 2026-05-03; output `docs/audits/p087-phase2-prototypes/option1-transcript-counts.txt`. Sanity check: PASS — `mitigate-incident` not in top 40, `manage-problem` 96, `run-retro` 149, `wr-architect:agent` 796.)
- [x] Prototype option 2 (commit-history heuristic) via `git log --name-only` + grep for each plugin's path. Produce a composite exercise index. (Done 2026-05-03; output `docs/audits/p087-phase2-prototypes/option2-commit-composite.txt`. itil index 4.11; retrospective 3.30; risk-scorer 3.14; architect 3.11.)
- [x] **Phase 2a — transcript axis shipped 2026-05-16 iter-2** (M-effort iter; commit forthcoming). `packages/itil/scripts/skill-invocations.sh` (canonical body, bash CLI parser + embedded Python 3 stdlib JSONL parser) + `packages/itil/bin/wr-itil-skill-invocations` (ADR-049 shim) + `packages/itil/scripts/test/skill-invocations.bats` (13 tests, 5 Confirmation criteria 1–5 from ADR-058 plus existence/executable/forward-extension/window-filter coverage; all green). NDJSON-per-surface output validated against live transcripts (top-of-list: `wr-risk-scorer:pipeline` 1454, `wr-architect:agent` 742, `wr-jtbd:agent` 673; matches the 2026-05-03 prototype intuition check).
- [x] **Phase 2b — git axis shipped 2026-05-16 iter-3 session 3** (M-effort iter; commit forthcoming). `packages/itil/scripts/plugin-exercise-index.sh` (canonical body, bash CLI parser + embedded Python 3 stdlib git-log parser) + `packages/itil/bin/wr-itil-plugin-exercise-index` (ADR-049 shim) + `packages/itil/scripts/test/plugin-exercise-index.bats` (19 tests covering ADR-058 §Confirmation criteria 6 (git-axis composite, including a fixture with literal `|` in commit subject per architect advisory), 7 (outside-git-repo + missing-packages), 8 (schema-version), plus opt-out / no-network / content-sanitisation / composite-index formula / breaking-marker / closed-tickets-window / window-days filter / category-overrides flag; all green). Runs `git log --reverse --name-only --pretty=format:%H|%aI|%s` once; window cutoff applied in-Python against `%aI` author-date (git's `--since=Nd` observed unreliable against future-year commit fixtures on 2026-05-16 — fail-soft refactor preserves the contract's NDJSON output shape verbatim while routing the date filter to Python). NDJSON-per-plugin output validated against live monorepo: itil composite_index 4.28 (prototype 4.11), retrospective 3.34 (prototype 3.30), risk-scorer 3.32, architect 3.22 — top-of-list matches 2026-05-03 prototype intuition. Performance 1.07s wall-clock on this workstation (boundary of ADR-058's ≤1.0s spec; not flagged as a reassessment trigger — `breaking_change_age_days=null` across all 13 plugins is expected absence of conventional-commit breaking markers in this codebase, not a script defect).
- [x] **Phase 2c — performance reassessment shipped 2026-05-16 iter-5 session 3** (S-effort iter, measurement + decision only — no script changes). Warm-cache `wr-itil-skill-invocations --window-days=30` measured 7.12s median (7.46 / 7.12 / 7.06 across three runs) against 5155 jsonl files (1.13 GB total, 380,898 lines) on this workstation (Apple M1 Pro, Darwin 25.3.0, Python 3.10.6) — confirms iter-2's 7.8s observation and **fires ADR-058 §Reassessment Triggers** (line 201: "Either script's worst-case cost grows above 5 seconds wall-clock on a warm-cache developer laptop: open a performance-tuning iter and re-quantify under ADR-023's Decision Outcome template"). Trigger margin: ~42% above the 5s threshold. **`wr-itil-plugin-exercise-index` warm-cache median 0.39s (0.39 / 0.52 / 0.39)** — well within ADR-058 line 128's ≤1.0s budget; no reassessment trigger fires on the git axis. Iter-3's 1.07s observation appears to have been a transient measurement (warm-cache git index now reflects the same monorepo from a settled state). Profile breakdown of the transcript-axis path: Python startup + `import json` 0.10s; `Path.rglob("*.jsonl")` enumeration alone 0.30s; rglob + per-file stat() 0.17s (variance-noise within enumeration cost); read every file no-parse 5.91s; full read + JSON parse 8.80s. **File I/O dominates** (~6s of the 7.12s wall-clock); enumeration negligible (0.30s); JSON parse adds ~3s. Optimization candidates surfaced — **tail-file filter** (skip files whose mtime is older than window — already in place, takes raw 8.80s parse-all to 7.12s observed); **binary-search to first in-window line** (JSONL is append-only; older lines = older invocations; current parse reads from byte 0 even when only the tail is in-window); **larger-window-tail file filter before parse** (read only the trailing N MB and parse). In-process mtime cache per ADR-058 line 131 carve-out gives ~0 gain within a single invocation (each invocation is fresh); disk-cache remains prohibited per ADR-053 §Confirmation #1. **Decision**: defer optimization to a new Phase 2d Investigation Task within P087 — keeps the optimization work WSJF-anchored to this ticket and within a single phasing pattern (2a → 2b → 2c → 2d) rather than splitting to a sibling ticket. ADR-058 amendment NOT shipped in this iter — the reassessment trigger is now firing with concrete numbers; the optimization iter that resolves the trigger re-quantifies under ADR-023's Decision Outcome template at THAT iter's landing, per the trigger language itself (ADR-058 line 201 "open a performance-tuning iter AND re-quantify" — re-quantify is the optimization iter's exit deliverable, not a separate pre-amendment).
- [x] **Phase 2d — transcript-axis performance optimization** (shipped 2026-05-17 iter-6 session 3). Substring pre-filter on `"tool_use"` discriminating-token added in `packages/itil/scripts/skill-invocations.sh` Python body before `json.loads()`; ~60% of in-window transcript lines short-circuit without paying the JSON parse cost (260,943 → 103,459 lines retained for full parse against the live corpus). Re-quantified warm-cache median 5.34s (5.32 / 5.34 / 5.44 across three runs) — 1.78s reduction (25%) from Phase 2c's 7.12s baseline. Still 0.34s above the 5s ADR-058 §Reassessment Triggers threshold (6.8% over). Substring-filter line is whitespace-tolerant (`"tool_use"` is a value token, not a key:value pair) and works against both compact and pretty-printed JSONL. False-positive fall-through invariant pinned by new bats fixture "Phase 2d: false-positive substring fall-through does not inflate counts" (14 tests now, all green). ADR-058 §Performance contract amended with the Decision Outcome — Phase 2d block per ADR-023 template; §Reassessment Triggers updated to show the trigger remains advisory-firing pending Phase 2e re-measurement.
- [x] **Phase 2e — binary-search-to-first-in-window byte-seek optimization** (shipped 2026-05-17 iter-7 session 3). Binary search on byte offset added in `packages/itil/scripts/skill-invocations.sh` Python body, gated at 256 KB threshold (files below threshold continue linear scan). Re-quantified warm-cache median **4.04s** (3.87 / 4.04 / 4.98 across three runs) against 5164 jsonl / ~1.08 GB corpus on this workstation — 1.30s reduction (24%) from Phase 2d 5.34s baseline; 3.08s reduction (43%) from Phase 2c 7.12s baseline. **The 5s ADR-058 §Reassessment Triggers threshold is now silenced** — warm-cache wall-clock sits 0.96s / 19% under budget. Cold-cache 7.81s (single run). NDJSON output identical to Phase 2d (235 records, same surface attribution and ordering). The bisect uses a cheap whitespace-tolerant byte-regex (`"timestamp"\s*:\s*"..."`) for per-probe timestamp extraction; readline-boundary alignment guarantees byte-safety; termination guaranteed by `hi = mid` on the in-window branch (line-aligned probes would stall under `hi = pos`). Four new bats fixtures (19 tests now, all green): byte-seek straddle correctness at the cutoff boundary, all-in-window large file (bisect falls through to offset 0), small-file linear-scan path correctness, non-monotonic graceful-degradation (architect advisory — synthetic violation under-counts gracefully without crash). ADR-058 §Performance contract amended with the Decision Outcome — Phase 2e block per ADR-023 template; §Reassessment Triggers updated to record trigger silencing. Input invariant (append-only monotonic timestamps within session jsonl) pinned in the same amendment.
- [x] **Phase 3 ADR-063 — presentation-layer contract shipped 2026-05-17 iter-7 session 4** (M-effort iter — contract-only; implementation deferred to Phase 3a/3b/3c follow-on sub-iters). `docs/decisions/063-plugin-maturity-presentation-layer.proposed.md` pins: (1) `plugin.json` `maturity:` field schema = rich record per-surface (`{schema_version: "1.0", band, computed_at, evidence: {invocations_30d, days_shipped, closed_tickets_window, breaking_change_age_days}}`) + string rollup on plugin root entry; (2) README badge rendering = **markdown text only** (no shields.io URL — external-dep blast radius + Bootstrapping clause compound rendering cannot be expressed in static badge URL + offline-broken), **prose-woven into value-framing lead prose** at plugin level (per ADR-051 anti-pattern; standalone header block REJECTED) + per-skill `Maturity` column in existing `## Skills` / `## How It Works` tables (band name only — compound bootstrapping rendering stays at rollup); (3) NEW `wr-itil-plugin-maturity-list` bin shim as Phase 3 contract (NOT deferred follow-on) — consumes `plugin.json` `maturity:` field from marketplace-cached path per ADR-003, NDJSON output with `schema_version: "1.0"`, exit-0-always per ADR-013 Rule 6, no Phase 2 NDJSON re-run; (4) marketplace.json extension **deferred** (YAGNI; canonical record already lives in `plugin.json`; reassessment trigger if upstream `/plugin browse` surfaces maturity-like fields); (5) `claude plugin list` extension **deferred to upstream** (filed alongside per-skill invocation analytics request); (6) Phase 3 sub-iters = **3a (population script) MUST precede 3b (renderer + advisory drift detector)** + **3c (bats doc-lint) ships alongside 3b or follow-on** — each a sibling problem ticket; (7) ADR-044 silent-framework carve-out for Phase 3a band recomputation, **scope-limited** (does NOT cover author-declared Deprecated, `supersededBy:` authoring, or Phase 4+ threshold tuning per P132 inverse-P078 trap); (8) ADR-057 Phase 3 (R6-gated escalation gate) remains deferred per ADR-058 §Phase 4+ (N=3, M=5) — distinct cluster axis from ADR-063 Phase 3 (presentation-layer rollout). Architect verdict YELLOW-with-adjustments (4 named adjustments folded in: schema_version pin, in-suite shim as Phase 3 contract not deferred, sub-iter ordering vs ADR-057 disambiguation, prose-weaving precedent enforcement). JTBD verdict YELLOW-with-adjustments (5 scope/wording + 4 JTBD outcome amendments queued for Phase 3 follow-on: JTBD-302 maturity-visibility outcome; JTBD-007 maturity-band-currency dimension; JTBD-101 promotion-criteria-visibility outcome per ADR-053 §Confirmation #4 deferred bullet; JTBD-003 at-glance-stability outcome). Ecosystem grounding per ADR-026: 10 prior-art citations (npm deprecate, GitHub archived, semver 0.x, Apache Incubating, TC39 stages, MDN BCD, Rust unstable, PEP 411, Linux CONFIG_EXPERIMENTAL removal, shields.io, VS Code preview) informed Decision Drivers + Considered Options.
- [x] **Phase 3a — population script shipped 2026-05-17 iter-9 session 4** (M-effort iter). `packages/itil/scripts/plugin-maturity-populate.sh` (canonical body, bash CLI parser + embedded Python 3 stdlib NDJSON parser + JSON writer) + `packages/itil/bin/wr-itil-plugin-maturity-populate` (ADR-049 shim) + `packages/itil/scripts/test/plugin-maturity-populate.bats` (17 tests covering ADR-063 §Confirmation criteria 1-3 — idempotency / bootstrapping vs steady-state band mapping / no `AskUserQuestion` per band recompute — plus schema-shape / rollup worst-case / hook null-sentinel / Deprecated-overlay preservation / fail-safe missing-input / no-network-primitive negative-grep; all green). Idempotent — re-running with pinned `--now=` produces byte-identical `plugin.json` output (architect adjustment H: testability override avoids field-exclusion logic). Schema extension: parallel top-level kind-keyed maps (`skills:`, `agents:`, `hooks:`, `commands:`) per architect adjustment A (NOT a flat `surfaces:` map — preserves Confirmation #10 kind-specific behavioural-test surface). Per-surface key normalisation table inlined in script header per architect adjustment B (skill → `wr-<plugin>:<dir-name>`; agent → `wr-<plugin>:<basename-without-.md>`; hook → not transcript-observable; command → `wr-<plugin>:<basename-without-.md>`). Hook surfaces emit `invocations_30d: null` sentinel per architect adjustment C — preserves "evidence axis n/a, not zero" semantics consistent with `breaking_change_age_days: null` precedent. Bootstrapping clause sunset auto-derives from `max(days_shipped across plugins) < 60` per architect adjustment D — no calendar-date hard-code (2026-06-06 anticipated lapse derived purely from data). Plugin-name attribution: filesystem path discovery (`packages/<bare-name>/`) accepts bare-form NDJSON `plugin:` field; plugin.json `name:` is prefixed `wr-<plugin>` but never matched against per architect adjustment F. Deprecated-overlay preservation per architect adjustment I: existing `band: "Deprecated"` records + `supersededBy:` pointer survive recompute verbatim (never overwritten by computed band; computed_at never refreshed); all other records get unconditional recompute-and-overwrite. AskUserQuestion negative-presence check case-insensitive per architect adjustment G. Phase 3a unblocks P238 (Phase 3b — renderer + drift detector); retroactive rollout across the live monorepo composes with Phase 3b. F9 in-suite display shim deliverable captured as new sibling ticket P244 per architect adjustment E (strict Phase 3a scope — F9 carved out so deliverable doesn't get lost).
- [x] **Phase 3b — README badge renderer + advisory drift detector shipped 2026-05-18 iter-8 session 6** (M-effort iter; `/wr-itil:work-problems` AFK orchestrator subprocess). `packages/itil/scripts/plugin-maturity-render.sh` (canonical body, bash CLI parser + embedded Python 3 stdlib JSON reader + README walker) + `packages/itil/bin/wr-itil-plugin-maturity-render` (ADR-049 shim) + `packages/itil/scripts/test/plugin-maturity-render.bats` (17 tests covering ADR-063 §Confirmation: prose-weaving + bootstrapping compound rendering + post-bootstrap band-only + per-skill column add + idempotency + badge-replacement + anti-pattern absence + fail-safe missing-maturity / missing-README + ADR-044 no-AskUserQuestion + ADR-035 no-network primitive + multi-plugin independence + dry-run preview; all green). Sibling drift detector `packages/retrospective/scripts/check-plugin-maturity-drift.sh` + `packages/retrospective/bin/wr-retrospective-check-plugin-maturity-drift` shim + `packages/retrospective/scripts/test/check-plugin-maturity-drift.bats` (14 tests covering 5 drift classes: missing-badge / stale-band / orphan-badge / anti-pattern-section / anti-pattern-url + clean fixture + multi-plugin aggregation + exit-0-always + ADR-035 no-network primitive + NDJSON output shape; all green). Renderer is idempotent — existing `*Maturity: ...*` spans are REPLACED not duplicated; second run produces byte-identical README. Compound rendering (`*Maturity: Experimental (suite-bootstrap window; <N> invocations / 30d).*`) stays at rollup per ADR-053 §Bootstrapping clause rendering; per-skill column cells carry band name only. Anti-patterns enforced: NEVER emits a standalone `## Maturity` section (anti-pattern absence asserted by bats); NEVER emits a shields.io URL / inline SVG. ADR-044 silent-framework carve-out honoured — band already computed by Phase 3a; renderer is mechanical and never invokes AskUserQuestion. ADR-035 privacy posture honoured — opt-out marker `.claude/.skill-metrics-opt-out` disables writes; no network primitive. Smoke test against live monorepo: 12 plugins / 0 drift instances (renderer correctly skips all 11 plugins lacking `maturity:` field; drift detector reports zero drift because rendered + canonical are both absent — coherent baseline). Architect verdict PASS (P087 Phase 3b architect review 2026-05-18 — scope soundness against ADR-053 / ADR-058 / ADR-063 / ADR-051 / ADR-049 / ADR-052 / ADR-013 Rule 6; HTML-comment-delimiter early adoption REJECTED per ADR-063 §Reassessment Triggers staged plan — prose-anchor first, escalate only on observed brittleness; single-iter cohesive 3b ship over 3b-i + 3b-ii split; per-skill column detect-and-add at right edge approved; drift-detector scope limited to badge-vs-record per P238 description). JTBD verdict PASS (JTBD-302 / JTBD-007 / JTBD-101 / JTBD-003 / JTBD-201 served via existing anchors; outcome-text amendments correctly deferred to Phase 3d / P240 to avoid premature JTBD churn). Phase 3b unblocks P087 closure path; remaining work = Phase 3c (P239 bats doc-lint) + Phase 3d (P240 JTBD amendments) + F9 in-suite display shim (P244) + retroactive mechanical rollout (Phase 3a + 3b runs against live monorepo with populated values committed). Effort line stays at L. P238 transitions Open → Verifying this iter.
- [ ] **Phase 3c — bats doc-lint per plugin**: asserts each plugin's `plugin.json` carries `maturity:` field per top-level entry; asserts rollup shape; asserts rollup band equals worst-case among constituents; asserts README prose-woven rollup matches canonical record; asserts anti-patterns absent (no standalone `## Maturity` section; no shields.io URL; no compound bootstrapping rendering in per-skill table cells). S-to-M-effort follow-on iter; ships alongside Phase 3b or as a follow-on. Sibling ticket to be captured under P087 follow-ons.
- [x] **Phase 3d — JTBD outcome amendments shipped 2026-05-18 iter-9 session 6** (S-effort iter; `/wr-itil:work-problems` AFK orchestrator subprocess). Four JTBD outcome-bullet amendments applied verbatim per ADR-063 §JTBD outcome amendments: JTBD-302 (`docs/jtbd/plugin-user/JTBD-302-trust-readme-describes-installed-behaviour.proposed.md`) — maturity-band visibility outcome with bootstrapping-window compound-evidence clause + ADR-053/ADR-058/ADR-063/P087 drivers; JTBD-007 (`docs/jtbd/solo-developer/JTBD-007-keep-plugins-current.proposed.md`) — currency-tracks-code-currency bullet extended with maturity-band currency as third dimension (recomputed by Phase 3a writer per ADR-044 silent-framework carve-out, rendered per ADR-063 §Phase 3b); JTBD-101 (`docs/jtbd/plugin-developer/JTBD-101-extend-suite.proposed.md`) — promotion-criteria-visibility outcome closing ADR-053 §Confirmation #4 deferred bullet, names the four evidence-record axes; JTBD-003 (`docs/jtbd/solo-developer/JTBD-003-compose-guardrails.proposed.md`) — at-glance stability outcome verbatim from ADR-063 line 293 (first maturity-aware outcome on JTBD-003). Architect verdict GREEN (no new ADR required; P132 inverse-P078 trap analysis passes — exact wording pinned in ADR-063; ADR-051 commit-hook drift detector targets `packages/<plugin>/README.md` only — JTBD files not in detector scope). JTBD verdict PASS (persona fit confirmed; amendment pattern matches existing precedent in JTBD-007/JTBD-302/JTBD-008/JTBD-301; all four amendments additive — no anchor citation removal). P240 transitions Open → Verifying this iter per ADR-022 P143 fold-fix amendment. No changeset triggered — JTBD files are `docs/jtbd/` documentation, not a published plugin surface (ADR-014 commit-grain doc-only carve-out).
- [x] **Retroactive mechanical rollout shipped 2026-05-18 iter-10 session 6** (S-effort iter; `/wr-itil:work-problems` AFK orchestrator subprocess). Ran `wr-itil-plugin-maturity-populate` against the live monorepo with NDJSON inputs from `wr-itil-skill-invocations --window-days=30` (252 records) + `wr-itil-plugin-exercise-index` (13 records); wrote 122 surfaces across 11 plugins. Then ran `wr-itil-plugin-maturity-render` to weave the rollup badge into each plugin's README value-framing lead prose line + add per-skill `Maturity` column to plugins with a `## Skills` section (itil only — architect / risk-scorer / others have `## Agent(s)` or section-less skill listing instead). 22 file changes total (11 `.claude-plugin/plugin.json` + 11 `README.md`). Bootstrapping=active (suite_oldest_days=39 < 60); `wr-architect:agent` → Alpha (provisional bootstrapping per ADR-053 §Bootstrapping clause — ≥100 invocations + ≥14 days met); all other surfaces + plugin rollups → Experimental per worst-case granularity contract. Drift detector `wr-retrospective-check-plugin-maturity-drift` reports 0 drift instances across all 12 packages — rendered badges match canonical records exactly. Anti-pattern absence verified (no standalone `## Maturity` section; no shields.io URL; no compound rendering in per-skill cells). Architect verdict PASS (P087 iter-10 architect review 2026-05-18, retroactive rollout — single ADR-014 commit covering 22 file changes + 11 patch-level changesets in a single multi-package `.changeset/*.md` file per ADR-021 changeset-grain; rejected the "11 separate .md files" framing in favour of multi-package frontmatter since the change IS cross-cutting and identical per-package). JTBD verdict PASS (mechanical activation of contract surfaces ADR-053 / ADR-063 already require; uniform Experimental rendering with `wr-architect:agent` Alpha as the differentiation signal is contract-correct per ADR-053 §Bootstrapping clause; rollout activates the four Phase 3d JTBD outcome amendments shipped in iter-9). **Compound-rendering gap observed**: render script's bootstrapping compound form (`*Maturity: <Band> (suite-bootstrap window; <N> invocations / 30d).*`) requires a `rollup_invocations_30d` field on the plugin root rollup that Phase 3a populate never writes (rollup schema is `{schema_version, band}` per ADR-063 §rollup schema). Renderer falls through to bare-band form. This is a Phase 3a/3b contract gap, not a rollout defect — captured as outstanding question for follow-on Phase 3 sub-iter triage. P087 effort line stays at L (Phase 3c P239 + F9 P244 + compound-rendering gap remain). P237 + P238 remain Verifying; this iter does NOT close them since closure is gated on a follow-on `/wr-itil:transition-problem` verification pass. No WSJF re-rate this iter (impact / likelihood / effort dimensions stable).
- [ ] Decide whether/how to ship option 4 (opt-in local telemetry hook) as a forward-looking complement. Per-plugin hook file or suite-wide.

**Iter-10 note (2026-05-18 session 6)**: Phase 3 retroactive mechanical rollout shipped (S-effort iter; `/wr-itil:work-problems` AFK orchestrator subprocess). Ran `wr-itil-plugin-maturity-populate` (with `wr-itil-skill-invocations --window-days=30` + `wr-itil-plugin-exercise-index` NDJSON inputs piped in) + `wr-itil-plugin-maturity-render` against the live monorepo — populated 122 surfaces across 11 plugins and rendered the rollup badge into each plugin's README value-framing lead prose line. Drift detector reports 0 drift instances across all 12 packages. One Alpha-bootstrapping surface (`wr-architect:agent` — meets ≥100 invocations + ≥14 days criterion); all other surfaces + plugin rollups → Experimental per worst-case granularity contract during the suite-bootstrap window (suite_oldest_days=39 < 60). Single ADR-014 commit + single multi-package `.changeset/*.md` file with 11 patch-level package bumps (one changeset file is the cleaner grain per ADR-021 since the change IS cross-cutting and identical per-package; the "11 separate files" framing was rejected by architect as unnecessary duplication). Compound-rendering gap observed but deferred — render expects `rollup_invocations_30d` field on plugin root rollup that populate never writes (rollup schema per ADR-063 is `{schema_version, band}` only); renderer falls through to bare-band form. This is a Phase 3a/3b contract gap, not a rollout defect — captured as outstanding question for follow-on Phase 3 sub-iter triage. Architect verdict PASS + JTBD verdict PASS — both verdicts assumed compound rendering works, but the rollout is contract-correct under the current renderer behaviour; remaining design choice is whether to amend populate (write `rollup_invocations_30d` to rollup) or amend renderer (derive on-fly during bootstrapping from per-surface evidence sum). P087 line-133 retroactive-rollout investigation task checked off; effort line stays at L (Phase 3c P239 + F9 P244 + compound-rendering gap remain). P237 + P238 stay Verifying; this iter does NOT close them — closure path is a follow-on `/wr-itil:transition-problem` verification pass once rollout-effects are observed in production use. No WSJF re-rate this iter.

**Iter-9b note (2026-05-18 session 6)**: Phase 3d JTBD outcome amendments shipped (S-effort iter; `/wr-itil:work-problems` AFK orchestrator subprocess). Four JTBD outcome-bullet amendments applied verbatim per ADR-063 §JTBD outcome amendments (lines 286-293): JTBD-302 maturity-band visibility (with bootstrapping-window compound-evidence clause), JTBD-007 maturity-band-currency as third currency dimension, JTBD-101 promotion-criteria-visibility (closes ADR-053 §Confirmation #4 deferred bullet), JTBD-003 at-glance stability. Architect verdict GREEN — no new ADR required; P132 inverse-P078 trap analysis passes since exact wording is pinned in ADR-063; ADR-051 commit-hook drift detector targets `packages/<plugin>/README.md` only — JTBD job files are not in detector scope so amendments do not introduce drift. JTBD verdict PASS — persona fit confirmed across plugin-user (JTBD-302), solo-developer (JTBD-007, JTBD-003), plugin-developer (JTBD-101); no scope-creep; amendment pattern `(Amended in P087 Phase 3)` matches existing precedent. All four amendments additive — no existing ADR / P-ticket / JTBD citations removed. P240 transitions Open → Verifying this iter per ADR-022 P143 fold-fix amendment. No changeset triggered (`docs/jtbd/` is documentation, not a published plugin surface — ADR-014 commit-grain doc-only carve-out). Effort line on P087 stays at L (Phase 3c P239 + F9 P244 + retroactive mechanical rollout remain). No WSJF re-rate this iter (impact / likelihood / effort dimensions stable).

**Iter-8 note (2026-05-18 session 6)**: Phase 3b — README badge renderer + advisory drift detector shipped (M-effort iter; `/wr-itil:work-problems` AFK orchestrator subprocess). `packages/itil/scripts/plugin-maturity-render.sh` (canonical body) + `packages/itil/bin/wr-itil-plugin-maturity-render` (ADR-049 shim) + 17 behavioural bats fixtures. Sibling `packages/retrospective/scripts/check-plugin-maturity-drift.sh` (canonical body) + `packages/retrospective/bin/wr-retrospective-check-plugin-maturity-drift` (ADR-049 shim) + 14 behavioural bats fixtures (5 drift classes covered). Renderer is idempotent; existing `*Maturity: ...*` spans are REPLACED not duplicated. Compound rendering stays at rollup; per-skill column cells carry band name only. Anti-patterns enforced (no standalone `## Maturity` section; no shields.io URL). ADR-044 silent-framework + ADR-035 privacy posture honoured. Architect verdict PASS — HTML-comment delimiter early adoption REJECTED per ADR-063 §Reassessment Triggers staged plan (prose-anchor first, escalate only on observed brittleness); single-iter cohesive 3b ship over 3b-i + 3b-ii split. JTBD verdict PASS — outcome-text amendments correctly deferred to Phase 3d / P240 to avoid premature JTBD churn. Smoke test against live monorepo: 12 plugins / 0 drift instances (renderer correctly skips all 11 plugins lacking `maturity:` field per Phase 3a not-yet-rolled-out state; drift detector reports zero drift because rendered + canonical are both absent — coherent baseline). Two changesets: `@windyroad/itil` minor (renderer) + `@windyroad/retrospective` minor (drift detector). P238 transitions Open → Verifying this iter; P237 (Phase 3a script) also folded Open → Verifying since Phase 3a script shipped in commit b840a7a 2026-05-17 and ticket has root cause + fix strategy + workaround documented per ADR-022 P143 fold-fix amendment. Effort line on P087 stays at L (Phase 3c P239 + Phase 3d P240 + F9 P244 + retroactive mechanical rollout remain). No WSJF re-rate this iter (impact / likelihood / effort dimensions stable). Spurious reconcile drift observed at iter start (31 STALE entries from stale `wr-itil-reconcile-readme` shim on `$PATH` pointing at 0.32.1 — P233 cluster recurrence pattern; canonical source AND 0.34.0 cached script both report clean; logged as outstanding observation; install-updates churn at next release cycle resolves per P233 Phase 1 chain).

**Iter-9 note (2026-05-17 session 4)**: Phase 3a population script shipped (M-effort iter). `packages/itil/scripts/plugin-maturity-populate.sh` (canonical body) + `packages/itil/bin/wr-itil-plugin-maturity-populate` (ADR-049 shim) + `packages/itil/scripts/test/plugin-maturity-populate.bats` (17 tests covering ADR-063 §Confirmation criteria 1-3 plus schema-shape / rollup worst-case / hook null-sentinel / Deprecated-overlay preservation / fail-safe missing-input / no-network-primitive negative-grep; all green). Approach was the architect-endorsed plan (P087 iter-9 architect review 2026-05-17, YELLOW-with-9-adjustments verdict — all 9 folded in). Architect adjustments: A (parallel top-level kind-keyed maps over flat surfaces map — preserves Confirmation #10 kind-specific behavioural-test surface); B (surface-key normalisation table inlined in script header); C (hook `invocations_30d: null` sentinel — not transcript-observable; band derived from git axis only; same precedent as `breaking_change_age_days: null`); D (bootstrapping sunset auto-derived from `max(days_shipped across plugins) < 60` — no calendar-date hard-code; 2026-06-06 anticipated lapse derives from data); E (Phase 3a scope strict; F9 in-suite display shim deliverable captured as new sibling ticket P244 so deliverable doesn't get lost); F (plugin-name attribution by filesystem path discovery — accepts bare-form NDJSON input; never matches against prefixed plugin.json `name:` field); G (no-AskUserQuestion check case-insensitive); H (`--now=ISO` testability override for idempotency byte-equality — avoids field-exclusion logic in bats); I (Deprecated-overlay preservation — existing `band: "Deprecated"` + `supersededBy:` pointer never overwritten by recompute; all other records get unconditional recompute-and-overwrite). JTBD review PASS (Phase 3a is writer-substrate per ADR-063 §Phase 3 sub-iter ordering — JTBD-302 reactivation surface lands at Phase 3b, not 3a; JTBD-201 served directly by rich-record `evidence:` audit-trail; JTBD-101 + JTBD-007 + JTBD-003 served via existing 2026-05-04 + Phase 3d-queued amendments; no Phase 3a-specific JTBD amendments triggered beyond the four already queued in ADR-063 §JTBD outcome amendments). Smoke test against live monorepo: 11 plugins × 121 surfaces written; bootstrapping=active (suite_oldest_days=39 < 60); `wr-architect:agent` → Alpha (736 invocations + 39 days_shipped — meets provisional Alpha bootstrapping criteria); plugin rollups → Experimental (worst-case across hook surfaces dominates per Confirmation #10 granularity contract); idempotency confirmed via pinned `--now`; populated values reverted from commit (iter scope strict per architect §E — retroactive rollout composes with Phase 3b per P087 line 133). Effort line stays at L (Phase 3b + 3c + 3d + F9 implementation work remains across P238 / P239 / P240 / P244). No WSJF re-rate this iter (impact / likelihood / effort dimensions stable). README refresh fires per P165 hook on P244 capture commit; refresh logged inline.

**Iter-5 note (2026-05-16)**: AFK orchestrator selected this ticket per WSJF priority; the iter-5 briefing was authored from stale state (described Phase 1 as pending). Phase 1 ADR-053 + Phase 2 ADR-058 already landed on 2026-05-03 / 2026-05-04. No ADR-level work remained in iter-5 scope. The next-natural-slice (Phase 2 implementation) is M-to-M+ effort and was deferred to a future iter with explicit scope-fit handoff. This entry surfaces it as a discrete Investigation Task so subsequent iter scoping is accurate.

**Iter-2 note (2026-05-16 session 3)**: Phase 2a transcript-axis shipped (`wr-itil-skill-invocations` script + shim + 5 Confirmation-criteria bats fixtures all green; 13 tests total). Performance observation against the live corpus on this workstation: 7.8s warm-cache wall-clock (5157 jsonl / 1.1 GB) — exceeds the 5s ADR-058 reassessment threshold by ~50%; Phase 2c performance-tuning iter queued as a discrete Investigation Task. Phase 2b (git axis) remains scoped at M-effort, sibling iter. ADR-058 Phase 4+ escalation gate's (N, M) criterion is unchanged; performance reassessment does not invalidate the (3, 5) numeric values.

**Iter-7 note (2026-05-17 session 4)**: Phase 3 ADR-063 — presentation-layer contract shipped (M-effort iter; contract-only — implementation deferred to Phase 3a/3b/3c follow-on sub-iters). `docs/decisions/063-plugin-maturity-presentation-layer.proposed.md` pins the `plugin.json` `maturity:` field schema (rich record per-surface + string rollup; `schema_version: "1.0"` per ADR-058 §Confirmation #8 precedent), README badge rendering format (markdown text only, prose-woven into value-framing lead prose per ADR-051 anti-pattern, per-skill Maturity column in existing Skills tables), `wr-itil-plugin-maturity-list` in-suite display shim (NDJSON output consumer-stable contract, reads marketplace-cached `plugin.json` per ADR-003), marketplace.json defer (YAGNI), `claude plugin list` defer (upstream), Phase 3 sub-iter ordering (3a → 3b → 3c), ADR-044 silent-framework carve-out scope (band recomputation only; NOT covering author-declared Deprecated / `supersededBy:` authoring / Phase 4+ threshold tuning per P132 inverse-P078 trap), and the explicit disambiguation of ADR-063 Phase 3 vs ADR-057 Phase 3 (different cluster axes that share the phase number — ADR-057 Phase 3 = R6-gated escalation, remains deferred per ADR-058 §Phase 4+ N=3, M=5). Architect verdict YELLOW-with-adjustments (4 adjustments: schema_version pin / in-suite shim as Phase 3 contract not deferred / sub-iter ordering vs ADR-057 disambiguation / prose-weaving precedent enforcement — all folded in). JTBD verdict YELLOW-with-adjustments (5 scope/wording + 4 JTBD outcome amendments queued for Phase 3 follow-on — JTBD-302 / JTBD-007 / JTBD-101 / JTBD-003 — all folded into ADR-063 follow-on capture). Ecosystem grounding per ADR-026: 10 prior-art citations (npm deprecate, GitHub archived, semver 0.x, Apache Incubating, TC39 stages, MDN BCD, Rust unstable, PEP 411, Linux CONFIG_EXPERIMENTAL removal, shields.io trade-offs, VS Code preview boolean + vscode#162651 freshness-bug lesson) informed Decision Drivers + Considered Options. Effort line stays at L (Phase 3a + 3b + 3c + 3d implementation work remains — each sibling ticket follow-on). No WSJF re-rate this iter (impact / likelihood / effort dimensions stable — contract-only iter does not move the ticket's residual scope). No README refresh trigger fires per Step 6's conditional refresh contract (no lifecycle transition; Effort unchanged; WSJF unchanged). **Phase 3 contract is now complete**; remaining work is Phase 3a (population script — M) + Phase 3b (renderer + drift detector — M) + Phase 3c (bats doc-lint per plugin — S-to-M) + Phase 3d (JTBD outcome amendments — S), captured as discrete sibling tickets under P087 follow-ons via `/wr-itil:capture-problem`. After Phase 3a + 3b + 3c land and the retroactive assessment populates the canonical fields + renders the badges, P087 transitions to Verifying.

**Iter-7 note (2026-05-17 session 3)**: Phase 2e performance optimization shipped — binary-search-to-first-in-window byte-seek added alongside the Phase 2d substring filter in `packages/itil/scripts/skill-invocations.sh` Python body. Approach was the architect-endorsed staged plan completion (P087 iter-7 architect review 2026-05-17, GREEN verdict — explicit "no new ADR required; within the explicit Phase 2e slice ADR-058 line 131 reserves and the reassessment trigger line 205 sanctions"). Bisect gated at 256 KB threshold (files below scan linearly from byte 0 — bisect overhead is not worth it for small files; size distribution survey showed ~88% of corpus files are under 256 KB but account for only ~8% of total bytes, so threshold-gating lets the optimization target the 12% of files holding 92% of read cost). Re-quantified warm-cache median **4.04s** (3.87 / 4.04 / 4.98 across three runs) against 5164 jsonl / ~1.08 GB corpus on this workstation — 1.30s reduction (24%) from Phase 2d 5.34s; 3.08s reduction (43%) from Phase 2c 7.12s baseline. Cold-cache 7.81s (single run). **The 5s ADR-058 §Reassessment Triggers threshold is now silenced** — warm-cache sits 0.96s / 19% under budget with measurement headroom for further corpus growth. NDJSON output identical to Phase 2d (235 records, same surface attribution and ordering; spot-check top-5 unchanged: `wr-risk-scorer:pipeline` 1454, `wr-architect:agent` 737, `wr-jtbd:agent` 668, `wr-risk-scorer` 375, `wr-itil-reconcile-readme` 284). Bisect implementation uses a whitespace-tolerant byte-regex (`"timestamp"\s*:\s*"..."`) for per-probe timestamp extraction, readline-boundary alignment for byte-safety, and `hi = mid` on the in-window branch for termination correctness (an earlier `hi = pos` formulation stalled on line-aligned probes; debug trace identified the loop, fix was a one-line tighten-rule change). Four new bats fixtures (19 tests now, all green): byte-seek straddle correctness, all-in-window large file (bisect falls through to offset 0), small-file linear-scan path correctness, non-monotonic graceful-degradation per architect advisory. Input invariant pinned in ADR-058 Phase 2e amendment: append-only monotonic timestamps within session jsonl files; synthetic violation under-counts gracefully without crash. JTBD review GREEN (JTBD-101 hardening-prioritisation outcome served — script now under the 5s threshold; JTBD-003 / JTBD-201 unaffected). WSJF unchanged this iter (perf-only optimization; impact / likelihood / effort dimensions stable). Effort line stays at L (Phase 3 mechanical rollout still remains). No README refresh trigger fires per Step 6's conditional refresh contract (no lifecycle transition; Effort unchanged; WSJF unchanged). **Phase 2 (transcript axis) is now performance-complete** — Phase 2a transcript-axis script + Phase 2b git-axis script both shipped and both under their respective ADR-058 performance budgets (transcript 4.04s under 5s, git 0.39s under 1.0s). Remaining work on P087 is Phase 3 (mechanical rollout: retroactive assessment + README badge rendering + `plugin.json` `maturity:` field population per ADR-053 §Decision Outcome) and the tail-end Investigation Tasks (presentation-layer detailed format design, bats doc-lint per plugin README, optional opt-in telemetry hook decision).

**Iter-6 note (2026-05-17 session 3)**: Phase 2d performance optimization shipped — substring pre-filter on the `"tool_use"` discriminating token added in `packages/itil/scripts/skill-invocations.sh` Python body. Approach was the architect-endorsed staged plan (P087 iter-6 architect review 2026-05-17, GREEN verdict): single 5-line diff, ADR-058 in-process-cache-OK carve-out honoured, ADR-053 §Confirmation #1 disk-cache prohibition honoured (the filter is per-line in-process, no cache at all), NDJSON output schema unchanged (`schema_version` stays 1.0). Re-quantified warm-cache median 5.34s against the same 5155 jsonl / 1.13 GB / 380,898 lines corpus — 1.78s reduction (25%) from Phase 2c's 7.12s baseline; matched the architect's expected-outcome ~5.3s prediction within 0.04s. The 5s ADR-058 reassessment threshold remains marginally exceeded (0.34s / 6.8% over) — Phase 2e binary-search-to-first-in-window queued as a discrete Investigation Task to close the residual gap; the architect-endorsed staged plan called this scenario out explicitly ("If 5.3s still exceeds the 5s threshold, the amendment records the gap and queues the binary-search fallback as Phase 2e"). False-positive fall-through invariant pinned by new bats fixture; 14 tests now green (up from 13). ADR-058 §Performance contract amended with the Decision Outcome — Phase 2d block per ADR-023 template + §Reassessment Triggers updated to show the trigger remains advisory-firing pending Phase 2e re-measurement. JTBD review GREEN (JTBD-101 hardening-prioritisation outcome served — faster script = signal users actually consume; JTBD-003 / JTBD-201 unaffected). WSJF unchanged this iter (perf-only optimization; impact / likelihood / effort dimensions stable). Effort line stays at L (Phase 2e ~S-to-M + Phase 3 mechanical rollout remains). No README refresh trigger fires per Step 6's conditional refresh contract (no lifecycle transition; Effort unchanged; WSJF unchanged).

**Iter-5 note (2026-05-16 session 3)**: Phase 2c performance reassessment shipped — investigation-only iter, no script changes. Confirms iter-2's 7.8s observation (now 7.12s median on 5155 jsonl / 1.13 GB / 380,898 lines, Apple M1 Pro, Python 3.10.6). Profile shows file I/O dominates: 5.91s read-no-parse → 8.80s read+parse, with tail-file mtime filter taking actual observed cost from 8.80s to 7.12s. `wr-itil-plugin-exercise-index` is healthy at 0.39s median (well within ADR-058 line 128 ≤1.0s budget; iter-3's 1.07s appears to have been transient). ADR-058 §Reassessment Triggers fires on the transcript axis; performance optimization deferred to a new Phase 2d Investigation Task within P087 (sibling pattern to Phase 2a / 2b / 2c rather than splitting to a separate ticket). ADR-058 amendment held until the Phase 2d optimization iter — trigger language ADR-058 line 201 specifies "open a performance-tuning iter AND re-quantify"; re-quantification is the optimization iter's exit deliverable. Effort line unchanged at L (Phase 2d ~M + Phase 3 mechanical rollout remains); no WSJF re-rate this iter; no README refresh trigger fires per Step 6's conditional refresh contract.

**Iter-3 note (2026-05-16 session 3)**: Phase 2b git-axis shipped (`wr-itil-plugin-exercise-index` script + shim + 19 bats fixtures covering ADR-058 §Confirmation criteria 6–8 plus opt-out / no-network / composite-formula / breaking-marker / closed-tickets-window / window-filter / category-overrides; all green). One non-trivial design diff from the ADR §Script contracts wording: the literal `git log --since=<window>d` form (ADR-058 line 89) was observed empirically unreliable against the bats fixture environment on 2026-05-16 — `git log --since='Apr 1 2026'`, `--since='60 days ago'`, `--since=60d` all returned empty against a 3-commit linear-history fixture with valid GIT_AUTHOR_DATE / GIT_COMMITTER_DATE values 10/90/120 days ago. The script preserves the documented `git log --name-only --pretty=format:%H|%aI|%s` invocation verbatim but routes the window cutoff to Python (against the `%aI` author-date field already extracted for `breaking_change_age_days` and `days_shipped`). This collapses the two-pass design (in-window pass + all-history pass) into a single git-log pass with in-Python filtering — both more efficient (1 git fork instead of 2) and more robust (resistant to git's date-parser quirks). Confirmation #6's NDJSON shape and field semantics are unchanged. The `days_shipped` field tracks `min(author_date)` per plugin (rather than topology-order "first commit touching plugin path") — also more robust against commit reordering / cherry-pick / rebase that can decouple commit-topology order from author-date order; the ADR-058 text "the OLDEST git commit touching `packages/<plugin>/`" is implementation-faithful under either reading, with `min(author_date)` chosen for portability. ADR-058 amendment NOT required — implementation discretion within the documented contract's tolerance, captured here for audit trail. Smoke-tested against live monorepo: itil 4.28 (prototype 4.11), retrospective 3.34 (prototype 3.30), risk-scorer 3.32, architect 3.22 — matches Phase 2 prototype output (drift accounted-for by elapsed time + new commits since 2026-05-03 prototype). Performance 1.07s warm-cache on this workstation (boundary of ADR-058's ≤1.0s spec; well under the 5s reassessment trigger). `breaking_change_age_days=null` across all 13 plugins reflects this codebase's preference for prose-conventional-commit form over `feat!`/`fix!`/`BREAKING` markers (an artefact of habits, not a script defect — the regex is correct).

## Decision record

**Phase 1 ADR landed 2026-05-03**: ADR-053 (`docs/decisions/053-plugin-maturity-taxonomy.proposed.md`) pins the five-band taxonomy (Experimental / Alpha / Beta / Stable / Deprecated), promotion/demotion criteria stated in objective-signal-shaped terms (days-shipped, invocation count, resolved problem-ticket count, breaking-change-free window), and dual-location signal (canonical: `plugin.json` `maturity:` field per surface; rendered: README header badge alongside ADR-051 JTBD anchor). Phase 1 ships only the ADR — no script, no hook, no CI assertion.

**Phase 2 ADR landed 2026-05-04**: ADR-058 (`docs/decisions/058-plugin-maturity-measurement-mechanism.proposed.md`) pins TWO read-only diagnostic scripts shipping under `packages/itil/`: `wr-itil-skill-invocations` (transcript axis — reads `~/.claude/projects/*/sessions/*.jsonl` → NDJSON per surface with per-window invocation count) and `wr-itil-plugin-exercise-index` (commit-history axis — `git log --since=60d --name-only` + problem-ticket aggregation → NDJSON per plugin with composite exercise index). Phase 2 is exit-0-always per ADR-013 Rule 6 fail-safe; reads only host-local data per ADR-035 privacy clauses (opt-out marker, no exfiltration, content-sanitisation, path-hashing, no-network-primitive bats assertion); composes with ADR-049 shim grammar; ADR-052-behavioural confirmation; ADR-023-quantified performance (≤ 1.5s warm-cache cost). Phase 2 ALSO amends ADR-053 in place with a "Bootstrapping clause" (everything Experimental for 60 days; provisional Alpha for ≥100 invocations + ≥14 days; auto-lapse 2026-06-06) AND extends JTBD-101 with a hardening-prioritisation desired-outcome bullet. Phase 4+ escalation gate's (N, M) numeric criterion pinned: N=3 consecutive releases, M=5 surfaces drifted, drift defined as Phase-3-computed band ≠ persisted `plugin.json` band. Prototype outputs preserved under `docs/audits/p087-phase2-prototypes/` for audit-trail; sanity check passes (`mitigate-incident`/`restore-incident`/`close-incident`/`link-incident` all score zero invocations; `wr-architect:agent` 796, `wr-jtbd:agent` 638, `wr-risk-scorer:pipeline` 1147 at top).

**Phase 3 ADR landed 2026-05-17 (session 4 iter-7)**: ADR-063 (`docs/decisions/063-plugin-maturity-presentation-layer.proposed.md`) pins the presentation-layer contract — `plugin.json` `maturity:` field schema (rich record per-surface + string rollup); README badge rendering (markdown text only, prose-woven into value-framing lead prose per ADR-051 anti-pattern, per-skill column in Skills tables); `wr-itil-plugin-maturity-list` in-suite display shim (NDJSON output, `schema_version: "1.0"`); marketplace.json + `claude plugin list` extensions deferred (YAGNI + upstream); ADR-044 silent-framework carve-out scope-limited for Phase 3a band recomputation. Phase 3 implementation decomposes into 3a (population script — M-effort) → 3b (renderer + advisory drift detector — M-effort) → 3c (bats doc-lint per plugin — S-to-M-effort) + 3d (JTBD outcome amendments — S-effort). Each is a sibling problem ticket captured under P087 follow-ons.

**Phase 3 implementation (sub-iters — pending)**: 3a population script → 3b renderer + drift detector → 3c bats doc-lint → 3d JTBD amendments. The retroactive assessment across all 11 `@windyroad/*` plugins is the natural outcome of Phase 3a + 3b runs against the live monorepo; the JTBD-101 promotion-criteria-visibility bullet recorded in ADR-053 §Confirmation #4 is folded into Phase 3d. Advisory drift detector (Phase 3b) is exit-0-always per ADR-013 Rule 6, sibling to ADR-051's `check-readme-jtbd-currency.sh`.

**Phase 4+ (escalation — conditional)**: ADR-013 Rule 6 escalation from advisory to release-blocking gate iff drift accumulates across N consecutive releases (initial proposal: three) without correction.

This ticket transitions to Known Error on the Phase 1 landing. It re-enters work for Phase 2 (separate iter; measurement-mechanism ADR + prototype) and again for Phase 3 (retroactive assessment + README integration). It closes only after Phase 3 ships — at which point every `@windyroad/*` plugin's README carries a maturity signal and the canonical `plugin.json` field is populated for every surface.
