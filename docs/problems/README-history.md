# `docs/problems/README.md` — Last-reviewed line history

> Forward-chronology archive of the `docs/problems/README.md` line-3 "Last reviewed" annotation. Newest fragment at the bottom under a `## YYYY-MM-DD` heading. This is **log territory** — grep / tail are the natural read paths. The README's display-tier line 3 carries only the most-recent fragment per the P134 truncation discipline; this file carries the displaced history.
>
> See `manage-problem` SKILL.md Step 5 "Last-reviewed line discipline (P134)" for the contract that writes here. Hard ceiling on line 3 (5120 bytes) is surfaced advisory-only by `packages/itil/scripts/check-problems-readme-budget.sh` per ADR-040 line 92's reusable accumulator-doc pattern (sibling to P099's `check-briefing-budgets.sh` on `docs/briefing/<topic>.md`).

## 2026-04-28 (pre-P134 truncation contract — bulk legacy archive)

The 76,582-byte line-3 prose blob accumulated from session-summary fragments prepended on every `manage-problem` Step 5 P094 / Step 7 P062 / `review-problems` Step 5 / `reconcile-readme` Step 5 refresh under the original "ever-growing prose paragraph" convention. P134 closes the accumulator on this surface; the truncated content is preserved verbatim below as the seed of the forward-chronology archive. Future refreshes append per-day fragments at the bottom, NOT into this legacy entry.

Last reviewed: 2026-04-28 **AFK iter 7 — P139 transitioned Open → Verification Pending; repo-local skill source-of-truth relocated `.claude/skills/install-updates/` → `scripts/repo-local-skills/install-updates/` with relative symlinks at the original path; ADR-030 amended (source-of-truth + resolution-layer split, Symlink Contract section, Reassessment trigger); 11 new symlink-contract bats; WSJF Rankings shrinks by one row, Verification Queue gains P139 at the tail of the 2026-04-28 cluster**. Earlier 2026-04-28 — P123 transitioned Known Error → Verification Pending; ADR-046 `proposed → accepted` (audit-log-only v1 slice landed: `packages/itil/hooks/lib/block-list.sh` + `docs/blocked-reporters.json` + sibling JSONL audit log + 10 behavioural bats; full itil hooks suite 95/95 green). Earlier 2026-04-28 retrospective — P058 / P112 / P119 closed via in-session evidence (regex-digit bats / retry-rollback bats / hook-deny exercise); P124 transitioned Verifying → Known Error with regression evidence (alphabetical-first stale-marker selection broke under 103-stale-announce-marker /tmp accumulation; brute-force workaround applied, fix candidates outlined in the new regression block); P124 then re-transitioned Known Error → Verifying same-day via Phase 3 mtime-selection fix (within-system selection `ls -t | head -1` per ADR-038 write-once announce-marker contract; new bats test 8 asserts newest-mtime UUID wins over older same-system markers; architect PASS-WITH-NOTES + JTBD-001 PASS); P139 unchanged. **P139 opened** — repo-local skills under `.claude/skills/` pay architect-gate overhead on every edit; user direction: relocate source-of-truth outside `.claude/` and symlink in for resolution; WSJF 6.0 (Severity 12 / Effort M); composes-with P131 (.claude/ ownership boundary); inserted at WSJF 6.0 tier (after P082 Open). **P131 known-error transition** — Phase 1 (declarative CLAUDE.md MANDATORY rule "never write project-generated artefacts under `.claude/`" alongside existing P085/P078 entries) + Phase 3 light-touch (clarifier blocks added to architect + JTBD Turn-1 emission text reframing exclusion list as READ tolerance only with explicit "agents must not write project-generated artefacts under `.claude/` (P131)" guidance + inline source comments on `*/.claude/plans/*.md) exit 0 ;;` rules in architect-enforce-edit + jtbd-enforce-edit hooks) shipped via `/wr-itil:work-problems` AFK iter; Phase 2 (load-bearing PreToolUse:Write|Edit enforcement hook denying agent writes under `.claude/` + approval-marker pattern composing with ADR-009 + behavioural bats coverage) DEFERRED to follow-on iteration; status remains Known Error per ADR-022 lifecycle (root cause confirmed, fix path clear, partial fix shipped, load-bearing piece not yet implemented — Verifying is reserved for "fix fully shipped, awaiting validation"); architect verdict ALIGNED no new ADR required this iteration (formal ADR codifying user-space vs project-space distinction recommended once Phase 2 lands); JTBD verdict PASS — JTBD-001 primary fit (declarative governance, no new gate friction), JTBD-101 composes (downstream adopters of windyroad gate hooks inherit clearer semantics); WSJF 4.5 unchanged (Open M → KE M, status multiplier 1.0 → 2.0 cancels with effort divisor 2 → 4 doubling on additional Phase 2 effort estimate, leaving the row at WSJF 4.5 within the same tier; Known-Error-first ordering keeps P131 at the top of the WSJF 4.5 tier above P132/P133); P063 strict-detection regex tripped on the "upstream territory" reference in the Out-of-scope block — AFK fallback per ADR-013 Rule 6 appended pending-upstream-report marker (false-positive signal flagged in marker text — the reference is "out-of-scope framing" not a missed dependency; user may downgrade if confirmed not actionable upstream); two investigation tasks remain (Phase 2 enforcement-hook approval-marker shape, Phase 2 enforcement-hook bats coverage) tracked inline in the ticket. Prior: 2026-04-28 **P130 known-error transition** — root cause confirmed: work-problems Step 5 dispatch is mechanically uniform `claude -p` subprocess invocation across every iter regardless of user presence; the monomodality is a cumulative artefact of P077/P084/P121 amendments that each correctly closed an AFK-mode gap without considering the interactive-mode surface; refined RCA surfaces an alternative reframing (enrich the orchestrator's observation surface via `--output-format stream-json` + live tail under the existing subprocess boundary, instead of a dual-mode dispatch) that avoids re-introducing P077/P084/P121 gaps; workaround documented (user opt-out signal `"just do it here"` per iter); design questions (presence-detector mechanism, dual-mode contract default branch, ADR-032 amendment shape, observation-surface alternative) are architect-design and route to a separate iter; transition Open → Known Error per ADR-022 lifecycle; WSJF 4.5 → 9.0 (Open M → KE M, status multiplier 1.0 → 2.0) jumps P130 to top of dev-work queue. Prior: 2026-04-28 **P138 verifying** — fold-fix commit ships P138 Open → Verification Pending per ADR-022; `docs/problems/README.md` WSJF Rankings now sorts by `(WSJF desc, Known-Error-first, Effort-divisor asc, Reported-date asc, ID asc)` so row order matches `/wr-itil:work-problems` Step 3 tie-break selection 1:1; `Reported` column added so the third tie-break input is visible to README readers; multi-key sort documented at all 5 render-block sites (manage-problem Step 5 P094 + Step 7 P062 + Step 9c presentation + Step 9e template, review-problems Step 3 + Step 5 README template, work-problems Step 1) with stable greppable cross-coupling marker `<!-- TIE-BREAK-LADDER-SOURCE: /wr-itil:work-problems SKILL.md Step 3 -->`; new behavioural + structural bats 13/13 green; architect PASS with one tightening (greppable marker, applied); JTBD-001 primary fit, JTBD-006 composes; this is the first README render with the new sort applied — WSJF 6.0 tier now shows P123 (KE M 2026-04-26) → P135 (KE M 2026-04-27) → P082 (Open M 2026-04-21) instead of the prior P135-on-top order, matching the orchestrator's actual selection in iter 5. Prior: 2026-04-28 **P135 known-error transition** — all 4 declarative phases (1 anchor, 5 measurement, 2 skill amendments, 3 AFK loop redesign) shipped to npm; Phase 4 enforcement is R6-gated with auto-flag wired in `run-retro` Step 2d (fires if lazy count ≥2 across 3 consecutive retros); 6 named bats files in place; transition reflects shipped-fix state pending lazy-AskUserQuestion-count metric verification across 3+ consecutive retros per ADR-022 lifecycle. Prior: 2026-04-28 **P138 opened** — user-surfaced 2026-04-28 mid-/wr-itil:work-problems iter 5: WSJF Rankings table renders rows in non-tie-break order and Reported date column is absent, so users can't predict which ticket the orchestrator will pick on ties; user saw P123 worked over P135 (top of WSJF 6.0 group) and assumed the orchestrator was broken; tie-break rule itself is correct (ADR-044 + work-problems Step 3) — bug is in the visualisation surface; Severity 10 (High band, Impact 2 × Likelihood 5), Effort M (3 SKILL.md render blocks + bats + one-shot re-render), WSJF 5.0; architect PASS pre-picking Option C (sort by ladder + add Reported column); JTBD-001 + JTBD-006 PASS (P137 plugin-user critique does not apply — internal governance surface, not published artefact). Prior: 2026-04-28 **P123 known-error transition** — ADR-046 (`docs/decisions/046-blocked-reporters-persistence.proposed.md`) lands diagnosis for blocked-reporters persistence: per-repo, hashed GitHub user IDs (Option C); audit-log-only v1 implementation contract; 3 Open Questions (block-list shape Q1, provenance Q2, un-block path Q3) block transition `proposed` → `accepted`; ticket transitions `.open.md` → `.known-error.md` per ADR-022 (root cause confirmed, awaiting Q1/Q2/Q3 resolution + downstream P079/P070 integration); architect PASS + JTBD PASS (2 minor JTBD-doc inconsistencies flagged for ticket cleanup, non-blocking — JTBD-201 misnamed as "downstream adopter" should cite tech-lead persona's adopter framing; no first-class JTBD covers "block malicious reporters" today, defensible as infrastructure). Prior: 2026-04-28 **P137 opened** — user-surfaced concern that plugin-published artefacts (SKILL.md, hooks, agents, CHANGELOGs) cite internal ADR-NNN / JTBD-NNN / P-NNN tokens that adopter projects can't resolve (or worse, resolve to UNRELATED tickets in adopter's own tree); concrete evidence: line "per ADR-037 + P081" now in `packages/itil/CHANGELOG.md` × 3 (lines 11/164/246) shipped in `@windyroad/itil@0.21.2`; Severity 20 Very High (Impact 4 × Likelihood 5), Effort XL (cross-package audit + new strategy ADR + 11-plugin mechanical sweep + CONTRIBUTING.md guardrail), WSJF 2.5; architect PASS + JTBD ISSUES-FOUND-then-applied (plugin-user persona is primary not solo-developer, JTBD-101 secondary, gap flagged for missing "trust artefacts resolve cleanly" job). Prior: 2026-04-28 **P064 verifying transition** — `/wr-itil:work-problems` AFK iter shipped overdue Known Error → Verifying transition; risk-scorer external-comms gate fix released 2026-04-26 in commit `a0713f3` (canonical hook in `packages/shared/hooks/external-comms-gate.sh` + per-package synced copy in `packages/risk-scorer/hooks/` + new subagent `wr-risk-scorer:external-comms` + on-demand skill `/wr-risk-scorer:assess-external-comms` + 12/12 bats green) but ticket was never transitioned out of `.known-error.md`; this iter transitions `.known-error.md` → `.verifying.md`; Status `Verification Pending`; WSJF 6.0 → 0 per ADR-022 (excluded from dev-work ranking); two checked Investigation Tasks remain `Deferred` but explicitly non-blocking per ticket (`/wr-itil:report-upstream` SKILL.md cross-ref + P038 voice-tone composite-marker upgrade). Prior: 2026-04-28 **P134 re-rated 4.5 → 5.0** — user-surfaced concern during /wr-itil:work-problems iter 2 ("very large blob of text … I don't know what value it provides"); convergent with iter 2 Step 2b retro Read-tool reload thrash 28k–36k tokens; architect review of the re-rate also hit Read failure 26.7k tokens at offset=1 limit=5; Likelihood corrected mislabel "Likely (3)" → policy-verbatim "Almost certain (5)"; Impact dropped Moderate→Minor (RISK-POLICY Impact-3 "npm publish disrupted" doesn't apply to dev-tooling friction). Prior: 2026-04-28 **P096 Phase 3 ADR-045 landed (verifying)** — `/wr-itil:work-problems` AFK iter shipped `docs/decisions/045-hook-injection-budget-for-pre-and-post-tool-use-hooks.proposed.md` as sibling to ADR-038, codifying five reusable patterns from P096 Phase 2 (silent-on-pass default, side-effect-only silent, silent-on-unchanged-state, hash-dedupe of repeated body content, once-per-session gating reuse) plus per-band byte budget table; ticket transitioned `.known-error.md` → `.verifying.md` (Status Verification Pending; WSJF 9.0 → excluded per ADR-022 Verification Pending lifecycle); architect verdict PASS (sibling-not-extension confirmed per ADR-040 + ADR-043 precedent); JTBD verdict PASS (JTBD-001/002/003/006/101 cited); bats coverage already in place from Phase 2 (no new tests this iter); release pending Step 6.5 drain. Prior: 2026-04-28 **P124 Phase 2 fix released** — `/wr-itil:work-problems` AFK iter shipped `packages/itil/hooks/lib/session-id.sh` zsh-portability fix (`shopt -s nullglob` subshell → portable `for f in ...; do [ -e "$f" ] || continue; ...` existence-check loop) per the ticket's "Regression Evidence (2026-04-27)" Citation 2 (main-turn P130 capture: `command not found: shopt` + 81-marker brute-force recovery). Phase 2 scope explicitly limited to portability; mtime-glob fix in ticket strategy intentionally superseded by Phase 1's `-announced-` marker switch + system-priority discipline. New `session-id.bats` test 7 pins the zsh-portability contract per ADR-037 + P081, 7/7 green. P124 transitioned `.known-error.md` → `.verifying.md` (Status Verification Pending; WSJF (12 × 0)/1 = 0 per ADR-022 Verification Pending lifecycle). Released `@windyroad/itil` patch in this commit. Architect + JTBD gates PASS. Prior: 2026-04-27 **batch verification transition** — 4 released tickets transitioned Known Error → Verification Pending in one ADR-014 batch-grain commit via `/wr-itil:transition-problems` (P127 verifying / P065 verifying / P126 verifying / P101 verifying). Releases: `@windyroad/itil@0.21.1` (P127 + P126), `@windyroad/itil@0.21.0` (P065 — earlier iter 1 drain), `@windyroad/retrospective@0.10.0` (P101). P096 deliberately retained as Known Error pending Phase 3 (ADR codifying hook injection budget policy, composes with P091). Single P062 README refresh at batch end; single ADR-014 commit covering all 4 renames + Status edits + `## Fix Released` sections + this README. Prior: 2026-04-26 **P129 opened** — P079 inbound assessment pipeline lacks version-aware classification. Three classification outcomes the user named on 2026-04-26: (1) already-fixed-in-newer-version → upgrade pushback, (2) recurred-in-newer-version → link-to-prior-closed + recurrence-handling, (3) still-active-in-current-version → standard new-ticket. Today P079's planned pipeline treats every inbound report as a fresh problem candidate; doesn't consult closed-ticket history; doesn't compare reporter-version against shipped fix-versions. Carve-out from P079 mirroring P123's precedent. Lean direction (per architect verdict): Option B-2 for recurrence semantics — append `## Recurrences` section to the existing closed ticket rather than expand ADR-022's suffix vocabulary. Effort L marginal → XL transitive via P038 (chains through P079); WSJF 1.5 transitive. Strictly blocked-by P128 (companion outbound ticket — needs schema for reporter-version parsing) + P079 (parent — pipeline must exist) + P038/P064 (inherited through P079). Architect PASS — confirms Option B-2 lean, strict block on P128 (not compose-with), inbound-only scope (don't bundle outbound-side recurrence detection); two cross-ref additions applied (ADR-031 directory layout, ADR-037 bats coverage). Single-commit creation per ADR-014 + P094 README refresh. Also lands a one-line Related-section cross-reference update on P128's file (bidirectional companion linkage). Prior: 2026-04-26 **P128 opened** — `/wr-itil:report-upstream` report body lacks a consolidated `## Versions` section. Today version info is scattered across the matched-template path's single `version` field, the structured-default fallback's two adjacent line items (`- Version:` + `- Claude Code version:`), and the local intake template's freeform `environment` textarea. Upstream maintainers can't determine version applicability without asking. Captured outbound half of user direction 2026-04-26 ("for the reporting upstream, it needs to include version information if it doesn't already"); inbound half captured separately as P129. Fix shape: amend ADR-033 Step 5 with a fixed `## Versions` schema (local plugin / upstream package / Claude Code CLI / Node / OS; `not detected` for un-discoverable slots); auto-population helper in `report-upstream` skill; mirror the schema in `.github/ISSUE_TEMPLATE/problem-report.yml` and downstream-scaffolded intakes (ADR-036). Effort M marginal → L transitive via P064 (risk-gate composition); Severity 12 High (Moderate 3 × Likely 4); WSJF 3.0 transitive. Composes with P070 (sibling existing-issue dedup gap on same skill surface), P038 (voice-tone gate composes), ADR-024 / ADR-033 / ADR-036 / ADR-037. Architect PASS — confirms ADR-033 amendment (not new ADR), single ticket (multi-concern check satisfied), downstream-symmetry composes cleanly with ADR-036. Single-commit creation per ADR-014 + P094 README refresh. Prior: 2026-04-26 **P101 → Known Error + ADR-043 authored + ADR-026/ADR-014 amended** — `@windyroad/retrospective` minor bump introduces two-layer context-usage analyzer per architect verdict on P101's Fix Strategy. Cheap layer: new Step 2c in `run-retro/SKILL.md` invokes new read-only diagnostic `packages/retrospective/scripts/measure-context-budget.sh` (8 buckets — hooks/skills/briefing/decisions/problems/jtbd/project-claude-md/memory + framework-injected sentinel) and renders a per-source-bucket table in retro summary; static budget ~2.5 KB output ceiling, well under 5%/200K cheap-layer envelope; fail-open defensive trip when ceiling exceeded. Deep layer: new skill `/wr-retrospective:analyze-context` produces `docs/retros/<date>-context-analysis.md` with HTML-comment trailer carrying the bucket-snapshot for delta-from-prior comparison; user-invoked only, never auto-fires per ADR-013 Rule 6; per-turn attribution from `.afk-run-state/*.jsonl`; per-plugin decomposition; ADR-026-grounded suggestions with comparable-prior citations (P095/P099/P100 reclamation precedents). Snapshot persistence chosen via architect verdict over gitignored JSON or `/tmp` markers — HTML trailer mirrors ADR-040's per-entry signal-score block precedent and satisfies ADR-026 cite+persist+uncertainty rule. ADR-038 stays scope-bounded to UserPromptSubmit governance prose; ADR-043 is the observability layer one level out. ADR-026 amended (analyze-context skill on per-agent prompt list); ADR-014 amended (`docs(retros): context analysis YYYY-MM-DD` row). Effort re-rated L → XL on architect verdict scope expansion. JTBD-001 (Enforce Governance Without Slowing Down) primary; JTBD-006 (Progress the Backlog While I'm Away) for AFK iteration summaries; JTBD-005 (Invoke Governance Assessments On Demand) for the deep skill. 28 behavioural bats on the diagnostic script + 12 doc-lint structural assertions on Step 2c + 18 doc-lint on the new SKILL.md; full retrospective suite 157/157 green. P091's "Build a measurement harness" investigation task closes as subsumed. AFK iter, single commit per ADR-014. Prior: 2026-04-26 **P127 opened** — scaffold-intake idempotency bats fixture (test 645) fails in CI but passes locally on commit `8653541` (and inherits red on every subsequent commit including `1e253c3`). 3 RCA candidates: non-deterministic template content (timestamps / session-ids in the done-marker), `cp -R` snapshot non-determinism (APFS vs ext4 mtime/permission semantics), or filesystem case-sensitivity. WSJF 9.0 (top of dev-work queue), S effort. Composes with P065 (Known Error parent), P126 (open — orchestrator-side companion gap surfaced same iter), P116 (closed — adjacent CI-visibility class). Prior: 2026-04-26 **P126 opened** — captured via `/wr-retrospective:run-retro` Step 4b Stage 1 from user correction "the end of the work-problems session didnt ask questions" (P078 contradiction-signal pattern). Failure-handling halt paths in `/wr-itil:work-problems` (Step 6.5 CI-failure, Step 0 session-continuity, Step 6.75 inter-iter dirty, ADR-042 Rule 5 above-appetite, network-failure) bypass Step 2.5's P122 interactive-default routing — accumulated user-answerable skips dump into the AFK summary table even when interactive. WSJF 6.0, M effort. Composes with P122 (verifying), P078 (verifying), P109 (closed), P036 (closed), P104 (verifying). Prior: 2026-04-26 **P109 + P118 + P089 → Closed** via `/wr-retrospective:run-retro` Step 4a interactive close-prompt: P109 verified by Step 0 session-continuity scan firing on this session's `.afk-run-state/iter-p103.json` April-22 stale 429 marker; P118 verified by `reconcile-readme.sh` exit 0 at session start AND post-iter-7; P089 verified by all 7 iter dispatches using `< /dev/null` and parsing `.total_cost_usd`/`.usage.*` cleanly. Briefing entries: `releases-and-ci.md` gains "iter retro 1135/0 claim ≠ CI green on same commit" (P065 / commit 8653541 / CI run 24950750567 evidence); `hooks-and-gates.md` gains iter-5's secret-leak-gate fixture-trap workaround. Reconciliation contract per P118 + ADR-014 amended ("Reconciliation as preflight robustness layer"). Prior: 2026-04-26 **P064 → Known Error** — `@windyroad/risk-scorer` minor bump (`packages/risk-scorer/`) ships the external-comms risk-leak gate per amended ADR-028. New canonical hook `packages/shared/hooks/external-comms-gate.sh` + helper `packages/shared/hooks/lib/leak-detect.sh` distributed via ADR-017 duplicate-script pattern (new `scripts/sync-external-comms-gate.sh` + `npm run check:external-comms-gate` CI step); risk-scorer per-package copy at `packages/risk-scorer/hooks/external-comms-gate.sh` + `packages/risk-scorer/hooks/lib/leak-detect.sh` (byte-identical). New subagent type `wr-risk-scorer:external-comms` (`packages/risk-scorer/agents/external-comms.md`) emits structured `EXTERNAL_COMMS_RISK_VERDICT: PASS|FAIL` + `EXTERNAL_COMMS_RISK_KEY: <sha>` consumed by extended `risk-score-mark.sh`. New on-demand skill `/wr-risk-scorer:assess-external-comms` per ADR-015. Surface coverage: `gh issue create|comment|edit`, `gh pr create|comment|edit`, `gh api .../security-advisories`, `gh api .../comments`, `npm publish`, and `PreToolUse:Write|Edit on .changeset/*.md` (P073 author-time gating — leaks blocked before the body lands in CHANGELOG.md and every published npm tarball). Hybrid leak detection per architect verdict (c): regex pre-filter (credentials, business-context-paired financial figures, business-context-paired user counts) hard-fails immediately; ambiguous prose delegated to the subagent for context-aware review against `RISK-POLICY.md` Confidential Information classes. `BYPASS_RISK_GATE=1` env var override (consistent with `git-push-gate.sh`); `RISK-POLICY.md`-absent → advisory-only mode (graceful adoption per ADR-008/ADR-025). Composite-marker scheme (combining a future voice-tone verdict with the risk verdict against the same draft) intentionally deferred until P038 ships its evaluator — both gates compose at the `PreToolUse:Bash` matcher level when both packages are installed; promotion to the composite marker is a localised follow-up in the canonical hook. 12 behavioural bats in `packages/risk-scorer/hooks/test/external-comms-gate.bats` (surface match, hard-fail leak deny, marker permit, BYPASS, advisory-only, changeset author, non-changeset path); 11 in `packages/shared/test/external-comms-gate-canonical.bats`; 7 in `packages/shared/test/sync-external-comms-gate.bats` (mirrors P095 + P026 drift coverage); 137/137 risk-scorer + shared green; full project bats green. Architect PASS (verdict c — canonical+sync+per-evaluator marker; new subagent file; hybrid leak shape; P073 changeset surface in scope) + JTBD PASS (JTBD-001 primary "police AI output" pain closed for the leak class; JTBD-101 reusable subagent+skill shape; JTBD-201 audit-trail integrity for outbound communications). No new ADR — ADR-028 amendment 2026-04-21 already governs; ADR-017 distribution pattern extended in-amendment to `hooks/`. `@windyroad/risk-scorer` minor via `.changeset/wr-risk-scorer-p064-external-comms-gate.md`. AFK iter, single commit per ADR-014. Prior: 2026-04-26 **P117 → Verification Pending** — `@windyroad/itil` minor bump adds new plural sibling skill `/wr-itil:transition-problems` for batch lifecycle transitions, mirroring the P071 singular/plural split precedent (`work-problem` vs `work-problems`). Accepts a space-separated list of `<NNN> <status>` pairs (e.g. `/wr-itil:transition-problems 063 close 067 close 092 close 094 close`). Per-ticket mechanic inlined per ADR-010 amended "copy, not move" (pre-flight + P063 detection + `git mv` + Edit + P057 re-stage per pair); NO Skill-tool re-invocation back to singular — that would reintroduce the very N×SKILL.md-reload cost P117 targets. ONE README refresh at end (P062 applied at batch grain) + ONE commit at end per ADR-014 batch-grain unit-of-work. Partial-failure semantics: skip-and-surface (succeeded pairs commit at end; failed pairs surfaced in summary; zero-success means no commit + failure-only summary). Three call sites now share the per-ticket mechanic via copy-not-move: singular `/wr-itil:transition-problem`, plural `/wr-itil:transition-problems` (this skill), and `manage-problem` in-skill Step 7. Drift management: contract-bats asserts the staging-trap `git add docs/problems/` phrase appears in BOTH this SKILL.md and the singular's SKILL.md so a future edit dropping the re-stage rule from one is immediately visible. 20 behavioural contract assertions green; sibling test suites (transition-problem-contract + work-problems Step-5 / Step-2.5 / preflight) re-run green confirming no drift introduced. Architect PASS — clean application of multiple existing decisions (ADR-010 amended, ADR-013 Rule 6, ADR-014 batch grain, ADR-022, ADR-032, ADR-037) at a new batch grain; no new ADR. JTBD-001 primary (eliminates N×SKILL.md reload tax + ownership-boundary violation at batch closures — exactly the failure mode the originating run-retro Step 4a session named); JTBD-006 composes (work-problems may delegate release-batched closures here during AFK orchestration); JTBD-101 (singular/plural split mirrors the established pattern, plugin-developers reading should immediately recognise the shape). `@windyroad/itil` minor via `.changeset/wr-itil-p117-batch-transition-skill.md`. AFK iter, single commit per ADR-014. Prior: 2026-04-26 **P099 → Verification Pending** — Tier 3 budget enforcement for the briefing tree shipped in `@windyroad/retrospective` minor bump. New advisory script `packages/retrospective/scripts/check-briefing-budgets.sh` (read-only diagnostic; reads `docs/briefing/*.md`; reports each topic file at or above `${BRIEFING_TIER3_MAX_BYTES:-5120}` bytes via `OVER <basename> bytes=<N> threshold=<N>`; README.md excluded as Tier 2; always exits 0 — overflow is signal not failure per JTBD-001 "without slowing down"; output sorted by basename for stable diffs). Mirrors `packages/itil/scripts/reconcile-readme.sh` placement and shape (architect verdict 2026-04-26: amendment to ADR-040 not sibling; per-plugin `scripts/` not skill-private; advisory not fail-closed; Step 3 ordering as final action after edits + Step 1.5 delete-queue persistence + README refresh; bats fixture co-located at `packages/retrospective/scripts/test/`). 14 behavioural bats per ADR-005 + P081 covering existence + executable + empty-dir + under-threshold + over-threshold + boundary-exact + README-excluded + env-var-override + non-md-ignored + missing-dir-exit-2 + sort-stability; all green; full retrospective suite 93/93 green. `run-retro` SKILL.md Step 3 gains the **Tier 3 budget rotation pass** as final action: invokes the script, surfaces each overflow via `AskUserQuestion` with four rotation shapes (split-by-subtopic / split-by-date / trim-noise / defer per ADR-013 Rule 1 four-option cap); AFK fallback per ADR-013 Rule 6 defers to retro summary's new "Topic File Rotation Candidates" section. Step 5 summary template gains the matching table. ADR-040 amended — Tier 3 promoted from "informational" to advisory enforcement; new Reassessment trigger (≥3 topic files exceed 2× ceiling for ≥2 retro cycles → revisit threshold or promote to fail-closed); reusable-pattern note (JTBD-101) names the advisory-script + bats + ADR-tier-budget-amendment triplet for future accumulator surfaces (risk register per P102, ADR index, problems index). JTBD-001 primary (governance-without-slowdown — amortised net-negative since topic files shrink toward ceiling on every retro cycle); JTBD-006 composes (AFK rotation deferred to user judgment on return — no silent edit per `feedback_verify_from_own_observation.md` shape); JTBD-101 (reusable triplet for future accumulator surfaces). Self-demonstrating outcome: smoke-test against current `docs/briefing/` shows all 6 topic files surface as overflow candidates (afk-subprocess.md 15883, agent-interaction-patterns.md 9653, governance-workflow.md 17275, hooks-and-gates.md 6626, plugin-distribution.md 8975, releases-and-ci.md 7385) — confirming the budget gap P099 names. AFK iter, single commit per ADR-014. Closes P100 cluster gap on Tier 3 budget specifically. Prior: 2026-04-26 **P125 → Verification Pending** — P057 staging-trap is now denied at commit time by new `PreToolUse:Bash` hook `packages/itil/hooks/p057-staging-trap-detect.sh` matching `git commit` invocations. Detection delegates to new shared helper `packages/itil/hooks/lib/staging-detect.sh::detect_p057_trap` (mirrors `lib/create-gate.sh` precedent from P119): runs `git diff --staged --name-status` + `git diff --name-only`; if any staged rename's `<new>` path appears as a working-tree modification, returns 1 with the path on stdout and a recovery hint on stderr; otherwise returns 0. Fail-open contract on non-git working tree / `git diff` parse error / missing index. Hook emits `permissionDecision: deny` JSON when the helper returns 1, naming the trap'd file inline and surfacing the literal `git add <new>` recovery command per ADR-013 Rule 1 mechanical-recovery shape. Architect APPROVED-WITH-AMENDMENTS verdict — three amendments applied: bats header cites ADR-005 (plugin testing strategy) not ADR-037 (skill testing); fail-open on parse-incomplete input mirrors `lib/create-gate.sh`; cost discoverability documented in helper docstring (~10-50ms per commit, two `git diff` invocations). JTBD-001 (Enforce Governance Without Slowing Down) primary — operationalises the documented rule without depending on agent attention; JTBD-006 (Progress the Backlog While I'm Away) composes — AFK iter loops are the highest-frequency offenders per the ticket evidence (P122 batch shipped commit `e7564ff` rename-only despite documentation). Voice-tone gate failed-open due to missing `docs/VOICE-AND-TONE.md` (pre-existing P073/P038 backlog gap) but agent's tighter draft suggestion applied; observed deny message ~348 bytes, well under the 400-byte ADR-038 progressive-disclosure cap. 10 behavioural bats per ADR-005 + P081 in `packages/itil/hooks/test/p057-staging-trap-detect.bats` covering 5 trap/allow paths from the ticket spec + 5 supporting paths. Hook registered in `packages/itil/hooks/hooks.json` PreToolUse Bash matcher; `docs/briefing/agent-interaction-patterns.md` line 8 cites the new hook as the enforcement layer the documentation didn't provide. Full project bats 1018+10/1028 green. `@windyroad/itil` patch via `.changeset/wr-itil-p125-staging-trap-hook.md`. AFK iter, single commit per ADR-014. Demonstrated end-to-end during this very transition: the renamed `.verifying.md` was Edit'd then `git add`'d explicitly before commit — the trap shape avoided by the discipline this ticket's hook now enforces. Prior: 2026-04-26 **P124 → Verification Pending** — `/wr-itil:manage-problem` Step 2 substep 7 now sources new agent-side helper `packages/itil/hooks/lib/session-id.sh::get_current_session_id` (env-var fast path → fixed system-priority scrape over `/tmp/<system>-announced-<UUID>` markers per ADR-038 → empty + non-zero exit on exhaustion); chains to existing `mark_step2_complete()` from `create-gate.sh` for the marker write itself (single source of truth for the marker-path convention). Architect-flagged refinements applied: switched from `-reviewed-` markers (mtime-fragile per ADR-009 sliding TTL + P111 subprocess refresh) to `-announced-` markers (write-once, no mtime sliding); selection by fixed system-priority order (architect → jtbd → tdd → itil-assistant-gate → itil-correction-detect → style-guide → voice-tone), not mtime, for deterministic discovery. `&&` short-circuits empty-SID writes so no `/tmp/manage-problem-grep-` empty-tail file is ever created. ADR-038 `## Related` cross-refs the new helper as the agent-side READ companion to its hook-side WRITE helpers — no new ADR needed (refinement of ADR-038's session-marker pattern). 6 behavioural bats per ADR-037 + P081 (env-var fast path, env-var ignores markers, architect-marker scrape, jtbd-marker fallback, no-markers empty + non-zero, deterministic priority order); all green. JTBD-001 (Enforce Governance Without Slowing Down) primary — replaces ad-hoc UUID scraping with a documented helper, preserving create-gate enforcement (P119) while removing the undocumented friction step. JTBD-006 (Progress the Backlog While I'm Away) composes — AFK loops creating tickets mid-iter no longer hit the discovery gap. `@windyroad/itil` patch via `.changeset/wr-itil-p124-session-id-helper.md`. AFK iter, single commit per ADR-014. Prior: 2026-04-26 **P120 + P121 + P122 closed** post-restart in fresh session via in-session verification per `feedback_verify_from_own_observation.md`. P120 end-to-end verified by running `/install-updates`: Step 6a cache-check fired, cached scope matched detected siblings, gate skipped, no AskUserQuestion fired (the exact behaviour the originating user complaint asked for). P121 + P122 verified by inspecting cached `~/.claude/plugins/cache/windyroad/wr-itil/0.19.6/skills/work-problems/SKILL.md` (new `Default branch — call AskUserQuestion` prose present at Step 2.5; legacy `non-interactive path the default for this skill` prose absent (grep count = 0); Step 5 carries `IDLE_TIMEOUT_S` / `SIGTERM_SENT` / `LAST_ACTIVITY_MARK` references). The architect-mandated second-source bats fixtures (`work-problems-step-2-5-routing.bats` 8/8 + `work-problems-step-5-idle-timeout-sigterm.bats` 10/10) close out the single-source-evidence concerns from each ticket's RCA. P122 same-session evidence: this turn's interactive AskUserQuestion call to choose verification path operationally demonstrates the orchestrator-side fix's spirit. Backlog now 5 dev-work tickets remaining (P082 / P099 + L-effort tail / 3.0-tier). Prior: 2026-04-26 **P122 → Verification Pending** — `/wr-itil:work-problems` Step 2.5 stop-condition #2 routing flipped: default branch now calls AskUserQuestion when available (the orchestrator's main turn is interactive by construction); Outstanding Design Questions table is the AskUserQuestion-unavailable fallback. Architect-FLAG cross-skill principle paragraph added: "orchestrator main turns default to AskUserQuestion when available; AFK persona served by subprocess-boundary contract under ADR-032, not by suppressing AskUserQuestion at the orchestrator layer". Step 6.5 Decisions Table row rewritten to match. New `packages/itil/skills/work-problems/test/work-problems-step-2-5-routing.bats` (8 doc-lint contract assertions per ADR-037: legacy "default for this skill" prose removed at Step 2.5 / "Default branch" prose present / subprocess-boundary principle present / user-answerable skip-reason scoping preserved (P103 boundary) / 4-question batching cap preserved / table-fallback-when-unavailable preserved / Decisions Table row names AskUserQuestion as default; 8/8 green; full project bats green). `@windyroad/itil` patch via `.changeset/wr-itil-p122-step-2-5-interactive-default.md`. JTBD-001 (Enforce Governance Without Slowing Down) primary — interactive users get questions asked instead of a deferred table. JTBD-006 (Progress the Backlog While I'm Away) preserved correctly — re-scoped to where it actually applies (iter subprocess workers; their AFK contract stays via Step 5 prompt template Constraint #3). Shared `lib/runtime-mode.sh` helper DEFERRED per architect verdict — the iter subprocess AFK contract is enforced at the prompt-template layer, not by stop-condition #2's branch. P103 anti-pattern boundary preserved — AskUserQuestion stays scoped to `user-answerable` skip-reasons only. Single commit per ADR-014. Prior: 2026-04-26 **Session retro — 2 codification tickets opened + 2 briefing entries added**: **P124 opened** (manage-problem Step 2 session-id discovery is brittle; agent must scrape `/tmp/architect-plan-reviewed-<UUID>` filenames since `${CLAUDE_SESSION_ID}` env var is unreliable; observed during P122 retro; WSJF 6.0, S effort, helper-script fix). **P125 opened** (P057 staging-trap recurs despite documentation; observed twice this session; hook-level enforcement candidate via PreToolUse:Bash on `git commit` detecting rename-then-modify-without-restage; WSJF 4.5, M effort). Briefing entries: `agent-interaction-patterns.md` gains "SKILL.md 'default for this skill' routing rules can be wrong-by-design when persona ≠ runtime mode" (P122 evidence) + "create-gate hook session-id discovery via marker-filename scraping" (P124 evidence). Pipeline-instability scan (Step 2b) detected: risk-scorer marker expired 3× during interactive design-question pauses (already documented in `hooks-and-gates.md`); P057 trap recurred (P125 captures); iter 1 retro silent-drop on P120 bats failure (caught only by iter 2 — adjacent to P088). No clean Verification Candidates this session (P120/P121 same-session, P122-P125 same-session). Prior: 2026-04-26 **Interactive design-question round (P122 follow-up)** — Captured user direction on 4 of 5 stop-condition #2 skipped tickets via `AskUserQuestion`: P079 (sibling ADR + assessment-pipeline replaces auto-vs-surface binary + all-3 GH channels + downstream-contract symmetry + JSON cache), P080 (new sibling skill `/wr-itil:update-upstream` + risk+voice-tone gated auto-comment), P115 (parked — "we don't use worktrees here"), P117 (resolved shape — new sibling skill `/wr-itil:transition-problems` plural). New ticket **P123 opened** (blocked-user list, per-repo scope) carved out from P079's clear-malicious branch. P079 + P080 re-rated 6.0 → 1.5 transitive (now blocked-by P038/P064). Demonstrates the P122 fix in practice: AskUserQuestion was available + user was at keyboard → asked the questions instead of emitting a deferred table. Prior: 2026-04-26 **P122 opened** — `/wr-itil:work-problems` stop-condition #2 defaults to AFK Outstanding Design Questions table even when AskUserQuestion is in the orchestrator's tool surface and the user is demonstrably at the keyboard. Surfaced 2026-04-26 by direct user correction at iter 3 stop of the AFK loop (12 user-answerable design questions across 5 skipped tickets emitted as table; user verbatim: "this was a really good example of where there are many outstanding design questions, but nothing was asked" — P078 strong-signal token). RCA: SKILL.md Step 2.5 prose says "non-interactive path the default for this skill" conflating persona (JTBD-006 AFK) with runtime mode. Fix shape: extend Step 2.5 routing so detection signal (subprocess-vs-main-turn primary lean per ADR-032) drives the branch; interactive default calls AskUserQuestion (batched ≤4, sequential >4); AFK explicit fallback emits table. Composes with P085 (verifying — assistant-side AskUserQuestion correctness) and P053 (closed — established the surface + skip-reason taxonomy this ticket consumes); tensioned with P103 (verifying — must NOT broaden to resolved-policy questions; skip-reason boundary preserved). Effort M, Severity 15 High (Moderate 3 × Almost Certain 5), WSJF 7.5 — top of dev-work queue. New shared helper candidate `packages/itil/hooks/lib/runtime-mode.sh` for cross-skill consistency. Filed via `/wr-itil:manage-problem` per P119's Step 2 duplicate-check contract (P078 capture-on-correction: user invoked the capture skill themselves, satisfying the OFFER step). Prior: 2026-04-26 **P121 → Verification Pending** — `/wr-itil:work-problems` Step 5 dispatch shape changed from foreground-synchronous to backgrounded + 60s poll loop with idle-timeout SIGTERM branch (default 3600s, env-overridable via `WORK_PROBLEMS_IDLE_TIMEOUT_S`); `LAST_ACTIVITY_MARK = max(DISPATCH_START_EPOCH, git log -1 --format=%at HEAD)` so skip-iterations are bounded by the dispatch-start floor (architect amendment 3 — explicit signal trade-off documented in SKILL.md). When idle exceeds the threshold, orchestrator sends SIGTERM to the stuck `claude -p` PID; SIGTERM empirically produces a clean JSON exit-flush per the 2026-04-25 P118 iter 5 production observation. Step 6 progress line annotates `(SIGTERM_SENT)` for distinguishable status per JTBD-006 audit-trail (architect+JTBD APPROVED-WITH-AMENDMENTS verdict — all four amendments applied: dispatch shape changed structurally not prose-only, ADR-032 amendment block added, signal trade-off explicit, distinguishable status preserved). ADR-032 amended 2026-04-26 with the backgrounded-poll-loop refinement under the subprocess-boundary variant. New `packages/itil/skills/work-problems/test/work-problems-step-5-idle-timeout-sigterm.bats` (10 tests — 4 behavioural with fake `claude -p` shim covering SIGTERM-fires/JSON-clean-flush/env-override/no-fire-within-threshold + 6 doc-lint contract assertions; 10/10 green) provides the second-source for the otherwise single-observation production evidence. Style-guide PASS (out-of-scope, prose-only). Voice-tone advisory-only (`docs/VOICE-AND-TONE.md` does not exist; ADR-028 still proposed — composes with P073 backlog). Full project bats 993/994 green; the 1 failure (`install-updates P120: REFERENCE.md documents set-equality match rule and plugin-list-change-no-op`, `.claude/skills/install-updates/test/install-updates-consent-cache.bats`) is **pre-existing** in P120's commits `76ff7b2`/`584d3ca` (prior AFK iter 1 work, repo-local install-updates skill) — surfaced here for follow-up triage but explicitly out-of-scope for P121 (S effort budget). `@windyroad/itil` patch via `.changeset/wr-itil-p121-idle-timeout-sigterm.md`. `docs/briefing/afk-subprocess.md` P121 entry updated to cite the new bats coverage. AFK iter 2, single commit per ADR-014. Prior: 2026-04-25 **P120 → Verification Pending** — `/install-updates` consent gate now reads `.claude/.install-updates-consent` BEFORE invoking `AskUserQuestion`; cache-hit path skips Step 6 entirely per ADR-013 Rule 5 (policy-authorised silent proceed) when cached `scope` matches detected sibling set via set equality; cache-miss-with-stale-cache (sibling-set change) re-prompts with previous answer surfaced as `(Recommended)`; cache-write at end of successful run; two equivalent escape hatches preserve dry-run access (`INSTALL_UPDATES_RECONFIRM=1` envvar or `rm .claude/.install-updates-consent`). Architect APPROVED-WITH-AMENDMENTS verdict converged + JTBD PASS + style-guide PASS + voice-tone PASS; required corrections all applied (Rule 5 not Rule 6 throughout, dry-run override path, Step 6.5 still runs on cache-hit, set-equality match rule explicit, sibling-marker coexistence note in ADR-034). Repo-local skill per ADR-030 — no changeset; effective on next `/install-updates` invocation. ADR-030 Confirmation amendment "Consent cache carve-out (P120, 2026-04-25 amendment)" + ADR-034 sibling-marker coexistence note + new `install-updates-consent-cache.bats` (15 doc-lint contract assertions, 15/15 green) + `.gitignore` adds both consent markers (P120 + ADR-034 sibling). Architectural precedent: ADR-034's `.claude/.auto-install-consent` per-project marker — same shape, different surface (SessionStart hook vs. manual Step 6 gate); two markers independent. JTBD-001 (Enforce Governance Without Slowing Down) primary — eliminates the redundant end-of-session round-trip. JTBD-006 (Progress the Backlog While I'm Away) — AFK loops no longer surface a redundant consent gate at end-of-loop. Critical pipeline-instability evidence: harness sandbox denies Edit/Write tool calls AND `printf >` Bash redirects to `.claude/` paths in AFK subprocess context regardless of `defaultMode: "dontAsk"` (deny message: "is a sensitive file"); the documented `python3 -c "Path(...).write_text(...)"` workaround (`docs/briefing/afk-subprocess.md` line 23) bypasses the harness deny and was the only landing path for SKILL.md / REFERENCE.md / new bats writes this iter. AFK iter, single commit per ADR-014. Prior: 2026-04-25 **P121 opened** — AFK orchestrator should SIGTERM stuck `claude -p` subprocesses after idle-timeout; surfaced from `/wr-retrospective:run-retro` Step 2b pipeline-instability scan (Subagent-delegation friction / Hook-protocol friction category) on 2026-04-25 AFK iter 5 (P118) evidence: wall-clock 2h 1min, final commit at 100min, then 70min idle (RSS oscillating 88-99 MB; 0-byte JSON; no further commits) before SIGTERM produced a clean 5649-byte response with valid `ITERATION_SUMMARY`. Cost: ~$8 of $12.86 was post-commit idle burn. Retro-on-exit (P086) cannot self-surface because retro IS the hang region. WSJF 8.0 (Severity 8 Med = Moderate 3 × Possible 3; Effort S; Open). Fix shape: extend `packages/itil/skills/work-problems/SKILL.md` Step 5 poll loop with `LAST_ACTIVITY_MARK` tracker (from `git log -1 --format=%at`) + idle-timeout branch (default 3600s, env-overridable via `WORK_PROBLEMS_IDLE_TIMEOUT_S`); when `now - LAST_ACTIVITY_MARK > IDLE_TIMEOUT_S`, send SIGTERM to the subprocess PID and continue polling until JSON arrives. Composes with P086 (subprocess-exit retro contract — needs an exit to fire), P089 (Step 5 dispatch robustness — same surface, complementary fix), P083 (iteration-worker forbids ScheduleWakeup — adjacent time-deferring concern). Stage 2 fix strategy resolved obvious-act (Option 2 — Skill improvement stub on Step 5 poll loop) per ADR-013 Rule 1 obvious-decision branch. Briefing entry added to `docs/briefing/afk-subprocess.md` covering the SIGTERM-flushes-JSON evidence + within-session cached-SKILL.md-vs-fresh-hook mismatch (composite — P121 + P120 retro both surfaced from this session). Prior: 2026-04-25 **P120 opened** — `/install-updates` consent gate fires on every invocation despite the answer being invariably "all confirmed siblings"; user-correction-driven (FFS-class strong-signal: "wastes time", "Don't ask me again") at the 2026-04-25 end-of-loop `/install-updates` invocation that shipped 36/36 installs across 6 projects. WSJF 10.0 (Severity 10 High = Minor 2 × Almost Certain 5; Effort S; Open). Fix shape: persistent per-project consent cache `.claude/.install-updates-consent` (parallel to P045 ADR-034's `.claude/.auto-install-consent`) — Step 6 reads the cache and skips the consent gate when the cached scope matches the current sibling set; cache-miss-with-stale-cache (sibling-set change) re-prompts with the previous answer pre-selected. ADR-030 Confirmation amendment expected (consent-gate exemption when cache hits). Composes with P061 (option-count shape, Verifying) and P045 (SessionStart auto-install — same architectural pattern at a different surface). Filed via `/wr-itil:manage-problem` per P119's Step 2 duplicate-check contract (P078 strong-signal-correction → ticket capture: contract held end-to-end this turn — user complaint, dedup grep, AskUserQuestion-considered-then-acted, ticket landed). Prior: 2026-04-25 **P118 → Verification Pending + README reconciled** — new reconcile-readme contract (script + skill + manage-problem/work-problems Step 0 preflight + ADR-014 amendment) shipped in `@windyroad/itil` patch via `.changeset/wr-itil-p118-reconcile-readme.md` (commit `9c50d03`). Self-bootstrap demonstration this commit: 12 standing drift entries corrected — P074 + P110 removed from WSJF Rankings (closed + verifying on disk); P073 + P079 + P080 + P081 + P082 + P087 + P088 added to WSJF Rankings (open on disk, never inserted by prior creation refresh); P110 added to Verification Queue; P056 + P075 removed from Verification Queue (closed on disk, P062 closure didn't refresh). Architect APPROVED Shape A + minimal Shape C (skill+script split, exit-code-driven preflight, narrative-aware edit application). JTBD-006 (Progress the Backlog While I'm Away) primary — orchestrator's Step 3 ranking now reads filesystem ground truth. JTBD-001 (Enforce Governance Without Slowing Down) secondary. AFK iter 5, single-commit transition + self-bootstrap per ADR-014 amended "Reconciliation as preflight robustness layer" sub-rule. Prior: 2026-04-25 **P111 → Verification Pending** — Agent/Task subprocess tool calls now refresh parent session's gate markers via Option 1 subprocess-completion refresh (architect verdict 2026-04-25). Multi-package patch across `@windyroad/{architect,jtbd,style-guide,voice-tone,risk-scorer}`. New shared helper `slide_marker_on_subprocess_return` in 5 byte-identical `gate-helpers.sh` copies; 5 new `*-slide-marker.sh` PostToolUse:Agent\|Bash hooks; risk-scorer slides only `${RDIR}/{commit,push,release}` (born markers deliberately not slid — preserves P090 2×TTL hard-cap invariant per ADR-009 footnote). Helper contract: touches only existing markers, never creates; skips on `tool_response.is_error`; fail-safe on parse error. ADR-009 amended with "Subprocess-boundary refresh (P111, 2026-04-25)" subsection citing ADR-032 line 123 cross-process isolation invariant — the parent's hook touches its OWN marker, no cross-process sharing. Composes orthogonally with P090's three-band TTL refinement (in-band Band B slide + on-return subprocess slide both touch the same `<action>` mtime; 2×TTL hard-cap from `<action>-born` still bounds total marker life). 36 new behavioural bats: 30 `slide-marker-on-subprocess-return.bats` (6 per plugin × 5 plugins) + 6 `architect-slide-marker.bats` hook-level integration tests. Full suite 935/935 green. Architect APPROVED (Option 1 acceptable; Option 2 rejected at ADR level by ADR-032 line 123; Option 3 conflicts with P090). JTBD PASS (advances JTBD-001 + JTBD-006; no regression). JTBD-001 (Enforce Governance Without Slowing Down) primary — eliminates the re-delegation cycle on subprocess return. JTBD-006 (Progress the Backlog While I'm Away) — `claude -p` AFK iteration pattern no longer hits mid-loop gate denials caused by subprocess timing artefacts. AFK iter 4, single commit per ADR-014. Prior: 2026-04-25 **P078 → Verification Pending** — strong-signal correction now triggers `MANDATORY` reminder via new `packages/itil/hooks/itil-correction-detect.sh` (`UserPromptSubmit`). Hook detects FFS / `DO NOT` / direct contradiction / `!!!` / "you always|never|keep" via new `CORRECTION_SIGNAL_PATTERNS` + `detect_correction_signal()` in `packages/itil/hooks/lib/detectors.sh` (parallel to existing `PROSE_ASK_PATTERNS` / `DIRECTION_PIN_PATTERNS` registry per architect verdict — extend, do not split). Injects systemMessage instructing the assistant to OFFER `/wr-itil:capture-problem` (today's fallback `/wr-itil:manage-problem`) BEFORE addressing the operational request. Once-per-session full block + terse-reminder per ADR-038 (`itil-correction-detect` marker key). New user-memory `feedback_capture_on_correction.md` for cross-project portability (Option C). `CLAUDE.md` pointer line added — full vocabulary stays in detectors.sh per ADR-038. 14 new behavioural bats (FFS / DO NOT / contradiction / `!!!` / "you always" / "you're not listening" / session-marker / terse-byte-budget / capture+manage fallback / different-session re-emit / empty-session-id / negative conversational / direction-pin doesn't trigger); full itil hook suite 52/52 green. Architect APPROVED + JTBD PASS — ADR-038 + ADR-032 + ADR-002 cover, no new ADR. `@windyroad/itil` patch via `.changeset/p078-correction-detect.md`. JTBD-001 (Enforce Governance Without Slowing Down) primary. AFK iter 3, single commit per ADR-014. Prior: 2026-04-25 **P070 → Verification Pending** — `/wr-itil:report-upstream` gains Step 4b dedup check (own re-run grep + third-party `gh issue list --search` + inline LLM semantic match per Direction decision 2026-04-21, NO subagent dispatch) and Step 5c comment path (`gh issue comment <n>` cross-reference body when match found). New disclosure-path string `commented-on-existing-issue` extends Step 7 back-write enumeration. AFK auto-comment branch is on **interim static heuristic** (always halt-and-save the drafted report to `## Drafted Upstream Report`) — maintainer-annoyance risk evaluator DEFERRED to compose with `wr-risk-scorer:external-comms` per ADR-028 line 117 (architect verdict; keeps P070 at Effort M and avoids cross-cutting work blocking on P064). ADR-024 amended (Decision Outcome adds Step 4b + 5c; Out of scope dedup bullet narrowed to residual `update-mode`; Confirmation criterion 2 extended). 9 new behavioural bats assertions; full P070 test file 24/24 green. AFK iter, single commit per ADR-014. Prior: 2026-04-25 **P119 → Verification Pending** — agent bypasses `/wr-itil:manage-problem` Step 2 duplicate-check by writing tickets directly to `docs/problems/` (AFK iter; `@windyroad/itil` patch via `.changeset/wr-itil-p119-manage-problem-enforce-create.md`). Fix shipped: new `packages/itil/hooks/manage-problem-enforce-create.sh` PreToolUse:Write hook matching `docs/problems/<NNN>-*.<status>.md` new-file paths gating on per-session marker `/tmp/manage-problem-grep-${SESSION_ID}` set by manage-problem SKILL.md Step 2 substep 7 (added in same commit). New helper `packages/itil/hooks/lib/create-gate.sh` (sibling of `lib/review-gate.sh`; different semantics — no TTL drift detection — kept separate per architect direction). Allow-lists `docs/problems/README.md` (chicken-and-egg) and existing files (Edit-flow / status transitions). 16 new behavioural bats in `packages/itil/hooks/test/manage-problem-enforce-create.bats` covering deny path / allow path / multi-concern split (Step 4b) / README exemption / Edit-flow exemption / status-suffix coverage / ADR-031 forward-compat / marker hygiene; full suite 876/876 green. Architect APPROVED + JTBD PASS — ADR-009 + ADR-038 cover the marker semantics, no amendment needed. Prior 2026-04-25 P119 open: 2026-04-25 **P089 → Verification Pending** — work-problems Step 5 dispatch robustness (two gaps coordinated in one ticket, per architect Option C / JTBD pass): **Gap 1** adds `< /dev/null` to the canonical `claude -p` dispatch command in `packages/itil/skills/work-problems/SKILL.md` Step 5, plus Flag rationale prose explaining the stdin warning is on stderr but becomes a stdout problem under `2>&1` capture (so adopters who separate streams don't need the redirect); **Gap 2** adds an Authority hierarchy paragraph to the Per-iteration cost metadata block and a matching Authority note to the Output Format Session Cost section — `.total_cost_usd` is cumulative-authoritative, `.usage.*` is best-effort and can reflect only the final-turn ack when the subprocess exits via a background-task completion notification (detection phrased descriptively — final-turn-sized usage alongside `duration_ms` orders of magnitude smaller than wall-clock — per architect option-b, no change to the named-field extraction list). 6 new behavioural bats in `work-problems-step-5-delegation.bats`; 30/30 file-level green; full suite clean. Architect PASS + JTBD PASS; no ADR amendment (within existing ADR-032 subprocess-boundary variant); no report-upstream (CLI behaviour correct-by-contract). `@windyroad/itil` patch bump via `.changeset/wr-itil-p089-step-5-stdin-redirect.md`. JTBD-006 (Progress the Backlog While I'm Away) — first-run adopter papercut eliminated + Session Cost observability restored. AFK iter, single commit per ADR-014. Prior: 2026-04-25 **P090 → Verification Pending** — risk-scorer commit-gate TTL gains a three-band policy in `packages/risk-scorer/hooks/lib/risk-gate.sh`: Band A (age < TTL/2) pass; Band B (TTL/2 ≤ age < TTL) if pipeline state-hash invariant since scorer-run pass + slide marker forward (bounded by 2×TTL hard-cap from new `<action>-born` sibling), else halt-on-drift as before; Band C (age ≥ TTL) halt as before. Mechanical-only — no LLM invocation in Band B (architect picked Option (c) hash-compare over Option (a) systemMessage-rescore; ADR-013 Rule 6 citation dropped since no decision is re-made). `git-push-gate.sh` push-gate refactored to call `check_risk_gate "push"` (was inline binary TTL) per architect advisory, inheriting the band logic; push-specific threshold guidance preserved via new `RISK_GATE_CATEGORY` export. `risk-score-mark.sh` writes `{commit,push,release}-born` siblings on every pipeline-scorer run (hard-cap anchor). ADR-009 "Three-band TTL refinement" footnote appended (architect: footnote sufficient). 9 new behavioural bats (Band A/B/C + hash-invariant slide + drift-denial + hard-cap + category exports); full suite 846/846 green. Architect-and-JTBD-and-risk-scorer gate-pass. WIP/architect/JTBD markers stay on binary TTL (out of scope — symmetric adoption deferred). **P111 orthogonal** (subprocess-vs-parent marker scoping — different axis; remains Open at WSJF 6.0). `@windyroad/risk-scorer` patch bump via `.changeset/wr-risk-scorer-p090-three-band-ttl.md` (architect: transparent false-positive reduction, not new envelope). JTBD-001 (Enforce Governance Without Slowing Down) + JTBD-006 (Progress the Backlog While I'm Away). AFK iter, single commit per ADR-014. Prior: 2026-04-25 **P112 → Verification Pending** — install-updates Step 7 uninstall+install window hardened with bounded retry (3× exponential backoff) + rollback (marketplace-cache refresh + one additional install). New Status vocabulary on the Step 8 final-report table (`✓ installed` / `✓ restored (rollback)` / `✗ lost (rollback failed)` / `✗ failed`). Repo-local skill per ADR-030 — no changeset required. 7 new behavioural contract-assertion bats; full suite 837/837 green. Architect GO + JTBD PASS + risk-scorer WIP 3/25 Low. Upstream dependency (the `claude plugin install` silent-no-op that drove the P106 workaround) noted in ticket; `/wr-itil:report-upstream` invocation deferred to interactive. AFK iter, single commit per ADR-014. Prior: 2026-04-25 **P109 → Verification Pending** — work-problems Step 0 preflight gains a session-continuity detection pass after the existing fetch/divergence check. Enumerates five signals (untracked `docs/decisions/*.proposed.md`, untracked `docs/problems/*.md`, `.afk-run-state/iter-*.json` with `is_error: true` / `api_error_status >= 400`, stale `.claude/worktrees/*` + `git worktree list` on `claude/*`, uncommitted SKILL.md/source/ADR edits). Routes interactive via `AskUserQuestion` with 4 options (resume / discard / leave-and-lower-priority / halt); routes AFK via halt-with-report per ADR-013 Rule 6. Detection only — worktree cleanup out of scope. ADR-019 extended within its 2026-07-18 reassessment window (no new ADR). `packages/itil/skills/work-problems/SKILL.md` + `docs/decisions/019-afk-orchestrator-preflight.proposed.md` + 16 new contract-assertion bats; full suite 830/830 green. `@windyroad/itil` minor bump via `.changeset/wr-itil-p109-preflight-session-continuity.md`. AFK iter, single commit per ADR-014. Prior: 2026-04-25 **P071 → Verification Pending** — all six phased-landing slices shipped across `@windyroad/itil@0.10.0–0.17.0` (commits `412443f` list-problems, `d8ab4c5` review-problems, `ffa85a7` work-problem, `91da109` transition-problem, `38756a8` list-incidents, `248edad` mitigate-incident, `4a25a60` restore/close/link-incident). 9 new skills + 9 thin-router forwarders on `manage-problem` + `manage-incident` + `deprecated-arguments: true` frontmatter + ADR-010 P071 amendment (Skill Granularity + Confirmation + Reassessment). Audit completeness confirmed 2026-04-21 AFK iter 2 — no further offenders. Single-commit AFK iter transition per ADR-014. Prior: 2026-04-25 **P083 → Verification Pending** — ScheduleWakeup forbidding clause in `packages/itil/skills/work-problems/SKILL.md` Step 5 (line 152) shipped in `@windyroad/itil@0.13.0` via commit `260768f` (alongside P084's subprocess-dispatch rewrite; the ScheduleWakeup clause rode the same Step 5 prompt template edit). Retroactive Open → Verification Pending transition (ADR-022: Known Error is for fix-not-yet-shipped; since fix IS shipped, Known Error is semantically wrong — direct transition to Verification Pending). Added 2 bats regression assertions to `packages/itil/skills/work-problems/test/work-problems-step-5-delegation.bats` (24/24 suite green; full suite 814/814 green; RED-without-clause empirically verified via temp-removal test). Test-infra only — no changeset per test-only carve-out. ADR-037 Permitted Exception (doc-lint contract assertion). AFK iter, single commit per ADR-014. Prior: 2026-04-24 **P085 fix released** → Verification Pending — `@windyroad/itil` minor bump adds UserPromptSubmit + Stop hook pair enforcing "obvious → act; ambiguous → AskUserQuestion; never prose-ask". Architect GO re-confirmed (hook event binding corrected from ticket's speculated PostToolUse to UserPromptSubmit + Stop — PostToolUse does not fire on pure assistant text). Two entry scripts + shared `lib/detectors.sh` registry per architect layout verdict; root `CLAUDE.md` gains 2-4 line pointer promoting the `feedback_act_on_obvious_decisions.md` memory rule to repo-level MANDATORY (full phrasing list stays in detector file per ADR-038 progressive disclosure); `scripts/sync-session-marker.sh` adds `itil` to CONSUMERS. 22 new behavioural bats tests (per P081 / `feedback_behavioural_tests.md`); full suite 812/812 green. Prior: 2026-04-24 **P118 opened** — systemic stale-README class ticketed: 10 open tickets missing from WSJF Rankings + 2 stale-verifying entries + stale VQ rows for already-closed tickets, all accumulated despite P094 + P062 both Closed. Follow-up from run-retro Step 2b during P108 close AFK iteration. Prior: 2026-04-24 **P108 closed — design decision: scorer's free-form `RISK_REMEDIATIONS:` prose + agent judgment is the intended contract; ADR-042 Rule 2a already defines the semantics; no code change needed. Minimal README reconcile only (P108 removed from dev-work; P102+P105 moved from dev-work to Verification Queue to match on-disk `.verifying.md`; P110 added to dev-work). Systemic stale-README gap — 10 other open tickets still missing from dev-work (P073, P079–083, P085, P087–090) — deferred to retro for pipeline-instability classification per P074.**. Prior: 2026-04-24 **P116 fix released — push:watch preflight counts local-only commits (warn-and-proceed per ADR-013 Rules 5/6; extracted to scripts/push-watch.sh; 18/18 bats green)**. Prior: 2026-04-24 **run-retro Step 4a — closed P063 + P067 + P092 + P094** from in-session evidence: P063 (external-RCA trigger + report-upstream invocation filed anthropics/claude-code#52831), P067 (ADR-033 classifier matched problem-first + bug_report.yml backward-compat fallback), P092 (install-updates npm-name transformation resolved 11/11 plugins correctly), P094 (README-refresh rode 5 manage-problem-class commits this session). Briefing additions: `plugin-distribution.md` gains 2 entries + amends 1 (installed_plugins.json worktree accumulation + TUI/agent enumerator asymmetry diagnostic); `governance-workflow.md` gains "gate approvals validate changes, not hypotheses" from P113 case study; `releases-and-ci.md` gains "local-commit accumulation masks CI regressions" from 64f6d3f exposure pattern. P116 opened — push preflight should count local-only commits and warn before batch-push (WSJF 9.0, S effort). P117 opened — no batch-transition surface for multiple tickets, forcing N× SKILL.md reload or inline ownership-boundary violation (WSJF 3.0, M effort; architect decision pending on shape). Prior: 2026-04-24 **P114 closed** — verified in-session via observed green CI on every commit since the fix (97c976e, 38a0f7e, 8f6e89e, e14040b, 245e09c); release PR #64 auto-merged; tests 699/700/701 flipped from `not ok` to `ok` as predicted. Prior: 2026-04-24 **P113 closed** — user restarted + re-tested post-cleanup; `/wr-itil:report-upstream` now appears at top of autocomplete (screenshot). Actual RCA was stale `wr-itil@0.1.0` pinned to abandoned git worktree (not the shipped allowed-tools fix, which is still kept as a valid semantic improvement). Abandoned `.claude/worktrees/upbeat-keller` also removed (`git worktree remove --force` + branch delete). P115 tracks the systemic install-updates gap. Prior: 2026-04-24 **P113 post-release experiment — shipped `Skill, Agent` fix did NOT resolve autocomplete**: user installed @windyroad/itil@0.18.1 + restarted + re-tested; `/wr-itil:report-upstream` still missing. Investigation revealed the **actual RCA**: stale `wr-itil@0.1.0` install entry in `~/.claude/plugins/installed_plugins.json` pinned to abandoned git worktree `.claude/worktrees/upbeat-keller` (installed 2026-04-18 when wr-itil was 0.1.0, orphaned since; 0.1.0 has only 3 skills, no report-upstream). TUI autocomplete reads first matching install entry; agent enumerator takes union — explains the agent-sees-it / TUI-doesn't discrepancy. Per-machine cleanup via `(cd .claude/worktrees/upbeat-keller && claude plugin uninstall wr-itil@windyroad --scope project)` removed stale entry. P113 stays Verification Pending awaiting post-cleanup re-test. Shipped allowed-tools fix retained — still a real semantic improvement. **P115 opened** — `/install-updates` Step 3 walks `../*/` siblings only, never descends into `.claude/worktrees/*` under current project, so worktree-pinned stale installs stay invisible (M effort, WSJF 3.0; architect decision needed on contract shape). Prior: **P114 opened → Verification Pending** — `packages/risk-scorer/agents/test/risk-scorer-structured-remediations.bats` tests 699/700/701 bare-word `grep -q "action_class"` collided with ADR-042 prose "No structured action_class column" in pipeline.md/wip.md/plan.md (introduced in 64f6d3f, revealed by b2424c8 push because intermediate commits never reached origin CI). Fix: tighten three assertions to `grep -qE '^\| *action_class\b'` — column-header syntax only, prose ignored. Local bats 14/14 green. Architect + JTBD PASS. No changeset (test-only, no runtime behaviour). Unblocks P113 release verification + PR #64. Prior: 2026-04-24 P113 fix released → Verification Pending — `packages/itil/skills/report-upstream/SKILL.md` line 4 gains `Skill, Agent` in `allowed-tools` (skill body invokes both but declared neither; the only AskUserQuestion-using itil skill missing `Skill`). Architect + JTBD reviews PASS. Effort S, single-line frontmatter edit, ships in next itil patch. Verification path: `/install-updates` + type `/wr-itil:report` in TUI and confirm `/wr-itil:report-upstream` appears in autocomplete. If still missing, reopen for upstream escalation. Prior context: 2026-04-23 P107 fix — default TTL extended 1800s → 3600s across all gate-marker plugins (architect, jtbd, style-guide, voice-tone, risk-scorer). ADR-009 amended; READMEs, briefing files, run-retro SKILL.md updated. P107 closed. Prior context: 2026-04-22 run-retro (post-ADR-041) — **P107 evidence appended** (architect+JTBD markers expired mid-ADR-041 landing at file ~11; parallel re-delegation unblocked; confirms the N ≥ 6 files pattern holds at release-commit layer). **P109 opened** (work-problems preflight Step 0 does not detect prior-session partial-work state — untracked ADRs, `.afk-run-state/iter-*.json` 429, stale worktrees; M, WSJF 4.5; session-continuity extension to P040 origin-fetch preflight). Prior context: 2026-04-22 ADR-041 landing — **P103 + P104 → Verification Pending** (ADR-041 "never release above appetite + auto-apply scorer remediations" landed; work-problems Step 6.5 / manage-problem Step 12 / manage-incident Step 15 gain the above-appetite auto-apply branch; `docs/changesets-holding/` blessed per Rule 7; ADR-018 + ADR-020 cross-reference ADR-041 from the same commit; bats contract-assertion file added). **P108 opened** (scorer remediation action-class vocabulary — closed enumeration needs extension beyond `move-to-holding`; L, WSJF 5.0; tracked work deferred from ADR-041 Rule 2a). Prior context: 2026-04-22 run-retro (post-slice-2) — **Closed P084, P036, P060, P054** via run-retro Step 4a from in-session evidence: P084 exercised across 4 subprocess iterations (Agent tool in-surface confirmed; architect+JTBD+risk-scorer delegations fired inside each); P036 Step 6.75 inter-iteration `git status --porcelain` check fired 4× correctly; P060 push:watch anchored on HEAD sha across 4 pushes (CI matched each SHA, no stale-workflow false-success); P054 release:watch drift-hash + auto-reducing-bypass contract held across 3 drains (itil 0.17.1 / itil 0.17.2 + patches / retrospective 0.8.0). **P107 opened** (architect+JTBD edit-gate markers expire during long multi-file edit batches — 3 Writes blocked mid-slice-2 requiring re-delegation; fix candidates: TTL extension, PostToolUse auto-refresh, batch-semantic marker). Prior context: 2026-04-22 interactive slice 2 — **P100 fix released → Verification Pending** (SessionStart hook `session-start-briefing.sh` in @windyroad/retrospective extracts and injects the `## Critical Points (Session-Start Surface)` roll-up from docs/briefing/README.md at matcher=startup; ADR-040 proposed; docs/BRIEFING.md stub deleted; held retrospective-minor changeset reinstated with combined slice-1+slice-2 scope; run-retro writer-side from slice 1 + consumer-side hook from slice 2 ship together as @windyroad/retrospective 0.7.0). **P102 opened** (no invocation surface creates risk register entries — docs/risks/ stays empty after P033 scaffolding; M, WSJF 7.5). **P103 opened** (work-problems orchestrator escalates resolved release decisions via AskUserQuestion — defeats AFK; M, WSJF 10.0). **P104 opened** (partial-progress outcome paints release queue into corner; M, WSJF 8.0). **P105 opened** (run-retro signal-vs-noise pass for briefing curation; M, WSJF 8.0 — split from P100 slice 2 per user direction). Prior context: 2026-04-22 AFK iter 4 — **P100 re-rated M → L (WSJF 10.0 → 5.0)** on partial-progress slice 1 annotation (directory migration + README index + transitional stub + run-retro SKILL.md writer-side update + architect/jtbd hook exemptions extended to docs/briefing/). Prior context: 2026-04-22 AFK iter 3 — **P094 fix released → Verification Pending** (manage-problem Step 5 now unconditionally refreshes README.md on new-ticket creation; Step 6 conditionally refreshes when Priority / Effort / WSJF change; Step 11 staging language extended to mandate the refreshed README rides the same commit as the creation / ranking-change update per ADR-014 single-commit transaction). Prior context: 2026-04-22 AFK iter 2 — **P093 fix released → Verification Pending** (transition-problem ↔ manage-problem circular delegation resolved: `/wr-itil:transition-problem` now hosts Step 7 inline — pre-flight + P063 + P057 re-stage + P062 refresh + ADR-014 commit; manage-problem forwarder round-trip clause stripped; ADR-010 amended with "Split-skill execution ownership / copy, not move" sub-rule; existing contract-assertion test inverted in place; full suite 736/736). Prior context: 2026-04-22 AFK iter 1 — **P092 fix released → Verification Pending** (install-updates SKILL.md Step 4 now uses `npm_name="@windyroad/${plugin_key#wr-}"` with ADR-002 naming-convention pointer + worked examples + "empty `npm view` means wrong name, not private" callout; repo-local per ADR-030, effective next invocation). Prior context: 2026-04-22 session — **P101 opened** (`wr-retrospective` has no context-usage analysis — opaque where session tokens are consumed; no guidance on what to trim; L, WSJF 3.0; user-proposed with codeburn as conceptual reference; subsumes P091's measurement-harness investigation task; preferred delivery: layered — cheap in run-retro + deep on demand). Prior context: **P057 + P095 closed** via run-retro Step 4a on in-session evidence (P057 staging-trap exercised 4x this session; P095 ADR-038 once-per-session behaviour observed ~15 prompts) + **P062 stale Verification-Queue row removed** (ticket was closed in a prior session; README wasn't refreshed at closure). Prior context: **P100 opened** (`wr-retrospective` does not auto-surface `docs/BRIEFING.md` at session start — cross-session learnings go unread in adopter projects; M, WSJF 10.0; user-identified during P098 verification — "we shouldn't have to add these things to CLAUDE.md. It should be automatic"; scoped narrowly to `wr-retrospective` after user clarification that `wr-itil`'s backlog stays on on-demand discovery). Prior context: **P099 opened** (docs/BRIEFING.md grows unbounded via run-retro appends; L, WSJF 3.75; composes with P098's progressive-disclosure cluster). Prior context: **P098 fix released → Verification Pending** (ADR-038 progressive-disclosure pattern applied to project/user-owned context contributors; in-repo fix: new project-level `CLAUDE.md` at repo root (24 lines, pointers only); `.claude/skills/install-updates/SKILL.md` split 238 → 149 lines / 13.5KB → 6.8KB with sibling REFERENCE.md; stale memory files pruned (project_state.md 12d old, project_jtbd_migration.md 7d old); BRIEFING note documents the SKILL+REFERENCE pattern as reference implementation for P097's expected generalisation ADR. ~/CLAUDE.md follow-up remains as user action). Prior context: **P095 fix released → Verification Pending** (ADR-038 "Progressive disclosure + once-per-session budget for UserPromptSubmit governance prose" landed with shared `session-marker.sh` helper, 5 per-plugin synced copies, 5 hook edits (architect-detect / jtbd-eval / tdd-inject with dynamic-state carve-out / style-guide-eval / voice-tone-eval), 45 new bats assertions across 7 new files; full suite 735/735 green; reclaims ~120KB / ~30k tokens per 30-turn 3-active-hook session). Prior context: **P091 split** into session-wide context budget meta (retitled + effort re-rated L → XL; WSJF 1.875) + P095 (now Verification Pending, was Known Error WSJF 7.5) + **P096 opened** (PreToolUse/PostToolUse hook injection cluster, L, WSJF 3.0 — ~25 hooks pending audit) + **P097 opened** (SKILL.md runtime size cluster, L, WSJF 3.0 — magnitude confirmed: manage-problem 55KB, work-problems 39KB, run-retro 36KB; fix path via progressive-disclosure REFERENCE.md split pending validation) + **P098 opened** (project/user-owned cluster: global `~/CLAUDE.md` + `.claude/skills/install-updates/SKILL.md` + MEMORY.md curation, M, WSJF 6.0). Unifying solution pattern across all four children: **progressive disclosure** — less info upfront with explicit affordances (agent pointers, REFERENCE.md paths, project-level CLAUDE.md) so the consumer can expand on demand. Prior context: 2026-04-22 session — **P029 + P059 closed** (verified in-session via run-retro Step 4a) + **P091 + P092 + P093 + P094 opened** (startup context; install-updates npm-name; transition-problem circular delegation; manage-problem README creation-refresh gap). Prior context: 2026-04-21 AFK iter 7 — **P076 transitioned to Verification Pending**: WSJF transitive-effort rule now lives inline in `/wr-itil:manage-problem`'s WSJF Prioritisation section. Full itil sweep: 340/340 green. Prior context (iter 7 iter 1): P067 classifier problem-first per ADR-033. Prior context (iter 6): P084 transitioned to Verification Pending; subprocess dispatch variant of ADR-032 now supersedes P077 Agent-tool variant on the Step 5 surface. **P130 transitioned Known Error → Verifying** — user-reframed Fix Strategy supersedes the original dual-mode-dispatch + presence-detector approach; SKILL.md prose-discipline only (new "Mid-loop ask discipline (orchestrator main turn)" subsection inside Non-Interactive Decision Making + Step 5 transient-user framing + Decision Table row); 20-assertion `work-problems-no-mid-loop-asking.bats` per ADR-037 + P081; full work-problems suite 175/175 green; project-wide bats 1310/1310 green; ADR-032 amendment NOT needed; out-of-scope per reframe: presence-signal helper / dual-mode dispatch / stream-json observation surface. **P085 transitioned Known Error → Verifying** — `PROSE_ASK_PATTERNS` extended in `packages/itil/hooks/lib/detectors.sh` to close 2026-04-28 Citation 1 phrasing-gap (orchestrator emitted "Awaiting your direction on whether to add it + resume on P123, or end the session." at Step 6.75 halt-summary; existing pattern list returned exit-code 1 on this text; post-fix matches via new "Awaiting your (direction\|input\|decision\|response\|confirmation\|answer\|reply)" + "Pending your ..." + "Once you confirm" entries); 5 new behavioural bats in `itil-assistant-output-review.bats` (17/17 green; full 85-test itil hooks suite green); Citation 2 (over-ask when framework prescribes answer) DEFERRED to P132 broader fix (framework-knowability detection — substantially harder than phrasing-list extension; composes with ADR-044 R6 numeric gate); architect verdict ALIGNED / PASS no new ADR; JTBD verdict ALIGNED / PASS JTBD-001 + JTBD-006 axes preserved.

## 2026-04-28 (P131 known-error → verifying transition — iter 8)

> Last reviewed: 2026-04-28 **AFK iter — P131 transitioned Known Error → Verification Pending**: Phase 2 load-bearing enforcement hook shipped — new `packages/itil/hooks/itil-claude-space-protection.sh` PreToolUse:Write|Edit hook denying agent writes to project-scoped `.claude/` paths not in user-space allow-list (settings.json, MEMORY.md, .install-updates-consent, scheduled_tasks.lock, *.local.json root-only, skills/commands/agents/hooks/projects/worktrees/ subtrees) unless approval marker `.claude/.agent-write-approved-<sha256-of-rel-path>` is present; new shared `lib/claude-space-gate.sh` helper; 34 behavioural bats per ADR-037 + P081, full 129-test itil hooks suite green. Architect PASS-WITH-NOTES (allow-list amended; ADR formalising user-space-vs-project-space distinction deferred to Phase 3 per ticket Fix Strategy); JTBD ALIGNED (JTBD-001 + JTBD-006 + JTBD-101); ADR-038 byte budget + ADR-045 silent-on-pass + ADR-013 Rule 6 fail-open observed.


## 2026-04-29 (P143 open → verifying transition — AFK iter 9)

> Last reviewed: 2026-04-28 **AFK iter — P140 transitioned Open → Verification Pending**: Phase 1 declarative SKILL.md amendment shipped — `packages/itil/skills/work-problems/SKILL.md` Step 6.5 "Failure handling" subsection rewritten with diagnose-then-classify routing (`gh run view --log-failed` per ADR-026 grounding), closed fixable-in-iter allow-list (P081-class stale-grep-string, hook stub mismatch, test ID drift, environmental flake), ambiguous-defaults-to-halt guard, fix-and-continue branch (each retry rides standard ADR-014 commit gate flow per ADR-042 Rule 3 precedent), 3-retry cap per iteration (not per failure-class), halt branch preserved for genuinely-unrecoverable (auth failure / npm publish rejection / semantic test requiring user judgment / repeated transient / outside allow-list), Step 2.5b cross-reference (P126) preserved on halt path; Decision Making table row added; Mid-loop ask discipline (P130) Step 6.5 CI-failure halt-point bullet narrowed to outside-allow-list / cap-reached scope; new behavioural bats `work-problems-step-6-5-fix-and-continue.bats` 28/28 green per ADR-037 + P081 covering each prose-contract assertion; full work-problems suite 203/203 green; architect verdict PASS (Phase 1-only scope correct, ADR-014 invariant preserved via per-retry gates per ADR-042 Rule 3, no new ADR needed; advisory closed-allow-list scope-creep guard added per ADR-044 deviation-candidate framing); JTBD verdict PASS (JTBD-006 primary restored, JTBD-001/002 compose intact, persona-misread risk addressed via closed-list + ambiguous-defaults-to-halt + per-iteration cap clarification); Phase 2 advisory classifier (`packages/itil/scripts/diagnose-ci-failure.sh`) DEFERRED — observe declarative discipline over 30 days per Fix Strategy.

> Earlier (preceding entry preserved): **AFK iter — P133 transitioned Open → Verification Pending**: Phase 1 immediate fix shipped at the proximate failure surface (`scripts/repo-local-skills/install-updates/SKILL.md` Step 6 inner loop now uses bash-array iteration `PLUGINS_TO_UPDATE=(...)` + `"${PLUGINS_TO_UPDATE[@]}"` — portable across bash and zsh, replaces the silent-fail `for plugin in $PLUGINS_TO_UPDATE` shape that masqueraded as 24 lost plugins on 2026-04-27) plus defensive rename in `packages/itil/scripts/reconcile-readme.sh` (`status` → `ticket_status` to avoid the zsh read-only built-in name) plus new P133 behavioural regression case in the bats suite (17/17 green). Audit of dispatch-named scope returned clean — the 4 SKILL.md files (run-retro, work-problems, manage-problem, transition-problem) had no bash-isms; itil hooks all have `#!/bin/bash` shebangs. Phase 2 (repo-wide audit) + Phase 3 (CI lint) deferred to compose with P136 (ADR-044 alignment audit master). Architect ALIGN, JTBD ALIGN (JTBD-001 + JTBD-007 + JTBD-101).

> Later — **P144 transitioned Open → Verification Pending** — gate-misfire recovery procedure documented in `manage-problem` SKILL.md Step 2 substep 7 + conditional recovery hint in `manage-problem-enforce-create.sh` deny message; ADR-048 sanctions and scopes the procedure with P142-auto-supersession criteria + audit-trail-preservation test ruling out P131 "any-marker-anywhere" generalisation; 14 new SKILL.md bats + 3 new hook bats per ADR-037 + P081; architect ALIGNED with four advisory items all incorporated; JTBD PASS. Earlier same day — **P146/P147/P148 opened — retro Stage 1 recovery from deferred-observation correction**: user surfaced the *"observations could have very easily been lost if I was in a rush"* correction; recovered today's retro's two deferred observations as P146 (iteration polling-regex deadlock) + P147 (P121 SIGTERM-clean-flush guarantee conditional), AND ticketed the meta-pattern as P148. Earlier — **P145 opened — Tier 3 rotation defers recurringly** (ADR-040 reassessment trigger satisfied; three candidate fix shapes recorded). Earlier — **AFK iter — P143 transitioned Open → Verification Pending**: `release-watch.sh` race-condition fold-fix (poll-loop `find_release_pr` shell function in `scripts/release-watch.sh`, 7 behavioural bats, ADR-022 amendment endorsing fold-fix Open → Verifying when ticket already documents root cause + fix strategy inline; architect re-confirm PASS, JTBD-006 PASS).

## 2026-05-02 (P148 known-error → verifying — retrospective Stage 1 fix released; commit lands work that landed 2026-04-29 AFK iter)

> Last reviewed: 2026-04-29 **P082 re-rated WSJF 6.0 → 1.5 (transitive XL via P038 per P076)** — added missing `## Dependencies` section to `docs/problems/082-no-voice-tone-or-risk-gate-on-commit-messages.open.md` (Blocked by: P038, P064; Composes with: P073) so Step 9b.1 graph traversal picks up the edge in future reviews; Effort line carries `<!-- transitive: XL via P038 (marginal: M) -->` audit comment per P076 step 5; WSJF Rankings re-rendered so P082 sits in the 1.5 tier alongside P038 / P073 (closes the user observation that "P038 should be higher on the list than P082"). Prior multi-fragment line 3 (P144 + P146/P147/P148 opens + P145 open + P143 verifying) rotated to `docs/problems/README-history.md` under the existing 2026-04-29 heading per P134.

> Later 2026-05-02 — **P148 transitioned Known Error → Verification Pending — retrospective Stage 1 anti-defer fix released**: `packages/retrospective/skills/run-retro/SKILL.md` Step 4b Stage 1 AFK-branch rewritten to (a) name `cause: skill_unavailable` as the only valid fallback gate, (b) require every Tickets Deferred entry carry an explicit `cause:` field, (c) enumerate the four named anti-pattern rationalisations the agent must NOT use (session-length pressure, lifecycle weight, retro-summary-defer preference, fabricated subcommands), (d) cite the user's verbatim correction phrase, (e) cite ADR-044 framework-mediated surface + P145 sibling pattern; Step 5 retro summary template gains a `### Tickets Deferred` section with `Observation | Cause | Citation` columns; new advisory script `packages/retrospective/scripts/check-tickets-deferred-cause.sh` walks `docs/retros/*.md` and emits `RETRO <date> file=<basename> deferred=<N> with_valid_cause=<M> violations=<K>` per file plus TOTAL summary, exit 0 always per ADR-040 declarative-first / ADR-013 Rule 6; 23 behavioural bats per ADR-037 + P081 (20 in `check-tickets-deferred-cause.bats` + 3 in `run-retro-stage-1-fallback-gating.bats`) — 23/23 green, full retrospective suite 127/127 green confirming no regression. Prior line-3 fragment (P082 re-rate WSJF transitive 6.0 → 1.5 via P038/P064) rotated to `docs/problems/README-history.md` under `## 2026-05-02` heading per P134.

> Later 2026-05-02 — **P149 + P150 opened — 2026-05-02 retro Step 2b pipeline-instability detections**: P149 (manage-problem Step 0 reconcile halt-on-drift directive overreaches — should distinguish uncommitted-rename-rooted drift from committed cross-session drift; observed during P148 release session where uncommitted prior-session AFK-iter rename triggered drift detection but inline refresh per ADR-014 single-commit grain was the correct path; fix is bounded SKILL.md amendment in three files + behavioural bats); P150 (Verification Queue rendered newest-first contradicts section header "oldest first"; documentation-vs-rendering mismatch across five SKILL.md render blocks; sibling fix shape to P138's tie-break-ladder marker; observed when inserting P148 into the queue today). Both Open / S / WSJF 4.0. Prior line-3 fragment (P148 verifying-transition + retrospective Stage 1 fix release) rotated to `docs/problems/README-history.md` under `## 2026-05-02` heading per P134.

> Later 2026-05-02 — **P141 known error — context-pressure-drops-changeset-reminder confirmed (40% miss rate across 5 publishable iters in 2026-04-28 AFK loop session); architect verdict 2026-05-02 approved hook-level enforcement at `git commit` time as same enforcement-layer pattern as P125 staging-trap hook (per-invocation deterministic, no markers); new PreToolUse:Bash hook `packages/itil/hooks/itil-changeset-discipline.sh` + `lib/changeset-detect.sh` helper + behavioural bats per ADR-005 + P081; ≤300-byte deny budget per ADR-045 Pattern 1; allow-list test paths + `README.md` + `*.md` under `docs/` (NOT `SKILL.md` per architect amendment — SKILL.md IS the publishable contract per ADR-037 framing); bypass via `BYPASS_CHANGESET_GATE=1`; defence-in-depth with P073 (different surface — Write/Edit on `.changeset/*.md`). WSJF 4.5 → 9.0 (KE multiplier). Prior line-3 fragment (P149 + P150 opens — 2026-05-02 retro Step 2b detections) rotated to `docs/problems/README-history.md` under `## 2026-05-02` heading per P134.**

> Later 2026-05-02 — **P141 fold-fix Known Error → Verification Pending — `feat(itil-hooks): enforce changeset discipline on git commit (closes P141)`**: new PreToolUse:Bash hook `packages/itil/hooks/itil-changeset-discipline.sh` + `lib/changeset-detect.sh` helper + 21 behavioural bats per ADR-005 + P081 (full itil hooks suite 153/153 green — 132 prior + 21 new, no regression); registered as third PreToolUse:Bash matcher in `packages/itil/hooks/hooks.json` alongside p057-staging-trap and pre-publish-intake; deny ≤300 bytes per ADR-045 Pattern 1 names plugin slug + `bun run changeset` recovery + `BYPASS_CHANGESET_GATE=1` escape + P141 cite; allow-list test paths (`test/`, `hooks/test/`, `scripts/test/`) + `README.md` + `*.md` under `docs/` (NOT `SKILL.md` per architect amendment); composes-with P073 (changeset author-time gate at Write/Edit — different surface, defence-in-depth); P141 row removed from WSJF Rankings, inserted at top of Verification Queue. Prior line-3 fragment (P141 known-error transition) rotated to `docs/problems/README-history.md` under `## 2026-05-02` heading per P134.

> Last reviewed: 2026-05-02 **P151 opened — Published skills reference repo-relative script paths**: 5 invocation sites in `@windyroad/itil` (manage-problem / work-problems / reconcile-readme SKILL.md) + `@windyroad/retrospective` (run-retro / analyze-context SKILL.md) — `bash packages/<plugin>/scripts/<name>.sh` hard-fails in adopter projects with exit 127 `No such file or directory`; sibling concern to P137 (semantic ID-leak) but distinct failure mode (hard-fail executable-path-leak vs P137's degraded-semantics on internal IDs); WSJF 5.0 (Sev 20 / Effort L) places at top of dev-work queue. Surfaced by user mid-loop during work-problems iter 1; captured via second-tier P144 python3-via-Bash recovery after first-tier announce-marker scrape failed (P124 Phase 3 mtime-selection regression). Prior line-3 fragment (P141 fold-fix verifying — `feat(itil-hooks): enforce changeset discipline on git commit (closes P141)`) rotated to `docs/problems/README-history.md` under `## 2026-05-02` heading per P134.

> Later 2026-05-02 — **P151 transitioned Open → Known Error — investigation confirmed `${CLAUDE_PLUGIN_ROOT}` is NOT exported at SKILL.md bash-invocation time (eliminates Candidate 1; hooks see the var because Claude Code interpolates `hooks.json` command strings before invocation, but skill bash dispatch goes through the agent's Bash tool with only `CLAUDE_CODE_*` env vars present). New Candidate 5 discovered + architect-concurred: every plugin's `bin/` dir is on `$PATH` at SKILL bash time (verified — 12+ windyroad bin/ dirs in PATH), so `bash packages/<plugin>/scripts/<name>.sh` becomes `wr-<plugin>-<name>` discoverable by name. Fix shape: `packages/<plugin>/bin/wr-<plugin>-<name>` shim wrappers (`exec "$(dirname "$0")/../scripts/<name>.sh" "$@"`) preserving canonical body under `scripts/` for bats; ADR-049 to codify the rule (architect concurred — sibling to ADR-002/003/017 plugin-boundary class). WSJF 5.0 → 10.0 (KE multiplier). ADR-049 drafting + 5 SKILL.md edits + shim creation deferred to follow-on iter. Prior line-3 fragment (P141 fold-fix verifying) rotated to `docs/problems/README-history.md` under `## 2026-05-02` heading per P134.

> Last reviewed: 2026-05-02 **P152 opened — No pressure or nudge for keeping documentation up to date**: user observed mid-loop during iter 2 that the project has dense pressure stack for code drift (architect / JTBD / risk-scorer / style-guide / voice-tone / TDD / changeset-discipline gates) but ZERO equivalent pressure for doc-content drift; user-proposed fix shape: leverage `docs/jtbd/` job files as canonical source of truth so README narrative is JTBD-anchored — every plugin's README cross-references the JTBD jobs it serves, drift detector compares cited jobs to current job files, advisory in Phase 1 + R6-gated hook in Phase 2 per P131/P135 escalation precedent. Sibling family with P137 (semantic ID-leak), P151 (executable-path-leak), P087 (maturity-label) — all about adopter-facing content quality on different axes (correctness vs currency vs labelling). WSJF 3.75 (Sev 15 / Effort L). Captured via second-tier P144 python3-via-Bash recovery (P124 mtime-selection regression bit again; resolved per ADR-048). Prior line-3 fragment (P151 known error transition — env var unavailable, bin/ on PATH selected) rotated to `docs/problems/README-history.md` under `## 2026-05-02` heading per P134.

## 2026-05-03 (P142 fold-fix Open → Verifying — AFK iter 5; supersedes ADR-048)

> Last reviewed: 2026-05-02 **P153 fold-fix Open → Verification Pending — `fix(retrospective): plugin-attribution shim closes P153 directory-enumeration leak (closes P153)`**: sibling fix to P151 / ADR-049 reassessment-criteria clause 3 — replaced two inline `packages/*/hooks` + `packages/*/skills` directory-enumeration glob loops in `analyze-context` SKILL.md Step 2 with single `wr-retrospective-list-plugin-attribution` invocation; new helper `packages/retrospective/scripts/list-plugin-attribution.sh` resolves source-tree first / `$PATH`-derived plugin-cache walk fallback / `PLUGIN-ATTRIBUTION not-measured reason=no-plugin-source-resolvable` ADR-026 sentinel when neither resolves; new 3-line shim `packages/retrospective/bin/wr-retrospective-list-plugin-attribution` per ADR-049 naming grammar; cross-plugin grep-as-lint `packages/shared/test/no-repo-relative-script-paths-in-skills.bats` extended with directory-enumeration `@test` block (`for X in packages/<plugin>/{hooks,skills,scripts,bin}` pattern) + new shim smoke test (11/11 green); new behavioural-contract bats `packages/retrospective/scripts/test/list-plugin-attribution.bats` (10/10 green covering source-tree single-plugin / multi-plugin / cache-fallback resolution from synthetic cache layout / sentinel branch / ADR-038 ≤150-byte per-row budget); architect APPROVED + JTBD ALIGNED (JTBD-301 plugin-user adopter-session value; JTBD-101 plugin-developer "clear patterns, not reverse-engineering"); no new ADR — ADR-049 reassessment-criteria clause 3 explicitly anticipated this surface; `@windyroad/retrospective` patch changeset queued (`.changeset/wr-retrospective-p153-list-plugin-attribution-shim.md`). P153 row moved Rankings → Verification Queue. Prior line-3 fragment (P151 fold-fix Known Error → Verification Pending released as @windyroad/itil@0.23.2 + @windyroad/retrospective@0.12.3) rotated to `docs/problems/README-history.md` under `## 2026-05-02` heading per P134.

## 2026-05-03 (P145 fold-fix Open → Verifying — defer escape hatch closed)

> Last reviewed: 2026-05-03 **P142 fold-fix Open → Verification Pending — `fix(itil-hooks): runtime-SID instrumentation supersedes ADR-048 (closes P142)`**: P124 Phase 4 structural fix; supersedes ADR-048 (gate-misfire recovery) in the same commit per ADR-022 fold-fix convention. New `packages/itil/hooks/itil-runtime-sid-marker.sh` PreToolUse:Bash|Write|Edit|Read hook captures runtime stdin `session_id` to per-machine, per-user, per-project marker (`/tmp/itil-runtime-sid-${USER}-${cksum_PWD}.current`); `packages/itil/hooks/lib/runtime-sid.sh` is the single source-of-truth path helper both producer (hook) and consumer (helper) source; `packages/itil/hooks/lib/session-id.sh` `get_current_session_id` now reads runtime marker FIRST (env var > runtime marker > Phase 3 announce-marker priority cold-path fallback). Architect-recommended Option (d) chosen over ticket-listed options (a)/(b)/(c) — pure-helper algorithms cannot disambiguate orchestrator-vs-subprocess context from filesystem state alone (would shift misfire from one context to the other). New ADR-050 (`docs/decisions/050-runtime-sid-instrumentation-via-pretooluse.proposed.md`) supersedes ADR-048 — `docs/decisions/048-gate-misfire-recovery-procedure.proposed.md` renamed `.superseded.md` with frontmatter `status` flip + supersession callout block + `superseded-by` link. SKILL.md Step 2 substep 7 recovery prose removed (replaced with one "Phase 4" pointer paragraph); `manage-problem-enforce-create.sh` conditional `RECOVERY_HINT` removed (deny stays terse); obsolete `manage-problem-p119-recovery-path.bats` deleted (structural-grep on removed prose). Behavioural coverage: new `runtime-sid-marker.bats` (7/7 green covering write / silence-on-stdout / exit code / overwrite / empty input / malformed JSON / fail-open on jq absence) + 4 new `session-id.bats` tests (runtime-marker priority over newer announce markers / empty-marker fallback / cold-path fallback / env-var precedence) + 2 reframed `manage-problem-enforce-create.bats` tests (post-ADR-050 deny-message-invariance contract); full itil hooks suite 163/163 green + manage-problem skill suite 139/139 green confirming no regression. Architect verdict PASS (Option (d) compliant with ADR-048 line 46 "Composes with P142 if P142 architect chooses this implementation" + ADR-045 Pattern 1 silent-on-pass binding + ADR-009 marker lifecycle preserved + ADR-037 + P081 behavioural-only + ADR-023 perf review noted: ~0.5-2ms per tool call, ≤1s wall-clock + ≤100KB tmpfs writes per session, ungoverned-risk explicitly accepted in ADR-050 Consequences); JTBD verdict ALIGNED (JTBD-001 primary — P119 create-gate reliability foundation; JTBD-006 — eliminates recurring AFK loop friction class; JTBD-101 — pattern reusable by future plugins via `lib/runtime-sid.sh` source-and-consume); `@windyroad/itil` patch changeset queued (`.changeset/wr-itil-p142-runtime-sid-instrumentation.md`). P142 row removed from WSJF Rankings, inserted at top of Verification Queue. Prior line-3 fragment (P153 fold-fix Open → Verification Pending released `fix(retrospective): plugin-attribution shim closes P153 directory-enumeration leak`) rotated to `docs/problems/README-history.md` under `## 2026-05-03` heading per P134.


## 2026-05-03 (P147 fold-fix Open → Verifying — SIGTERM conditional caveat)

> Last reviewed: 2026-05-03 **P145 fold-fix Open → Verification Pending — `fix(retrospective): MUST_SPLIT closes the Tier 3 defer-recurrence escape hatch (closes P145)`**: closes the recurring-defer accumulator gap on `docs/briefing/<topic>.md` topic files. Today's symptoms reproduced exactly: 6 of 6 topic files OVER the 5,120-byte Tier 3 ceiling, with 3 at ≥ 2× ceiling (afk-subprocess at 4.0× / governance-workflow at 3.4× / agent-interaction-patterns at 2.2×) — directly satisfies ADR-040's "≥ 3 topic files exceed 2× the configured ceiling for ≥ 2 consecutive retro cycles" reassessment trigger. Architect-approved Option H (lightest hybrid) chosen over ticket-listed options 1/2/3: extend `packages/retrospective/scripts/check-briefing-budgets.sh` to emit `MUST_SPLIT <basename> reason=ratio-exceeds-2x` for files at ratio ≥ 2.0× threshold (in addition to existing `OVER` line), and amend `packages/retrospective/skills/run-retro/SKILL.md` Step 3 Tier 3 heuristic with a Branch A that forecloses the `trim-noise` / `leave-as-is` defer escape hatches for `MUST_SPLIT` files — agent picks `split-by-subtopic` if a coherent boundary exists, else `split-by-date` as the safe default (mtime-sort + median-age archive, mechanical and AFK-safe per JTBD-006). Branch B (only `OVER`, no `MUST_SPLIT`) retains the original four-option heuristic — files in 1.0×-2.0× range get one or two more retros to escalate. Architect verdict APPROVE: 2.0× threshold IS already in ADR-040's reassessment trigger so MUST_SPLIT promotes the existing policy from policy-revisit-time to per-cycle enforcement (single-sourced, no policy duplication); ADR-005 / ADR-013 Rule 1+6 / ADR-038 / ADR-040 / ADR-044 / ADR-022 all compliant; no new ADR needed (one-line bookkeeping update to ADR-040 Confirmation + More-Information sections only). JTBD verdict PASS: JTBD-001 (silent-agent stage; user not slowed; 4× ticking accumulator removed) + JTBD-006 (split-by-date is mechanical mtime-sort, AFK-safe, no human judgment) + JTBD-101 (extends reusable advisory triplet `MUST_<action> <id> reason=<code>` shape cleanly for future accumulator surfaces — problems README, risk register). 6 new behavioural bats per ADR-005 + ADR-037 + P081 in `packages/retrospective/scripts/test/check-briefing-budgets.bats` covering MUST_SPLIT-at-2x-boundary / MUST_SPLIT-not-under-2x / MUST_SPLIT-with-OVER / MUST_SPLIT-not-without-OVER / MUST_SPLIT-respects-env-override / mixed-block-sort-determinism — RED→GREEN cycle confirmed (4 tests failed before script edit; all 20 tests pass after); architect-recommended SKILL.md modification incorporated (split-by-date documented as safe default to protect against weak-boundary split-by-subtopic picks); rejected options: ticket option 1/2 (per-file HTML comment defer-count trailer fights git-blame-as-history; byte-ratio signal IS a derived measure of the same accumulation), ticket option 3 (sibling `/wr-retrospective:rotate-briefing` skill is overweight for the same outcome). `@windyroad/retrospective` patch changeset queued (`.changeset/p145-must-split-defer-escape-hatch.md`). Effort downgraded M → S after architect review (P047 — creation-time estimate drift); WSJF re-rated 4.5 → 9.0 reflecting the lighter implementation. P145 row removed from WSJF Rankings, inserted at top of Verification Queue. Prior line-3 fragment (P142 fold-fix Open → Verification Pending released `fix(itil-hooks): runtime-SID instrumentation supersedes ADR-048`) rotated to `docs/problems/README-history.md` under `## 2026-05-03 (P145 fold-fix Open → Verifying — defer escape hatch closed)` heading per P134.


## 2026-05-03 (P149 fold-fix Open → Verifying — Step 0 carve-out)

> Last reviewed: 2026-05-03 **P147 fold-fix Open → Verification Pending — `fix(itil): SIGTERM exit-flush conditional caveat closes P147 stuck-before-emit gap (closes P147)`**: P121 SIGTERM-clean-flush guarantee is conditional, not universal. The original P118 evidence (2026-04-25) was for subprocesses that had already emitted `ITERATION_SUMMARY` before going idle (clean 5,649-byte JSON exit-flush at 121min wall-clock). The 2026-04-29 P146 incident falsified the universal generalisation: a subprocess deadlocked in a `bash until`-loop polling a backgrounded-task output file BEFORE `ITERATION_SUMMARY` emission, manual SIGTERM at 68m34s produced exit 143 + 0-byte JSON file (`claude -p --output-format json` writes the entire response as a single blob ON normal exit; SIGTERM-before-blob-write means no JSON is ever written). Fix: `packages/itil/skills/work-problems/SKILL.md` Step 5 gains a new "SIGTERM exit-flush is conditional, not universal (P147)" subsection conditioning the clean-flush claim on prior `ITERATION_SUMMARY` emission, naming the stuck-before-emit subclass observed at P146, and documenting the metadata-loss-event handling shape (verify work integrity from `git log` + `git status --porcelain`, halt the AFK loop per exit-code semantics, reconstruct cost from the Anthropic billing dashboard); Related entry adds a P147 cross-reference; `docs/briefing/afk-subprocess.md` entry adds the Fixed-2026-05-03 closing line; `packages/itil/skills/work-problems/test/work-problems-step-5-idle-timeout-sigterm.bats` extends with 1 behavioural fake-shim test (fake `claude_no_emit` shim that traps SIGTERM and exits 0 WITHOUT writing any stdout, asserting `JSON_BYTES=0` after the orchestrator-shape harness fires SIGTERM, mirroring the 2026-04-29 P146 incident shape) + 3 doc-lint contract assertions per ADR-037 Permitted Exception co-located with the existing P121 prose-drift guards (conditional-caveat phrase / metadata-loss-event handling shape / P147 citation). 14/14 bats green; architect verdict APPROVE no required amendments — ADR-026 grounding noted (quantitative claims about `0-byte JSON` and `exit 143` are explicitly grounded against the 2026-04-29 P146 incident citation in the prose); conditional caveat is the right shape per ADR-029 diagnose-before-implement (behavioural change to detect ITERATION_SUMMARY-shaped output before SIGTERM-decide is a separate decision surface, deferred to a sibling ticket per ADR-014 single-commit-per-batch); doc-lint assertions belong in the existing fixture per ADR-037 (co-locating with existing P121 prose-drift guards keeps the contract surface together). JTBD verdict ALIGNED — JTBD-006 (Progress the Backlog While I'm Away) primary fit, halt-and-investigate shape aligns with persona's "stops gracefully when it hits a blocker" outcome and "Decisions that would normally require my input are resolved using safe defaults" constraint, audit-trail outcome preserved via independent `git log` + `git status --porcelain` reconstruction. Mechanism unchanged (SIGTERM remains the right recovery primitive — the refinement is documentation accuracy + the metadata-loss-event handling shape, not a behavioural change to the dispatch). `@windyroad/itil` patch changeset queued (`.changeset/p147-sigterm-conditional-caveat.md`). P147 row removed from WSJF Rankings, inserted at top of Verification Queue. Prior line-3 fragment (P145 fold-fix Open → Verification Pending released `fix(retrospective): MUST_SPLIT closes the Tier 3 defer-recurrence escape hatch`) rotated to `docs/problems/README-history.md` under `## 2026-05-03 (P147 fold-fix Open → Verifying — SIGTERM conditional caveat)` heading per P134.


## 2026-05-03 (P150 fold-fix Open → Verifying — VQ sort direction)

> Last reviewed: 2026-05-03 **P149 fold-fix Open → Verification Pending — `fix(itil): Step 0 reconcile carve-out for uncommitted-rename drift closes P149 (closes P149)`**: AFK iter 8 carves out an uncommitted-rename detection branch from manage-problem Step 0 + work-problems Step 0 reconcile-readme Exit-1 routing. New `packages/itil/scripts/classify-readme-drift.sh` helper + `wr-itil-classify-readme-drift` `$PATH` shim per ADR-049 cross-references drift IDs from reconcile-readme stdout against `git status --porcelain docs/problems/` filtered for staged renames (`R`/`RM`); `INLINE_REFRESH covered=N` (every drift ID is destination of a staged rename) defers to in-flow P094/P062 refresh per ADR-014 single-commit grain and continues to Step 1 inline; `HALT_ROUTE_RECONCILE uncovered=N` (committed cross-session drift OR mixed) halts and routes to `/wr-itil:reconcile-readme` as today. work-problem (singular) has no Step 0 reconcile preflight (relies on cache freshness via review-problems), so only two SKILL.md files edited. New behavioural-contract bats `packages/itil/scripts/test/classify-readme-drift.bats` 13/13 green covering INLINE/HALT/mixed/RM/parse-error/no-git-repo/default-arg branches + shim parity tests in `packages/shared/test/no-repo-relative-script-paths-in-skills.bats`. Architect PASS (no new ADR required; aligns with ADR-014 line 136 single-commit grain; inline-classify branch IS the ADR-013 Rule 6 fail-safe; detection heuristic correctly scoped — `R`/`RM` prefix-match + destination path parsed for post-rename status; bounded as SKILL.md refinement under ADR-014's existing reassessment window 2026-10-16; P145 MUST_SPLIT precedent is directly analogous SKILL-amendment-without-ADR pattern). JTBD PASS (JTBD-006 AFK loop continuity + JTBD-001 governance-without-friction + JTBD-201 audit trail; halt path remains intact for committed cross-session drift, so P118 cross-session safety net preserved; no persona regresses). `@windyroad/itil` patch changeset queued (`.changeset/p149-step-0-uncommitted-rename-carve-out.md`). P149 row removed from WSJF Rankings, inserted at top of Verification Queue. Prior line-3 fragment (P147 fold-fix Open → Verification Pending released `fix(itil): SIGTERM exit-flush conditional caveat closes P147 stuck-before-emit gap`) rotated to `docs/problems/README-history.md` under `## 2026-05-03 (P149 fold-fix Open → Verifying — Step 0 carve-out)` heading per P134.


## 2026-05-03 (P146 fold-fix Open → Verifying — bats-polling discipline)

> Last reviewed: 2026-05-03 **P150 fold-fix Open → Verification Pending — `fix(itil): Verification Queue oldest-first sort direction encoded at all six render sites closes P150 (closes P150)`**: AFK iter 9 closes the recurring drift between `docs/problems/README.md` Verification Queue section header ("Ranked by release age, oldest first") and the rendered table (de-facto newest-first across multiple recent README versions). Sibling fix shape to P138's WSJF Rankings tie-break sort — encoded canonical sort direction `Released date ASC` (oldest at row 1; same-day releases tiebreak by ID ASC) at all six SKILL.md render sites + new greppable HTML-comment marker `<!-- VQ-SORT-DIRECTION: oldest-first per ADR-022 -->` so future ladder changes know to update every render block. Render sites edited: `manage-problem` Step 5 P094 + Step 7 P062 + Step 9c presentation + Step 9e template (4 occurrences), `review-problems` Step 3 + Step 5 README template, `transition-problem` Step 7 README refresh, `transition-problems` Step 4a batch render, `reconcile-readme` Step 4 row-insertion, `list-problems` VQ render block. New behavioural-contract bats `packages/itil/skills/manage-problem/test/manage-problem-readme-vq-sort-order.bats` 13/13 green covering marker presence at every render site (6) + Released-date ASC direction prose (2) + drift-re-opens-P150 warning (2) + behavioural fixture sort with 4 .verifying.md fixtures of known dates asserting row 1 = oldest entry + same-day-Released ID-ASC tiebreaker + P048-aligned likely-verified-first ordering (3). README VQ table re-rendered top-to-bottom = oldest → newest (66 rows total = 65 original + P150); undated rows preserve original markers verbatim, dated rows get `Likely verified?` markers recomputed against today_date so stale `(0 days)` markers refresh per the ticket's compose-with item. Architect verdict PASS — no new ADR required (ADR-022 Decision Outcome line 63 already says "oldest first"; ADR-014 covers single-commit grain; P138 fix-shape established as in-repo precedent), marker grammar matches `TIE-BREAK-LADDER-SOURCE` shape with source-of-truth pointer per architect amendment, inline fold-fix Open → Verifying endorsed per ADR-022 P143 amendment when pre-flight criteria met inline. JTBD verdict PASS — JTBD-006 primary (AFK loop continuity — actionable closure candidates surface at top of queue, not bottom; precondition for the AFK handoff working at all), JTBD-001 secondary (governance enforced via greppable marker + bats fixture, sibling P138 pattern), no persona regression (no documented need for newest-first VQ framing). `@windyroad/itil` patch changeset queued (`.changeset/p150-vq-sort-direction.md`). P150 row removed from WSJF Rankings, inserted at top-of-fresh in Verification Queue per ADR-022 fold-fix mechanic. Prior line-3 fragment (P149 fold-fix Open → Verification Pending released `fix(itil): Step 0 reconcile carve-out for uncommitted-rename drift closes P149`) rotated to `docs/problems/README-history.md` under `## 2026-05-03 (P150 fold-fix Open → Verifying — VQ sort direction)` heading per P134.

## 2026-05-03 (P152 fold-fix Open → Verifying — JTBD-anchored README drift advisory)

> Last reviewed: 2026-05-03 **P146 fold-fix Open → Verification Pending — `fix(itil): work-problems Step 5 iteration prompt forbids bats-output regex-poll antipattern (closes P146)`**: AFK iter 10 closes the agent-learned bash-until-loop-deadlock antipattern that consumed 68m34s wall-clock on 2026-04-29 (iter 1, PID 23580 child PID 16408) — a backgrounded `bats --tap` run polled with regex `^[0-9]+ tests?,` (bats's *default* console-summary line) which TAP-format output never emits, so the until-loop spun forever after bats completed; manual SIGTERM produced exit 143 + 0-byte JSON (the stuck-before-emit subclass P147 catalogued). Audit confirmed the polling idiom is NOT taught by any SKILL.md or agent prompt in the repo (`grep -rn "until.*grep" packages/` and `grep -rn "^[0-9]+ tests?,"` returned no matches outside this ticket itself) — agent-learned from generic bash + bats Bourne literature. Per the ticket's "agent-learned, no source-of-truth" Fix Strategy branch: prompt-discipline rule + behavioural assertion. `packages/itil/skills/work-problems/SKILL.md` Step 5 iteration prompt body Constraints list extended with a P146 clause that (1) explicitly forbids polling `bats` output with the bats-console-summary regex against TAP-format output, (2) names `wait $bg_pid` (Unix idiom — completion signaled by process exit, no regex required) or Bash-tool `run_in_background=true` + `BashOutput` exit-state polling as the safe substitute, (3) explains the TAP-vs-console-summary divergence so future contributors don't "fix" the rule incorrectly (the TAP plan line `^[0-9]+\.\.[0-9]+` is the format-stable sentinel if regex-polling is genuinely required), and (4) cites P146 inline. Related-section P146 cite added with full incident summary + cross-reference to the new behavioural fixture. New `packages/itil/skills/work-problems/test/work-problems-step-5-bats-polling-discipline.bats` 5/5 green per ADR-037 permitted exception covering prohibition phrase + safe-substitute pointer + P146 cite + TAP/console-summary divergence explanation + Related-section cite — same shape as the existing P083 ScheduleWakeup-ban and P089 stdin-redirect fixtures. Full work-problems suite 212/212 green confirming no regression. Architect verdict APPROVED at risk 1/25 (Low) — SKILL.md prose addition + 1 bats fixture; no executable code change; no commit-gate path touched; scope narrowed from the ticket's "two SKILL.md surfaces" suggestion to **work-problems Step 5 only** because (a) the polling-loop antipattern surface IS AFK-iteration-shaped (bats fork-and-poll inside `claude -p` subprocess), not manage-problem-shaped (manage-problem Step 11 commit-gate delegates to risk-scorer, doesn't fork bats), (b) precedent — P083 (ScheduleWakeup ban) and P135 (AskUserQuestion ban) are both AFK-iteration-worker discipline rules that live in work-problems Step 5 only, and (c) DRY — mirroring would require a shared snippet to prevent wording drift, and the manage-problem surface doesn't currently exhibit the failure mode. JTBD verdict PASS — JTBD-006 primary fit ("loop should be safe to run for extended periods" + "may be away for minutes or hours" outcomes); JTBD-001 + JTBD-101 secondary fits. `@windyroad/itil` patch changeset queued (`.changeset/p146-bats-polling-discipline.md`). P146 row removed from WSJF Rankings, inserted into Verification Queue between P145 and P147 per ID-ASC same-day tiebreak. Prior line-3 fragment (P150 fold-fix Open → Verification Pending released `fix(itil): Verification Queue oldest-first sort direction encoded at all six render sites closes P150`) rotated to `docs/problems/README-history.md` under `## 2026-05-03 (P146 fold-fix Open → Verifying — bats-polling discipline)` heading per P134.

> Later 2026-05-03 — **P152 fold-fix Open → Verification Pending — `feat(retrospective): JTBD-anchored README drift advisory script (closes P152 Phase 1)`**: AFK iter 11 closes the asymmetric pressure-stack the ticket surfaces — the project has dense gates for code drift (architect / JTBD / risk-scorer / style-guide / voice-tone / TDD / changeset-discipline / staging-trap / install-updates) but ZERO gates for README content drift, so plugin READMEs hand-drift between releases. Empirical first-run baseline 2026-05-03 from the new advisory: 12/12 plugins flagged with `drift_instances=12`; only 1/12 plugins (`itil`) has any JTBD anchor at all (and even that one has `skill-inventory-drift` from documenting 2 of 16+ shipped skills). Per the user's framing of P152 *"leverage the JTBD pages so we can help the reader understand the value through the jobs it helps them do"*, the fix shape anchors plugin README narrative on JTBD job IDs as canonical source of truth. `@windyroad/retrospective` minor bump ships: ADR-051 codifying the normative rule (every `@windyroad/*` plugin README MUST cite at least one `JTBD-\d{3}` ID that resolves under `docs/jtbd/<persona>/JTBD-NNN-*.md`, any status suffix) + recommended `## Jobs to be Done` section with persona-grouped subsections (architect verdict on D2 chosen over rigid D1 / status-quo D3 / generated D4); JTBD-302 newly filed `Trust That the README Describes the Plugin I Just Installed` plugin-user persona job; JTBD-007 amended to extend currency scope from code-currency to doc-content-currency + cite ADR-051; new advisory script `packages/retrospective/scripts/check-readme-jtbd-currency.sh` walks `packages/*/README.md`, greps `JTBD-\d{3}` distinct citations, resolves against `docs/jtbd/`, emits per-package `README package=<name> has_jtbd_anchor=<yes|no> cited_jobs=<N> known_jobs=<M> drift_hints=<csv>` lines + `TOTAL packages=<N> with_jtbd=<M> drift_instances=<K>` summary; drift hints vocabulary `missing-jtbd-section` / `stale-jtbd-citation` / `deprecated-jtbd-citation` (resolves only to .deprecated.md or .superseded.md) / `skill-inventory-drift` (skills/<skill> directory not named in README); exit code always 0 per ADR-013 Rule 6 / ADR-040 declarative-first / ADR-051 Phase 1 advisory contract; new bin shim `packages/retrospective/bin/wr-retrospective-check-readme-jtbd-currency` (3-line exec wrapper) per ADR-049 naming grammar; 12 behavioural bats `packages/retrospective/scripts/test/check-readme-jtbd-currency.bats` 12/12 green covering script-exists / parse-error-on-missing-packages-dir / parse-error-on-missing-jtbd-dir / empty-packages-dir / drift-fixture (no-JTBD → missing-jtbd-section hint) / clean-fixture (current JTBD cited → empty hints) / stale-ID-fixture (cited ID has no resolving file → stale-jtbd-citation hint) / deprecated-only-fixture (cite resolves only to .deprecated.md → deprecated-jtbd-citation hint) / skill-inventory-drift positive case / skill-inventory-drift negative case / multi-package aggregation / package-without-README skipped — per ADR-005 + P081 behavioural-not-structural. Architect verdict PASS-no-blockers — Phase 1 design approved with five issues addressed inline (primary drivers refined to plugin-user persona + JTBD-302 + JTBD-007; Considered Options expanded to D1/D2/D3/D4 with D2 chosen; Confirmation §1 names drift/clean/stale/deprecated bats cases explicitly; script naming `check-readme-jtbd-currency.sh` retained; bin shim `wr-retrospective-check-readme-jtbd-currency` matches hosting package per ADR-049); architect post-review confirmed Phase 2 escalation criterion (`drift_instances ≥ 2` across 3 consecutive `chore: version packages` releases) appropriate as ADR-038-shape "trip the cheap layer if static budget invalidates"; JTBD-resolution scope lenient (any status suffix) correct per the persona-level currency framing where deprecated-only is a flag not a fail. JTBD verdict PASS — co-primary drivers JTBD-302 + JTBD-007 confirmed; JTBD-101 / JTBD-001 / JTBD-202 / JTBD-301 cited as composition jobs; section heading `## Jobs to be Done` aligned with repo's JTBD vocabulary; persona-grouped substructure recommended over flat list to surface all four personas; JTBD-007 cheap edit incorporated (Desired Outcome bullet + Related decisions section). Out of scope for Phase 1 (filed as follow-on work): retroactive refresh of the 12 plugin READMEs to JTBD-anchored shape (mechanism-first / retroactive-after pattern matches iter-1 P141 + iter-2 P151 precedent); wiring detector into `/wr-retrospective:run-retro` Step 2b (deferred until empirically validated); generalisation to adopter-project surfaces (marketing HTML / public docs / changelog narrative — different source-of-truth anchor); walking `.github/ISSUE_TEMPLATE/*.yml` per JTBD-lead's Phase 1.5 recommendation; Phase 2 R6-gated load-bearing hook (escalates if drift threshold met across 3 consecutive releases). `@windyroad/retrospective` minor changeset queued (`.changeset/p152-jtbd-anchored-readme-drift-advisory.md`). P152 row removed from WSJF Rankings, inserted into Verification Queue at the most-recent end per ADR-022 P150 sort direction (oldest-first). Prior line-3 fragment (P146 fold-fix Open → Verification Pending released `fix(itil): work-problems Step 5 iteration prompt forbids bats-output regex-poll antipattern closes P146`) rotated to `docs/problems/README-history.md` under `## 2026-05-03 (P152 fold-fix Open → Verifying — JTBD-anchored README drift advisory)` heading per P134.


## 2026-05-03 (P087 Phase 1 fold-fix Open → Known Error — maturity taxonomy ADR-053)

> Last reviewed: 2026-05-03 **P081 Layer A fold-fix Open → Verification Pending — `feat(tdd): review-test agent + ADR-052 (P081 Layer A → Verifying)`**: AFK iter 13 ships behavioural-tests-default contract for skill testing — ADR-052 (new) supersedes ADR-037; structural-grep on prose docs permitted only with documented justification + linked harness-gap ticket per ADR-026 grounding. ADR-005 amended (Permitted-Exception sub-clause excludes prose-doc greps; preserves hooks.json / file-existence / safety-construct exceptions). New `packages/tdd/agents/review-test.md` semantic classifier (multi-framework; mechanical/silent stage; verdict JSON-in-fenced-block; harness_gap cite-or-null). New `packages/tdd/hooks/tdd-review-test.sh` PostToolUse:Edit|Write advisory hook (silent on non-test / outside-PWD / env-skip / justification-comment / non-existent file paths). 15 behavioural bats green dogfood (no source greps); full 71-test TDD hook suite green. @windyroad/tdd minor changeset queued. P081 row removed from WSJF Rankings, inserted into Verification Queue (oldest-first ID-ASC tie-break). Iter 13 SIGTERM-recovered post-commit per P147 stuck-before-emit subclass — semantic work landed cleanly, ITERATION_SUMMARY emit incomplete (line-3 rotation backfilled by orchestrator main turn). Layer B planned as follow-on iter. Prior line-3 fragment (P152 fold-fix Open → Verifying — JTBD-anchored README drift advisory) rotated to `docs/problems/README-history.md` under existing `## 2026-05-03` heading per P134.


## 2026-05-03 (P097 Phase 1 fold-fix Open → Known Error — SKILL.md runtime budget policy ADR-054)

> Last reviewed: 2026-05-03 **P087 Phase 1 fold-fix Open → Known Error — `docs(decisions): ADR-053 plugin maturity taxonomy (P087 Phase 1)`**: AFK iter 14 ships the maturity-signal contract for `@windyroad/*` plugins / skills / agents / hooks. ADR-053 (new, proposed) pins the five-band taxonomy (Experimental / Alpha / Beta / Stable / Deprecated) per Option D2 over rejected D1 (three-band — too coarse) / D3 (continuous score — defeats on-the-tin readability) / D4 (self-reported — drift-prone per RCA Option 3); promotion/demotion criteria stated in objective-signal-shaped terms (days-shipped, invocation count, resolved problem-ticket count, breaking-change-free window) deliberately deferring measurement-source pinning to a Phase 2 ADR after prototype evidence (architect Option A1 cut over A2 over-commit / A3 placeholder); dual-location signal per Option D5 (canonical: `plugin.json` `maturity:` field per skill/agent/hook entry, computed-and-written by Phase-2 tooling not author-edited; rendered: README header badge alongside ADR-051 JTBD anchor block) chosen over D6 (extra `docs/maturity/` surface) and D7 (compute-on-the-fly with no audit trail); per-skill / per-agent / per-hook granularity with per-plugin rollup as worst-case-band aggregate (matches the user's framing `mitigate-incident` vs `manage-problem`); advisory-first per ADR-013 Rule 6 — Phase 1 ships zero executable, gates nothing, mirrors ADR-051 trajectory. Architect verdict PASS-no-blockers (no conflicts with ADR-051 / ADR-013 Rule 6 / ADR-049 bin-shim grammar / ADR-022 lifecycle precedent / ADR-010 supersededBy precedent; A1 scope cut + per-skill granularity + dual-location D5 all approved); JTBD verdict PASS — JTBD-302 (Trust That the README Describes the Plugin I Just Installed) elevated to co-primary alongside JTBD-007 (currency dimension); JTBD-003 / JTBD-101 / JTBD-201 retained as composition drivers; JTBD-101 outcome-list extension ("promotion criteria visible to contributors") recorded as Phase 3 follow-up; orthogonality with ADR-051 confirmed (maturity is per-plugin/per-skill not per-job) plus tension between JTBD-003 conservative-bands and JTBD-101 fewer-clearer-bands explicitly acknowledged. P087 transitions Open → Known Error on this Phase 1 landing — re-enters work for Phase 2 (measurement-mechanism ADR + `/insights` + session-transcript parser + commit-history heuristic prototype) and Phase 3 (retroactive assessment across all `@windyroad/*` plugins + render README badges + advisory drift detector sibling to ADR-051 + JTBD-101 outcome update); closes only after Phase 3 ships. Phase 4+ ADR-013 Rule 6 escalation criterion (advisory → release-blocking gate iff drift accumulates across 3 consecutive releases) carried verbatim from ADR-051 precedent. No code change; ADR-only Phase 1 per P047 staged-landing pattern. Prior line-3 fragment (P081 Layer A fold-fix Open → Verifying — review-test agent + ADR-052 behavioural-tests-default) rotated to `docs/problems/README-history.md` under `## 2026-05-03 (P087 Phase 1 fold-fix Open → Known Error — maturity taxonomy ADR-053)` heading per P134.



> Later 2026-05-03 — **P097 Phase 1 fold-fix Open → Known Error — `feat(retrospective): SKILL.md runtime budget advisory + ADR-054 (P097 Phase 1)`**: AFK iter 15 ships the SKILL.md runtime-budget contract — the third instance of the contract-first / measurement-second / rollout-third pattern this week (after ADR-051 JTBD-anchored READMEs and ADR-052 behavioural-tests-default). Re-measurement during the iter showed accelerating pressure: top-3 SKILL.md offenders grew 50-144% in eleven days since the 2026-04-22 audit (work-problems 39KB → 95KB +144%; manage-problem 55KB → 82KB +50%; run-retro 36KB → 70KB +93%). ADR-054 (new, proposed) codifies the per-paragraph `[runtime]` / `[reference]` / `[deprecated]` content classification taxonomy + sibling REFERENCE.md lazy-load pattern with explicit "Read REFERENCE.md § X for Y" pointers + per-skill pointer-overhead ceiling (≤ 20 pointers / ≤ 1.6 KB) that prevents defeating the byte budget by spamming pointers + byte budgets WARN ≥ 8,192 (rotation candidate, defer permitted) / MUST_SPLIT ≥ 16,384 (no defer); vocabulary mirrors ADR-040 / P145 OVER / MUST_SPLIT pair on the briefing-tree surface verbatim so adopters learn one concept across two surfaces; REFERENCE.md reads declared mechanical / silent per CLAUDE.md P132 + ADR-044 silent-framework category to prevent defensive over-asking on each lazy-load. New `packages/retrospective/scripts/check-skill-md-budgets.sh` advisory walks `<root>/packages/*/skills/*/SKILL.md` + `<root>/.claude/skills/*/SKILL.md` (excludes REFERENCE.md siblings per ADR-054), emits `OVER <plugin>/<skill> bytes=N threshold=N` + `MUST_SPLIT <plugin>/<skill> reason=ratio-exceeds-must-split` lines sorted by identifier with block ordering (OVER block then MUST_SPLIT block); env-var overrides via `SKILL_MD_WARN_BYTES` / `SKILL_MD_MUST_SPLIT_BYTES`; exit 0 always per ADR-013 Rule 6 / ADR-040 declarative-first; new bin shim `packages/retrospective/bin/wr-retrospective-check-skill-md-budgets` per ADR-049 grammar; 21 behavioural bats `packages/retrospective/scripts/test/check-skill-md-budgets.bats` 21/21 green covering existence / executable / empty-tree-silent / under-WARN-silent / WARN-band-OVER-only / WARN-boundary-inclusive / under-MUST_SPLIT-no-MUST_SPLIT / MUST_SPLIT-boundary-inclusive / well-over-MUST_SPLIT / mixed-skills-only-over-shown / REFERENCE.md-excluded / .claude-skills-discovered / project-local-plus-plugin / WARN-env-override / MUST_SPLIT-env-override / threshold-zero-sanity / default-cwd-arg / missing-root-exit-2 / non-SKILL.md-files-ignored / sort-stability / OVER-MUST_SPLIT-block-ordering — per ADR-052 behavioural-default (no greps of script source); full `packages/retrospective/scripts/test/*.bats` suite 129/129 green (no regressions). Dogfood baseline against current repo: 20 SKILL.md files OVER WARN; 9 MUST_SPLIT (manage-incident, manage-problem, mitigate-incident, report-upstream, review-problems, transition-problem, transition-problems, work-problems, run-retro). Architect verdict APPROVED-WITH-CHANGES (6 deltas folded in: script home `packages/retrospective/scripts/` not `packages/itil/scripts/` per `consumer-cohort` precedent; WARN/MUST_SPLIT semantics named explicitly mirroring ADR-040 Branch A/B; pointer ceiling normative not advisory; P132 carve-out explicit; frontmatter no `supersedes:` + `reassessment-date: 2026-11-03`; ADR-052 Migration § cited verbatim); JTBD verdict APPROVED clean — JTBD-001 / JTBD-006 / JTBD-101 all direct fits; no conflicting persona constraints. Phase 2-3 (retroactive `[reference]` extraction across the top-10 SKILL.md offenders) explicitly **`Blocked by: P081`** Layer B — the 2026-04-27 P097 Phase 1 audit found 80 of 116 manage-problem contract assertions structural-grep SKILL.md prose; behavioural retrofit needs P081 Layer B harness primitives first per ADR-052 opportunistic-as-touched migration shape. P097 transitions Open → Known Error on this Phase 1 landing — re-enters work for Phase 2 (top-3 offender extraction) and Phase 3 (remaining top-10 + project-local install-updates) once P081 Layer B unblocks; closes only after Phase 2-3 ship. `@windyroad/retrospective` minor changeset queued (`.changeset/p097-skill-md-budget-advisory.md`). Prior line-3 fragment (P087 Phase 1 fold-fix Open → Known Error — maturity taxonomy ADR-053) rotated to `docs/problems/README-history.md` under `## 2026-05-03 (P097 Phase 1 fold-fix Open → Known Error — SKILL.md runtime budget policy ADR-054)` heading per P134.

> Later 2026-05-03 — **P154 opened — P137 namespace-prefix detector must run against npm pack output not source tree**: user-surfaced 2026-05-03 mid-iter-20 *"hey, for P137, should it run against the package.tgz that gets produced by npm pack?"*. Iter 20 (P033 Phase 2b) sibling-finding empirically validated the gap — packages/itil/package.json files array missing `"scripts/"` so bin/wr-itil-* shims published broken across @windyroad/itil@0.23.2 → 0.24.0 (5 production versions). ADR-055 source-tree advisory's 2880 drift_instances baseline missed it because source tree exposes scripts/ even when published tarball doesn't. P154 extends ADR-055 with a tarball-shape detector mode: `npm pack --json` per-workspace + extract + walk + cross-check files-array integrity. Phase 1 advisory exit-0 always per ADR-013 Rule 6. Phase 2 R6-gated load-bearing CI gate per P135 escalation precedent. Sibling family with P137 / P151 / P140 — closes the prevention surface that P140 Step 6.5 fix-and-continue currently catches as remediation only. WSJF 7.5 (Sev 15 / Effort M). Top of dev-work queue. Captured via second-tier P144 python3-via-Bash recovery (P124 mtime regression still active — fix shipped in itil@0.23.3 but cached @0.23.1 in this session; /install-updates pending). R1 hot-fix (commit 3f671b9, P140 fix-and-continue, ADR-042 above-appetite remediation closing 15/25 → 3/25 residual) just landed itil's scripts/ files-array fix, addressing the production broken-shim bug for next-version adopters. Prior line-3 fragment (P081 Layer A iter 13 SIGTERM-incomplete backfill) rotated to docs/problems/README-history.md per P134.

## 2026-05-03 (P158 opened — ADR-051 Phase 1 detector shipped but not wired into retro Step 2b)

> Last reviewed: 2026-05-03 **P155 + P156 + P157 opened — ADR-032 child tickets per P014 master split**: user direction post-AFK-loop-restart applied — split P014 (ADR-032 master tracker) into its three planned children (originally named P154/P155/P156 in iter-12 outstanding_questions plan but P154 was taken by P137 npm-pack-detector follow-up captured 2026-05-03 mid-iter-20; children renumber to P155/P156/P157). P155 ship `/wr-itil:capture-problem` skill — lightweight aside-invocation surface for problem capture during foreground work; minimal duplicate-check + skeleton-fill + no-inline-README-refresh; closes P078 capture-on-correction OFFER pattern + P148 Tickets-Deferred legacy. P156 ship `/wr-architect:capture-adr` skill — sibling for ADR capture; skeleton MADR with deferred-flagged sections; expand-existing-skeleton path on canonical create-adr review. P157 ship pending-questions-surface SessionStart hook — auto-surface accumulated `outstanding_questions` from `.afk-run-state/outstanding-questions.jsonl` when user returns interactive; closes the queue-file lifecycle gap (this very session sat 16 hours with 2 entries that only surfaced because user explicitly asked). All three Open / M / WSJF 6.0; ADR-032 design landed via P088 settling user-direction-scoped (a)/(b)/(c). P142 runtime-SID hook empirically verified working in this restarted session — three Write-tool ticket creations succeeded clean with no P144 recovery dance. Prior line-3 fragment (P154 opening — P137 npm-pack-detector follow-up) rotated to docs/problems/README-history.md per P134.

## 2026-05-03 (P158 fold-fix Open → Verifying — ADR-051 Phase 1 detector wired into retro Step 2b)

> Last reviewed: 2026-05-03 **P158 opened — ADR-051 Phase 1 detector shipped but not wired into run-retro Step 2b**: pre-audit docs-currency sweep (parent: 8df1692 retroactive ADR-051 README refresh + R002-R006 risk register scaffolding) surfaced via P078 user correction *"did you just manually update all the readmes, or did you fix the problems that was preventing them from being updated?"*. Investigation: detector + bin shim shipped (`packages/retrospective/scripts/check-readme-jtbd-currency.sh` + `packages/retrospective/bin/wr-retrospective-check-readme-jtbd-currency`); ADR-051 Confirmation criterion 5 explicitly deferred Step 2b wiring to follow-on iter pending empirical validation. The 8df1692 refresh IS that empirical validation — detector now reports `TOTAL packages=12 with_jtbd=12 drift_instances=1` (residual: `retrospective` skill-inventory-drift). Empirical-validation precondition met; deferred wiring becomes actionable. Effort S, Severity 12 High (Impact 4 Significant × Likelihood 3 Possible), WSJF 12.0 — top of dev-work queue. Sibling out-of-scope: JTBD-index drift (`docs/jtbd/README.md` missing JTBD-007 + JTBD-302 entries detected pre-refresh) not covered by ADR-051 detector — separate ticket needed for index-vs-files detector. Prior line-3 fragment (P155 + P156 + P157 opened — ADR-032 child tickets per P014 master split) rotated to docs/problems/README-history.md per P134.

## 2026-05-03 (P159 opened — JTBD currency detector should be load-bearing commit-hook with auto-fix, not retro advisory)

> Last reviewed: 2026-05-03 **P158 fold-fix Open → Verification Pending — `feat(retrospective): wire ADR-051 Phase 1 JTBD currency advisory into run-retro Step 2b (P158)`**: closes the deferred follow-on named in ADR-051 Confirmation criterion 5. SKILL.md amendment in run-retro Step 2b invokes `wr-retrospective-check-readme-jtbd-currency` on every retro and surfaces drift in the Pipeline Instability section per ADR-013 Rule 6 interactive-or-AFK branching. `packages/retrospective/README.md` documents the wiring + lists every shipped advisory shim under a new `## Advisory scripts` section, closing the residual `skill-inventory-drift` finding. Released as `@windyroad/retrospective@0.16.0` (commit `df47ad1` via release PR #111 merged at `d944388`). Detector run on post-fix tree reports `drift_instances=0` (was 1 post-`8df1692`, ≥12 pre-`8df1692`). Phase 2 escalation criterion (load-bearing hook iff `drift_instances ≥ 2` across 3 consecutive `chore: version packages` releases without correction) captured inline in SKILL.md. Architect verdict: PASS-WITH-CAVEATS applied. JTBD verdict: PASS — JTBD-302 primary, JTBD-007 + JTBD-101 composing. External-comms gate PASS (key 391033b9...). Risk-scorer: commit=3 push=2 release=2 (within appetite). Prior line-3 fragment (P158 opened — ADR-051 Phase 1 detector shipped but not wired) rotated to docs/problems/README-history.md per P134.

## 2026-05-03 (P160 opened — Ship quota-pacing surface to prevent weekly-quota exhaustion)

> Last reviewed: 2026-05-03 **P159 opened — JTBD currency detector should be load-bearing commit-hook with auto-fix, not retro advisory**: P078 capture-on-correction immediately after P158 fix-released — user *"the drift detector shouldn't be part of the retro. It should be something we are always running and fixing"*. AskUserQuestion clarified surface = `git commit` PreToolUse:Bash hook running detector on post-commit tree *especially* when staged set does NOT touch README.md (most common drift class: contributor adds a skill/hook/agent, forgets the README); auto-fix = use JTBD framing to inform existing prose, NOT bolt on a separate `## Jobs to be Done` section. Supersedes P158's retro-wiring direction (P158 stays Verifying as advisory backup; P159 is the load-bearing primary). Implications: (1) ADR-051 Recommended Section Structure clause + Confirmation criterion 5 need amendment; (2) the 12 plugin READMEs refreshed in `8df1692` carry the wrong section shape and need re-integration to weave JTBD content organically; (3) auto-fix is an agent-judgment surface — wr-jtbd:agent reads the plugin's actual surface and weaves JTBD-NNN citations into existing prose. Effort L (could grow to XL); WSJF 3.0. Sibling out-of-scope: revisit advisory-then-escalate as the default for drift-class detectors generally if architect confirms. Prior line-3 fragment (P158 fold-fix Open → Verification Pending — ADR-051 Phase 1 detector wired into retro Step 2b) rotated to docs/problems/README-history.md per P134.

## 2026-05-03 (P154 fold-fix Open → Verifying — ADR-049 publish-manifest tarball-shape detector)

> Last reviewed: 2026-05-03 **P160 opened — Ship quota-pacing surface to prevent weekly-quota exhaustion**: user-direction with live statusline evidence (`7d: 36% behind, resets 5d 17h` — burning weekly quota at ~1.5x sustainable) + cross-user corroboration (James Nowland, same pain class — "blown through my AI with code"). Existing statusline measures burn rate but does not regulate it; gap is a prescriptive layer analogous to RISK-POLICY.md but for token quota across Claude Code + claude.ai chat + Cowork. Load-bearing on JTBD-001 (governance gates drive verbosity; must be tunable against weekly cadence), JTBD-006 (AFK orchestrators heaviest single-class consumer; un-paced overnight loops can exhaust weekly quota in one run), JTBD-302 (adopters inherit governance verbosity; must tune independently). Open design questions deferred to architect review: plugin home, read surface, enforcement tier (advisory/blocking/adaptive auto-throttle), QUOTA-POLICY.md schema, AFK orchestrator Step 6.5/6.6 integration, cross-tool quota awareness, upstream feature-request coordination via /wr-itil:report-upstream. Effort XL (new plugin or significant cross-package surface; ADR required). WSJF 1.25. Prior line-3 fragment (P159 opened — JTBD currency detector load-bearing commit-hook with auto-fix) rotated to docs/problems/README-history.md per P134.


## 2026-05-03 (P155 fold-fix Open → Verifying — capture-problem skill + ADR-032 amendment)

> Last reviewed: 2026-05-03 **P154 fold-fix Open → Verifying — `feat(retrospective): ship check-tarball-shipped-shims.sh advisory + dogfood-fix scripts/ in retrospective files array (P154 Phase 1)`**: closes the iter-20 sibling-finding regression class — `bin/wr-<plugin>-<name>` shims that exec into `../scripts/<name>.sh` but ship broken because `package.json#files` omits `scripts/`. The source-tree-walking detector shipped in `@windyroad/retrospective@0.15.0` (P137 Phase 1, ADR-055) measures source-tree namespace-prefix drift but cannot see this publishing-manifest leak. P154 closes the prevention surface from the publish-manifest side via a sibling advisory `check-tarball-shipped-shims.sh` that runs `npm pack --dry-run --json` per workspace and asserts every ADR-049-grammar shim's exec target is also in the tarball. Pre-fix detector reported 5 broken shims under `@windyroad/retrospective` (the iter-20 regression class replicated across 5 sibling check-* shims); post-fix silent-on-pass — all targets resolve in the tarball. Architect verdict: PASS; JTBD verdict: PASS (JTBD-302 primary, JTBD-101 secondary). External-comms gate PASS (key d9e3c203...). Phase 2 deferred: wire into run-retro Step 2b alongside check-internal-id-leaks.sh + check-readme-jtbd-currency.sh; promote to load-bearing hook iff drift_instances ≥ 1 across 3 consecutive releases. Prior line-3 fragment (P160 opened — Ship quota-pacing surface to prevent weekly-quota exhaustion) rotated to docs/problems/README-history.md per P134.


## 2026-05-03 (P157 fold-fix Open → Verifying — pending-questions-surface SessionStart hook + ADR-032 amendment)

> Last reviewed: 2026-05-03 **P155 fold-fix Open → Verifying — `feat(itil): ship /wr-itil:capture-problem skill (foreground-lightweight aside-invocation) + ADR-032 amendment`**: closes the heavyweight-only-capture-path gap (parent P014 ADR-032 child) — the lightweight aside-invocation surface for problem capture during foreground work. New skill at `packages/itil/skills/capture-problem/{SKILL.md, REFERENCE.md, test/capture-problem.bats}` per ADR-038 progressive-disclosure split + ADR-052 behavioural-tests-default; SKILL.md hosts ~3-4 turn flow (Steps 0-7) with zero AskUserQuestion branches per ADR-044 framework-mediated mechanical-stage carve-out (duplicate-check / priority-default / effort-default / multi-concern routing / empty-arg fail-safe all framework-mediated); composes with manage-problem create-gate (P119) via shared `/tmp/manage-problem-grep-${SESSION_ID}` marker; deferred-README-refresh contract distinguishes capture-problem from manage-problem (capture-time speed vs README authoritativeness; trailing pointer to /wr-itil:review-problems is user-visible signal); ADR-032 amended with new "Foreground-lightweight-capture variant (P155 amendment, 2026-05-03)" section between Observable-output contract and Scope; 14 behavioural bats green covering P119 create-gate composition / next-ID formula / title-only conservative duplicate-grep / skeleton-fill template / allowed-tools surface / deferred-README-refresh contract; architect verdict PASS-with-resolution (initial review found ISSUES — 1 ADR-032 background-vs-foreground naming conflict resolved via amendment; 2 ADR-031 path layout matches on-disk flat reality; 5 deferred-README-refresh contract documented inline; 4/7/8 minor SKILL.md content additions); JTBD verdict PASS (JTBD-001 reviews-under-60s + JTBD-006 sibling-finding capture conversion ~50%→~100% + JTBD-101 SKILL.md+REFERENCE.md split discoverable; plugin-user out of scope). Unblocks P078 (capture-on-correction OFFER pattern) and simplifies P148 (Tickets Deferred retro section becomes legacy). Sibling P156 (capture-adr) + P157 (pending-questions-surface hook) remain Open under same parent. Prior line-3 fragment (P154 fold-fix Open → Verifying — ADR-049 publish-manifest tarball-shape detector) rotated to docs/problems/README-history.md per P134.


## 2026-05-04 (P159 Phase 1 fold-fix Open → Verifying — readme-jtbd-currency commit-hook + ADR-051 amendment + P158 closure + P161 filing)

> Last reviewed: 2026-05-03 **P157 fold-fix Open → Verifying — `feat(itil): ship itil-pending-questions-surface SessionStart hook + ADR-032 amendment + work-problems Step 5 export (closes P157)`**: closes the queue-file lifecycle gap — accumulated `outstanding_questions` entries from `.afk-run-state/outstanding-questions.jsonl` now surface deterministically on session start when the user returns from an AFK loop that halted before its Step 2.5 / Step 2.5b emit point (manual stop, quota exhaustion, network failure). Third and final ADR-032 child of P014 (master tracker) — sibling to P155 (capture-problem, commit `86e99e5`) and P156 (capture-adr, commit `d28bd51`) shipped earlier this AFK loop. New SessionStart hook `packages/itil/hooks/itil-pending-questions-surface.sh` parses JSONL queue via `jq -e .` (malformed lines silently skipped per defensive SessionStart-must-not-block-startup contract), dedupes on `(rank, category, ticket_id, question)` tuple, ranks per ADR-044 6-class taxonomy precedence (deviation-approval > direction > one-time-override > silent-framework > taste > correction-followup), emits markdown directive listing entries + cleanup directive instructing the agent to rewrite the queue file with resolved entries removed after each `AskUserQuestion` batch + batching note when entry count > 4 citing the ADR-013 Rule 1 `<=4 per call` cap. Silent-on-no-content per ADR-040 Mechanism step 1 (missing/empty/whitespace-only/all-malformed → exit 0 zero stdout). AFK-iter cross-context-leak prevention via `WR_SUPPRESS_PENDING_QUESTIONS=1` env-var self-suppress (architect's implementation choice (a) of the two ADR-032 line 127 enumerations — simpler than orchestrator-side queue drain/restore, idempotent, no state to restore on crash); the orchestrator's `/wr-itil:work-problems` Step 5 dispatch block exports the env var immediately before each `claude -p` subprocess spawn so the orchestrator's accumulated session-level queue does not leak into iter subprocess contexts as cross-context noise. Wired into `packages/itil/hooks/hooks.json` as second SessionStart entry with matcher `"startup"` (mirrors `wr-retrospective` `session-start-briefing.sh` ADR-040 Option A precedent — Option B's `UserPromptSubmit` + once-per-session marker rejected on the same reasoning: SessionStart is the semantically correct event for boot-time artefact surfaces). 19 behavioural bats `packages/itil/hooks/test/itil-pending-questions-surface.bats` 19/19 green per ADR-052 (silent-on-no-content × 3, surfacing × 2, full 6-class precedence ranking × 2, dedup × 2, batching × 2, cleanup directive × 1, env-var self-suppress × 2, hooks.json wiring × 1, work-problems Step 5 export × 1, malformed-JSON skip × 2, exists × 1). Dogfood end-to-end: ran hook against this session's real 9-entry queue — output ranks 1× deviation-approval (P154) first, 6× direction (BRIEFING_TIER3 / P014 / P156×3 / P160) next, 2× silent-framework (P154×2) last; batching note fires (9 > 4); cleanup directive present. ADR-032 amended with new section `### Pending-questions-surface variant — JSONL queue at SessionStart (P157 amendment, 2026-05-03)` between the P156 amendment and Scope — disambiguates the two pending-questions surfaces (markdown variant `pending-questions-surface.sh` UserPromptSubmit per ADR-032 line 169 for paused-background-subagent-state tokens; JSONL variant `itil-pending-questions-surface.sh` SessionStart for AFK-loop-accumulated queue), names ADR-040 Option A precedent + ADR-040 Mechanism step 1 silent-on-no-content + ADR-044 6-class precedence, documents the two-hook split + the env-var self-suppress contract; variant-selection precedence pinned (SessionStart-JSONL is LEAD post-P157 for AFK-loop direction-question surfacing across session boundaries; markdown UserPromptSubmit remains LEAD for paused-subagent-state tokens). Architect verdict PASS-WITH-NOTES (8 actionable items folded in: ADR-040 cited over ADR-045 for SessionStart silent-on-no-content; env-var self-suppress per implementation choice (a); ADR-032 amendment over new ADR; explicit cleanup directive in additionalContext; work-problems Step 5 export of the env var alongside the hook ship; behavioural bats includes WR_SUPPRESS_PENDING_QUESTIONS=1 case; ADR-040 plain-stdout shape over additionalContext-keyed; cross-reference ADR-040 Mechanism step 1 not ADR-045 Pattern 1). JTBD verdict PASS (JTBD-006 primary — Progress the Backlog While I'm Away — closes the documented "queued for my return, not guessed at" desired-outcome gap; JTBD-001 secondary — direction-class observations resolve before user begins foreground work; JTBD-101 tertiary — extends the suite via reusable SessionStart-JSONL pattern). Closes the ADR-032 child trio (P014 master-tracker now has all three children fix-released this AFK loop). Prior line-3 fragment (P155 fold-fix Open → Verifying — capture-problem skill + ADR-032 amendment) rotated to docs/problems/README-history.md per P134.


## 2026-05-04 (P162 Open — codify dogfood-graduation criteria with counterfactual risk assessment for held changesets)

> Last reviewed: 2026-05-04 **P159 Phase 1 fold-fix Open → Verifying — `feat(retrospective): ship retrospective-readme-jtbd-currency PreToolUse:Bash commit-hook + ADR-051 amendment + JTBD-302/007 outcome refresh + 2 tactical README drift fixes (closes P159 Phase 1, closes P158, opens P161)`**: migrates the JTBD-anchored README rule from advisory-only to load-bearing-from-the-start at commit time per the user correction *"the drift detector shouldn't be part of the retro. It should be something we are always running and fixing"*. New PreToolUse:Bash hook `packages/retrospective/hooks/retrospective-readme-jtbd-currency.sh` registered in `packages/retrospective/hooks/hooks.json` denies `git commit` when the existing detector reports `drift_instances > 0`; same shape as P141 changeset-discipline (substring-matches `git commit` invocations; `BYPASS_JTBD_CURRENCY=1` env override; fail-open outside git work tree, in adopter projects without `./packages/` or `./docs/jtbd/`, on detector-script failure, on parse error; per-invocation deterministic no-marker; deny-band ≤300 bytes per ADR-045 truncating drift_hints CSV to first hint; deny redirects to wr-jtbd:agent recovery + hand-edit fallback per ADR-013 Rule 1 graceful degradation). 19 behavioural bats `packages/retrospective/hooks/test/retrospective-readme-jtbd-currency.bats` 19/19 green per ADR-052 — drift-detection × 7 (no-anchor / skill-inventory-drift / slug name / recovery path / deny-band / release commits / `--amend`); allow × 4 (clean / BYPASS / non-Bash / non-commit); fail-open × 5 (outside git tree / no packages/ / no docs/jtbd/ / empty JSON / malformed JSON); silent-on-pass × 3. ADR-051 amendment in-place (status remains `.proposed.md`): new Decision Driver "Load-bearing-from-the-start for drift class" carves drift detectors out of advisory-then-escalate gradualism (drift class is mechanical / structurally bounded / no socialisation period needed / gradualism re-creates the failure mode); Recommended Section Structure clause **rewritten** rejecting bolt-on `## Jobs to be Done` sections as compliance-theatre anti-pattern (lead prose loses framing pressure, JTBD context arrives after value model formed) — prose-weaving target guidance added (lead value-framing section names JTBD job; per-skill / per-hook / per-agent descriptions name the job they serve), persona-primacy preservation rules carried over, anti-pattern citation explicit; new Normative rule 4 codifies the commit-hook surface; Confirmation criterion 5 amended (Phase 1 surface = commit-hook + retro Step 2b advisory backup); criterion 6 added (hook bats coverage); criterion 8 amended (original "escalate-after-3-releases" trigger superseded; new trigger monitors load-bearing surface for false-positives + undetected semantic drift + generalisation pressure); Reassessment Criteria refreshed for the load-bearing surface; Out-of-scope updated (Phase 2 12-README prose-weaving refresh deferred; auto-fix orchestration via wr-jtbd:agent grant-Edit decision deferred; load-bearing-as-default-for-drift-class generalisation queued at P161). JTBD-302 Desired Outcome bullet 6 rewritten ("load-bearing at commit time per P159; retro/release-time advisories ride as backup signals"); JTBD-007 line 22 rewritten ("enforced at commit time via PreToolUse:Bash hook; retro/release-time advisories ride as backup signals"); both Related decisions blocks updated. Tactical bootstrap drift fixes per ADR-053 bootstrapping clause: architect README adds `/wr-architect:capture-adr` skill mention (clears skill-inventory-drift from `d28bd51`); itil README adds `/wr-itil:capture-problem` skill mention (clears skill-inventory-drift from `86e99e5`). Post-fix detector signal: `TOTAL packages=12 with_jtbd=12 drift_instances=0` — bootstrap commit clears the gate naturally without BYPASS. Architect verdict PASS — five advisory observations folded in (hook placement in retrospective confirmed; fire-on-any-`git commit` confirmed; in-place ADR-051 amendment without supersession; P158 lifecycle Verifying → Closed; deny string graceful-degradation fallback). JTBD verdict PASS (re-reviewed after JTBD policy edits) — three same-commit doc edits applied (JTBD-302 + JTBD-007 + ADR-051 amendment carries prose-weaving target + persona-primacy + anti-pattern). External-comms verdict PASS. P158 closed (retro Step 2b wiring shipped under `df47ad1` survives as backup advisory after primary surface migration). P161 filed for the broader drift-class-generalisation observation P159 explicitly carved out (observation-only ticket; deferred until 2-3 more drift-class detectors arrive following the load-bearing-from-the-start shape). Phase 2-3 explicitly deferred: 12-README prose-weaving refresh + auto-fix orchestration via wr-jtbd:agent grant-Edit decision. Prior line-3 fragment (P157 fold-fix Open → Verifying — pending-questions-surface SessionStart hook + ADR-032 amendment) rotated to docs/problems/README-history.md per P134.



## 2026-05-04 (P163-P166 batch capture — 4 iter sibling-findings from /wr-itil:work-problems iter 7 surfacing pass)

> Last reviewed: 2026-05-04 **P162 opened — codify dogfood-graduation criteria for held changesets with counterfactual risk assessment**: surfaced by user direction at `/wr-itil:work-problems` AFK loop iter 7 surfacing pass — verbatim *"this is a really good pattern... we should make that a standard going forward... what are the concrete signals that will tell us when it's ready to be included in the published packages?"* and *"we should do a counterfactual risk assessment - 'what is the risk of delaying the release of this longer?' i.e. what are the risks users are facing without this new feature"*. Three changesets currently held (P085 since 2026-04-24, P064 since 2026-04-26, P159 since 2026-05-04) with no formalized exit criteria — user rejected calendar-time windows ("arbitrary and too long") and chose risk-scorer-driven counterfactual scoring. Reinstate criterion: `delay-risk ≥ release-risk`. Phase 1 lands a new ADR (sibling to ADR-042) defining the graduation contract + per-class graduation matrix; Phase 2 implements the counterfactual scoring extension reading `.afk-run-state/dogfood-evidence.jsonl`; Phase 3 retroactively applies criteria to P085/P064/P159; Phase 4 amends `docs/changesets-holding/README.md` Process. WSJF 6.0 (Severity 12 / Effort M). Investigation Tasks deferred to architect/JTBD review at next interactive session.



## 2026-05-04 (README reconciliation — P167 added to WSJF Rankings after capture-problem deferred-refresh contract gap)

> Last reviewed: 2026-05-04 **P167 captured via /wr-itil:capture-problem at the start of this session**: risk register aggregate reads as "don't ship" — sparse coverage + undercredited controls. Captured under the misreading that `docs/risks/` was a separate aggregation from `.risk-reports/`; user correction during follow-up clarified that `docs/risks/` is the persistent catalog of per-action risks consumed by per-action assessments (one universe of risks, same 4/Low appetite uniformly). RISK-POLICY.md amended (commit 9e339d0) with new ## Risk Catalog section codifying the catalog-consumption workflow + new ## Control Composition section codifying the layered-control likelihood-reduction rule (independent paths → 1-band-per-path, capped at Rare floor; ADR-026 grounded). P167's RC3 (per-action vs lifetime appetite) marked superseded; RC1 (sparse coverage) and RC2 (controls undercredited) stand. Original Phase 1-3 plan (manual R007-R011 authoring + R002/R005 extensions + R001-R006 re-rate) superseded by user direction toward delete-and-bootstrap (next ticket). Prior line-3 fragment (P163-P166 batch capture from iter 7) rotated here per P134.


## 2026-05-04 (README reconciliation — P168 added to WSJF Rankings after capture-problem deferred-refresh contract gap, sibling to P167)

> Last reviewed: 2026-05-04 **README reconciliation — P167 added to WSJF Rankings (1.5 / Open / M)**: P118 preflight detected drift when /wr-itil:capture-problem next invocation hit Step 0 — exposes capture-problem deferred-README-refresh contract gap (ADR-032 design tradeoff; capture-problem opts out of P094 inline-refresh). Reconciliation per P118 + ADR-014 amended "Reconciliation as preflight robustness layer". P167 captures risk-register-aggregate-reads-as-dont-ship symptom; corrected framing landed in RISK-POLICY.md commit 9e339d0 (## Risk Catalog + ## Control Composition sections). Prior line-3 fragment (P163-P166 batch capture) rotated to docs/problems/README-history.md per P134.


## 2026-05-04 (P167 transition Open → Known Error — substantive design successor P168 captured + cross-referenced; root cause documented; Effort dropped M → S; WSJF re-rated 1.5 → 6.0)

> Last reviewed: 2026-05-04 **README reconciliation — P168 added to WSJF Rankings (1.5 / Open / M)**: P118 preflight detected drift on next /wr-itil:work-problem invocation — same capture-problem deferred-README-refresh contract gap (ADR-032) that displaced P167 yesterday. Reconciliation per P118 + ADR-014 amended "Reconciliation as preflight robustness layer". P168 is the substantive design successor to P167 (risk-scorer should bootstrap-from-empty + consume-catalog per RISK-POLICY.md ## Risk Catalog section landed in commit 9e339d0). Inserted within the WSJF=1.5 tier ordered by tie-break ladder: Effort-divisor M (P080, P167, P168) ahead of L/XL; same Reported date 2026-05-04 as P167 → ID asc places P168 immediately after P167. Prior line-3 fragment (P167 reconciliation) rotated to docs/problems/README-history.md per P134.


## 2026-05-04 (P168 transition Open → Known Error — architect + JTBD reviews complete; Effort re-rated M → XL; ADR-058 design path documented)

> Last reviewed: 2026-05-04 **P167 transition Open → Known Error — substantive design successor P168 captured + cross-referenced; root cause documented**: Effort dropped M → S post-correction (residual = cross-reference + transition only; design work delegated to P168); WSJF re-rated 1.5 → 6.0 and moved within the 6.0 tier ahead of P162 per Known-Error-first tie-break. Pre-flight checks satisfied: RC1 + RC2 valid (RC3 superseded), policy framing landed in commit 9e339d0 (RISK-POLICY.md ## Risk Catalog + ## Control Composition sections), reproduction test cited as P168 Confirmation criterion, workaround documented (corrected catalog framing + P168 delegation). External-root-cause detection: skipped — no upstream/external markers in RCA; internal to project. Per /wr-itil:work-problem singular contract; next invocation should target P168 for the substantive design. Prior line-3 fragment (P168 reconciliation) rotated to docs/problems/README-history.md per P134.


## 2026-05-04 (P168 transition Known Error → Verification Pending — wipe + extractor + reinstate held changeset; user direction discharged)

> Last reviewed: 2026-05-04 **P168 transition Open → Known Error — architect + JTBD reviews complete; Effort re-rated M → XL; ADR-058 design path documented**: architect verdict ISSUES-FOUND with 8 distinct edits across 2 commits. JTBD verdict PASS centred on JTBD-001 with JTBD-006/202 binding. WSJF re-computed 1.5 → 0.75 — P168 moves to top of WSJF=0.75 tier per Known-Error-first tie-break ahead of P012/P034.

## 2026-05-04 (README reconciliation — P169 added to WSJF Rankings after capture-problem deferred-refresh contract gap)

> Last reviewed: 2026-05-04 **P168 Known Error → Verification Pending — wipe + extractor; user direction 2026-05-04 discharged**: ADR-059 Commit 3 (8edaf7b). WIPED R001-R006 + TEMPLATE.md + docs/risks/README.md + install-updates risk-register templates + broken markdown refs in P158/P159 (per "FFS WIPE THE RXXX risks ... THEY ARE WRONG"). Wrote `extract-risks-from-reports.sh` (~270 lines two-phase) + bin shim + 17 behavioural bats. Generated R007 from this repo's actual corpus. Generated docs/risks/README.md. Entry shape lives in create-risk skill + extractor (per "There shouldn't be a template in the directory"). Reinstated held P168 changeset → .changeset/ per the held-area's documented reinstate trigger. @windyroad/risk-scorer minor bump on the release queue. Prior line-3 fragment (P168 Open → KE) rotated per P134.

## 2026-05-05 (batch transition — close P155, P134, P149 via run-retro Step 4a close-on-evidence)

> Last reviewed: 2026-05-04 **README reconciled** — 1 drift entry corrected: P169 added to WSJF Rankings (1.5 / Open / M, severity 3 Low). P118 preflight detected drift on next /wr-itil:capture-problem invocation — same capture-problem deferred-README-refresh contract gap (ADR-032) that displaced P167/P168 yesterday. P169 captures the recogniser-shape catalogue → plugin operationalisation workstream (Phase 1 scorer-side, Phase 2 bootstrap-side); inserted within the WSJF=1.5 tier per tie-break ladder (M-effort group, Reported-date asc places P169 after P080). Reconciliation contract per P118 + ADR-014 amended ("Reconciliation as preflight robustness layer"). Prior line-3 fragment (P168 KE → Verifying) rotated per P134.


## 2026-05-05 (P170 re-rate — Effort M → XL after architect+JTBD AMEND verdicts on ADR-060)

> Last reviewed: 2026-05-05 **batch transition: P155 close, P134 close, P149 close** (3 tickets) via run-retro Step 4a close-on-evidence per ADR-022 + ADR-044 framework-resolved silent dispatch. Specific in-session citations per ticket recorded in the Closed section. Prior line-3 fragment (P169 README reconciliation) rotated to docs/problems/README-history.md per P134.

## 2026-05-05 (README reconciliation — add missing P171 WSJF Rankings row)

> Last reviewed: 2026-05-05 **P170 re-rate** Effort M → XL, Priority 3 (Low) → 8 (Medium); WSJF 1.5 → 1.0; architect+JTBD AMEND verdicts on ADR-060 (proposed) discharge 2 of 6 P170 investigation tasks. 14 architect findings (2 critical: I1 enforcement contradictory, I2 needs load-bearing test) + 1 JTBD critical block (JTBD-008 phantom anchor) gate Phase 1 implementation; 3 of 4 P170 deferred decisions resolved in-ADR (RFC ID grammar = `RFC-<NNN>`, WSJF placement = RFC-level Phase 1, AskUserQuestion authority classes per ADR-044). Prior line-3 fragment (batch transition P155/P134/P149 close) rotated to docs/problems/README-history.md per P134.

## 2026-05-05 (README reconciliation — add missing P172 WSJF Rankings row from capture-problem deferred-refresh)

> Last reviewed: 2026-05-05 **README reconciled** — 1 drift entry corrected: P171 (MISSING wsjf-rankings row added at WSJF 1.5 tier per tie-break ladder, after P169, before P045). Reconciliation contract per P118 + ADR-014 amended ("Reconciliation as preflight robustness layer"). Prior line-3 fragment (P170 re-rate) rotated to docs/problems/README-history.md per P134.

## 2026-05-06 (README reconciliation — add missing P173 WSJF Rankings row from capture-problem deferred-refresh)

> Last reviewed: 2026-05-05 **README reconciled** — 1 drift entry corrected: P172 (MISSING wsjf-rankings row added at WSJF 1.5 tier per tie-break ladder, after P171, before P045 — drift was the deferred-refresh state from a same-session /wr-itil:capture-problem invocation, NOT cross-session). Reconciliation contract per P118 + ADR-014 amended ("Reconciliation as preflight robustness layer"). The capture-problem deferred-refresh contract interacting with manage-problem Step 0 preflight is itself the kind of skill-contract bug P172 captures. Prior line-3 fragment (P171 reconcile) rotated to docs/problems/README-history.md per P134.

## 2026-05-06 (batch transition — close P147 P146 P130 via run-retro Step 4a close-on-evidence)

> Last reviewed: 2026-05-06 **README reconciled** — 1 drift entry corrected: P173 (MISSING wsjf-rankings row added at WSJF 1.5 tier per tie-break ladder, after P172, before P045 — cross-session drift, P173 was captured 2026-05-06 in a prior session without staging the README refresh). Reconciliation contract per P118 + ADR-014 amended ("Reconciliation as preflight robustness layer"). Prior line-3 fragment (P172 reconcile) rotated to docs/problems/README-history.md per P134.

## 2026-05-06 (README reconciliation — add missing P174 WSJF Rankings row)

> Last reviewed: 2026-05-06 **batch transition** — close P147 + P146 + P130 (3 verifyings) via run-retro Step 4a close-on-evidence per ADR-022 + ADR-026 grounding. P147 stuck-before-emit subclass observed empirically (iter 1 PID 21560, exit 143, 0-byte JSON, 60min wall-clock; SKILL.md caveat held); P146 prohibition prose carried in iter 2 dispatch prompt and iter completed clean (no deadlock); P130 orchestrator subprocess-vs-direct-action discipline executed correctly twice (subprocess for P170 XL work, direct for I001 short interactive). Prior line-3 fragment (P173 reconcile) rotated to docs/problems/README-history.md per P134.

## 2026-05-07 (README reconciliation — add missing P175 WSJF Rankings row)

> Last reviewed: 2026-05-06 **README reconciled** — 1 drift entry corrected: P174 (MISSING wsjf-rankings row added at WSJF 1.5 tier per tie-break ladder, after P173, before P045 — cross-session drift, P174 was captured 2026-05-06 in a prior session without staging the README refresh). Reconciliation contract per P118 + ADR-014 amended ("Reconciliation as preflight robustness layer"). Prior line-3 fragment (batch transition P147 + P146 + P130 close) rotated to docs/problems/README-history.md per P134.

## 2026-05-10 (P170 transition Open → Known Error with RCA closure)

> Last reviewed: 2026-05-07 **README reconciled** — 1 drift entry corrected: P175 (MISSING wsjf-rankings row added at WSJF 1.5 tier per tie-break ladder, after P174, before P176 — cross-session drift, P175 was captured 2026-05-06 in this session via /wr-itil:capture-problem with deferred-refresh contract). Reconciliation contract per P118 + ADR-014 amended ("Reconciliation as preflight robustness layer"). Prior line-3 fragment (P174 reconcile) rotated to docs/problems/README-history.md per P134.

## 2026-05-11 (README reconciliation — add missing P179 P180 P181 P182 WSJF Rankings rows)

> Last reviewed: 2026-05-10 **P170 transition Open → Known Error** with RCA closure per ADR-022 lifecycle. RCA closed using session evidence + base-rate sweep across 178 tickets (38/178 carry "Phase N" language; 50/178 have multi-commit Fix Strategy sections; 16/178 explicitly name multi-commit/coordinated; 36/178 have sibling-tree references — true base rate 20-28%, well above original N=4 framing). Session itself produced 26 commits across 8 iters on P170 — meta-recursive proof of strain pattern. Adopter-impact deferred-to-monitor (no production adopters yet pre-Slice-6). P178 captured the orchestrator's prior habit of treating architect-PASS as RCA substitute. Prior line-3 fragment (P175 reconcile) rotated to docs/problems/README-history.md per P134.

## 2026-05-11 (P167 transition Known Error → Verification Pending)

> Last reviewed: 2026-05-11 **P164 Open → Verifying** — latent octal-eval bug in next-ID formula across all 6 ticket-creator skills fix-released this iter (`/wr-itil:work-problems` AFK loop). Scope expanded from the originally-named 4 to 6 after grep verification (`manage-problem`, `capture-problem`, `capture-rfc`, `create-adr`, `capture-adr`, `create-risk`). `10#` base-10 prefix applied across all 6 SKILL.md; 2 new regression bats fixtures cover the `099 → 100` boundary; full 28-test contract bats green. Manual sanity check confirms unfixed formula fires `bash: 099: value too great for base` and fixed formula returns `100`. Prior line-3 fragment (4 drift entries corrected — P179, P180, P181, P182) rotated to docs/problems/README-history.md per P134.

## 2026-05-12 (P165 transition Open → Verification Pending)

> Last reviewed: 2026-05-11 **P167 Known Error → Verification Pending** — Risk register "don't ship" misreading fix-released in commit `9e339d0` (2026-05-04). `RISK-POLICY.md` gained `## Risk Catalog` framing (per-action risks consumed under uniform 4/Low appetite) + `## Control Composition` rule (defense-in-depth composes multiplicatively on likelihood) — eliminates the aggregate "don't ship" misread at the policy layer. RC3 (per-action vs lifetime appetite) was superseded as a misreading. Optional TEMPLATE.md citation pointer task is moot — TEMPLATE.md was wiped by P168's wipe-and-bootstrap; bootstrap-derived risks compose with the policy directly. Substantive structural successor P168 (consume-catalog + bootstrap-from-empty) is in `verifying` since the same release wave. AFK transition iter, `/wr-itil:work-problems`. Prior line-3 fragment (P164 octal-eval Open → Verifying) rotated to docs/problems/README-history.md per P134.

## 2026-05-12 (P170 transition Known Error → Verification Pending; Phase 2 framework code shipped)

> Last reviewed: 2026-05-12 **P165 Open → Verification Pending** — P094 / P062 README-refresh enforcement gap closed via new PreToolUse:Bash commit-gate hook `itil-readme-refresh-discipline.sh` (sibling shape to P125 staging-trap + P141 changeset-discipline). Hook denies `git commit` whose staged set contains a `docs/problems/<state>/NNN-*.md` ticket change without `docs/problems/README.md` refresh; deny names offending ticket ID, recovery (`git add docs/problems/README.md`), and bypass env (`BYPASS_README_REFRESH_GATE=1`). 22-test behavioural bats fixture green; full itil hook suite 231/231 green (pre-fix 209). Architect + JTBD PASS — no new ADR; ADR-009/013/014/022/038/045 all apply unchanged. AFK transition iter, `/wr-itil:work-problems`. Prior line-3 fragment (P167 RC3 misreading fix-release) rotated to docs/problems/README-history.md per P134.

## 2026-05-12 (P185 re-rate — deferred → Severity 10 / Effort M / WSJF 5.0)

> Last reviewed: 2026-05-12 **P170 Known Error → Verification Pending** — Phase 2 framework code shipped this session across Slices 2.5/3/4/5/6/7/8/9/10/11/13/14/15-partial/16 (10 commits `b60f576` → this transition). Story-tier MVP (capture-story / manage-story / reconcile-stories / list-stories) + story-map-tier MVP (capture-story-map / manage-story-map / reconcile-story-maps / list-story-maps) + RFC frontmatter `stories:` extension + working-the-problem traversal rewrite + STORY-MAP-001 HTML bootstrap + RFC-003 capture + 7 bootstrap stories + voice-tone + style-guide policy files. Hook exemption globs (Slice 2.5) await marketplace release cycle to take effect for adopters; STORY-MAP-001 + STORY-001..007 in-repo dogfood validates the framework's meta-recursive proof. Verification gate per ADR-022: forward-dogfood post-release on capture-story-map + capture-story + manage-story + work-problem traversal. Prior line-3 fragment (P165 README-refresh-discipline hook) rotated to docs/problems/README-history.md per P134.

## 2026-05-12 (P185 re-rate + P186 capture+re-rate + P016/P017/P047 close-on-evidence)

> Last reviewed: 2026-05-12 **P185 re-rate + P186 capture+re-rate + P016/P017/P047 close-on-evidence** — Step 2 re-rated P185 (capture-problem Step 1.5 over-asks) from deferred → Severity 10 / Effort M / WSJF 5.0. Step 4 prompt batch closed P016, P017 on user direct confirmation (multi-concern split prompts exercised in production at 25 days) and P047 on direct session-evidence (the live re-rate of P185 itself IS the lifecycle-transition re-rate P047 designed for — closing on observation not age). User critiqued the `Likely verified?` age-as-evidence framing during the same Step 4 batch → P186 captured via /wr-itil:capture-problem (sibling proxy-for-evidence anti-pattern to P185 at the review-problems Step 3/5 surface) and re-rated inline to Severity 10 / Effort M / WSJF 5.0. P024 held in VQ (`wr-risk-scorer:wip` is on-demand, no nudge fired this session); P048 held pending P186 fix. P185 and P186 tied at WSJF 5.0 — ID asc places P185 ahead. Prior line-3 fragment (P170 Phase 2 transition) rotated to docs/problems/README-history.md per P134.

## 2026-05-12 (README reconciled — P187 added; prior P185 re-rate cohort rotated)

> Last reviewed: 2026-05-12 **README reconciled** — 1 drift entry corrected: P187 added to WSJF Rankings (deferred re-rate placeholder at WSJF 1.0 / Severity 3 Med / Effort M, mirroring sibling deferred captures P183 / P184). Reconciliation contract per P118 + ADR-014 amended ("Reconciliation as preflight robustness layer"). Prior line-3 fragment (P185 re-rate + P186 capture+re-rate + P016/P017/P047 close batch) rotated to docs/problems/README-history.md per P134.

## 2026-05-13 (README reconciled — P188 added)

> Last reviewed: 2026-05-13 **P185 fold-fix Open → Verification Pending** — `/wr-itil:capture-problem` Step 1.5 derive-first refactor (lexical-signal classifier + stderr-advisory contract; cat-5 → cat-4 silent-framework with cat-5 fallback on ambiguity per architect verdict; 17 new behavioural bats; SKILL.md Rule 6 + Composition tables updated; 32/32 capture-problem.bats green; full I2-no-type-branching.bats 9/9 green) lands via `@windyroad/itil` patch in AFK iter 2 of `/wr-itil:work-problems`. P185 row removed from WSJF Rankings, inserted at bottom of Verification Queue per ADR-022 fold-fix mechanic. Prior line-3 fragment (P187 add) rotated to docs/problems/README-history.md per P134.

## 2026-05-13 (README reconciled — P188 added; prior P185 fold-fix rotated)

> Last reviewed: 2026-05-13 **README reconciled** — 1 drift entry corrected: P188 added to WSJF Rankings (deferred re-rate placeholder at WSJF 1.0 / Severity 3 Med / Effort M, mirroring sibling deferred captures P183 / P184 / P187). Reconciliation contract per P118 + ADR-014 amended ("Reconciliation as preflight robustness layer"). Prior line-3 fragment (P185 fold-fix Open → Verifying) rotated to docs/problems/README-history.md per P134.

## 2026-05-14 (P064 Verifying → Closed)

> Last reviewed: 2026-05-13 **P170 Verifying → Known Error revert** — second-attempt premature Verification Pending transition (commit `3e35206`, 2026-05-12) reverted. Agent labelled P170 Phase 3 + Phase 4 as "deferred by design" in ticket body without user-deferral citation; framing then propagated to today's "anything deferred?" question as established intent. User correction: *"I never deferred those phases."* P170 returns to Known Error with Phase 3 + Phase 4 explicitly in-scope; sibling [[P189]] captures the class-of-behaviour at the ticket-body authoring surface (distinct from [[P184]] transition surface + [[P179]] untracked-phase-defer). P170 row removed from Verification Queue, re-inserted at WSJF 1.0 tier ahead of P183 per Known-Error tie-break (Known-Error-first). Prior line-3 fragment (P188 add reconcile) rotated to docs/problems/README-history.md per P134.

## 2026-05-14 (P038 Open → Verifying fold-fix)

> Last reviewed: 2026-05-14 **P064 Verifying → Closed** — `wr-risk-scorer:external-comms` evaluator (ADR-028 amended risk half) verified by direct session evidence per `feedback_verify_from_own_observation`: 30/30 bats green in-session (12 `packages/risk-scorer/hooks/test/external-comms-gate.bats` + 11 `packages/shared/test/external-comms-gate-canonical.bats` + 7 `packages/shared/test/sync-external-comms-gate.bats`), all artefacts present (canonical hook + leak-detect lib + per-package synced copy + subagent + assess-external-comms skill), release commit `a0713f3` (2026-04-26) intact. Verification Queue row removed. Closure unblocks the risk half of P079's assessment-pipeline pushback + acknowledgement comment paths; P038 voice-tone evaluator half remains the open P079 prerequisite. Prior line-3 fragment (P170 Verifying → Known Error revert) rotated to docs/problems/README-history.md per P134.

## 2026-05-15 (README reconciled — P190 added)

> Last reviewed: 2026-05-14 **P038 Open → Verification Pending (fold-fix)** — voice-tone evaluator half of ADR-028 amended external-comms gate ships alongside the existing risk evaluator (P064) via `@windyroad/voice-tone` minor + `@windyroad/risk-scorer` patch. Per-evaluator marker scheme `external-comms-<id>-reviewed-<KEY>` replaces the original combined-marker design (ADR-028 amendment 2026-05-14 ratifies + supersedes; architect + JTBD PASS). Per-package `external-comms-evaluator.conf` (NOT synced) declares evaluator id + subagent + verdict prefix + assess-skill + policy file + leak-prefilter; canonical byte-identical hook sources it at runtime. New voice-tone subagent + on-demand skill + PostToolUse:Agent mark hook; surface coverage mirrors P064 (gh issue/pr/api, npm publish, `.changeset/*.md` author-time). Bats: 13 new voice-tone + extended risk-scorer (12→13) + canonical (11→12) + sync (7→9). ADR-015 Scope table gains BOTH evaluator rows (retroactive risk-scorer + new voice-tone). P038 row removed from WSJF Rankings, appended to Verification Queue. Closure unblocks the voice-tone half of P079's pushback + acknowledgement comment paths. Prior line-3 fragment (P064 Verifying → Closed) rotated to docs/problems/README-history.md per P134.

## 2026-05-15 (P162 Phase 4 fragment rotated for P186 fold-fix)

> Last reviewed: 2026-05-15 **P162 Phase 4 landed — Rule 8 drain-condition disjunct prose** — ADR-018/020 amended with the graduatable-held-changeset disjunct per ADR-061 Rule 8; docs/changesets-holding/README.md Process gains step 5 (orchestrator-evaluated graduation flow). Closes I002 silent-stop failure mode at the drain-condition layer. Docs-only; no source/script change; no ranking-bearing field changed on P162 (Phase 4 Investigation Task checkbox + Change Log entry are narrative-only); README inventory unchanged. Prior fragment supplanted in-place per P134.


## 2026-05-15 (P186 fragment rotated for P132 Phase 2a-i)

> Last reviewed: 2026-05-15 **P186 Open → Verification Pending (fold-fix per ADR-022 P143 amendment)** — evidence-first `Likely verified?` cell shape (`yes — observed: <evidence>` / `no — not observed` / `no — observed regression`) supersedes original P048 Candidate 4 14-day age-based heuristic. Greppable `<!-- LIKELY-VERIFIED-CELL-SHAPE: evidence-based per P186 -->` marker at every render site (P138 / P150 fix-shape precedent); drift-tripwire prose at primary owners. 69 age-marker VQ rows in `docs/problems/README.md` re-rendered to `no — not observed`; behavioural bats `review-problems-likely-verified-cell-shape.bats` 17/17 green; full sibling suite re-run green 158/158 + 150/150. P186 row removed from WSJF Rankings, inserted at bottom of VQ per ADR-022. Architect + JTBD PASS. Prior line-3 fragment (P162 Phase 4 Rule 8 prose) rotated to docs/problems/README-history.md per P134.


## 2026-05-15 (P132 Phase 2a-i fragment rotated for P132 Phase 2a-ii)

> Last reviewed: 2026-05-15 **P132 Phase 2a-i shipped — manage-incident Step 4 derive-first refactor** — closes the 2026-05-06 I001 declaration regression (3 of 4 lazy sub-questions become 0 of 1; Scope alone is the surviving genuine ADR-044 cat-1 surface). Step 4 rewritten from "Use AskUserQuestion for anything not in $ARGUMENTS" to a derive-first dispatch table mirroring `/wr-itil:capture-problem` Step 1.5 (P185 worked example): Title kebab-derived from prose; Symptoms pulled verbatim; Start time three-source dispatch (timestamp regex / `git log --diff-filter=A --follow` first-touch / wall-clock UTC); Severity RISK-POLICY matrix lookup + named-evidence cross-reference with cat-5 fallback on ambiguity; Scope retained as cat-1 direction-setting. Each derived field emits a single-line stderr advisory per ADR-026 grounding. 7 new Surface 2 assertions in `manage-incident-adr-044-contract.bats` (cat-4 cross-ref / Title derive / Start-time derive / Severity derive / Scope-retains-AskUserQuestion negative-of-negative guard / P132 traceability / ADR-026 stderr advisory) — 18/18 file-local + 53/53 manage-incident suite green (RED → GREEN demonstrated). Phase 2a-ii (manage-problem create flow) + Phase 2a-iii (create-adr argument-collection) deferred to subsequent iters per ADR-014 commit-grain. Architect + JTBD PASS — no new ADR (ADR-044/013/011/026/052 already authorise). P132 stays Known Error pending the deferred slices; this iter closes the manage-incident surface only. Prior line-3 fragment (P186 evidence-first Likely-verified cell shape) rotated to docs/problems/README-history.md per P134.

## 2026-05-16 (P132 Phase 2a-ii fragment rotated for P166 + P163 close)

> Last reviewed: 2026-05-15 **AFK loop quota-halt wrap — P230 + P231 captured (README-refresh hook misfire + BYPASS env-var deny-msg correction); P132 Phase 2a-iii direction set to "extract shared helper first" per user** — `/wr-itil:work-problems` quota-exhausted at iter 5 (P166 — resets 3pm Sydney; user direction: resume P166 first post-quota). Backlog grew 84 → 86 via the 2 captures. Per user answer at orchestrator main-turn wrap, Phase 2a-iii becomes a 2-step sub-phase: (2a-iii-A) extract shared `derive-first-dispatch` helper + retrofit existing 3 surfaces (capture-problem / manage-incident / manage-problem), (2a-iii-B) create-adr adopts the helper as 4th surface — eliminates pattern-lock copy-paste drift class. Prior line-3 fragment (P132 Phase 2a-ii) rotated to `docs/problems/README-history.md` per P134. **P132 Phase 2a-ii shipped — manage-problem Step 4 derive-first refactor** — second declaration-skill surface to ship the derive-first dispatch after manage-incident Step 4 in commit b7cc645 (Phase 2a-i). `packages/itil/skills/manage-problem/SKILL.md` Step 4 rewritten from "Use AskUserQuestion for anything not in `$ARGUMENTS`" to a dispatch table: Title kebab-derived from prose (cat-4 silent-framework) + stderr advisory; Priority (Impact × Likelihood) derived from RISK-POLICY matrix lookup against description signals (impact / likelihood / named anchors) with stderr advisory; ambiguous-evidence falls back to AskUserQuestion as cat-5 (taste); Description retained as cat-1 direction-setting fallback ONLY when `$ARGUMENTS` carries no prose at all. Three declaration-skill surfaces now share the I2-isomorphic stderr advisory shape (`/wr-itil:capture-problem` Step 1.5 / `/wr-itil:manage-incident` Step 4 / `/wr-itil:manage-problem` Step 4) — pattern-lock point per architect verdict before Phase 2a-iii (`/wr-architect:create-adr` argument-collection) extends the same dispatch to a fourth. New behavioural bats file `manage-problem-adr-044-step4-derive-first.bats` — 10 assertions (cat-4 cross-ref / cat-1 fallback cross-ref / Title derive / Priority derive / Description-retains-Ask negative-of-negative / P132 traceability / ADR-026 stderr advisory / cross-skill consistency cross-ref / Step 4b multi-concern preservation guard / Step 2 duplicate-check preservation guard) — 10/10 file-local + 168/168 manage-problem suite green (RED → GREEN demonstrated; 7 of 10 RED before the SKILL.md refactor). Phase 2a-iii (create-adr argument-collection) remains deferred to a subsequent iter per ADR-014 commit-grain. Architect + JTBD PASS — no new ADR (ADR-044/013/026/052 already authorise + P132 Phase 2a explicit fix strategy). P132 stays Known Error pending the deferred Phase 2a-iii slice. Prior line-3 fragment (P132 Phase 2a-i manage-incident Step 4 derive-first) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-16 (P166 + P163 close fragment rotated for P038 close-on-evidence)

> Last reviewed: 2026-05-16 **P166 + P163 closed via hook-side sha256 derivation (ADR-028 amended 2026-05-16)** — `/wr-itil:work-problems` AFK iter 5 resumed from yesterday's quota halt; P166 (precomputed-sha256 helper for external-comms agent double-invocation, WSJF 3.0 S effort) chosen for fix shape (b) hook-side compute via prompt-parsing (not sidecar — architect-flagged correctness gap under interleaved surfaces); shared helper `packages/shared/hooks/lib/external-comms-key.sh` derives `sha256(DRAFT + '\n' + SURFACE)` from agent's `tool_input.prompt` (`SURFACE:` line + `<draft>...</draft>` block); both PostToolUse mark hooks (`risk-scorer/hooks/risk-score-mark.sh` external-comms branch + `voice-tone/hooks/external-comms-mark-reviewed.sh`) source the helper; backward-compat fallback to agent-emitted KEY preserved one release cycle; both agent prompts drop `EXTERNAL_COMMS_<EVAL>_KEY` from MANDATORY verdict; both `assess-external-comms` SKILL.md updated with structured-prompt requirement; canonical gate denial message updated; ADR-028 amendment documents contract change + P163 fix-strategy reversal (option a→b — preserves ADR-013 Rule 2 "agents stay [Read, Glob] only" instead of eroding it); 20 new behavioural bats across 3 files all GREEN; sync verification OK across all 6 canonical/copy pairs; performance impact monotonically improving (~$685/year saved, ~225-450s/day wall-clock). Both P166 (Open) and P163 (Open) transitioned to Verification Pending in same commit per ADR-014 + ADR-022 fold-fix. Backlog 86 → 84 (two open tickets closed; verifying count +2). Prior line-3 fragment (2026-05-15 AFK quota-halt wrap + P132 Phase 2a-ii) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-16 (P132 Phase 2a-iii-A shipped; prior P230 + P231 close fragment rotated)

> Last reviewed: 2026-05-16 **P230 + P231 closed via `/wr-itil:work-problems` AFK iter 2 fold-fix** — sibling tickets on the README-refresh-discipline hook surface (P165). P230 (WSJF 6.0 Open S) added narrative-only short-circuit to `packages/itil/hooks/lib/readme-refresh-detect.sh`: when staged ticket set has no ranking-bearing field change (Priority / Effort / Status / WSJF / Type field-line diff, title-line diff, A/D ticket-path entry, R<NN> rename involving ticket paths per `--name-status -M`) AND `packages/itil/scripts/reconcile-readme.sh` reports exit=0 against current README, return 0 (allow silently). Ranking-bearing edits remain gated by ADR-014 single-commit grain regardless of reconcile state. P231 (WSJF 3.0 Open S) corrected the deny-message bypass advertisement from misleading inline-prefix syntax to `BYPASS_README_REFRESH_GATE=1 via .claude/settings.json env (P173)` — P173 recurrence at the README-refresh-hook surface; Option A (correct), not Option B (remove), per architect verdict. 29/29 hook bats green (7 new behavioural cases covering narrative-only allow x 2, ranking-bearing field deny x 2, git mv rename deny, narrative+drift deny, P231 deny-message assertion). WSJF Rankings −2 (P230 + P231 rows removed). Prior line-3 fragment (P038 close earlier this turn) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-16 (P132 Phase 2a-iii-B shipped; prior P132 Phase 2a-iii-A fragment rotated)

> Last reviewed: 2026-05-16 **P132 Phase 2a-iii-A shipped via `/wr-itil:work-problems` AFK iter 3** — extracted shared derive-first dispatch helper `packages/itil/lib/derive-first-dispatch.sh` exposing 4 functions (`emit_stderr_advisory` for the I2-isomorphic stderr advisory format `<skill>: derived <field>=<value> from <source>; <reversibility>`, `derive_kebab_slug` for Title derivation, `lexical_classify_two_sided` for two-sided binary classifier shipping at capture-problem Step 1.5, and `risk_policy_matrix_lookup` for Impact × Likelihood matrix derivation shipping at manage-incident Step 4 Severity + manage-problem Step 4 Priority). Retrofitted 3 declaration-skill surfaces (capture-problem Step 1.5, manage-incident Step 4, manage-problem Step 4) to name the helper as canonical mechanism source-of-truth — surface-specific signal definitions stay inline per architect verdict "helper preserves per-surface signal definitions; only the dispatch mechanism is shared". Capture-problem stderr advisory verb renamed `classified` → `derived` to align with helper's I2-isomorphic format across all three surfaces. 19 new behavioural bats in `packages/itil/scripts/test/derive-first-dispatch.bats` covering advisory format + slug derivation + classifier + matrix lookup + cross-skill consistency; 297/297 tests green across capture-problem + manage-incident + manage-problem + helper suites. P132 stays Known Error (WSJF 4.5); Phase 2a-iii-B (`/wr-architect:create-adr` argument-collection as 4th adopter) remains DEFERRED to subsequent iter per ADR-014 commit-grain discipline. Architect verdict PASS (no new ADR — ADR-044/026/013/002 already authorise); JTBD verdict PASS (JTBD-001 primary; JTBD-006 + JTBD-101 compose). Prior line-3 fragment (P230 + P231 close earlier this turn) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-16 (P232 transition Open → Verifying — bash polling-antipattern hook + SKILL.md extension shipped; prior P087 iter-5 + P232 captured fragment rotated)

> Last reviewed: 2026-05-16 **P087 Investigation Tasks refined via `/wr-itil:work-problems` AFK iter 5** — orchestrator selected P087 (WSJF 3.0 Known Error L) per WSJF priority, but iter-5 briefing was authored from stale state (described Phase 1 ADR as pending; ADR-053 actually landed 2026-05-03 + Phase 2 ADR-058 landed 2026-05-04 with prototype outputs preserved under `docs/audits/p087-phase2-prototypes/`). No ADR-level work remained in iter-5 scope. Phase 2 *implementation* (`packages/itil/bin/wr-itil-skill-invocations` + `packages/itil/bin/wr-itil-plugin-exercise-index` shims + canonical bodies under `scripts/` + bats per ADR-058 §Confirmation 9-fixture surface) split out as its own discrete unchecked Investigation Task — previously implicit under "Draft ADR ... measurement mechanism" and inadvertently treated as covered when ADR-058 landed. Estimate M-effort if scoped to one script + its 5 fixtures; M+ for both scripts in one iter. Binding ADR citations: ADR-035 privacy (opt-out marker, path-hashing, no-network-primitive) + ADR-049 shim grammar + ADR-052 behavioural-tests-default + ADR-023 quantified performance contract. "Design the presentation layer" task clarified as partly resolved by ADR-053's location decision (canonical `plugin.json` `maturity:` field + rendered README header badge); remaining open is implementation-detail format (Phase 3). Outcome partial-progress; no commit-gate fired (Investigation Tasks list refinement only — narrative-only edit to a problem ticket, risk-scorer commit=1 push=1 release=1). Prior line-3 fragment (P132 Phase 2a-iii-B shipped earlier this session) rotated to `docs/problems/README-history.md` per P134. **P232 captured — bash until-loop pgrep self-referential deadlock (new variant of P146); SKILL.md prompt warning insufficient** — Iter 4 (P132 Phase 2a-iii-B) deadlocked in retro phase after main commit `da1a3fe` landed; 4 zsh polling loops each ran `until ! pgrep -f 'bats --recursive'; do sleep N; done` where pgrep's `-f` flag matched the polling loops' OWN command lines (which contain `'bats --recursive'` as the literal pattern), so each loop saw the other 3 as "still running" and span forever. Manual SIGTERM at 103m wall-clock; exit 143 + 0-byte JSON per P147 stuck-before-emit (retro lost; main work `da1a3fe` preserved). 3 fix options enumerated: Option A prompt-discipline extension citing both failure modes (TAP-vs-console-summary + self-referential pgrep); **Option B preferred** — Bash-tool PreToolUse hook refusing `until ! pgrep -f` / `while pgrep -f` polling patterns and advising `wait $bg_pid` or `BashOutput` instead; Option C Stop-hook scanner for visibility-only. WSJF 4.5 Med M.

## 2026-05-16 (P087 iter-5 surfaced Phase 2 implementation as discrete task; prior P132 Phase 2a-iii-B shipped fragment rotated)

> Last reviewed: 2026-05-16 **P132 Phase 2a-iii-B shipped via `/wr-itil:work-problems` AFK iter 4** — completing Phase 2a's full 4-surface scope. `/wr-architect:create-adr` Step 2 retrofitted as the fourth adopter of the shared derive-first dispatch helper; canonical helper relocated `packages/itil/lib/derive-first-dispatch.sh` → `packages/shared/derive-first-dispatch.sh` per ADR-017 (architect verdict ISSUES FOUND → resolved: cross-package source from itil would have violated the ADR-017 self-contained-package property because architect skills published as `@windyroad/architect` cannot depend on itil-package files at runtime). Per-package synced copies at `packages/itil/lib/derive-first-dispatch.sh` AND `packages/architect/lib/derive-first-dispatch.sh`; new `scripts/sync-derive-first-dispatch.sh` mirrors the P026 `sync-install-utils.sh` pattern with `--check` mode + `npm run sync:derive-first-dispatch` + `check:derive-first-dispatch` + CI step in `.github/workflows/ci.yml` + 7-test drift fixture `packages/shared/test/sync-derive-first-dispatch.bats`. create-adr SKILL.md Step 2 rewritten from a single "Use AskUserQuestion to collect the decision context" instruction to a 12-field derive-first dispatch table: silent-framework cat-4 on Title (kebab from prose), status=`proposed`, date=today, reassessment-date=today+3mo, Context-and-Problem-Statement (verbatim from `$ARGUMENTS` prose), consulted/informed defaults; direction-setting cat-1 retained on Decision Drivers / Considered Options / Decision Outcome / Consequences / Confirmation / decision-makers (architect verdict: `git config user.name` would conflate "committer" with "decision-maker" — multi-party-decision mis-attribution risk; once-per-ADR ask is low-friction). 13 new behavioural bats in `packages/architect/skills/create-adr/test/create-adr-adr-044-contract.bats` mirroring `manage-problem-adr-044-step4-derive-first.bats`; cross-skill consistency block in `packages/itil/scripts/test/derive-first-dispatch.bats` extended from 3 surfaces to 4 + byte-identical-to-canonical guard. P132 transitions Known Error → Verification Pending in this commit per ADR-022 P143 fold-fix; Phase 2b detection hook stays DEFERRED — R6 gate (lazy-AskUserQuestion ≥2 across 3 retros) has NOT fired. Architect verdict PASS subject to ADR-017 resolution applied; JTBD verdict PASS (JTBD-001 primary; JTBD-006 + JTBD-101 compose). Prior line-3 fragment (Phase 2a-iii-A landing 2026-05-16 iter 3) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-16 (P232 fold-fix fragment rotated for P087 Phase 2a ship)

> Last reviewed: 2026-05-16 **P232 fold-fix Open → Verification Pending via `/wr-itil:work-problems` AFK iter 1** — ships Option B PreToolUse:Bash hook `packages/itil/hooks/itil-bash-polling-antipattern-detect.sh` denying polling-loop shape regex `(until|while)[[:space:]]+!?[[:space:]]*(pgrep|pkill[[:space:]]+-0)` + Option A `work-problems` SKILL.md Step 5 iter prompt extension citing both P146 (TAP-vs-console-summary) AND P232 (self-referential pgrep) failure modes with worked-example syntax. 17 behavioural bats green; deny message cites P232 + names both recovery alternatives (`wait $bg_pid` shell-native, Bash-tool `BashOutput` harness-native) inside ADR-045 deny-path 200-700 byte band. Architect APPROVE (sibling to `p057-staging-trap-detect.sh` precedent; no new ADR); JTBD ALIGN (JTBD-006 primary AFK-orchestrator beneficiary). Per ADR-022 P143 fold-fix when Open ticket documents root cause + fix strategy + workaround inline. Prior line-3 fragment (P087 iter-5 refinement + P232 captured) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-16 (P087 Phase 2a fragment rotated for P087 Phase 2b ship)

> Last reviewed: 2026-05-16 **P087 Phase 2a shipped via `/wr-itil:work-problems` AFK iter 2 session 3** — ships ADR-058 transcript-axis maturity script `packages/itil/scripts/skill-invocations.sh` + `packages/itil/bin/wr-itil-skill-invocations` ADR-049 shim + 13-test behavioural bats fixture covering ADR-058 §Confirmation criteria 1–5 (NDJSON shape, opt-out marker, no-network-primitive negative-grep, content-sanitisation / path-hashing, inaccessible-directory) plus existence / forward-extension flag / window-days filter. Reads `~/.claude/projects/**/*.jsonl` recursive; emits one NDJSON record per surface (schema_version 1.0; axis / surface / kind / plugin / window_days / invocations / first_invocation_iso / last_invocation_iso). Exit-0-always per ADR-013 Rule 6. Smoke-tested against live workstation transcripts; top surfaces match 2026-05-03 prototype intuition check. Phase 2b (git axis `wr-itil-plugin-exercise-index`) and Phase 2c (performance reassessment — 7.8s warm-cache observation against 5157 jsonl exceeds ADR-058's 5s threshold) queued as discrete Investigation Tasks. Changeset held under `docs/changesets-holding/` per ADR-042 Rule 2 dogfood — R009 new-user-facing-surface modulator drove residual 6/25 Medium → 4/25 Low after hold; reinstate ≥2026-05-23 OR Phase 2b joint release OR risk re-scores Low. Architect GREEN; JTBD ALIGNED (JTBD-101 primary hardening-prioritisation outcome served). Prior P232 fold-fix fragment rotated to `docs/problems/README-history.md` per P134.

## 2026-05-17 (P132 REVERT + P087 Phase 2b stacked fragments rotated for P132 Phase 2b ship)

> Last reviewed: 2026-05-17 **P132 REVERTED Verifying → Known Error after orchestrator main-turn P130 violation** — orchestrator main-turn fired `AskUserQuestion` between iter 3 + iter 4 of 2026-05-16 session 3 asking the user to pick iter 4's target — a mechanical-stage transition explicitly forbidden by the Mid-loop ask discipline subsection of work-problems SKILL.md. User AFK overnight; answered 2026-05-17 morning with strong-signal P078 correction. Phase 2a 4-surface derive-first refactor (capture-problem Step 1.5 + manage-incident Step 4 + manage-problem Step 4 + create-adr Step 2) closed the per-skill-SKILL surfaces but NOT the orchestrator-main-turn-between-iters surface. Phase 2b structural-enforcement hook (UserPromptSubmit / Stop hook detecting AskUserQuestion calls firing in SKILL-explicit no-ask zones) remains load-bearing. P132 added back to WSJF Rankings at WSJF 4.5 (Med). Prior line-3 fragment (P087 Phase 2b shipped) rotated to `docs/problems/README-history.md` per P134. **P087 Phase 2b shipped via `/wr-itil:work-problems` AFK iter 3 session 3** — ships ADR-058 git-axis maturity script `packages/itil/scripts/plugin-exercise-index.sh` + `packages/itil/bin/wr-itil-plugin-exercise-index` ADR-049 shim + 19-test behavioural bats fixture covering ADR-058 §Confirmation criteria 6 (git-axis composite incl. literal-`|`-in-subject parser defence per architect advisory), 7 (outside-git-repo + missing-packages), 8 (schema-version) plus opt-out / no-network / content-sanitisation / composite-index formula / breaking-marker / closed-tickets-window / window-days filter / category-overrides flag. Runs `git log --reverse --name-only --pretty=format:%H|%aI|%s` once; window cutoff applied in-Python against `%aI` author-date (git's `--since=Nd` form observed empirically unreliable against the bats fixture environment — refactor preserves ADR-058 §Script contracts NDJSON output verbatim and routes the date filter to Python; ADR-058 amendment NOT required, captured in iter-3 note for audit trail). Single git-log pass collapses Phase 2 contract's two-pass design (≤1 git fork; 1.07s warm-cache on this workstation — under the 5s reassessment trigger). NDJSON-per-plugin: itil composite_index 4.28 (prototype 4.11), retrospective 3.34 (prototype 3.30), risk-scorer 3.32, architect 3.22 — top-of-list matches 2026-05-03 prototype intuition. `days_shipped` tracks `min(author_date)` (robust against commit-topology / rebase / cherry-pick reordering — implementation discretion within ADR-058 wording tolerance). Changeset held under `docs/changesets-holding/` per ADR-042 Rule 2 sibling to Phase 2a — R009 new-user-facing-surface-no-dogfood-window modulator drives residual above appetite without hold; reinstate ≥2026-05-23 joint with Phase 2a (cross-axis dogfood window), OR Phase 2c performance refinement triggers joint release, OR risk re-scores Low. Architect GREEN-with-literal-pipe-advisory (applied verbatim); JTBD ALIGNED (JTBD-101 hardening-prioritisation outcome's git-axis half now served alongside Phase 2a's transcript-axis half). P087 Investigation Task list updated: Phase 2a + Phase 2b both checked; Phase 2c + Phase 3 + Phase 4+ remain queued. Prior P087 Phase 2a fragment rotated to `docs/problems/README-history.md` per P134.


> Last reviewed: 2026-05-17 **P132 Phase 2b shipped via `/wr-itil:work-problems` AFK iter 4 session 3** — ships structural-enforcement Stop hook `packages/itil/hooks/itil-mid-loop-ask-detect.sh` detecting orchestrator main-turn `AskUserQuestion` calls fired mid-loop inside `/wr-itil:work-problems` via three-signal transcript-scan: (1) last assistant turn contains `AskUserQuestion` tool_use, (2) earlier assistant message issued a `Skill` tool_use to `wr-itil:work-problems`, (3) no `ALL_DONE` / `## Work Problems Summary` terminal marker emitted since the activation. When all three match the hook emits a structured `stopReason` advisory citing P130 + Mid-loop ask discipline subsection + ADR-044 framework-resolution boundary. **Advisory only — never blocks** per architect verdict (hard block on legitimate halt-point AskUserQuestions would itself violate JTBD-006 outcome 5). Registered in `packages/itil/hooks/hooks.json` Stop array alongside sibling `itil-assistant-output-review.sh` (P085 prose-ask detector). 13 behavioural bats green covering positive detection × 3 (P130 cite, ADR-044 cite, intermixed-content variant) + silent-exit paths × 7 (no orchestrator activation, ALL_DONE post-loop, ## Work Problems Summary post-loop, last turn no AskUserQuestion, missing transcript_path, non-existent file, empty transcript) + malformed-JSONL crash-safety + ADR-045 advisory-budget; 268/268 full itil hooks bats suite green. Per-surface configuration (`ORCHESTRATOR_SKILL` + `TERMINAL_MARKER_RE` variables at top of script) makes detection pattern parametric per JTBD verdict's extensibility note — future Phase 2c/2d adopters (run-retro Step 4b Stage 1, /install-updates Step 6a) copy + retarget without forking. Closes the orchestrator-main-turn-between-iters surface that the Phase 2a 4-surface SKILL.md derive-first refactor empirically did NOT cover (2026-05-17 regression: orchestrator asked iter-target selection between iters 3 and 4; halted AFK loop for hours). Architect verdict PASS (no new ADR — joint coverage of ADR-013 / ADR-044 / ADR-045 / ADR-052 / ADR-005); JTBD verdict PASS (JTBD-006 primary, JTBD-001 + JTBD-101 compose); pipeline scorer commit=4 push=4 release=4 (all Low, within appetite). **Status stays Known Error** pending in-the-wild observation per ADR-022 P143 — structural hook fires post-turn (advisory biases NEXT turn); verification requires "no analogous regression on the orchestrator-main-turn surface across at least one subsequent AFK session", not same-commit assertion. Prior stacked fragments (P132 REVERT + P087 Phase 2b) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-17 (P087 Phase 2c fragment rotated for P087 Phase 2d ship)

> Last reviewed: 2026-05-17 **P087 Phase 2c shipped via `/wr-itil:work-problems` AFK iter 5 session 3** — performance-reassessment iter prescribed by ADR-058 §Reassessment Triggers (line 201). `wr-itil-skill-invocations --window-days=30` warm-cache median 7.12s across three runs (Apple M1 Pro, Darwin 25.3.0, Python 3.10.6) against 5155 jsonl / 1.13 GB / 380,898 lines — confirms iter-2's 7.8s observation; ~42% above the 5s reassessment threshold. `wr-itil-plugin-exercise-index` healthy at 0.39s median (well within ADR-058 line 128 ≤1.0s budget); iter-3's 1.07s observation appears transient. Profile breakdown: Python startup + json import 0.10s; `Path.rglob("*.jsonl")` enumeration alone 0.30s; rglob + per-file stat() 0.17s; read every file no-parse 5.91s; read + JSON parse every line 8.80s — **file I/O dominates** (~6s); enumeration negligible; mtime-gate filter takes 8.80s parse-all to 7.12s observed. **Decision**: defer transcript-axis optimization to a new Phase 2d Investigation Task within P087 (sibling-phase pattern 2a → 2b → 2c → 2d, WSJF-anchored to P087's L effort) rather than split to a separate ticket or amend ADR-058 ahead of optimization. Trigger language at ADR-058 line 201 ("open a performance-tuning iter AND re-quantify") makes re-quantification the optimization iter's exit deliverable, not a separate pre-amendment. Optimization candidates surfaced for Phase 2d: binary-search-to-first-in-window-line (JSONL append-only — older lines = older invocations; reduces parse population); larger-window-tail file filter before parse; in-process mtime cache (per ADR-058 line 131 carve-out; disk cache remains prohibited per ADR-053 §Confirmation #1). Investigation-only iter; no script changes; no ADR amendment; no test changes. Ticket effort and WSJF unchanged at L / 3.0; no conditional README-refresh trigger fires per Step 6's contract — README refresh proceeds anyway per the mechanical P165 ticket-edit gate. Prior line-3 fragment (P132 Phase 2b shipped) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-17 (P087 Phase 2d fragment rotated for P087 Phase 2e ship)

> Last reviewed: 2026-05-17 **P087 Phase 2d shipped via `/wr-itil:work-problems` AFK iter 6 session 3** — performance-optimization iter implementing the architect-endorsed staged plan (substring-filter-first / binary-search-only-if-needed). Adds a 13-line substring pre-filter on the discriminating `"tool_use"` token before `json.loads()` in `packages/itil/scripts/skill-invocations.sh` Python body. ~60% of in-window transcript lines (user messages, tool_result blocks, snapshots, title records) carry no `"tool_use"` value at all and now short-circuit without paying the JSON parse cost (against the live corpus: 260,943 in-window lines → 103,459 retained for full parse, 39.6%). Re-quantified warm-cache median **5.34s** (5.32 / 5.34 / 5.44 across three runs) against the same 5155 jsonl / 1.13 GB / 380,898 lines Phase 2c baseline — 1.78s reduction (25%) from the 7.12s Phase 2c median, matching the architect's expected-outcome ~5.3s prediction within 0.04s. The 5s ADR-058 §Reassessment Triggers threshold remained marginally exceeded (0.34s / 6.8% over) — Phase 2e binary-search-to-first-in-window queued as a discrete P087 Investigation Task to close the residual gap (the architect-endorsed staged plan called this scenario out explicitly: "If 5.3s still exceeds the 5s threshold, the amendment records the gap and queues the binary-search fallback as Phase 2e"). Substring filter is whitespace-tolerant (`"tool_use"` is a value token, not a key:value pair) and works against both compact and pretty-printed JSONL. NDJSON output schema unchanged (`schema_version` stays 1.0); privacy posture unchanged; ADR-013 Rule 6 exit-0-always preserved. False-positive fall-through invariant pinned by new bats fixture "Phase 2d: false-positive substring fall-through does not inflate counts" — 14 tests now green (up from 13). ADR-058 §Performance contract amended with the Decision Outcome — Phase 2d block per ADR-023 template; §Reassessment Triggers updated to show the trigger remains advisory-firing pending Phase 2e re-measurement. Architect GREEN; JTBD GREEN (JTBD-101 hardening-prioritisation outcome served — faster script = signal users actually consume); risk-scorer WIP PASS Low (3); pipeline PASS Very Low (2). Ticket effort and WSJF unchanged at L / 3.0; no conditional README-refresh trigger fires per Step 6's contract — README refresh proceeds anyway per the mechanical P165 ticket-edit gate. Prior line-3 fragment (P087 Phase 2c shipped) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-17 (session 3 retro wrap fragment rotated for P162 Phase 2b deferral-condition-met ship)

> Last reviewed: 2026-05-17 **Session 3 retro wrap captures P234 + P235 after user catches fictional-defer at Tier 3 rotation** — agent deferred 3 MUST_SPLIT briefing files in retro summary with "cascade case: archive-of-archive tier design needed" rationale; user responded "Don't defer"; rotation executed in 10 turns without hitting the cited obstruction. User then named the structural defect: *"Like, when did you think that work was going to get done??"* P234 captures the class-of-behaviour (fictional-defer / under-do inverse of P132 over-ask; sibling to P148 / P145 — both surfaces of the same framework-resolution-boundary violation). P235 captures the sibling fictional defer (Signal-vs-Noise pass on 146-entry briefing backlog) as the SCHEDULED-FUTURE-SURFACE per P234 remediation pattern. Tier 3 budget rotation APPLIED: 3 NEW deep archive tier files (hooks-and-gates-archive-pre-2026-05-04 / governance-workflow-archive-mid / governance-workflow-archive-pre-2026-04-23); 0 MUST_SPLIT files post-rotation. Prior line-3 fragment rotated to README-history per P134. **Session 3 retro run + P233 captured (iter subprocess plugin cache stale after release)** — `/wr-retrospective:run-retro` fired manually after iter 7 SIGTERM (the iter's own retro phase was where the P232 antipattern recurred and deadlocked at 90 min wall-clock; retro metadata lost per P147; session-wrap retro recovers the retro layer from the orchestrator's main turn). Retro outputs: `docs/retros/2026-05-17-session-3.md` (Step 5 summary) + `docs/retros/2026-05-17-ask-hygiene.md` (P135 Phase 5 trail; lazy=1 — the morning P130 violation; R6 numeric gate NOT fired but user's direct correction triggered the same response ahead of the gate — P132 Phase 2b shipped same-day commit 841db68 + @windyroad/itil@0.30.3). Briefing: 2 new entries to `docs/briefing/afk-subprocess.md` (cache-staleness + orchestrator-main-turn-ask-discipline). New ticket P233 captured at WSJF 4.0 Med Open with preferred fix Option C (orchestrator auto-invokes /install-updates in Step 6.5 post-release-drain). Tier 3 budget pass: 3 MUST_SPLIT files (governance-workflow / hooks-and-gates / hooks-and-gates-archive) — rotation deferred per cascade case (destination archives also OVER; needs archive-of-archive tier design before mechanical split-by-date is safe). Context cheap layer: +8.3% bytes vs prior retro (no single bucket >20% delta). JTBD currency: clean. Prior line-3 fragment (P087 Phase 2e shipped) rotated to `docs/problems/README-history.md` per P134. **P087 Phase 2e shipped via `/wr-itil:work-problems` AFK iter 7 session 3** — performance-optimization iter completing the architect-endorsed staged plan (substring-filter-first / binary-search-only-if-needed). Adds a binary-search-to-first-in-window byte-seek before the line iterator in `packages/itil/scripts/skill-invocations.sh` Python body. Files at or above a 256 KB threshold bisect to the earliest byte offset whose line carries a `timestamp` at or after the cutoff, then linear-scan from there; files below threshold continue linear-scan from byte 0 (bisect overhead is not worth it for small files). JSONL is append-only within a single session jsonl file — older lines appear earlier by author-timestamp monotonicity — so the bisect skips the historical pre-cutoff portion of long-lived session files without paying the read cost. Re-quantified warm-cache median **4.04s** (3.87 / 4.04 / 4.98 across three warm runs) against a 5164 jsonl / ~1.08 GB corpus on this workstation — 1.30s reduction (24%) from Phase 2d's 5.34s baseline; 3.08s reduction (43%) from Phase 2c's 7.12s baseline. Cold-cache 7.81s (single run). **The 5s ADR-058 §Reassessment Triggers threshold is now silenced** — warm-cache wall-clock sits 0.96s / 19% under budget with measurement headroom for further corpus growth. NDJSON output schema and record count unchanged (235 records on the live corpus, identical surface attribution and ordering; top-of-list spot-check matches: `wr-risk-scorer:pipeline` 1454, `wr-architect:agent` 737, `wr-jtbd:agent` 668, `wr-risk-scorer` 375, `wr-itil-reconcile-readme` 284). Privacy posture unchanged; ADR-013 Rule 6 exit-0-always preserved. Bisect uses a whitespace-tolerant byte-regex (`"timestamp"\s*:\s*"..."`) for timestamp probes; readline-boundary alignment guarantees byte-safety; loop termination guaranteed by `hi = mid` on the in-window branch (an earlier `hi = pos` formulation stalled on line-aligned probes — debug trace caught it; one-line tighten-rule change fixed). Append-only monotonic-timestamps within a single session jsonl is the documented input invariant — pinned in ADR-058 Phase 2e amendment; synthetic violation under-counts gracefully without crashing or emitting malformed NDJSON. Four new behavioural bats fixtures pin the byte-seek correctness boundary (straddle, all-in-window, small-file linear, non-monotonic graceful-degradation per architect advisory); 19 tests now green (up from 14 in Phase 2d). ADR-058 §Performance contract amended with the Decision Outcome — Phase 2e block per ADR-023 template; §Reassessment Triggers updated to record the threshold silencing. Architect GREEN (no new ADR required — within the explicit Phase 2e slice ADR-058 reserves); JTBD GREEN (JTBD-101 hardening-prioritisation outcome served — script now under the 5s threshold); pipeline PASS Very Low (commit=2 push=2 release=2). **Phase 2 (transcript axis) is now performance-complete** — Phase 2a + Phase 2b scripts both under their respective ADR-058 budgets (transcript 4.04s under 5s, git 0.39s under 1.0s). Remaining P087 work is Phase 3 mechanical rollout (retroactive assessment + README badge rendering + `plugin.json` `maturity:` field population per ADR-053 §Decision Outcome) plus tail-end presentation-detail tasks. Ticket effort stays L (Phase 3 mechanical rollout remains); WSJF unchanged at 3.0; no conditional README-refresh trigger fires per Step 6's contract — README refresh proceeds anyway per the mechanical P165 ticket-edit gate. Prior line-3 fragment (P087 Phase 2d shipped) rotated to `docs/problems/README-history.md` per P134.


## 2026-05-17 (P162 deferral-condition-met fragment rotated for P234 RCA iter ship)

> Last reviewed: 2026-05-17 **P162 Change Log append — Phase 2b deferral condition met via `/wr-itil:work-problems` AFK iter session 4.** The 2026-05-13 user direction deferred Phase 2b (atomic-cohort Class 3b RFC cohort enumeration) until an atomic-cohort accumulates in `docs/changesets-holding/`; holding now contains P170 Phase 3+4 atomic-cohort (2 entries sharing "Phase 3+4 end-of-chain user verification" reinstate trigger per ADR-060 commit-grain decomposition) + P087 cross-axis sibling cohort (2 entries sharing joint-release trigger). Proceed-vs-defer-Phase-2b-implementation queued as outstanding_question for AFK loop-end batched user authorisation per ADR-044 cat-1 direction-setting + the verbatim "Re-evaluate when..." phrasing. Captures the deferral-condition-met state in the ticket's durable audit surface; closes the P184-class silent-loss failure mode where conditional defers live only in conversation context and silently lose work on session boundary. Architect + JTBD PASS; risk-scorer 1/0/0 Very Low; ranking-bearing fields unchanged (P094 conditional-refresh trigger does not fire; README refresh proceeds per the mechanical P165 ticket-edit gate). Prior line-3 fragment (session 3 retro wrap + P087 Phase 2e ship) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-17 (P234 RCA iter fragment rotated for P234 Phase 1 ship)

> Last reviewed: 2026-05-17 **P234 RCA populated via `/wr-itil:work-problems` AFK iter session 4** — all 4 Investigation Tasks ticked + grounded against session 3 retro (0 fictional defers post-correction; "deferred per Branch B" in Topic File Rotation is a legitimate defer per run-retro SKILL Step 3 Branch B's documented next-retro-budget-check scheduled-future-surface). Two-axis test for legitimate-vs-fictional defers defined: SCHEDULED-FUTURE-SURFACE axis (ticket ID, named skill invocation step, named hook, CI workflow step, ADR-with-date) + mechanical-actionability axis. Detection-surface refined to PostToolUse:Write/Edit on `docs/retros/*.md` (higher-precision shape than the ticket title's Stop-hook framing). P148 cross-reference confirmed; `check-tickets-deferred-cause.sh` is the Option B sibling-script template. Fix Strategy refined with Option A hook implementation skeleton mirroring the just-shipped P132 Phase 2b sibling hook `itil-mid-loop-ask-detect.sh` (commit 841db68, @windyroad/itil@0.30.3) — hook event, detection signal triplet, exception allowlist, advisory shape per ADR-045 budget, per-surface configuration variables, behavioural bats fixture shape. Added `## Scheduled Future Surface for Fix Shipping` section naming the orchestrator WSJF queue + P234's persistent Open state as the legitimate scheduling mechanism for Phase 1 implementation (avoids the meta-recursive trap of P234 itself being a fictional defer per the P179 carve-out). Worked Example annotated with subsequent-correction note (SVN backlog → P235 capture demonstrates the prescribed remediation pattern). Status unchanged (Open); Priority / Effort / WSJF unchanged (12 / M / 6.0); no conditional README-refresh trigger fires per Step 6's contract — README refresh proceeds anyway per the mechanical P165 ticket-edit gate. Prior line-3 fragment (P162 Phase 2b deferral-condition-met ship) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-17 (P234 Phase 1 ship fragment rotated for P234 Open → Known Error transition)

> Last reviewed: 2026-05-17 **P234 Phase 1 shipped via `/wr-itil:work-problems` AFK iter 3 (same-day, post-RCA session)** — `packages/itil/hooks/itil-fictional-defer-detect.sh` PostToolUse:Write|Edit|MultiEdit advisory hook on `docs/retros/*.md` detecting defer-rationale phrases (`next retro` / `next session` / `defer pending` / `defer with cause:` / `deferred per`) lacking a SCHEDULED-FUTURE-SURFACE citation in the +/-5 line context window. Valid surfaces: ticket ID (P\d{3} / STORY-\d{3} / R\d{3} / RFC-\d{3}), named skill invocation (/wr-foo:bar), hook/script path (*.sh), CI workflow (.github/workflows/), or dated ADR (ADR-\d{3} + YYYY-MM-DD both in window). Allowlist: `deferred per Branch B` (Branch B's next-retro `check-briefing-budgets.sh` trigger is the scheduled surface inside the SKILL contract). Stderr advisory + exit 0; never blocks; mirrors `itil-rfc-trailer-advisory.sh` PostToolUse precedent + the just-shipped sibling P132 Phase 2b hook `itil-mid-loop-ask-detect.sh` (commit 841db68, @windyroad/itil@0.30.3) per-surface-config shape. 14-test behavioural bats fixture (ADR-052 + P081) pins detection / allowlist / crash-safety / ADR-045 <1000B budget. Closes the under-do half of the ADR-044 inverse-correctness pair (P132 over-ask / P234 under-do). Reviewers PASS (architect ADR-057 Phase-2 slot, JTBD-006 + JTBD-101, WIP risk Low, external-comms PASS, voice-tone PASS, pipeline 4/4/4 Low at all three layers). Status remains Open pending @windyroad/itil@0.30.4 publish; ranking-bearing fields unchanged (P094 conditional-refresh does not fire; README refresh proceeds per the mechanical P165 ticket-edit gate). Prior line-3 fragment (P234 RCA iter ship) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-17 (P234 Open → Known Error transition fragment rotated for P233 Open → Known Error transition)

> Last reviewed: 2026-05-17 **P234 Open → Known Error transition via `/wr-itil:work-problems` AFK iter 4** — released-fix detection: Phase 1 Option A hook `packages/itil/hooks/itil-fictional-defer-detect.sh` shipped commit 9117246 + version-packages 07d3224 + merge 50626e7 → @windyroad/itil@0.30.4 per CHANGELOG.md `## 0.30.4`. Per the ticket's `## Scheduled Future Surface for Fix Shipping` discipline, the orchestrator WSJF queue (deterministic mechanism) re-selected P234 in this iter for the post-release transition — exactly the path the section described, meta-validating the SCHEDULED-FUTURE-SURFACE invariant the ticket itself defines. Pre-flight per `/wr-itil:transition-problem` Step 4: 4/4 root-cause + investigation-tasks + reproduction + workaround + Effort-bucket-revalidation all PASS. P063 detection fired on `@windyroad/itil` scoped-npm + prose tokens `upstream` / `external` / `vendor` — all matches are self-references to our own packages OR prose-context inverse-correctness/conditional-deferral framing per P179 carve-out; SKILL false-positive recovery path applied (stable marker written to `## Related`). File renamed `docs/problems/open/234-...md` → `docs/problems/known-error/234-...md`; Status field Open → Known Error; row re-ordered above P162 per the WSJF-desc + Known-Error-first sort key (P138). Verification criterion for subsequent Known Error → Verifying transition: no analogous fictional-defer recurrence in `docs/retros/*.md` across at least one subsequent retro exercising Tier 3 budget rotation / Signal-vs-Noise pass / Step 4b Stage 1 Tickets Deferred surfaces (mirrors P132 Phase 2b verification framing). Phase 2 (Option B `check-defer-rationales.sh` cross-session trend detector) + Phase 3 (Option C SKILL.md prose hardening) remain CONDITIONAL backlog per the P179 carve-out — SCHEDULED-FUTURE-SURFACE for Phase 2/3 = orchestrator WSJF queue re-selection of P234 at recurrence trigger (same pattern Phase 1 used). Per P233 cache-stale: the just-released hook is NOT yet active in the local plugin cache for this iter — orthogonal Pipeline Instability not a verification blocker. Architect/JTBD reviews skipped per `/wr-itil:transition-problem` SKILL contract (metadata-only transition; `docs/problems/` is excluded from architect/JTBD gates); risk-scorer commit gate satisfied per Step 8. Prior line-3 fragment (P234 Phase 1 shipped) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-17 (P246 refinement + P165/P232 graduation fragment rotated for P248/P249/P250 re-rate pass)

> Last reviewed: 2026-05-17 **P246 refinement + P165/P232 evidence-based graduation (session 4 wrap)** — user direction *"Dogfooding makes sense, but it shouldn't be time based, it should be until we are happy that it's working as desired"* refined P246's framing: dogfood criterion is positive-evidence-of-working-as-desired, NOT elapsed time. New P246 Investigation Tasks added for ADR-061 amendment + holding README sweep. P165 (README refresh discipline) + P232 (Bash polling antipattern hook) graduated from holding per positive evidence (4 sessions clean for P165; 1 day clean for P232 across many bash invocations). P166+P163 explicitly NOT graduated — negative evidence per P198 (5+ recurrences). Cohort relationship between P165 and P166+P163 severed at this pass per the per-entry-evidence principle. **Earlier 2026-05-17 review (session 4 iter 5)**: P233 Open → Known Error transition via `/wr-itil:work-problems` AFK iter 5 session 4 — Phase 1 implementation landed in-iter: `/wr-itil:work-problems` SKILL.md Step 6.5 Drain action gains a step-4 `/install-updates` chain after successful within-appetite `release:watch`, plus a Post-release cache refresh subsection documenting conditional-on-release + non-blocking-on-failure + ADR-013 Rule 5 policy authorisation + ADR-044/P130 mid-loop ask discipline preservation. Empirical anchor: `docs/briefing/afk-subprocess.md` (Just-shipped gate-class hooks DON'T protect the immediate-next iter) confirmed iter subprocesses re-resolve plugin cache on spawn, not parent-inherited — architect re-review APPROVED after evidence presented. Verification criterion for subsequent Known Error → Verifying transition: no analogous cache-staleness recurrence after the next gate-class hook release shipped via the orchestrator's `release:watch` + chained `/install-updates` — observe across at least one subsequent AFK loop exercising Step 6.5 Drain action step 4. Self-recursion note: modifying work-problems SKILL.md is forward-looking — the current orchestrator session is mid-execution from cached version 0.29.0 and does NOT pick up this change; future `/wr-itil:work-problems` invocations after cache refresh pick up step 4.

## 2026-05-17 (P248/P249/P250 re-rate + VQ-evidence-correction fragment rotated for inbound-cache-refresh pre-flight)

> Last reviewed: 2026-05-17 **P248/P249/P250 re-rated + VQ-evidence-correction** — `/wr-itil:review-problems` interactive pass after session 4 wrap. Re-rates from placeholder Likelihood=1: P248 (token-cost-for-WSJF-effort) → Severity 6 WSJF **3.0** (joins P087/P097/P182/P161 tier); P249 (reporter response check) → Severity 9 WSJF **4.5** (joins P132/P246/P247 fictional-defer/symmetric-gap tier); P250 (Step 6.5 within-appetite no-drain) → Severity 12 WSJF **6.0** (joins P234/P162 top tier). P247 confirmed unchanged at 9/4.5. VQ closure pass skipped per user re-affirmation of P186 evidence-first principle (verbatim: *"You can't use elapsed time as a trigger to close. You need observed evidence"*) — running SKILL.md cache 0.29.0 still encodes the age heuristic, but source @windyroad/itil 0.32.1 has P186 fix shipped at commit 4825603 (evidence-first cell shape with three canonical values `yes — observed: <evidence>` / `no — not observed` / `no — observed regression`). This iter runs from the pre-fix cache, demonstrating the P233 cache-staleness pattern in real-time: the in-flight `/wr-itil:review-problems` invocation cannot pick up the just-released P186 SKILL refactor because cache resolution happens at skill-load time, not at fix-ship time. Inbound-discovery channel poll deferred (no AFK orchestrator context for this interactive invocation; cache age 198226s > 86400s ttl per Step 4.5 fail-soft non-blocking branch — flag for next AFK pass). No tickets closed; no auto-transitions fired (the 4 ticket edits this pass are Priority/Effort/WSJF re-rates on already-Open tickets — no Open → Known Error promotion warranted because root cause + workaround are still deferred to investigation). 65 of 74 verifying tickets ≥14 days old but all stay `no — not observed` in the column — age is the Released-column signal, not the Likely-verified signal. Prior line-3 fragment (P246 refinement + P165/P232 graduation, session 4 wrap) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-17 (inbound-discovery cache refresh fragment rotated for P234 Known Error → Verification Pending transition)

> Last reviewed: 2026-05-17T11:21:52Z **inbound-discovery cache refresh via `/wr-itil:work-problems` Step 0b pre-flight iter** — TTL-expiry auto-recheck branch (Step 4.5b) fired: cache age 198278s > 86400s `ttl_seconds`. Poll outcome: 31-issue set unchanged on `github-issues:windyroad/agent-plugins` (same numbers 42/56-63/76-87/97-98/110/117/120-126; no new reports, no closed reports); discussions still skipped (disabled, HTTP 410); security-advisories still skipped (external-comms-gate LIST-misclassification preserved per #125 / local P198). Pipeline classification still deferred per 2026-05-15 user direction (external-comms-gate sha-bug bottleneck on per-report ack comments; every cache report's `classification: pending-pipeline-processing` + `cache_audit_note` preserved). `docs/problems/.upstream-cache.json` rewritten with `last_checked` + per-channel `fetched_at` refreshed; reports payload otherwise byte-identical. `docs/audits/inbound-discovery-log.md` appended with the discovery-pass entry per ADR-062 § Audit-log surface. Inbound Upstream Reports README section populated from cache (was empty body in prior render). No backlog re-score this pass (this morning's interactive P248/P249/P250 re-rate already refreshed the WSJF queue). No auto-transitions; no VQ closures (P186 evidence-first principle — no in-session observed evidence this iter). Prior line-3 fragment (P248/P249/P250 re-rate + VQ-evidence-correction) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-17 (P234 K→V transition fragment rotated for P082+P251 session-direction-setting commit)

> Last reviewed: 2026-05-17 **P234 Known Error → Verification Pending transition via `/wr-itil:work-problems` AFK iter 5** — Phase 1 hook `itil-fictional-defer-detect.sh` active in cache at `@windyroad/itil@0.32.1` (Phase 1 originally landed in `@windyroad/itil@0.30.4` commit `9117246`, release commit `50626e7`; subsequent itil releases preserved the hook). Verification criterion empirically met: zero analogous fictional-defer recurrences across 5 subsequent retros post-iter-4 transition (iters 5/6/7/8/session-4-wrap), with Tier 3 budget rotation surface exercised legitimately via Branch B `leave-as-is` (iter-5 line 46, iter-6 line 21) and Branch A force-trigger SKILL-contract citation (session-4-wrap line 94). Pre-flight per `/wr-itil:transition-problem` Step 4 for `verifying` destination: fix implemented PASS + release marker available PASS. File renamed `docs/problems/known-error/234-...md` → `docs/problems/verifying/234-...md` per ADR-031 / RFC-002 T5a per-state subdir; Status field Known Error → Verification Pending; `## Fix Released` section appended with release marker + fix summary + awaiting-verification line + 4-citation exercise evidence; row moves from WSJF Rankings (Known Error) → Verification Queue with `Likely verified?` cell populated `yes — observed:` per P186 evidence-first principle (real session-observed evidence). Phase 2 (Option B `check-defer-rationales.sh` cross-session trend detector) + Phase 3 (Option C SKILL.md prose hardening) remain CONDITIONAL backlog per the P179 carve-out — neither has graduated; SCHEDULED-FUTURE-SURFACE for those phases = orchestrator WSJF queue's re-selection of P234 at recurrence trigger. Architect/JTBD reviews skipped per `/wr-itil:transition-problem` SKILL contract (metadata-only transition; `docs/problems/` excluded from architect/JTBD gates); risk-scorer commit gate satisfied per Step 8. Partial-pathspec staging used per orchestrator dirty-for-known-reason constraint (P234 + README + history paths only; pre-existing 9 staged renames + `.claude/settings.json` mod preserved untouched). Prior line-3 fragment (inbound-discovery cache refresh) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-17 (P082 + P251 capture fragment rotated for run-retro session-5 wrap)

> Last reviewed: 2026-05-17 **P082 cognitive-accessibility scope expansion + P251 capture (RFC-first trace invariant not enforced) via `/wr-itil:work-problems` orchestrator main turn after user correction** — P082 (`docs/problems/open/082-no-voice-tone-or-risk-gate-on-commit-messages.md`) gains a third evaluator domain alongside voice-tone + content-risk: cognitive accessibility (WCAG 2.2 SC 3.1.5 Reading Level, COGA plain-language guidance). Symmetric scope flag added for P038/P064/P073 — every reader-facing prose surface deserves the same clarity bar. Cognitive-accessibility evaluator plugin TBD (candidate `@windyroad/cognitive-a11y` or voice-tone extension); aligns with the global `cognitive-accessibility` agent persona already in `~/CLAUDE.md`. P251 captured as new ticket: "RFC-first trace invariant not enforced — fixes start without RFC, story map, or JTBD trace" (Open, M, deferred-rated, JTBD-008 trace, solo-developer persona). Names the symmetric gap: ADR-060 I1 enforces RFC→Problem trace at RFC capture; the inverse Problem→RFC trace at fix-time is NOT enforced, so problems accrete task-lists inline rather than scoping RFCs + story maps + JTBD traces before fix work commences. JTBD-008 § "Trace invariant" + "Capture-time scoping" Desired Outcomes name this directly. Both edits committed via partial-pathspec staging (P082 + P251 + README + README-history paths only; .claude/settings.json mod preserved untouched). Prior line-3 fragment (P234 K→V transition) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-17 (run-retro session-5 wrap fragment rotated for P252 fix + P253 capture)

> Last reviewed: 2026-05-17 **/wr-retrospective:run-retro session-5 wrap — P252 captured + iter-1 retro artefacts staged** — P252 (`reconcile-readme false-positive parses Inbound Upstream Reports Matched local ticket column as Verification Queue`) captured at WSJF 4.5 / Severity 9 MedH / M effort — 3 friction events documented this session (P082 commit blocked, P251 commit blocked, P251+P082 bundling workaround applied). Root cause: verification-queue parser regex matches `| P<NNN> |` patterns without section-anchor scoping; mis-attributes Inbound Upstream Reports `Matched local ticket` cross-refs as Verification Queue rows. Fix Strategy = improve `packages/itil/scripts/reconcile-readme.sh` + sibling `classify-readme-drift.sh` parser to anchor on section headers. Iter-1 retro artefacts staged (`docs/retros/2026-05-17-session-5-iter-1-p234-verifying.md` + `-ask-hygiene.md`) per P086 — were left as untracked working-tree state by iter 1 because P165 gate denied iter's retro commit (the same global-staged-set-vs-pathspec scope issue iter-1 surfaced as silent-framework observation). Ask Hygiene Pass: 3 AskUserQuestion calls this session, all direction-setting per ADR-044 (drain-ordering, RFC-ceremony-depth, delegation-strategy choices); lazy=0; TREND lazy_first=1 lazy_last=0 delta=-1 across 9 retros. Context Usage Cheap Layer: decisions=1.4MB / skills=891KB / problems=388KB / hooks=371KB / memory=227KB / briefing=127KB / jtbd=41KB / project-claude-md=4KB. 14 briefing topic files OVER 5KB threshold (0 MUST_SPLIT); ratios still ≤2x ceiling — Branch B `leave-as-is` applies pending convergence. Prior line-3 fragment (P082 cognitive-accessibility + P251 capture) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-18 (P162 Phase 3 baseline graduation evaluation landed)

> Last reviewed: 2026-05-17 **P252 reconcile-readme parser fix + P253 capture (no house-cleaning cadence)** — P252 fixed at commit 52a50e9: `packages/itil/scripts/reconcile-readme.sh` now scopes the Verification Queue slice to terminate at `## Inbound Upstream Reports` (ADR-062 / RFC-004) when present, eliminating the 31 false-positive `STALE verification-queue` entries that blocked Step 0 preflight in capture-problem / manage-problem / work-problems / review-problems. 3 new behavioural bats fixtures added; 30/30 bats green; `packages/itil/scripts/reconcile-readme.sh docs/problems` exits 0 against the repo README (cached `wr-itil-reconcile-readme` shim still resolves to `@windyroad/itil@0.32.1` pending release). Architect + JTBD reviews PASS, no new ADR required. P253 captured as new ticket: "No process or cadence for house cleaning of cruft and deprecations" — features or behaviours get deprecated but never removed (Open, M, Priority/Effort deferred to next `/wr-itil:review-problems`; type=technical derived from description-signal reading per user feedback memory `feedback_derive_classification_dont_ask.md`, since the SKILL classifier's regex set lacks 'cruft'/'deprecated'/'removed' but the description is semantically unambiguous codebase hygiene). Prior line-3 fragment (run-retro session-5 wrap) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-18 (P250 Phase 1 landed — Step 6.5 drain-on-releasable-material amendment rotated for P250 Phase 1)

> Last reviewed: 2026-05-18 **P162 Phase 3 baseline graduation evaluation landed** — `/wr-itil:work-problems` AFK orchestrator iter ran `packages/risk-scorer/scripts/evaluate-graduation.sh` against the 3 currently-held changesets (P170 Phase 3+4 cohort + P166+P163 entry). Verdict: 0/3 graduate at baseline (P170 cohort: Class 3b priority=8 resolved-on-status BUT Rule 4 evidence floor "Phase 3 + Phase 4 end-of-chain user verification" unfired; P166+P163: Class 3a priority=6 vp-blocked per Rule 2 carve-out + negative evidence per P198). The exercise validates Class 3a + Class 3b join surface against real held entries — ADR-061 Confirmation criterion 3 deliverable. P162 transitions Open → Verification Pending per architect-condition + ADR-022 fold-fix pattern (fix released across Phase 1 ADR-061 + Phase 2a/2b risk-scorer minors + Phase 4 docs amendments; only end-of-chain user verification of baseline output remains). No `reinstate-from-holding` emitted ⇒ `docs/changesets-holding/README.md` Rule 6 mutation does NOT trigger. Prior line-3 fragment (P252 reconcile-readme parser fix + P253 capture) rotated to `docs/problems/README-history.md` per P134.


## 2026-05-18 (P247 Phase 1 landed — Branch B evidence-based rotation rotated for P247 Phase 1)

> Last reviewed: 2026-05-18 **P250 Phase 1 landed — Step 6.5 drain-on-releasable-material amendment** — `/wr-itil:work-problems` AFK orchestrator iter 2 amended `packages/itil/skills/work-problems/SKILL.md` Step 6.5 to pivot the drain decision on *presence of releasable material* (any unpushed commits OR `.changeset/` entries OR ADR-061 Rule 1 graduation-eligible held entries), removing the defective "Within appetite (≤ 3/25) — no drain needed" clause that encoded accumulation-permitted-below-threshold semantics. ADR-018 Amendment 2026-05-18 landed in the same commit per ADR-014 single-unit-of-work; bats coverage added at `packages/itil/skills/work-problems/test/work-problems-step-6-5-always-drain.bats` (24 contract-assertion tests covering the new three-band shape, regression guards against re-introducing the defective wording, and ADR-018 amendment presence). User-direction citation: "If it's low risk, you should release." P250 transitions Open → Known Error per ADR-022; transitions Known Error → Verifying on release. Architect verdict: three-band preferred over binary (preserves no-op fast-path); JTBD verdict: aligned with JTBD-006 Outcome 2 (never accumulate). Siblings P246/P247/P234/P145/P148 remain Open for their own SKILL surfaces. Prior line-3 fragment (P162 Phase 3 baseline graduation) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-18 (P249 Phase 1 landed — /wr-itil:check-upstream-responses skill ships outbound symmetric counterpart to ADR-062 inbound discovery)

> Last reviewed: 2026-05-18 **P247 Phase 1 landed — Branch B evidence-based rotation (sibling-class to P246)** — `/wr-itil:work-problems` AFK orchestrator iter 5 amended `packages/retrospective/skills/run-retro/SKILL.md` Step 3 Tier 3 Branch B to eliminate the "leave-as-is" fall-through; new contract makes split-by-date the safe-default fall-through when none of the three concrete triggers (subtopic boundary / date stratification / ≥3 noise entries) fire, mirroring Branch A's existing precedent. ADR-013 Rule 5 (policy-authorised silent proceed) + ADR-044 framework-mediated surface citations inlined in Branch B prose. User direction cited verbatim in SKILL prose: *"The 14 files are over the limit, but you are deferring splitting them. Why? When are you hoping they will get dealt with?"* (2026-05-17 session-4 wrap retro). New bats fixture `packages/retrospective/skills/run-retro/test/run-retro-step-3-tier-3-branch-b-evidence-based.bats` — 11 assertions covering behavioural input-signal layer + narrow SKILL-prose backstops per P081; existing `check-briefing-budgets.bats` 20/20 still pass. P247 Phase 2 (rotate the 14 currently-OVER files under the new contract) deferred to a separate iter per ADR-014 commit grain (architect verdict 2026-05-18). Architect verdict: PASS (mirrors P246 evidence-based pattern; no new ADR required). JTBD verdict: PASS (JTBD-001 friction-removal, JTBD-006 AFK-safe-default, JTBD-101 reusable pattern). P247 transitions Open → Known Error at this commit. Sibling-class meta-fix sequence now: P250 (work-problems Step 6.5 drain) + P246 (Step 6.5 cohort-graduation pre-check) + P247 (run-retro Branch B); P234 / P145 / P148 remain at their own surfaces. Prior line-3 fragment (P250 Phase 1 drain-on-releasable-material) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-18 (P087 Phase 3b landed — renderer + drift detector ship via AFK iter 8)

> Last reviewed: 2026-05-18 **P249 Phase 1 landed — /wr-itil:check-upstream-responses skill ships outbound symmetric counterpart to ADR-062 inbound discovery** — `/wr-itil:work-problems` AFK orchestrator iter 6 shipped the outbound response-check surface that closes the symmetric gap to ADR-062's inbound discovery pipeline. (See README-history.md prior 2026-05-18 P249 entry for the full fragment; rotated here as Prior line-3 displacement per P134.)

## 2026-05-18 (P087 Phase 3d landed — JTBD outcome amendments rotated for P087 Phase 3d ship)

> Last reviewed: 2026-05-18 **P087 Phase 3b landed — README badge renderer + advisory drift detector ship via `/wr-itil:work-problems` AFK iter 8 session 6** — `packages/itil/scripts/plugin-maturity-render.sh` + `packages/itil/bin/wr-itil-plugin-maturity-render` ADR-049 shim (17 behavioural bats green: badge insertion, bootstrapping compound rendering, post-bootstrap band-only rendering, per-skill column add, idempotency, badge-replacement, anti-pattern absence, fail-safe missing-maturity / missing-README, ADR-044 no-AskUserQuestion, ADR-035 no-network primitive, multi-plugin independence, dry-run preview) + sibling drift detector `packages/retrospective/scripts/check-plugin-maturity-drift.sh` + `packages/retrospective/bin/wr-retrospective-check-plugin-maturity-drift` ADR-049 shim (14 behavioural bats green covering 5 drift classes: missing-badge / stale-band / orphan-badge / anti-pattern-section / anti-pattern-url). Renderer is idempotent — existing `*Maturity: ...*` spans are REPLACED not duplicated; second run produces byte-identical README. Compound rendering stays at rollup per ADR-053 §Bootstrapping clause; per-skill column cells carry band name only. Anti-patterns enforced (no standalone `## Maturity` section; no shields.io URL). ADR-044 silent-framework + ADR-035 privacy posture honoured. Architect verdict 2026-05-18 PASS: HTML-comment delimiter early adoption REJECTED per ADR-063 §Reassessment Triggers staged plan (prose-anchor first, escalate only on observed brittleness); single-iter cohesive 3b ship over 3b-i + 3b-ii split. JTBD verdict 2026-05-18 PASS: JTBD-302/JTBD-007/JTBD-101/JTBD-003/JTBD-201 served via existing anchors; outcome-text amendments correctly deferred to Phase 3d (P240) to avoid premature JTBD churn. Smoke test against live monorepo: 12 plugins / 0 drift instances baseline (renderer skips 11 plugins lacking `maturity:` field per Phase 3a not-yet-rolled-out state; drift detector reports zero drift because rendered + canonical are both absent — coherent). Two changesets: `@windyroad/itil` minor + `@windyroad/retrospective` minor — both land in same iter commit per ADR-014 single-unit-of-work. P237 (Phase 3a) folded Open → Verification Pending per ADR-022 P143 amendment since Phase 3a script shipped 2026-05-17 in commit `b840a7a` and ticket has root cause + fix strategy + workaround documented; P238 (Phase 3b) transitions Open → Verification Pending this iter. P087 remains Known Error — Phase 3c (P239 bats doc-lint) + Phase 3d (P240 JTBD amendments) + F9 (P244 in-suite display shim) + retroactive mechanical rollout still pending. Prior line-3 fragment (P249 Phase 1 check-upstream-responses) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-18 (P087 Phase 3d JTBD-outcome-amendments fragment rotated for P264 capture)

> Last reviewed: 2026-05-18 **P087 Phase 3d landed — JTBD outcome amendments ship via `/wr-itil:work-problems` AFK iter 9 session 6** — Four JTBD outcome-bullet amendments applied verbatim per ADR-063 §JTBD outcome amendments lines 286-293: JTBD-302 (`docs/jtbd/plugin-user/JTBD-302-trust-readme-describes-installed-behaviour.proposed.md`) maturity-band visibility outcome with bootstrapping-window compound-evidence clause + ADR-053/ADR-058/ADR-063/P087 drivers; JTBD-007 (`docs/jtbd/solo-developer/JTBD-007-keep-plugins-current.proposed.md`) maturity-band-currency third currency dimension extension (P159 currency bullet now spans code currency + README-content currency + maturity-band currency); JTBD-101 (`docs/jtbd/plugin-developer/JTBD-101-extend-suite.proposed.md`) promotion-criteria-visibility outcome closing ADR-053 §Confirmation #4 deferred bullet (names the four evidence-record axes `invocations_30d` / `days_shipped` / `closed_tickets_window` / `breaking_change_age_days`); JTBD-003 (`docs/jtbd/solo-developer/JTBD-003-compose-guardrails.proposed.md`) at-glance stability outcome verbatim from ADR-063 line 293 (first maturity-aware outcome on JTBD-003). Architect verdict GREEN — no new ADR required; P132 inverse-P078 trap analysis passes since exact wording is pinned in ADR-063; ADR-051 commit-hook drift detector targets `packages/<plugin>/README.md` only — JTBD job files are not in detector scope so amendments do not introduce drift. JTBD verdict PASS — persona fit confirmed across plugin-user (JTBD-302), solo-developer (JTBD-007 + JTBD-003), plugin-developer (JTBD-101); no scope-creep; amendment pattern `(Amended in P087 Phase 3)` matches existing precedent in JTBD-007 + JTBD-302 + JTBD-008 + JTBD-301. All four amendments additive — no existing ADR / P-ticket / JTBD citations removed. P240 transitions Open → Verification Pending per ADR-022 P143 fold-fix amendment (the amendment shipped inline; no separate release required since `docs/jtbd/` is documentation, not a published `@windyroad/*` plugin surface — ADR-014 commit-grain doc-only carve-out applies; no changeset triggered). P087 remains Known Error — Phase 3c (P239 bats doc-lint) + F9 (P244 in-suite display shim) + retroactive mechanical rollout still pending. Prior line-3 fragment (P087 Phase 3b landed) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-18 (P264 reconcile-readme capture fragment rotated for P265 P165-hook-trailer-gap capture)

> Last reviewed: 2026-05-18 **P264 captured — reconcile-readme script misattributes Inbound Upstream Reports section IDs as Verification Queue rows** — Captured via `/wr-itil:capture-problem` during `/wr-itil:work-problems` session 7 Step 0 preflight, where the very bug being captured triggered 31 false-positive STALE drift entries (P198-P228). The reconcile script's section-boundary detection (`packages/itil/scripts/reconcile-readme.sh` lines 105-117) only knows four section headers (`## WSJF Rankings`, `## Verification Queue`, `## Closed`, `## Parked`); the `## Inbound Upstream Reports` section added per ADR-062 § Step 9e renderer leaks its Matched-local-ticket `| P### |` column values into VQ extraction. Fix: extend section-boundary detection to recognise the fifth section, terminating `VQ_END` at `INBOUND_START`. Composes with P118 (reconcile-as-preflight-robustness), P199 (capture-problem same-session reconcile halt), P094 (refresh-on-create), P062 (refresh-on-transition), ADR-062 (Inbound renderer). Risk: commit=1 push=1 release=0 — single new `docs/problems/open/` file + README refresh. README refresh applied inline per P165 hook discipline; capture-problem SKILL.md Step 6 deferred-refresh clause was superseded by P094 + P165 evolved policy. Prior line-3 fragment (P087 Phase 3d landed) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-18 (P265 P165-hook-trailer-gap capture fragment rotated for P250 K → V transition)

> Last reviewed: 2026-05-18 **P265 captured — P165 hook does not recognise `RISK_BYPASS: adr-031-migration` trailer, blocks Step 0a auto-migrate** — Captured inline during `/wr-itil:work-problems` session 7 Step 0a friction. `packages/itil/hooks/itil-readme-refresh-discipline.sh` gates `git commit` on staged-ticket-without-README-refresh; only documented bypass is `BYPASS_README_REFRESH_GATE=1` env var. Migration commit emitted by `migrate_problems_to_per_state_layout` carries `RISK_BYPASS: adr-031-migration` trailer per ADR-031 § Backward Compatibility but the hook's `detect_readme_refresh_required` helper reads only `git diff --staged --name-only` — no trailer awareness. Fix: extend the helper to read `tool_input.command` (already extracted) and bypass on registered RISK_BYPASS trailers via allow-list. Sweep sibling PreToolUse:Bash gates (P125, P141) for the same gap. Sibling capture P264 rotated to history. Prior line-3 fragment (P264 captured — reconcile-readme script misattributes Inbound Upstream Reports section IDs) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-18 (P250 K → V fragment rotated for P246 K → V transition)

> Last reviewed: 2026-05-18 **P250 Known Error → Verification Pending — Step 6.5 drain-on-releasable-material fix shipped in `@windyroad/itil@0.32.3`** — Fix landed 2026-05-18 commit `e9fb7f0`, consumed in version-packages commit `4a0e1b7` 2026-05-17 21:29 UTC, merged via PR #141 (`4df08ec`); current cache `@windyroad/itil@0.35.2` across 4 subsequent release cycles (0.33.0/0.34.0/0.35.0/0.35.1/0.35.2) with zero regression. Three-band classification pivoting on releasable material (Above appetite → ADR-042 auto-apply; Within appetite + releasable material → drain; Within appetite + empty queue → no drain); ADR-018 amended in same commit per ADR-014 single-unit-of-work; 24 contract-assertion bats green. In-session verification evidence: session 6 iter 3 (2026-05-17 07:56:29 AEST) drained via `push:watch` only at 1/1/1 within-appetite score with unpushed commits — pre-fix wording would have skipped drain. Transition Known Error → Verification Pending per ADR-022 (changeset removal in `4a0e1b7` IS the canonical fix-shipped signal per P143 fold-fix amendment). Verification window remains in-flight — 5 AFK iterations across ≥2 sessions of low-risk iters; recovery path: `/wr-itil:transition-problem 250 known-error` after reverting commit `e9fb7f0`. Prior line-3 fragment (P265 captured — P165 hook does not recognise `RISK_BYPASS: adr-031-migration` trailer) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-18 (P246 K → V fragment rotated for P247 K → V transition)

> Last reviewed: 2026-05-18 **P246 Known Error → Verification Pending — Step 6.5 cohort-graduation pre-check fix shipped in `@windyroad/itil@0.33.0`** — Fix landed 2026-05-18 commit `229539c`, consumed in version-packages commit `a032ca9` 2026-05-17 22:29 UTC, merged via PR #142 (`e243fc3`); current cache `@windyroad/itil@0.35.2` across 4 subsequent release cycles (0.34.0/0.35.0/0.35.1/0.35.2) with zero regression. Pre-check invokes `wr-risk-scorer-evaluate-graduation` before the Drain action when within-appetite-with-releasable-material branch fires AND `docs/changesets-holding/` is non-empty; 3-status taxonomy (`resolved` → auto-graduate per Rule 5 + Rule 3b cohort propagation; `vp-blocked` → skip per Rule 2; `halt-no-resolution` → framework-prescribed halt per Rule 1a terminal); `docs/changesets-holding/README.md` Process step 5 amended evidence-based-not-time-based per user direction; 39 contract-assertion bats green. In-session verification evidence: cohort-graduation pre-check fired this session and graduated `wr-itil-p170-phase4-p4-1-related-problems-lookup-row.md` + `wr-itil-p170-phase3-p3-1-phase4-p4-2-step-1-5b-jtbd-trace.md` atomic-cohort end-to-end per `status=resolved` emission (Rule 1a deterministic join + Rule 3b cohort propagation + Rule 5 silent proceed + Rule 6 audit-trail append all exercised); the `p166-p163-external-comms-hook-side-sha256.md` held entry was correctly NOT graduated per negative evidence floor (P198 sibling friction recurrence). Transition Known Error → Verification Pending per ADR-022 (changeset removal in `a032ca9` IS the canonical fix-shipped signal per P143 fold-fix amendment). Verification window remains in-flight — 5 AFK iterations across ≥2 sessions of low-risk iters; recovery path: `/wr-itil:transition-problem 246 known-error` after reverting commit `229539c`. Prior line-3 fragment (P250 Known Error → Verification Pending — Step 6.5 drain-on-releasable-material) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-18 (P247 K → V fragment rotated for P249 K → V transition)

> Last reviewed: 2026-05-18 **P247 Known Error → Verification Pending — Step 3 Tier 3 Branch B evidence-based rotation fix shipped in `@windyroad/retrospective@0.19.0`** — Fix landed 2026-05-18 commit `b22e006`, consumed in version-packages commit `1ef3157` 2026-05-17 23:01:05 UTC, merged via PR #143 (`10aecdf`); current cache `@windyroad/retrospective@0.20.2` across 3 subsequent release cycles (0.20.0/0.20.1/0.20.2) with zero regression on the Step 3 Tier 3 Branch B evidence-based rotation surface. Branch B always rotates — "wait for more signal to accumulate" named in-prose as the fictional-defer anti-pattern P247 closes; concrete triggers (subtopic / date / ≥3 noise entries) remain with split-by-date safe-default fall-through mirroring Branch A precedent; trim-noise branch tightened (if still OVER after trim → fall through to split-by-date in same retro turn, do NOT defer); ADR-013 Rule 5 + ADR-044 framework-mediated surface citations inlined; user direction cited verbatim *"The 14 files are over the limit, but you are deferring splitting them. Why? When are you hoping they will get dealt with?"*. Behavioural+structural bats fixture 11 assertions green per ADR-037 (5 behavioural input-signal fixtures against `check-briefing-budgets.sh` exercising Branch A / Branch B selector ratios 1.0x/1.5x/1.96x/under-threshold/2.0x + 6 narrow SKILL-prose backstops per P081 linking prose contract to driver ticket + sibling-class precedent + governance authorities). Transition Known Error → Verification Pending per ADR-022 (changeset removal in `1ef3157` IS the canonical fix-shipped signal per P143 fold-fix amendment). Phase 2 (rotate the 14 currently-OVER topic files under the new contract) remains deferred with this ticket as scheduled-future-surface per P179 carve-out — Phase 2 deferral does NOT block K → V because Phase 1 is a self-contained shipped slice, identical scope-bounding shape to P246's K → V transition iter-2 this session. Sibling-class linkage with P246 (same fictional-defer class, different SKILL surface, same evidence-based principle ADR-061 Rule 1) — third K → V transition this session after P250 iter-1 and P246 iter-2, demonstrating the K → V fold-fix shape (release-vehicle citation, Phase deferral via P179 SFS, single-commit grain per ADR-014, README Verification Queue insert per P186 evidence-cell shape) across distinct SKILL surfaces. Verification window remains in-flight — 5 AFK iterations across ≥2 sessions of low-risk iters; recovery path: `/wr-itil:transition-problem 247 known-error` after reverting commit `b22e006`. Prior line-3 fragment (P246 Known Error → Verification Pending — Step 6.5 cohort-graduation pre-check) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-18 (P249 K → V fragment rotated for P239 Open → Verifying transition)

> Last reviewed: 2026-05-18 **P249 Known Error → Verification Pending — /wr-itil:check-upstream-responses skill ships outbound symmetric counterpart to ADR-062 inbound discovery in `@windyroad/itil@0.34.0`** — Fix landed 2026-05-18 commit `49dd0ba` "feat(itil): P249 Phase 1 — /wr-itil:check-upstream-responses skill ships outbound symmetric counterpart to ADR-062 inbound discovery", README skill-inventory follow-up `e4e100b`, consumed in version-packages commit `11caa25` 2026-05-17 23:41:35 UTC, merged via PR #144 (`5388ba3` 2026-05-18 09:46:05 AEST); current cache `@windyroad/itil@0.35.2` across 3 subsequent release cycles (0.35.0/0.35.1/0.35.2) with zero regression on the `/wr-itil:check-upstream-responses` skill surface (cache verification confirms SKILL.md present in both 0.34.0 and 0.35.2). Phase 1 ships `/wr-itil:check-upstream-responses` skill: script `packages/itil/scripts/check-upstream-responses.sh` + bin shim `wr-itil-check-upstream-responses` + `packages/itil/skills/check-upstream-responses/SKILL.md` + 13 behavioural bats tests (mocks `gh` via `--gh-bin` flag for testability); polls `## Reported Upstream` back-link sections in local problem tickets, queries `gh issue view` read-only, diffs against `docs/problems/.outbound-responses-cache.json`, appends audit-log entry to `docs/audits/outbound-responses-log.md` (outbound symmetric counterpart to ADR-062's inbound discovery pipeline). ADR amendments in same commit per ADR-014 single-unit-of-work: ADR-014 commit-message-convention table amended (new row for `chore(problems): check upstream responses ...`); ADR-024 Confirmation amended (back-link section URL field now load-bearing for two skills — report-upstream writes it, check-upstream-responses reads it); ADR-062 `## Related` amended (forward-pointer to P249 Phase 1 sibling — completes the symmetric inbound/outbound feedback loop at the assessment-pipeline level). Transition Known Error → Verification Pending per ADR-022 (changeset removal in `11caa25` IS the canonical fix-shipped signal per P143 fold-fix amendment). Phase 2 (external-reporter-as-our-reporter — composes with `.github/ISSUE_TEMPLATE/problem-report.yml` intake; structured feedback channel for plugin users) remains Open as a separate concern per P179 deferred-with-scheduled-future-surface pattern — Phase 2 is a distinct user surface (intake-template structured feedback) orthogonal to Phase 1's polling shape, NOT a residual phase of the same fix. Fourth K → V transition this session (after P250 iter-1, P246 iter-2, P247 iter-3) — distinct surface (skill-ship vs SKILL.md contract amendment / Step 6.5 mechanic refinement / clause-removal) but identical transition shape demonstrating the K → V fold-fix pattern's portability across skill-creation, SKILL.md amendment, Step 6.5 refinement, and clause-removal surfaces (release-vehicle citation, Phase deferral via P179 SFS, single-commit grain per ADR-014, README Verification Queue insert per P186 evidence-cell shape). Verification window remains in-flight — 5 AFK iterations across ≥2 sessions of low-risk iters; recovery path: `/wr-itil:transition-problem 249 known-error` after reverting commit `49dd0ba`. Prior line-3 fragment (P247 Known Error → Verification Pending — Step 3 Tier 3 Branch B evidence-based rotation) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-18 (P239 Open → V fragment rotated for P266 capture)

> Last reviewed: 2026-05-18 **P239 Open → Verification Pending — Phase 3c bats doc-lint per plugin ships in @windyroad/itil patch** — Fix landed 2026-05-18 session 7 iter 5 via `/wr-itil:work-problems` AFK orchestrator subprocess; ships `packages/itil/scripts/test/plugin-maturity-doc-lint.bats` (11 behavioural tests covering ADR-063 §Phase 3c contract A1-A5). Single fixture with dynamic plugin discovery — NO per-plugin duplication. Architect verdict YELLOW-with-4-adjustments all folded in (A1 negative-presence regression fence; A2 anchored regex tightening; A3 header-comment scope-limit; A4 closed-enum schema_version per ADR-058 #8). JTBD PASS. Bats-only approach approved. Smoke test 11/11 green across all 11 plugins. Fold-fix Open → Verification Pending per ADR-022 P143 amendment. Fifth lifecycle transition this session (first Open → V demonstrating fold-fix portability across both lifecycle entry-points). (See P239 ticket file + iter-5 retro for full content; rotated here as Prior line-3 displacement per P134.)

## 2026-05-18 (P266 capture fragment rotated for P252 → Verifying + P264 close-as-duplicate transitions)

> Last reviewed: 2026-05-18 **P266 captured — Agent follows SKILL.md ≤3-no-drain clause despite P250 amended framing when releasable material exists** — Captured inline during `/wr-itil:work-problems` session 7 loop-end Step 2.5 surfacing after user direction *"If it's safe (within risk tolerance) then the answer is always release. FFS"* (P078 strong-signal correction; class-of-behaviour `agent-over-defends-SKILL.md-against-user-direction`). Sibling to P250 at same Step 6.5 surface — P250 captures the DEFECT (≤3-no-drain encodes accumulation); P266 captures the META-DEFECT (agent applies the defective clause verbatim despite P250's fix being in Verifying with the corrected framing). Fix: amend `packages/itil/skills/work-problems/SKILL.md` Step 6.5 Classification clause + ADR-018 cadence framing; replace three-band (≤3/=4/≥5) with two-band (within-appetite ≤4 + releasable material → drain; ≥5 → ADR-042). User one-time-override applied to current session — drain immediately. Composes with P132 (inverse-P078 trap class), P078 (capture-on-correction OFFER), ADR-013 Rule 5, ADR-018, ADR-044. Prior line-3 fragment (P239 Open → Verification Pending — Phase 3c bats doc-lint) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-18 (P252+P264 transition fragment rotated for P267+P268+P269 batched session-7 capture)

> Last reviewed: 2026-05-18 **P252 Open → Verification Pending + P264 closed as duplicate of P252** — Per user direction at session 7 loop-end Step 2.5 surfacing: *"P252 → Verifying, close P264 as duplicate"*. P252 fix shipped 2026-05-17 commit `52a50e9` "fix(itil): scope reconcile-readme VQ slice to terminate at Inbound Upstream Reports (closes P252)" / version-packages commit `55dde23` 2026-05-18 / PR #148 merge commit `d450683` 2026-05-18; released as `@windyroad/itil@0.35.3` during session 7 loop-end Step 6.5 drain post-FFS user-correction. Fold-fix Open → Verification Pending per ADR-022 P143 amendment (changeset removal in `55dde23` IS the canonical fix-shipped signal). Sibling P264 captured 2026-05-18 was a duplicate (capture-problem 3-keyword title-only grep top-10 truncation missed P252); closed referencing P252 as authoritative. Sixth lifecycle transition this session (after P250 / P246 / P247 / P249 K → V + P239 Open → V); first session 7 instance of Open → V via duplicate-fix-shipped-elsewhere shape. Prior line-3 fragment (P266 captured — Agent follows SKILL.md ≤3-no-drain clause despite P250 amended framing) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-18 (P267+P268+P269 capture fragment rotated for P266 Open → Verifying fold-fix)

> Last reviewed: 2026-05-18 **P267 + P268 + P269 captured — batched session-7 follow-on tickets from Step 2.5 AskUserQuestion surfacing** — P267 codify `derive-release-vehicle.sh` helper (K→V wrong-cite class; iter-1 retro Observation 1; user picked capture+defer); P268 P165 hook substring-matches `git commit` anywhere in command body (not just actual invocations; observed iter-1 retro write + my grep — user picked capture+defer); P269 amend `plugin-maturity-populate.sh` to write `rollup_invocations_30d` on plugin root rollup for compound rendering (P087 Phase 3 gap; user picked populate-side write over renderer-side derive). All three deferred — re-rate at next `/wr-itil:review-problems`. Session 7 totals: 5 iters worked (P246/P247/P249/P250 K→V + P239 Open→V); 4 captures committed (P264 closed as duplicate of P252 / P265 / P266 / P267+P268+P269 batched); 1 release shipped (@windyroad/itil@0.35.3). Prior line-3 fragment (P252 Open → Verification Pending + P264 closed as duplicate) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-18 (P266 Open → Verifying fragment rotated for P270 capture)

> Last reviewed: 2026-05-18 **P266 Open → Verifying (fold-fix of P250)** — meta-defect (agent applies SKILL.md ≤3-no-drain clause verbatim) resolved by the same `@windyroad/itil@0.32.3` artefact that shipped P250's amended Step 6.5 three-band classification (source commit `e9fb7f0` / version-packages commit `4a0e1b7` / PR #141 merge `4df08ec`); orchestrator iter selected P266 with WSJF=9.0; investigation confirmed (a) SKILL.md `packages/itil/skills/work-problems/SKILL.md:548-551` contains amended classification with no ≤3-no-drain clause, (b) ADR-018 lines 116-143 contain the "Amendment 2026-05-18 — Drain trigger is releasable material" section, (c) bats fixture `work-problems-step-6-5-always-drain.bats` 24/24 green including explicit ≤3-no-drain regression guard, (d) sibling-pattern audit across `packages/**/SKILL.md` returned zero residual "below-appetite no-action" clauses encoding accumulation; fold-fix transition per ADR-022 P143 amendment (parent ticket fix-released artefact simultaneously resolves child surface). Prior line-3 fragment (P267 + P268 + P269 captured) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-18 (P270 capture fragment rotated for P253 rename to per-state subdir layout)

> Last reviewed: 2026-05-18 **P270 captured — Agent waits for human to initiate upstream report instead of filing on detect (feedback delay class)** — User-initiated mid-loop capture via `/wr-itil:capture-problem` per CLAUDE.md MANDATORY capture-on-correction rule. User-direction class-of-behaviour signal: "When the agent detects that the issue is upstream, it should report it ASAP". Captures the next class beyond P063 (closed) — even with the `Upstream report pending` marker, reports do not get filed proactively because ADR-024 Consequences security-path interactive carve-out was over-generalised to ALL upstream reports. Fix candidate: AFK orchestrator Step 4 + manage-problem Step 6 gain a non-security auto-fire branch preserving the security interactive gate verbatim. 17 title-grep matches — siblings P063, P070, P079 (user highlighted), P080, P220, P249, P254. Risk 1/1/1 Very Low (docs-only). Prior line-3 fragment (P266 Open → Verifying fold-fix of P250) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-18 (P253 rename fragment rotated for P269 Open → Verifying transition)

> Last reviewed: 2026-05-18 **P253 rename to per-state subdir layout (RFC-002 / ADR-031)** — Pure file rename `docs/problems/253-...open.md → docs/problems/open/253-...md` aligns P253 with the per-state subdirectory layout shipped by ADR-031. The rename was staged at session 8 open (originating in a prior session's auto-migrate routine) and carried as unstaged dirt across iter 1 + iter 2 (the iters restored-staged to preserve their own ADR-014 single-unit-of-work grain). User-requested commit at orchestrator main turn post-iter-2; rides as a discrete commit per ADR-014. README WSJF row content unchanged (P253 ID + title + WSJF + status all stable across the rename — only file path changed). P165 hook flagged the state-subdir rename as ranking-bearing per the over-broad classifier (sibling defect P265 captured); pragmatic resolution: line-3 P134 rotation lands the audit signal in the same commit. Prior line-3 fragment (P270 captured) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-18 (P269 Open → Verifying fragment rotated for P271 capture)

> Last reviewed: 2026-05-18 **P269 Open → Verifying** — Amend `plugin-maturity-populate.sh` to write `rollup_invocations_30d` + `bootstrapping` on plugin root rollup for compound rendering (P087 Phase 3 gap). Iter 2 shipped 4 commits — `7ca47ef` source + ADR-063 amendment + 9 new bats, `f3c0d26` retroactive rollout (11/11 plugins carry new fields; 7/11 READMEs activate compound rendering), `3040b69` fold-fix doc-lint regex accepting space-before-paren in compound form, `d945727` retro. Released as `@windyroad/itil@0.35.4` (version-packages `28ac9e7` / PR #150 merge `6127ed8`); fold-fix Open → Verifying per ADR-022 — bats 22/22 + 19/19 + 13/13 green; ADR-058 §Confirmation #8 additive-within-2.0 cited; architect PASS + JTBD PASS with 5 adjustments folded; lazy-AskUserQuestion=0. File rename Open → Verifying deferred to orchestrator main turn per ADR-014 grain boundary; orchestrator owns the transition commit. Prior line-3 fragment (P253 rename to per-state subdir layout) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-18 (P271 capture fragment rotated for P268 Open → Verifying fold-fix)

> Last reviewed: 2026-05-18 **P271 captured — review-problems not auto-fired when needed (user has to remember)** — User-initiated mid-loop capture via `/wr-itil:capture-problem` (orchestrator main turn while iter 3 P268 running in background). User direction: "I'm finding that I have to remember to run review-problems. I'm expecting it to run automatically when needed." 8 capture-problem tickets across sessions 7+8 (P252/P264/P266/P267/P268/P269/P270/P271) all carry deferred-placeholder text; 0 user-invoked review-problems firings; signal is invisible. Trigger candidates: N captures since last review (≥3), README `Last reviewed:` >7d, AFK preflight stale rankings, pre-iter dispatch with placeholder WSJF, pre-release-cadence drain with placeholder Priority batch. Sibling P187 (orchestrator preflight unblock) is one specific surface of the same class; P110 + P246 + P247 + P190 are sibling defer/auto-fire/framework-resolution patterns. Risk 1/1/1 Very Low (docs-only). Prior line-3 fragment (P269 Open → Verifying fold-fix) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-18 (P268 Open → Verifying fragment rotated for P272+P273+P274+P275 batched sibling-hook captures)

> Last reviewed: 2026-05-18 **P268 Open → Verifying** — P165 hook substring-matches `git commit` anywhere in command body, not just actual `git commit` invocations. Fix shape B (per ticket recommendation): new shared helper `packages/itil/hooks/lib/command-detect.sh` exposes `command_invokes_git_commit` — iterative prefix-strip (leading whitespace, env-var assignments quoted+unquoted, `cd <path> &&`) + leading-token check `^git[[:space:]]+commit([[:space:]]|$)`. Consumed by `packages/itil/hooks/itil-readme-refresh-discipline.sh` (replaces case-statement substring match `*"git commit"*` at lines 80-83). 28 helper bats fixtures + 10 P268-prefixed integration regression fixtures (cases pre-fix would have falsely fired: `grep -n 'git commit' file.md`, cat-heredoc with phrase in body, `echo 'the git commit gate ...'`, `sed -i 's/git commit/.../' file`, `git log --grep 'git commit'`; plus boundary check `git commit-tree`). Sibling-hook sweep complete: 4 hooks share the same anti-pattern (`retrospective-readme-jtbd-currency.sh:126`, `itil-rfc-trailer-advisory.sh:94`, `itil-changeset-discipline.sh:78`, `p057-staging-trap-detect.sh:65`) — captured as separate tickets per ADR-014 one-concern-per-ticket; helper is in the right shape for each refactor. Fold-fix Open → Verifying per ADR-022 — bats 28/28 helper + 39/39 readme-refresh + 19/19 retrospective-sibling green; architect PASS (no new ADR required, ADR-005 + ADR-009 + ADR-045 precedent covers; helper stays in `packages/itil/hooks/lib/` until `packages/shared/hooks/lib/` promotion is opened for the sibling refactor commits per ADR-017); JTBD review marker green. Orchestrator Step 6.5 owns release-cycle drain. Prior line-3 fragment (P271 captured — review-problems not auto-fired when needed) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-18 (P272+P273+P274+P275 capture fragment rotated for 8-ticket re-rate pass)

> Last reviewed: 2026-05-18 **P272 + P273 + P274 + P275 captured — P268 sibling-hook sweep findings** — Four PreToolUse:Bash sibling hooks confirmed sharing the substring-match anti-pattern that P268 closed in `itil-readme-refresh-discipline.sh`. P272 `packages/itil/hooks/itil-changeset-discipline.sh:78`; P273 `packages/itil/hooks/p057-staging-trap-detect.sh:65`; P274 `packages/itil/hooks/itil-rfc-trailer-advisory.sh:94` (advisory-class, not deny); P275 `packages/retrospective/hooks/retrospective-readme-jtbd-currency.sh:126` (cross-package — consumes `command_invokes_git_commit` via ADR-017 sync or `packages/shared/hooks/lib/` promotion). All four captured as separate tickets per ADR-014 one-concern-per-ticket; each blocked-by P268 fix-released for the helper availability. Batched into one commit per the 86f42e8 precedent (one logical sweep = one capture batch grain). Architect verdict on P268 noted `packages/shared/hooks/lib/` promotion is "a follow-up decision when the sibling-hook refactors land" — P275's refactor commit is the natural opening point. Each ticket carries WSJF=3.0 (Priority 3 Medium × Effort S); deferred Priority/Effort placeholders awaiting next /wr-itil:review-problems. Prior line-3 fragment (P268 Open → Verifying fold-fix) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-18 (P272 fix-released rotated for /wr-itil:work-problems iter)

> Last reviewed: 2026-05-18 **8-ticket re-rate pass — P265/P267/P270/P271/P272/P273/P274/P275 re-grounded against RISK-POLICY.md; WSJF Rankings re-sorted per P138 multi-key** — `/wr-itil:review-problems` subprocess dispatched mid-AFK-loop per P271 user direction "I'm expecting it to run automatically when needed". Eight Open tickets carrying `(deferred — re-rate at next /wr-itil:review-problems)` placeholders re-graded against RISK-POLICY.md Impact × Likelihood: **P272 + P273 lift to WSJF 10.0** (Impact 2 × Likelihood 5 = 10 High, Effort S — P268 sibling deny-class hooks, observed firing in session 8); **P265 lifts to WSJF 8.0** (Impact 2 × Likelihood 4 = 8 Med, Effort S — Step 0a auto-migrate block, every AFK invocation with flat-layout debt); **P274 + P275 to 5.0** (Impact 1 × Likelihood 5 = 5 Med, Effort S — advisory-class P268 siblings); **P270 + P271 to 4.0** (Impact 2 × Likelihood 4 = 8 Med, Effort M — feedback-delay class + framework-auto-fire class, both this-session captures); **P267 stays at 3.0** (Impact 2 × Likelihood 3 = 6 Med, Effort M — K→V citation-helper, 160bserved incidence). Skill contract Step 2.5 transitive-effort propagation: zero upstream Open/Known-Error blockers for any of the 8 re-rated tickets; P272-P275 `blocked-by P268` (in verifying → 0-contribution per architect carve-out, P076). Top-of-table now P272 → P273 → P265 → P274 → P275 → P270 → P271 → P267 (multi-key sort by WSJF desc, Effort divisor asc, Reported date asc, ID asc, per P138). VQ already current — all 90 verifying tickets render with P186 evidence-first `Likely verified?` cells; oldest-first sort intact. Inbound Upstream Reports section unchanged (31-report cohort still `pending-pipeline-processing` per 2026-05-15 user direction). The broader ~72 older tickets still carrying deferred-placeholder text in their bodies remain out-of-scope per orchestrator brief; that residual is exactly what P271 captures (auto-fire trigger needed). Prior line-3 fragment (P272+P273+P274+P275 batched sibling-hook captures from P268 sweep) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-18 (P272 Open → Verifying fold-fix fragment rotated for P276+P277+P278 batched captures)

> Last reviewed: 2026-05-18 **P272 Open → Verifying fold-fix — `itil-changeset-discipline.sh` substring-match → `command_invokes_git_commit` leading-executable helper** — `/wr-itil:work-problems` AFK iter 4 dispatched against top-WSJF actionable ticket (P272 at WSJF 10.0 post-re-rate). Replaces the case-statement `*"git commit"*` substring match at `packages/itil/hooks/itil-changeset-discipline.sh:78` with `source lib/command-detect.sh; command_invokes_git_commit "$COMMAND" || exit 0` — same fix shape P268 just shipped to the sibling `itil-readme-refresh-discipline.sh` hook, applied verbatim to the next sibling enforcement-layer hook per ADR-014 one-concern-per-ticket. Coverage: 10 P272-prefixed behavioural bats fixtures appended to `packages/itil/hooks/test/itil-changeset-discipline.bats` mirroring the P268 regression suite — grep / grep-rn / sed / echo / git-log / cat-heredoc allow paths, `git commit-tree` boundary allow, plus three positive-regression deny cases (bare `git commit`, `cd && git commit`, `VAR=value git commit`); bats 31/31 changeset-discipline + 28/28 helper + 39/39 readme-refresh-discipline green (no cross-regression). Architect PASS (pure shape-replication; no new ADR), JTBD aligned (JTBD-006 solo-developer work-backlog-afk). VQ row inserted after P269 per same-day ID ASC tiebreak; WSJF Rankings row removed. Siblings P273 / P274 / P275 remain Open as separate tickets per the same one-concern-per-ticket discipline; P273 (also WSJF 10.0, S, `p057-staging-trap-detect.sh:65`) is the next candidate at iter 5. Prior line-3 fragment (8-ticket re-rate pass — P265/P267/P270/P271/P272/P273/P274/P275 re-grounded against RISK-POLICY.md) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-19 (P276+P277+P278 capture fragment rotated for P273+P274+P275 batched Open → Verifying)

> Last reviewed: 2026-05-19 **P276 + P277 + P278 captured — batched iter-2 deviation-approval observations from session 8 Step 2.5 surfacing** — User direction at AskUserQuestion: "Capture all 3 as new tickets". P276 external-comms gate marker over-fires on PASS-class content edits (P073 surface; iter-2 cost ~$0.30-0.50/iter on 2 unnecessary re-reviews); P277 P165 README-refresh hook doesn't distinguish iter-staged from cross-turn-staged tickets when AFK subprocess + orchestrator main turn share working tree (iter-2 hit this on concurrent P270 capture; workaround `git restore --staged`); P278 renderer-package-counts-the-readme-changes convention scope vs P141 per-package source change discipline (ADR-021 boundary clarification — iter-2 cycle 4 changeset rewrites before gates passed). All three deferred — re-rate at next /wr-itil:review-problems; default WSJF 1.0/1.0/1.5 from skeleton Priority 3 × Effort M/M/S placeholders. Batched into one commit per iter-3 ae6e049 precedent (one logical sweep = one capture batch grain). Outstanding-questions queue truncated post-surfacing per Step 2.5 cleanup contract. Prior line-3 fragment (P272 Open → Verifying fold-fix) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-19 (P273+P274+P275 batched Open → Verifying fragment rotated for P279 capture)

> Last reviewed: 2026-05-19 **P273 + P274 + P275 batched Open → Verifying fold-fix — three PreToolUse/PostToolUse Bash hooks across two packages stop substring-matching `git commit` in argument vectors** — Iter 6 dispatched batched sibling sweep against the remaining three P268 siblings. Replaces case-statement `*"git commit"*` substring match at p057-staging-trap-detect.sh (P273 deny-class), itil-rfc-trailer-advisory.sh (P274 advisory-class), and retrospective-readme-jtbd-currency.sh (P275 deny-class, cross-package). Architect verdict directed Option B: promote `command-detect.sh` canonical to `packages/shared/hooks/lib/command-detect.sh` matching existing precedent there. Infrastructure: `scripts/sync-command-detect.sh` + `npm run check:command-detect` CI step + 6/6 ADR-017 § Confirmation bats. Behavioural coverage: 25 P273/P274/P275-prefixed fixtures mirroring P268 regression suite. Released `@windyroad/itil@0.35.7` + `@windyroad/retrospective@0.20.4`. Closes the P268 sibling-hook sweep — all five siblings (P268/P272/P273/P274/P275) now consume the shared helper. Prior line-3 fragment (P276+P277+P278 batched captures) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-19 (P279 capture fragment rotated for P281+P282+P283 re-rate via focused /wr-itil:review-problems)

> Last reviewed: 2026-05-19 **P279 captured — ADR-017 § Consequences flat-layout doc vs hook-helpers nested clustering (housekeeping)** — User-directed Step 2.5 capture via AskUserQuestion ("Capture as new ticket — Recommended"). Iter 6 architect verdict surfaced two coexisting conventions in `packages/shared/`: flat layout for cross-cutting libs (e.g. `derive-first-dispatch.sh` per ADR-017 § Consequences current wording), nested-under-`hooks/lib/` for PreToolUse/PostToolUse hook helpers (e.g. `session-marker.sh`, `leak-detect.sh`, `external-comms-key.sh`, now `command-detect.sh`). Architect flagged the doc/practice drift as "useful housekeeping but not gate-required" — defer-friendly. Proposed fix: amend ADR-017 § Consequences with a one-line note acknowledging both clustering patterns. WSJF 1.0 deferred (Priority 3 Med × Effort S). Risk 1/1/1 Very Low (docs-only). Closes the iter-6 outstanding-question queue. Prior line-3 fragment (P273+P274+P275 batched Open → Verifying fold-fix) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-23 (P281+P282+P283 focused re-rate fragment rotated for P283 Open → Known Error transition)

> Last reviewed: 2026-05-23 **P281 + P282 + P283 re-rated from deferred-placeholders via focused `/wr-itil:review-problems`** — three session-captured tickets folded from WSJF 1.0 (Priority 3 Med × Likelihood 1) into proper cohorts: **P282 → WSJF 4.0** (12 High × M; agent records validation evidence in `.verifying.md` body but doesn't transition V→Closed; concrete instance voder-mcp-hub P027; sibling of P068 narrower run-retro surface + P228 K→V analogue), **P281 → WSJF 3.0** (9 Med High × M; capture-problem SKILL template references pre-ADR-031 flat-path shape; concrete adopter evidence voder-mcp-hub P032 at flat path commit 6c73880), **P283 → WSJF 3.0** (9 Med High × M; architect agent should always AskUserQuestion when recording new decision unless direction given; tool-surface limitation noted — architect tools = Read/Glob/Grep only). Bulk re-score of remaining 109 open/known-error tickets DEFERRED — focused refresh only. Step 4.5 inbound-discovery cache stale (last_checked 2026-05-17, past 24h TTL) — assessment-pipeline pass not run this turn; cache freshness preserved for separate focused inbound-discovery invocation. Step 4 verification-queue close-pass (94 verifying tickets) NOT surfaced via AskUserQuestion — the V→Closed gap is itself the new top-cohort ticket P282 (WSJF 4.0). Sort drift observed in WSJF Rankings: rows ~103-112 carry out-of-cohort entries (P258, P263 at WSJF 3.0 sit at end of table; P254-P262 1.5 cluster below 0.75 cohort) — re-opens P138 tie-break-ladder lock enforcement; not corrected this pass. Prior line-3 fragment (P279 capture) rotated to `docs/problems/README-history.md` per P134.

## 2026-05-23 (P283 Open→Known-Error fragment rotated for inbound-cache-refresh + mechanical-WSJF-re-rate pre-flight)

> Last reviewed: 2026-05-23 **P283 Open → Known Error, then re-rated M → XL (two-pronged scope) — architect must AskUserQuestion for direction when recording a new decision** — root cause (architect verdict): the verdict surface has only PASS/ISSUES-FOUND, no structured Needs-Direction type, so an undocumented-decision-with-2+-options auto-picks or prose-asks. Two-pronged fix per user direction: (1) make the recording flow ask — Shape B Needs-Direction verdict + main-agent translation + new sibling ADR (Shape A rejected = ADR-013 Option A); (2) retroactive AskUserQuestion review+confirm of the 52 existing .proposed.md ADRs. create-adr/capture-adr already compliant on prong 1. WSJF 3.0→9.0→2.25 (Known Error ×2.0; prong-2 sweep = XL; prong 1 worked first). Prior line-3 fragment (P281+P282+P283 focused re-rate) rotated to README-history.md per P134.

## 2026-05-23 (inbound-cache-refresh + WSJF-re-rate fragment rotated for P073 close)

> Last reviewed: 2026-05-23 **Inbound cache refreshed (+4 new upstream reports #137/#138/#139/#149 → 35 total) + mechanical WSJF re-rate (P138 sort-drift corrected)** — dedicated Step 0b inbound-discovery pre-flight for `/wr-itil:work-problems` (cache was ~6 days stale, TTL-expiry auto-recheck). P038 + P064 now closed, so transitive blockers collapse per P076: **P073 → 12.0** (new #1; marginal S), P082 → 6.0, P079/P080/P129 → 3.0. Known-Error ×2.0 multiplier restored on P087/P097 (→6.0) + P170 (→2.0); invalid `/3` divisors on P281/P282 corrected; placeholder M-divisor arithmetic fixed (0.75 → 1.5). Bulk placeholder Impact×Likelihood re-rate (≈76 Likelihood-1 stubs) + Open→Known-Error transitions DEFERRED to next interactive pass per established incremental cadence + auto-decision-drift caution (grounded subagent estimates queued at loop-end). Inbound pipeline classification remains discovery-only per 2026-05-15 user direction (external-comms-gate bug, P198). Prior P283 Open→Known-Error fragment rotated to README-history.md per P134.

## 2026-05-24 (P073-close fragment rotated for scoped Open → Known Error transition pass)

> Last reviewed: 2026-05-23 **P073 closed (verification close-on-evidence) — changeset external-comms gate shipped + bats green** — `/wr-itil:work-problems` AFK iter worked the new #1 (WSJF 12.0). P073's deliverable (the `.changeset/*.md` PreToolUse:Write/Edit surface row, `SURFACE="changeset-author"`) was already live in the shared canonical `external-comms-gate.sh`, synced byte-identically into both per-package copies, and released in risk-scorer 0.10.3 + voice-tone 0.5.3 — it landed inside P064's fix (commit `a0713f3`) and was part of the gate the user verified when closing P064. Authored close condition (ADR-028 amendment: surface in both copies + bats pass) met: 47/47 gate bats green incl. canonical guard "matches .changeset/*.md … surface (P073)" + behavioural deny tests in both evaluator suites. Follow-on marker-mechanics bugs P276 (over-fires on PASS-class edits) + P198 (marker key) tracked separately, do NOT reopen surface scope. Reversible via `/wr-itil:transition-problem 073 known-error`. Prior inbound-cache-refresh fragment rotated to README-history.md per P134.

## 2026-05-26 (45-transitions fragment rotated for P285+P286 README reconciliation)

> Last reviewed: 2026-05-24 **45 Open → Known Error transitions (scoped transition-only pass — no bulk re-rate)** — `/wr-itil:work-problems` loop-end direction (user, AFK): apply all eligible Step 2.10 Open→Known-Error transitions. Transitioned the 32 upstream-mirror tickets P198–P229 (confirmed root cause + documented holding-pattern workaround; fix owned upstream) plus 13 our-own diagnosed technical defects with standing workarounds / pinned fixes (P171, P181, P182, P245, P258, P259, P260, P261, P262, P265, P267, P277, P279). Each receives the mechanical Known-Error ×2.0 WSJF multiplier only — P265 → 16.0 (#1), P258 → 12.0, then the 6.0 / 3.0 KE tiers. NO Impact×Likelihood re-rate this pass: the ≈83 deferred-placeholder severities stay intact and fold incrementally in small batches per P271. Ambiguous feature-gap + behavioural-pattern tickets left Open (skip-silently per Step 2.10 conservative bar). Prior P073-close fragment rotated to README-history.md per P134.

> Last reviewed: 2026-05-26 **README reconciled** — 2 drift entries corrected: P285, P286 (MISSING from WSJF Rankings; both were captured at flat `docs/problems/NNN-*.open.md` paths — a P281 straggler instance in this repo — so the P094 refresh-on-create never picked them up; added to the WSJF 1.5 band per their deferred-placeholder Severity 3 / Open / M). Reconciliation contract per P118 + ADR-014 amended ("Reconciliation as preflight robustness layer"). Prior 45-transitions fragment rotated to README-history.md per P134.

## 2026-05-26 (P265 fragment rotated for review-problems re-rank pass)

> Last reviewed: 2026-05-26 **P265 known error → verification pending** — the P165 README-refresh commit gate now recognises the `RISK_BYPASS: adr-031-migration` trailer (narrow allow-list, byte-identical grep to `risk-score-commit-gate.sh`), so rename-only Step 0a layout-migration commits no longer deadlock. 43/43 bats green (red-green confirmed; 4 new P265 fixtures); ADR-014 bypass-token registry updated to name both recognising gates. Shipped in `@windyroad/itil` patch (changeset queued). Live verification = the next Step 0a run auto-migrates the P285/P286 flat-layout stragglers once installed. Prior README-reconcile fragment rotated to README-history.md per P134.

## 2026-05-26 (review-re-rank fragment rotated for P258 known-error → verification-pending transition)

> Last reviewed: 2026-05-26 **review re-rank pass: 7 released-fix tickets → Verification Pending** — P069/P198/P209/P219/P280/P284/P286 had fixes verified released (npm + `@windyroad/risk-scorer@0.11.0` CHANGELOG + `architect@0.8.0` published) but sat in Open/Known Error; moved to the Verification Queue with cited Fix Released sections. Also: 2 Open→Known Error auto-transitions (P173, P177 — root cause + workaround documented; WSJF 1.5→6.0); WSJF corrections (P258→15.0, P297/P303/P169→4.5, P287→3.0). Inbound discovery (TTL auto-recheck): #149 closed upstream, no new third-party reports, classification still deferred. The agent-behaviour-class uniform-uplift proposed by a re-score batch was rejected (interpretation drift). Prior P265 fragment rotated to README-history.md per P134.

## 2026-05-26 (P258 fragment rotated for P182 known-error → verification-pending transition)

> Last reviewed: 2026-05-26 **P258 Known Error → Verification Pending (documented learning shipped)** — plugin.json reserved-keys P0 learning completed: validator accepted top-level key shape enumerated + recognised-vs-unrecognised key behaviour documented in `docs/briefing/plugin-distribution.md` (grounded against the Claude Code plugins reference — recognised keys are type-checked → wrong-typed value is a hard install failure; unrecognised keys are warning-only); ADR-063 Amendment 2026-05-18 root-cause phrasing refined; ADR-058 amendment determined not applicable (NDJSON-stdout surface, no manifest validator); P263 cross-referenced with the `claude plugin validate --strict` finding. Excluded from WSJF (multiplier 0) per ADR-022. Prior review-re-rank fragment rotated to README-history.md per P134.

## 2026-05-26 (P182 fragment rotated for P279 known-error → verification-pending transition)

> Last reviewed: 2026-05-26 **P182 Known Error → Verification Pending (fix committed, release pending)** — `measure-context-budget.sh` problems bucket now walks both the flat and per-state-subdir layouts (dual-tolerant per RFC-002 T4 / ADR-031, dedup on ticket ID, per-state wins), fixing the ~99% under-count + phantom delta in run-retro Step 2c / analyze-context reports; real-repo bucket corrected 286,459 → 3,797,225 bytes; sole remaining flat-glob (sibling scripts already dual-tolerant); 3 new bats, full file 34/34 green; `@windyroad/retrospective` patch changeset queued (release deferred to orchestrator Step 6.5 drain). Excluded from WSJF (multiplier 0) per ADR-022. Prior P258 fragment rotated to README-history.md per P134.

## 2026-05-26 (P279 fragment rotated for P173 known-error → verification-pending transition)

> Last reviewed: 2026-05-26 **P279 Known Error → Verification Pending (docs-only housekeeping shipped)** — ADR-017 § Consequences § Neutral now documents the two shared-helper destination-path conventions under one sync mechanism: cross-cutting libs (`derive-first-dispatch.sh` etc.) sync flat into each package's `lib/`; hook helpers (`session-marker.sh`, `command-detect.sh` etc.) sync into each package's `hooks/lib/` beside their consuming hooks — both share the same `sync-<name>.sh` + `check:<name>` CI drift gate + self-contained-package guarantee, only the destination subpath differs by role. New `packages/shared/README.md` pointer-doc (the lone package without one) maps the layout → role→subpath rule → ADR-017 as authoritative. Architect + JTBD PASS, no new ADR (housekeeping carve-out). Docs-only: no changeset, no npm publish (`packages/shared/` unpublished + ADR repo-internal). Architect advisory queued: ADR-017 reassessment trigger (>5 shared modules) now met (~8 helpers). Excluded from WSJF (multiplier 0) per ADR-022. Prior P182 fragment rotated to README-history.md per P134.

## 2026-05-26 (P173 fragment rotated for P259 known-error → verification-pending transition)

> Last reviewed: 2026-05-26 **P173 Known Error → Verification Pending (gate deny-message fix shipped)** — the deny messages of the three affected PreToolUse gates (`itil-changeset-discipline.sh`, `retrospective-readme-jtbd-currency.sh`, `external-comms-gate.sh` — canonical + synced copies across itil/retrospective/risk-scorer/voice-tone) now lead with the accurate in-flight recovery and state the `BYPASS_*_GATE` env bypass takes effect only when set in Claude Code's process env before the session starts. Only the LOCAL deny-message accuracy was fixed; the core mechanism (a Bash-subshell env var never reaches the hook process) is a Claude Code runtime characteristic (upstream/by-design) and was NOT changed — enforcement logic, exit codes, and marker semantics are byte-for-byte unchanged. TDD GREEN (bats added/extended per gate); SID-mismatch sub-finding deduped to P142/P260 (not split). Released `@windyroad/itil@0.35.9` + `@windyroad/retrospective@0.21.2` + `@windyroad/risk-scorer@0.11.1` + `@windyroad/voice-tone@0.5.5` (commit `0ea7196`) via the AFK loop Step 6.5 drain. Excluded from WSJF (multiplier 0) per ADR-022. Prior P279 fragment rotated to README-history.md per P134.


## 2026-05-26 (P259 fragment rotated for P139 verification-pending → closed transition)

> Last reviewed: 2026-05-26 **P259 Known Error → Verification Pending (settings.json snapshot-and-restore shipped)** — `/install-updates` Step 4 (repo-local skill source `scripts/repo-local-skills/install-updates/SKILL.md`) now snapshots `.claude/settings.json` via a working-tree `cp` BEFORE the uninstall+install loop and restores it on any `lost` outcome (`restore_settings_on_loss`), so a broken-manifest install cascade can no longer gut the enabledPlugins map (the 2026-05-18 P0 cut 13 plugins → 2). Full-file restore is non-clobbering for same-run successes because enabledPlugins carries no version pin (version advance lives in the global cache, not settings.json); invariant pinned in a code comment. Recovery path `git checkout HEAD -- .claude/settings.json` documented in `docs/briefing/plugin-distribution.md`. Behavioural bats 5/5 + full install-updates suite 32/32 green; bash+zsh syntax-clean (P133); architect + JTBD PASS, no new ADR (in-scope ADR-030 robustness; ADR-004 `--scope project` preserved). Repo-local skill — no changeset/npm release; orchestrator pushes. REFERENCE.md stale consent-gate prose left to P285. Excluded from WSJF (multiplier 0) per ADR-022. Prior P173 fragment rotated to README-history.md per P134.

## 2026-05-26 (P139 fragment rotated for P261 stream-timeout-salvage transition)

> Last reviewed: 2026-05-26 **P139 Verification Pending → Closed (repo-local symlink contract verified in-session)** — closed on Step 4a evidence during the P259 work iteration, which exercised P139's documented verification criteria exactly: edited the relocated source `scripts/repo-local-skills/install-updates/{SKILL.md,test/}`, architect + JTBD gates fired on the `scripts/` path through normal review (not blocked-on-`.claude/`), the `.claude/skills/install-updates/SKILL.md` symlink resolved (bats read source edits via the symlink), and P139's dedicated contract bats `install-updates-symlink-contract.bats` ran 10/10 green within the full 32/32 install-updates suite. Removed from Verification Queue. Prior P259 fragment rotated to README-history.md per P134.

## 2026-05-26 (P261 fragment rotated for P262 capture-deferred-readme transition)

> Last reviewed: 2026-05-26 **P261 Known Error → Verification Pending (is_error:true stream-timeout salvage carve-out shipped)** — Step 5 exit-code semantics now document the deterministic SALVAGE-vs-HALT branch (is_error:true + staged files + iter-authored bats pass → orchestrator commits the staged work from its own main turn with iter-attribution; fresh commit gate on its own SESSION_ID per ADR-009; else halt), with the line-486 orchestrator-commit rule qualified to name this single bounded exception; ADR-032 gains the "is_error:true stream-timeout salvage (P261 amendment)" sub-variant (commit-authorship carve-out + one-commit-grain — amend-folding inapplicable, no iter commit exists + ADR-009 fresh-gate behaviour + distinction from P121/P147/P146); behavioural fixture `work-problems-step-5-stream-timeout-salvage.bats` 13/13 green, full work-problems suite 316/316, briefing "What You Need to Know" entry added. `@windyroad/itil` patch changeset queued; orchestrator Step 6.5 drains release. Excluded from WSJF (multiplier 0) per ADR-022. Prior P139 fragment rotated to README-history.md per P134.


## 2026-05-26 (P262 fragment rotated for P283 prong-2 oversight-drain + KE→verification-pending transition)

> Last reviewed: 2026-05-26 **P262 Known Error → Verification Pending (capture-deferred-readme RISK_BYPASS token shipped)** — the P165 README-refresh gate conflicted with `/wr-itil:capture-problem` Step 6's deferred-README-refresh contract (ADR-032), denying every `docs(problems): capture` commit. Architect verdict (framework-resolvable, no new ADR): register a reason-named token `capture-deferred-readme` into the existing P265 `RISK_BYPASS`-trailer allow-list in `packages/itil/hooks/lib/readme-refresh-detect.sh`; capture-problem Step 6 emits the trailer; the "do NOT stage README" instruction stays (preserving the deferred-refresh contract). README-gate-ONLY asymmetry (NOT registered in `risk-score-commit-gate.sh` — capture commits are risk-scored normally) documented in the ADR-014 bypass-token table. Rejected Option A (stage-README-with-placeholder) as a [Decision Conflict] with ADR-032; rejected hard-coded subject-prefix match as fragile + registry-invisible. 48/48 readme-refresh-discipline bats green incl. 5 P262 fixtures (red-green confirmed). `@windyroad/itil` patch changeset queued; orchestrator Step 6.5 drains release. Excluded from WSJF (multiplier 0) per ADR-022. Prior P261 fragment rotated to README-history.md per P134.

## 2026-05-26 (P283 fragment rotated for P283-drain queue-surfacing pass — P163/P198 closed, P304/P305/P306 captured, 3 decisions persisted)

> Last reviewed: 2026-05-26 **P283 Known Error → Verification Pending (architect AskUserQuestion-on-decision + decision-oversight drain — both prongs shipped)** — Prong 1 (`@windyroad/architect@0.8.0`, already live): architect emits a Needs-Direction verdict + born-confirmed `create-adr`, stopping NEW poor auto-decisions. Prong 2 mechanism (`@windyroad/architect@0.9.0` + `@windyroad/itil@0.35.12`, ADR-066): `human-oversight` frontmatter marker + `wr-architect-detect-unoversighted` grep detector + `architect-oversight-nudge.sh` SessionStart nudge (AFK-self-suppressing) + `/wr-architect:review-decisions` drain skill — graduated from the held changeset + released via the AFK drain on direct user direction, with in-repo dogfood evidence (nudge fires against the repo's unoversighted set + self-suppresses under `WR_SUPPRESS_OVERSIGHT_NUDGE=1`). Prong-2 drain of the existing set complete (10 ADRs reviewed interactively this session — the user's own oversight, the eat-our-own-dogfood loop): 4 confirmed (018/019/052/054), 2 amended+confirmed (060 canonical execution spine = Problem→RFC→story-map(s)→stories with ADRs alongside; 043 deep context-layer MUST be auto-triggered), 2 rejected-pending-supersede (034 — user: no network auto-install "horrible"; 055 — user: do not expose IDs in published artefacts), 2 deferred (047 blocked by P297, 063 blocked by P300). 3 follow-ups surfaced (supersede 034, supersede 055, implement the 043 auto-trigger) are NEW tickets, not P283 scope. Excluded from WSJF (multiplier 0) per ADR-022. Prior P262 fragment rotated to README-history.md per P134.

## 2026-05-26 (P283-drain fragment rotated for P132/P233/P234 batch verification-close)

> Last reviewed: 2026-05-26 **P283-drain queue-surfacing pass — P163/P198 closed (verified), P304/P305/P306 captured, 3 oversight decisions persisted** — closing out the P283 prong-2 drain: surfaced the AFK loop's 8 accumulated outstanding-questions to the user. (1) 3 genuine decisions resolved + persisted: ADR-050 amended in-place (falsified "orchestrator+own-subprocess not a race" claim corrected → it IS a race per P260; Option C mitigation recorded; oversight re-confirmed); ADR-017 reassessment fired (>5 modules) → user-directed "move to bundler" → new **P304**; P177 → option (a) amend `itil-changeset-discipline.sh` to recognise `docs/changesets-holding/` (fix strategy recorded). (2) 5 observations surfaced as questions: **P163 + P198 closed** Verification Pending → Closed (incidental-exercise verification evidence cited); new **P305** (post-Edit silent-revert hazard) + **P306** (`classify-readme-drift.sh` D+A-pair uncovered, extends closed P149); migrate-layout `shopt`/zsh recurrence appended to P133; P142 noted pre-fix-on-stale-cache (not a regression), left verifying. The 3 P283 reworks (supersede 034, supersede 055, 043 auto-trigger) were already ticketed as P299/P298/P295 (prior drain) — no duplicates. Excluded from WSJF (multiplier 0) per ADR-022. Prior P283 fragment rotated to README-history.md per P134.

## 2026-05-26 (batch-verification-close fragment rotated for P138 verification-pending → closed transition)

> Last reviewed: 2026-05-26 **Batch verification-close — P132 / P233 / P234 Verification Pending → Closed** — user-directed review of the verifying queue. Of 91 verifying rows, 8 carried `yes — observed` evidence; 3 met the close gate (recorded multi-session evidence, zero regression, transitioned ~8 days ago not this session, author marked verification window met): **P132** (over-ask inverse-P078 fix — 3 subsequent AFK sessions 4/5/6 zero regressions), **P233** (stale-cache `/install-updates`-chain fix — 5 release cycles + churn-commit `40fc6a1` proof, zero P232 recurrence), **P234** (fictional-defer advisory hook — 5 subsequent retros zero recurrences). Held the other 5: P246 / P250 ("verification window remains in-flight" per author), P266 (same shipped artefact as in-flight P250), P262 / P283 (transitioned today — same-session, cannot self-verify per run-retro Step 4a step 8). Closed via `/wr-itil:transition-problems`; reversible per ADR-044 recovery path. Excluded from WSJF (multiplier 0) per ADR-022. Prior P283-drain fragment rotated to README-history.md per P134.

## 2026-05-26 (P138 verification-close fragment rotated for README reconciliation — P307/P308/P309 added)

> Last reviewed: 2026-05-26 **P138 Verification Pending → Closed** (verification close-on-evidence per run-retro Step 4a / ADR-044) — during a `/wr-itil:work-problems` + run-retro session, Step 3 selection read this README's WSJF Rankings table top-to-bottom and took row 1 (P177) as the pick with NO in-memory re-sort; the rendered order matched the tie-break ladder 1:1 (WSJF-6.0 group: P177 → P260/P267 → P087/P097 → P082/P263/P282 — KE-first, effort asc, date asc), which is precisely P138's fix. Reversible via `/wr-itil:transition-problem 138 known-error`. Excluded from WSJF (multiplier 0) per ADR-022. Prior batch-verification-close fragment (P132/P233/P234) rotated to README-history.md per P134.

## 2026-05-26 (README reconciliation fragment rotated for P177 Known Error → Verification Pending transition)

> Last reviewed: 2026-05-26 **README reconciled** — 3 drift entries corrected: P307, P308, P309 (open on disk, missing from WSJF Rankings — committed cross-session drift from the prior session's capture commits; classifier returned HALT_ROUTE_RECONCILE uncovered=3, no staged renames). Added at WSJF 3.0 (Priority 3 / Effort M, deferred — re-rate at next `/wr-itil:review-problems`). Reconciliation contract per P118 + ADR-014 amended ("Reconciliation as preflight robustness layer"). Prior P138 verification-close fragment rotated to README-history.md per P134.

## 2026-05-26 (P177 K→V fragment rotated for README reconciliation — P310 added)

> Last reviewed: 2026-05-26 **P177 Known Error → Verification Pending** — the P141 changeset-discipline gate now recognises `docs/changesets-holding/<name>.md` held-window entries (option a, fix commit `a8823be`) released in `@windyroad/itil@0.35.13`; held-window-bound `packages/<plugin>/` source now lands in a single commit (no more 2-commit work-then-move workaround). The K→V transition was deferred at release time per ADR-018 and never executed, leaving the ticket stale in `known-error/` (the prior salvage+release session ended after retro without it) — this transition reconciles the lifecycle to the released reality. Awaiting user verification. Excluded from WSJF (multiplier 0) per ADR-022. Prior README-reconciliation fragment (P307/P308/P309) rotated to README-history.md per P134.

## 2026-05-26 (P310-reconcile fragment rotated for P311 README reconciliation)

> Last reviewed: 2026-05-26 **README reconciled** — 1 drift entry corrected: P310 (open on disk, missing from WSJF Rankings — committed cross-session deferred-refresh drift from capture-problem). Reconciliation contract per P118 + ADR-014 amended ("Reconciliation as preflight robustness layer"). Prior P177 K→V fragment rotated to README-history.md per P134.

## 2026-05-26 (P311-reconcile fragment rotated for P260 Known Error → Verifying on release)

> Last reviewed: 2026-05-26 **README reconciled** — 1 drift entry corrected: P311 (open on disk, missing from WSJF Rankings — committed cross-session deferred-refresh drift from the capture-problem P311 correction capture). Reconciliation contract per P118 + ADR-014 amended ("Reconciliation as preflight robustness layer"). Prior P310-reconcile fragment rotated to README-history.md per P134.

## 2026-05-26 (P260 K→V fragment rotated for P312/P313/P314/P315 README reconciliation)

> Last reviewed: 2026-05-26 **P260 Known Error → Verifying** — the ADR-050 Option-C concurrent-session create-gate fix released in `@windyroad/itil@0.35.14` (release commit `bf1ebdd`); brought under the RFC framework as RFC-007 (retro-fit per ADR-071 — every fix goes through an RFC) so the held changeset could release under the new unconditional gate. Moved from WSJF Rankings to the Verification Queue per ADR-022. Prior P311-reconcile fragment rotated to README-history.md per P134.

## 2026-05-27 (P312/P313/P314/P315-reconcile fragment rotated for P316 README reconciliation)

> Last reviewed: 2026-05-26 **README reconciled** — 4 drift entries corrected: P312 / P313 / P314 / P315 (open on disk, missing from WSJF Rankings — committed deferred-refresh drift from this session's review-decisions-drain + P314-rework captures). Reconciliation contract per P118. Prior P260 K→V fragment rotated to README-history.md per P134.

## 2026-05-27 (P316-reconcile fragment rotated for P317-P322 README reconciliation)

> Last reviewed: 2026-05-27 **README reconciled** — 1 drift entry corrected: P316 (open on disk, missing from WSJF Rankings — committed deferred-refresh drift from this session's run-retro Step 2b P316 capture). Reconciliation contract per P118 + ADR-014 amended ("Reconciliation as preflight robustness layer"). Prior P312/P313/P314/P315-reconcile fragment rotated to README-history.md per P134.

## 2026-05-28 (P317-P323-reconcile fragment rotated for P324 README reconciliation)

> Last reviewed: 2026-05-27 **README reconciled** — 7 drift entries corrected: P317 / P318 / P319 / P320 / P321 / P322 / P323 (open on disk, missing from WSJF Rankings — committed deferred-refresh drift from the 2026-05-27 run-retro P319-P322 captures + P317/P318/P323 fix-session captures). All deferred-rated (Priority/Effort placeholders) — re-rate at next /wr-itil:review-problems. Reconciliation contract per P118 + ADR-014 amended ("Reconciliation as preflight robustness layer"). Prior P316-reconcile fragment rotated to README-history.md per P134.

## 2026-05-30 (P324-reconcile fragment rotated for P325/P326 README reconciliation)

> Last reviewed: 2026-05-28 **README reconciled** — 1 drift entry corrected: P324 (open on disk, missing from WSJF Rankings — deferred-refresh drift from this session's P324 capture; WSJF 2.25 = Priority 9 ÷ L). Reconciliation contract per P118 + ADR-014 amended ("Reconciliation as preflight robustness layer"). Prior P317-P323-reconcile fragment rotated to README-history.md per P134.

## 2026-05-30 (P325/P326-reconcile fragment rotated for P327 README reconciliation)

> Last reviewed: 2026-05-30 **README reconciled** — 2 drift entries corrected: P325 (open on disk, missing from WSJF Rankings — deferred-refresh drift from P325 capture; WSJF 4.0) and P326 (open on disk, missing from WSJF Rankings — deferred-refresh drift from P326 capture; WSJF 3.0). Reconciliation contract per P118 + ADR-014 amended ("Reconciliation as preflight robustness layer"). Prior P324-reconcile fragment rotated to README-history.md per P134.

## 2026-05-30 (P327-reconcile fragment rotated for P328 README reconciliation)

> Last reviewed: 2026-05-30 **README reconciled (run-retro)** — 1 drift entry corrected: P327 (open on disk, missing from WSJF Rankings — deferred-refresh drift from this session's P327 capture; WSJF 1.5). Reconciliation contract per P118 + ADR-014 amended ("Reconciliation as preflight robustness layer"). Prior P325/P326-reconcile fragment rotated to README-history.md per P134.

## 2026-05-30 (P328-reconcile fragment rotated for inbound-discovery preflight)

> Last reviewed: 2026-05-30 **README reconciled (work-problems Step 0)** — 1 drift entry corrected: P328 (open on disk, missing from WSJF Rankings — deferred-refresh drift from this session's P328 capture; WSJF 1.5). Reconciliation contract per P118 + ADR-014 amended ("Reconciliation as preflight robustness layer"). Prior P327-reconcile fragment rotated to README-history.md per P134.

## 2026-05-30 (inbound-discovery fragment rotated for P316 K→V)

> Last reviewed: 2026-05-30 **inbound-discovery refresh (work-problems Step 0b preflight)** — TTL-expiry auto-recheck against `windyroad/agent-plugins`; 34 active `[problem]` reports polled (#149 still closed-upstream historical entry), 0 new third-party reports, all maintainer-authored + locally mirrored; pipeline classification remains deferred per 2026-05-15 user direction; discussions still HTTP 410, security-advisories still gate-blocked (P276/P198/#125). Cache rewritten `last_checked: 2026-05-30T01:20:15Z`; audit-log entry appended to `docs/audits/inbound-discovery-log.md`. Prior P328-reconcile fragment rotated to README-history.md per P134.


## 2026-05-30 (P316 K→V fragment rotated for P282 O→V fold-fix)

> Last reviewed: 2026-05-30 **P316 K→V transition (work-problems iter-5)** — `@windyroad/architect@0.12.0` + `@windyroad/jtbd@0.9.0` released via PR #175 / merge `241db7f` shipped the ADR-066 amendment introducing `human-oversight: rejected-pending-supersede` + `supersede-ticket: P<NNN>`. Detector + build-upon predicate honour the new exclusion; JTBD sibling mirrors symmetrically; drain SKILLs capture the supersede ticket via follow-up AskUserQuestion on Reject; compendium renderer surfaces the disposition. ADR-034/047/055/063 backfilled and verified excluded post-fix. Citation derived deterministically via second real-world dogfood of `wr-itil-derive-release-vehicle` (after P267 iter-2). Prior inbound-discovery fragment rotated to README-history.md per P134.


## 2026-05-30 (P282 O→V fold-fix fragment rotated for P325 O→KE)

> Last reviewed: 2026-05-30 **P282 O→V fold-fix (work-problems iter-6)** — run-retro Step 4a gains sub-step 9 "Prior-session evidence drain (P282)" reading `docs/problems/README.md` Verification Queue for rows whose `Likely verified?` cell begins with `yes — observed:` (P186 cell shape), preserving sub-step 8 same-session exclusion via git-log rename detection, dispatching `/wr-itil:transition-problem <NNN> close` per the sub-step 5-7 contract with `(prior-session README cell)` source marker in the Decision column. Architect verdict PASS (no new ADR — thin extension of ADR-022 + ADR-014; ADR-074 substance-confirm trip-wire DOES NOT FIRE because semantic substance already pinned). JTBD verdict PASS (c)>(d)>(a)>(b) — explicit on-disk evidence beats inference on current edit. New bats `run-retro-step-4a-prior-session-evidence-drain.bats` 12/12 green; full run-retro suite 150/150 green. Closes the 2026-05-26 evidence gap (8/91 rows carried `yes — observed:` from prior sessions; none auto-closed; queue hit 134 KB exceeding Read-tool 25K-token cap). Prior P316 K→V fragment rotated to README-history.md per P134.

## 2026-05-30 (P325 O→KE fragment rotated for P302 O→KE)

> Last reviewed: 2026-05-30 **P325 O→KE (work-problems iter-9)** — CI workflow first-party action pins bumped to Node-24-supporting majors ahead of GitHub's 2026-06-02 forced-flip / 2026-09-16 Node-20 removal: `actions/checkout@v4`→`@v5`, `actions/setup-node@v4`→`@v5`, `actions/github-script@v7`→`@v8` across `ci.yml`, `release.yml`, `release-preview.yml` (8 pin lines total in 3 files). `changesets/action@v1` left alone (third-party, separate release cadence). Workflow `node-version: 20` field unchanged — independent of action runtime. Architect verdict PASS (no new ADR — action-pin bumps fall under the existing "version bumps" exclusion; no ADR covers runner Node versions). JTBD verdict PASS (serves JTBD-101 "Extend the Suite with New Plugins" CI/release pipeline keep-alive; preserves "Must not break existing plugins" constraint + "slow test-fix-release cycles" pain-point). P063 external-root-cause detection NO_MATCH (strict tokens absent). K→V follow-on completes in a later orchestrator pass after release-and-verify. Prior P282 O→V fold-fix fragment rotated to README-history.md per P134.

## 2026-05-30 (P302 O→KE fragment rotated for work-problems wrap retro)

> Last reviewed: 2026-05-30 **P302 O→KE (work-problems iter-10)** — `/wr-architect:review-decisions` Step 3 + `/wr-jtbd:confirm-jobs-and-personas` Step 3 + `docs/briefing/agent-interaction-patterns.md` now carry the substance-first presentation rule for decision-confirmation prompts: `AskUserQuestion` `question` field MUST lead with the one-line Decision Outcome (`"This ADR decides: X"`) / job statement / persona definition; sibling-ADR / supersession / Considered-Options recording-shape meta relegated to trailing clause or omitted. Worked bad/good examples grounded in the ADR-045 ("a sibling ADR codifying five patterns…") and ADR-020 ("a separate ADR citing ADR-014…") 2026-05-25 drain re-asks. Applies ADR-074 *name the substance, not the grain* to the confirm-prompt surface; extends ADR-026 grounding from artifact body to the `AskUserQuestion` `question` text. Architect verdict PASS (no new ADR — fits cleanly under ADR-066 + ADR-068 + ADR-026 + ADR-013 + ADR-044 envelope; ADR-074 advisory cross-reference added). JTBD verdict PASS (serves JTBD-005 on-demand-governance speed-without-sacrificing-quality, JTBD-006 AFK clear-summary, JTBD-101 clear-patterns-not-reverse-engineering). Create-adr Step 5 confirm covered via the briefing note generalisation — no separate SKILL.md edit (its presentation surface differs since the user just authored the decision). Prior P325 O→KE fragment rotated to README-history.md per P134.

## 2026-05-30 (work-problems wrap retro fragment rotated for P332 README reconciliation)

> Last reviewed: 2026-05-30 **work-problems wrap retro** — captured 2 codification tickets: P330 (derive-release-vehicle helper UX gap — 3-touch K→V cycle; 2-of-3 dogfood evidence) + P331 (transition-problem Step 7 P134 Last-reviewed rotation silently skipped iter-7+iter-8 evidence chain). Both Open/S deferred-rate WSJF 3.0. Prior P302 O→KE fragment rotated to README-history.md per P134.


## 2026-05-30 (README reconciliation fragment rotated for P302 K→V)

> Last reviewed: 2026-05-30 **README reconciled** — 3 drift entries corrected: P333, P334, P335. Reconciliation contract per P118 + ADR-014 amended ("Reconciliation as preflight robustness layer").


## 2026-05-30 (P302 K→V fragment rotated for P087 K→V)

> Last reviewed: 2026-05-30 **P302 K→V (work-problems AFK iter)** — `@windyroad/architect@0.12.1` released via PR #178 / merge `d929acd` (version-packages commit `5244b5f`) shipped the substance-first presentation rule for decision-confirmation prompts to `/wr-architect:review-decisions` Step 3 + `docs/briefing/agent-interaction-patterns.md`. JTBD mirror at `/wr-jtbd:confirm-jobs-and-personas` Step 3 is intentionally held with `docs/changesets-holding/p288-jtbd-persona-oversight.md` and rides that changeset's graduation. Citation derived deterministically via `wr-itil-derive-release-vehicle P302` per the helper's structured key:value output (fourth real-world dogfood after P267 / P316 / P281). Prior README-reconciled fragment rotated to README-history.md per P134.

## 2026-05-30 (P087 K→V fragment rotated for P097 K→V)

> Last reviewed: 2026-05-30 **P087 K→V (work-problems AFK iter)** — Phase 1 (ADR-053 taxonomy) + Phase 2a/b/c/d/e (`wr-itil-skill-invocations` + `wr-itil-plugin-exercise-index` measurement scripts, both under their ADR-058 performance budgets) + Phase 3 (ADR-063 presentation contract) + Phase 3a (`wr-itil-plugin-maturity-populate`) + Phase 3b (`wr-itil-plugin-maturity-render` + `wr-retrospective-check-plugin-maturity-drift`) + Phase 3c (bats doc-lint) + Phase 3d (JTBD-302/007/101/003 outcome amendments) + retroactive mechanical rollout (11 plugin READMEs + 122 surfaces in plugin.json) all shipped over the 2026-05-03 → 2026-05-18 window. Closure criteria stated in §Decision record line 172 met — every `@windyroad/*` plugin's README carries the maturity badge and the canonical `plugin.json` `maturity:` field is populated; drift detector reports 0 drift instances. Sibling P244 (F9 in-suite display shim) remains Open as a separate carve-out per architect adjustment E. Residual direction-class items queued for follow-on (do NOT block closure): option 4 opt-in telemetry hook decision + compound-rendering gap fix direction. Prior README-reconciled fragment rotated to README-history.md per P134.

## 2026-05-30 (P097 K→V fragment rotated for P082 O→KE)

> Last reviewed: 2026-05-30 **P097 K→V (work-problems AFK iter 3)** — defined scope (Phase 1 declarative contract + empirical baseline + Phase 2-3 carve-out) complete and shipped across 2026-05-03 → 2026-05-17 window. Phase 1 (@windyroad/retrospective 2026-05-03): ADR-054 proposed + `check-skill-md-budgets.sh` advisory detector + `wr-retrospective-check-skill-md-budgets` bin shim per ADR-049 + 21 behavioural bats per ADR-052 + dogfood baseline (20 OVER WARN / 9 MUST_SPLIT) IS the Phase 2-3 prioritisation backlog. Empirical baseline (work-problems iter 8 2026-05-17): first canonical ADR-054 worked example `analyze-context/SKILL.md` → sibling `REFERENCE.md` (−7.7% bytes; 18/18 contract bats + 21/21 detector bats green; architect PASS / JTBD PASS / Risk CONTINUE Very Low). Phase 2-3 carve-out via P179 umbrella-per-cohort architect Q6: P241 MUST_SPLIT cohort (`Blocked by: P081` Layer B), P242 install-updates project-local, P243 WARN-band cohort (defer-permitted). Phase 4 ADR-054 acceptance flows through standard wr-architect human-oversight drain (P283/ADR-066), not P097's responsibility. Prior README-reconciled fragment rotated to README-history.md per P134.

## 2026-05-30 (P082 O→KE fragment rotated for P263 O→KE)

> Last reviewed: 2026-05-30 **P082 Open→KE (work-problems AFK iter 4)** — RCA confirmed + fix direction pinned. All three blockers CLOSED (P038 voice-tone external-comms gate, P064 risk external-comms gate, P073 changeset-author gate); P082 unblocked at marginal-M effort per P076 transitive rule. Extension target confirmed: canonical `external-comms-gate.sh` (byte-identical across `packages/voice-tone/hooks/` + `packages/risk-scorer/hooks/` per ADR-017 sync; per-package `external-comms-evaluator.conf` for identity). Decision: **Option A** (extend the canonical hook's Bash surface-detection block with a `git commit*` branch + `-m`/`--message` body extraction). Per-evaluator marker scheme already supports N-way composition. **Phase 1 (P082 marginal M):** voice-tone + content-risk evaluators on `git-commit-message` surface; behavioural bats per P081 covering AI-tell deny, leak-pattern deny, `--amend` interception, `BYPASS_RISK_GATE=1` short-circuit. **Phase 2 (sibling ticket to capture):** cognitive-accessibility evaluator across all four external-comms surfaces (gh / npm / changeset / commit) — requires new plugin OR voice-tone third-evaluator amendment + ADR-028 amendment. Sub-concerns 1-6 resolved inline. Implementation iter must delegate to `wr-architect:agent` when editing `packages/**/*.sh`. Prior iter 3 fragment (P097 K→V) rotated to README-history.md per P134.

## 2026-05-30 (P263 Phase 1 fragment rotated for P341 + P342 batch K-E transition 2026-05-31)

> Last reviewed: 2026-05-30 **P263 Phase 1 implementation landed (work-problems AFK iter 7)** — Option A non-strict `claude plugin validate` gate shipped per the iter-6 architect-GREEN'd design: canonical body `packages/itil/scripts/plugin-validate-ci-gate.sh` + ADR-049 shim `packages/itil/bin/wr-itil-plugin-validate-ci-gate` + 11/11-green behavioural bats `packages/itil/scripts/test/plugin-validate-ci-gate.bats` (Fixture A ADR-063 safe-extension positive, Fixture B P258 reproduction negative, mixed-pass/fail no-short-circuit, no-plugins fail-safe, shim PATH-resolution). CI wiring after `Dry-run per-plugin installers` with pinned `@anthropic-ai/claude-code@2.1.150`. ADR-063 §Confirmation #11 amended with implementation-note documenting prose→implementation refinement chain. Architect PASS + JTBD PASS + risk PASS + voice/tone PASS. Changeset `@windyroad/itil` minor. KE remains — orchestrator owns K→V transition after release lands. **Prior iter 6 RCA highlights preserved**: design tension `--strict` would reject ADR-063 `maturity:` safe-extension (all 11 plugins), so NON-strict is the correct gate per P258 refined recognised/unrecognised key distinction. Latent itil YAML Parse error in `transition-problems/SKILL.md` surfaced iter 6 — orchestrator captured separately, NOT in P263's scope.

## 2026-05-31 (P325 K→V + P336/P337/P338/P343 backfill fragment rotated for P344 backfill)

> Last reviewed: 2026-05-31 **P325 K→V transition + P336/P337/P338/P343 README backfill** — P325 (Node 20 action pins) → Verification Pending on @windyroad/architect@0.13.0 / itil@0.39.0 / retrospective@0.22.0 / risk-scorer@0.11.3 release (CI test 2145 root cause was stale 0.11.0 plugin shim on PATH overriding 0.12.2 — captured as P343 + memory `feedback_if_you_see_something_broken_fix_it`); also backfilling WSJF Rankings entries for P336 (transition-problems YAML — fix shipped same release), P337 (compendium Decision Outcome gap → ADR-078 amended to Option 9), P338 (P082 Phase 2 cognitive-a11y plugin sibling), P343 (install-updates PATH-ordering gap). Reconcile preflight reported 5 drift entries; all resolved in this commit.

## 2026-05-31 (README reconciliation fragment rotated for /wr-itil:review-problems Step 0b pre-flight refresh)

> Last reviewed: 2026-05-31 **README reconciled** — 1 drift entry corrected: P344 (work-problems orchestrator JTBD-predicate-check) added to WSJF Rankings (Open M, Reported 2026-05-31). Reconciliation contract per P118 + ADR-014 amended ("Reconciliation as preflight robustness layer"). Prior fragment (P325 K→V + P336/P337/P338/P343 backfill) rotated to README-history.md per P134.

## 2026-05-31 (Step 0b pre-flight refresh fragment rotated for P334 O→Closed)

> Last reviewed: 2026-05-31T00:00 **Step 0b pre-flight refresh** — inbound-discovery cache TTL-expired (~99939s>86400s) auto-recheck: 8 NEW upstream reports captured (#180/#181/#183/#184/#185/#186/#187/#188 — all maintainer governance/UX); 13 deferred-placeholder tickets re-rated honestly against RISK-POLICY scales (P334/P336 WSJF 3.0→12.0 surface as release-blockers; P337/P343/P330/P331 → 6.0; P332/P335 → 4.5; P328 → 4.0; P338 honest-drop 3.0→1.0; P329/P333/P344 confirmed at 3.0). Prior fragment rotated to README-history.md per P134.

## 2026-05-31 (P334 O→Closed iter-1 fragment rotated for P336 O→Closed iter-2)

> Last reviewed: 2026-05-31T (iter 1 work-problems AFK) **P334 Open→Closed** — fix already shipped in `@windyroad/architect@0.12.2` (commits `3945878`+`3e53a94`+`e9f7ce4`); verification close-on-evidence per ADR-044 (local --check exit 0 + CI green on commit bad2eac run 26701674556). Lifecycle was missed at session-8 wrap; iter 1 of session 9 closes the lifecycle on already-shipped work. Prior fragment rotated to README-history.md per P134.

## 2026-05-31 (P082+P345 iter 3 fragment rotated for P346 backfill iter 4)

> Last reviewed: 2026-05-31T (iter 3 work-problems AFK) **P082 KE→Verifying** — Phase 1 commit-message surface shipped (canonical `packages/shared/hooks/external-comms-gate.sh` extended with `git commit*` branch + HEREDOC body extraction; ADR-017 byte-identical sync to voice-tone + risk-scorer; per-evaluator marker scheme reused; 46/46 bats GREEN; `.changeset/p082-commit-message-surface.md` bumps both packages). Phase 2 cognitive-a11y carry-out covered by P338. **P345 backfill into WSJF Rankings** — pre-existing drift caught by Step 0 classifier (HALT_ROUTE_RECONCILE uncovered=2); fixed inline per ADR-014 single-commit grain rather than splitting reconcile. Prior fragment (P336 O→Closed iter 2) rotated to README-history.md per P134.
