# Governance Workflow

Cross-session learnings about ADRs, architect and JTBD reviews, risk scoring, voice-tone, and the session-wide unifying patterns (progressive disclosure, canonical+sync, SKILL+REFERENCE).

## What You Need to Know

### Slice-handoff stub markers preserve refactor seams across an RFC's lifecycle (2026-05-15)

When a slice ships a temporary stub that a later slice replaces, mark it inline with an HTML comment naming the stub + the slice that owns the replacement. Pattern observed RFC-004 Slice C → Slice F: `<!-- SLICE-C-FLAG-STUB: $ARGUMENTS string-match for --force-upstream-recheck is a Slice C minimal stub; Slice F (RFC-004) owns proper argument parsing + TTL-expiry auto-recheck. Remove this string-match when Slice F lands; replace with Slice F's parsed-flag variable. -->` The marker (a) makes the refactor seam discoverable in the source, (b) lets Slice E's bats assert the stub is present (Slice C) AND assert the stub is absent (Slice F replaced it), and (c) survives RFC document edits because it's in the runtime artefact, not the planning artefact. Architect-approved pattern (Slice C review 2026-05-15). Reusable across any multi-slice RFC with cross-slice handoffs.
<!-- signal-score: 2 | last-classified: 2026-05-15 | first-written: 2026-05-15 -->

### ADR-against-SKILL numbering reconciliation via substring anchors (2026-05-15)

When an ADR is authored against a stale view of a SKILL's step numbering (e.g. ADR-062 named "Step 8.5" / "Step 9e" but the current SKILL has only 7 steps), do NOT mid-stream-amend the ADR for naming pedantry (violates ADR-006 deliberation discipline). Instead: insert the new step at the current numbering's natural position (e.g. Step 4.5) AND preserve the ADR's substring anchors verbatim in the SKILL's prose via an HTML comment marker. Anchor preservation keeps ADR-XXX § Confirmation criterion N string-anchorable (greps for "Step 8.5" still hit) without rewriting either the ADR or the SKILL's numbering. Pattern shipped RFC-004 Slice C (2026-05-15). Architect issues 1+2 of the slice review name this contract.
<!-- signal-score: 2 | last-classified: 2026-05-15 | first-written: 2026-05-15 -->

### R009 SKILL-prose-class above-appetite is the standing catalog baseline, not a per-action regression (2026-05-15)

The pipeline scorer flags R009 (functional defects in shipped behaviour) at 8/25 Medium on every SKILL/agent-prose ship — above the 4/Low appetite. This is the **documented catalog baseline** per RISK-POLICY.md § Risk Catalog clause 3 ("standing class signal, not per-action defect"). Per-action specific controls (architect + JTBD + external-comms PASS + bats coverage on the test surface) are sufficient to proceed via acknowledged-residual + `BYPASS_RISK_GATE=1` with explicit rationale in the commit body citing clause 3. The R009 catalog floor stays Medium until P012 master harness lands behavioural synthetic-channel coverage. Observed RFC-004 Slices C/E/F/G ship cycle 2026-05-15 — every slice's pipeline scorer flagged 8/25 above-appetite for the same R009 baseline class; user-pinned direction "release and install" + the four per-action controls cited above resolved each acknowledged-residual proceed.
<!-- signal-score: 2 | last-classified: 2026-05-15 | first-written: 2026-05-15 -->


- **Risk appetite is Low (4)**. Changes scoring Medium (5+) need explicit acknowledgement. See `RISK-POLICY.md`.
- **All ADRs in `docs/decisions/` are still `.proposed.md`** — none ratified. Amendments are cheap: prefer amending over superseding. When a P-problem intersects a proposed ADR, revise the ADR rather than adding a compatibility clause.
- **ADR-002 plugin dependency graph lists `@windyroad/tdd` as standalone.** Cross-plugin features (e.g. P018 JTBD traceability, P017 shared concern-splitting helper) must update the graph explicitly; don't silently add deps.
- **Pre-2026-04-26 entries archived to [`governance-workflow-archive.md`](./governance-workflow-archive.md)** (most recently rotated 2026-05-13 per Tier 3 budget MUST_SPLIT, P145 ratio-exceeds-2x). Now covers six pre-2026-04-23 entries plus four 2026-04-23/24/25 entries on BUFD ranked-iteration rejection, gate-validates-change-not-hypothesis, auto-create-on-missing-doc pattern, and post-landing ADR cleanup. Load the archive alongside this file when full historical context is needed.
- **Meta-recursive bootstrap is acceptable when introducing framework primitives.** When codifying a new framework primitive (RFC, lifecycle stage, ADR class), the framework's own first artefact may have to live under the existing structure pending Phase 1 implementation — the RFC framework can't exist without the decision-capture that introduces it. Acknowledge the recursion explicitly (e.g. P170's *"Implementation Tasks (workaround section — until RFC framework arrives)"* with a forward pointer, or ADR-060's Phase 1 scope clause naming P168 as the first retrospective RFC). The user-correction signal that surfaces this conflation tends to be *"are these X or are they Y?"* — soft-challenge phrasing to a flat list that's mixing two task classes. Apply the structural split inline; don't paper over the recursion. Observed 2026-05-04 / 2026-05-05 on P170 + ADR-060 RFC framework introduction.
- **`docs/risks/` is the persistent catalog of per-action risks — NOT a separate aggregation from `.risk-reports/`.** Per RISK-POLICY.md commit `9e339d0` `## Risk Catalog` section: per-action assessments READ the catalog, filter to risks applicable to THIS action, assess controls FOR THIS ACTION against the **same 4/Low appetite**, and append newly-conceived risk classes back to the catalog. There is no per-action vs lifetime aggregation distinction; same appetite uniformly. A catalog residual above appetite is a **real signal** that baseline controls are insufficient for typical actions — either add controls (drop baseline residual) or accept that per-action assessments must add specific controls each time. The waste this design eliminates: re-deriving the same ~327 risk titles across 181 reports per gap analysis. The agent today implements neither the consume-catalog nor the bootstrap-from-empty workflow — see P168 for the substantive design ticket. Two-correction-round cost on 2026-05-04 (P167 misreading) before the simple catalog model surfaced; keep the framing simple on first read. ADR-042's "never release above appetite" invariant remains in force for per-action pipeline scoring.
- **Substantive ADR amendments after architect+JTBD reviews trigger a 3-pass review cycle on accept.** When the architect or JTBD review surfaces ≥ 5 amendments on a `.proposed.md` ADR, applying those amendments invalidates the prior reviews — the JTBD edit-gate detects "policy file changed since last review" on the rename + status flip and re-fires; the architect can also re-fire if the substantial amendments changed any cited driver. Budget for **3 review passes** when accepting a substantively-amended ADR: (1) initial architect review, (2) initial JTBD review, (3) re-review on accept (one or both gates re-fire). Each pass is 30-90s of subagent time. Observed 2026-05-05 on ADR-060 accept after 14 architect + 8 JTBD amendments — the JTBD policy-changed re-fire was correct (JTBD-001 + JTBD-101 had been edited as part of the amendments). The re-review found no new issues; the cycle is structural, not redundant, because the amendments DID change what the gate is reviewing. Plan: the heavyweight re-review pass is the cost of substantive amendments; light amendments (≤ 3 surface edits) usually skip it. <!-- signal-score: 0 | last-classified: 2026-05-05 | first-written: 2026-05-05 -->
- **Bootstrap-exemption marker is the canonical pattern for retroactive migration into a newly-introduced framework primitive.** When a new framework primitive (RFC tier, story tier, story-map tier) ships and a Phase 1 dogfood requires retrofit of prior-session artefacts, the new artefacts ship with a `<!-- bootstrap-exempt: <retrofit-scope> per <ADR + amendment> -->` HTML comment marker inline with the frontmatter (ADR-060 line 339 + ADR-053 Bootstrapping precedent). The marker permits the artefact to bypass runtime invariant gates (I7/I8/I9/I10 for stories; I3/I4 for story-maps) during the one-time migration; non-bootstrap captures with the marker fail per behavioural test. Apply this pattern whenever the migration's "represent what already shipped" use case would otherwise be blocked by the invariant gates the new framework just introduced. Observed 2026-05-12 P170 Phase 2 Slice 15 — 7 STORY-NNN bootstrap stories under `docs/stories/done/` + STORY-MAP-001 under `docs/story-maps/in-progress/` all shipped with the marker; the in-flight Phase 2 framework code (capture-story etc) treats the marker as fail-on-non-bootstrap-capture per ADR-052 behavioural-test contract. <!-- signal-score: 0 | last-classified: 2026-05-12 | first-written: 2026-05-12 -->
- **Single-trailer vocabulary for story-tier commits**: `Refs: STORY-NNN` for BOTH capture commits AND implementation commits per ADR-060 line 307 + amendment 2026-05-10 nitpick N2. The capture-vs-implementation discrimination happens on **commit-subject prefix** (`feat(itil): capture STORY-NNN ...` is capture; any other subject is implementation), NOT on trailer verb. Avoid the two-trailer split (`Refs: STORY-NNN` for capture + `Implements: STORY-NNN` for implementation) — that re-introduces parser-precedence complexity the single-trailer vocabulary collapses. The same shape holds for RFC-tier commits with `Refs: RFC-NNN`. Observed 2026-05-12 P170 Phase 2 Slice 7 architect amend finding 1 — the proposed two-trailer split was rejected and the single-trailer vocabulary applied verbatim to capture-story SKILL.md Step 6 commit-message template. <!-- signal-score: 0 | last-classified: 2026-05-12 | first-written: 2026-05-12 -->

> **Sibling brief**: cross-session "what will surprise you" learnings — ADR mechanics, JTBD reviewer behaviour, the `git ls-tree` blob-SHA next-ID trap, README-refresh reconciliation, and smaller workflow gotchas — live in `governance-workflow-surprises.md` (split out 2026-05-03 per P145 MUST_SPLIT contract for Tier 3 budget compliance). Read alongside this file for the full governance-workflow surface.
