# Releases and CI

Cross-session learnings about push:watch, release:watch, changesets, npm publish, and the GitHub Actions pipeline.

## What You Need to Know

- **GITHUB_TOKEN pushes don't trigger pull_request events**. The release preview uses `workflow_run` trigger instead. If adding new PR-triggered workflows for changesets PRs, use the same pattern.
- **Atomic-cohort full graduation scales empirically to 13 changesets / 5 packages with no false-positives** (ADR-060 finding 12). Graduate a held cluster atomically (`git mv docs/changesets-holding/*.md .changeset/`) on user-comfort signal, not elapsed time. (verifying P162)
- **`.changeset/` empty + `docs/changesets-holding/` non-empty can self-reinforce a release-pressure stall**: ADR-042 auto-moves every changeset to holding, so the ADR-018/020 drain (needs `.changeset/` non-empty) never fires — commits accumulate unpushed AND nothing publishes. Only manual graduation recovers it. Monitor held-cluster size + last-push age proactively. (verifying P162)
- **A cached shim and the in-repo script can disagree mid-session** when the in-repo fix is still in the unreleased holding cluster; trust the in-repo script as ground truth and audit-trail-record the bypass per ADR-044 + ADR-013 Rule 5. (verifying P162)
- **Dogfood criterion is positive-evidence-of-working, NOT elapsed time.** Graduate a held entry when its surface fired correctly with no false-positive; KEEP HELD when it has negative evidence (e.g. hook misfired), regardless of days. Per-entry evidence beats sibling-cohort piggybacking. (verifying P246)
- **npm won't overwrite a published version**. Preview publishes use pre-release suffixes (e.g., `0.1.2-preview.13`) to avoid blocking `changeset publish` from publishing the clean version to `latest`.
- **The `push:watch` script** in root `package.json` is the sanctioned way to push — it runs `git push` then watches the CI run. The git-push-gate hook blocks bare `git push`.
- **Never run `changeset version` locally.** The release pipeline handles versioning. To release: push with `npm run push:watch`, the pipeline creates a release PR, merge it with `npm run release:watch`.
- **Authoring a `.changeset/*.md` triggers the external-comms review gate once per installed evaluator** (risk-scorer + voice-tone both gate `.changeset/*.md` writes per P073). Each denies the Write until ITS own marker exists. In-flight recovery: delegate to BOTH `wr-risk-scorer:external-comms` AND `wr-voice-tone:external-comms`, each with a prompt starting `SURFACE: changeset-author` and wrapping the changeset body — WITHOUT the `---` frontmatter, which the gate strips before hashing (P010) — in `<draft>...</draft>`. The PostToolUse mark hooks derive the keys; the retry Write of the identical body then passes. `BYPASS_RISK_GATE=1` is pre-session env only (P173). <!-- signal-score: 0 | last-classified: 2026-05-26 | first-written: 2026-05-26 -->
- **Pre-2026-05-05 entries archived to [`releases-and-ci-archive.md`](./releases-and-ci-archive.md)** (rotated 2026-05-13 per Tier 3 MUST_SPLIT). Load the archive when investigating older release / CI behaviour.

## What Will Surprise You

- **`changeset publish` E404 on an EXISTING scoped package masks an npm-auth/2FA failure — NOT a missing package or transient blip.** When `'@windyroad/X@N' is not in this registry` fires while siblings "publish fine", the siblings only SKIPPED (already-published, read-only) — the new-version package is the only one exercising the publish token, and npm returns 404 instead of 403/EOTP to mask the auth failure. Diagnostic: `npm view X version` (confirms package exists) → `npm whoami` with the token (valid but no publish scope) → swap in a fresh token to surface `EOTP This operation requires a one-time password`. Root cause: a classic "Publish" token requires interactive OTP CI can't supply. Fix: regenerate as an **Automation / Bypass-2FA / Granular** token, update `NPM_TOKEN`/`NPM_AUTH_TOKEN` secrets, re-run `release:watch`. Until fixed, main self-poisons (Quality-Gates passes; only the publish job fails). See P284. <!-- signal-score: 0 | last-classified: 2026-05-25 | first-written: 2026-05-25 -->
- **Multi-day commit accumulation surfaces latent test-fixture regressions on first push** — the regression often traces to an earlier commit in the chain that wiped a fixture source without a matching test sweep. Before wiping/renaming files referenced by tests, sweep `grep -r '<deleted-name>' packages/*/{scripts,skills,hooks}/**/test` first; synthesize fixture-local equivalents inline so tests aren't coupled to canonical state. Sibling to P116 (batched-push hazard). <!-- signal-score: 0 | last-classified: 2026-05-25 | first-written: 2026-05-05 -->

- **On patchy network, `push:watch`/`release:watch` fail at the long CI/publish waits; retry catches a window.** The bare `git push` lands even when the watch fails — the commit is pushed once `git rev-list origin/main..HEAD`=0, regardless of the watch. A git-only DNS failure while `npm`/`curl` work = stale resolver (post-Pty-restart), clears on retry. (2026-05-26)
