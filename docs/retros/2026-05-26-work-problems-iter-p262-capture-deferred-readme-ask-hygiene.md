# Ask Hygiene — 2026-05-26 work-problems AFK iter (P262 capture-deferred-readme RISK_BYPASS token)

Surface: AFK `/wr-itil:work-problems` iteration subprocess (P086 retro-on-exit). No interactive user present (ADR-013 Rule 6); `AskUserQuestion` is structurally unavailable mid-loop (P135 / ADR-044).

| Call # | Header | Classification | Citation |
|--------|--------|----------------|----------|
| — | (none) | — | No `AskUserQuestion` calls this iteration — all decisions framework-resolved: resolution-shape verdict delegated to architect (Option B via P265 token allow-list), JTBD + external-comms (risk + voice-tone) gate delegations, risk commit gate, ADR-022 K→V transition semantics, P062/P134 README refresh. Per the manage-problem SKILL contract. |

**Lazy count: 0**
**Direction count: 0**
**Override count: 0**
**Silent-framework count: 0**
**Taste count: 0**
**Correction-followup count: 0**

Notes: The architect-verdict delegation resolved the SKILL-vs-hook contract reconciliation cleanly to a single framework-resolvable option (no new ADR, no genuine user-direction/taste tradeoff per ADR-044) — so it was implemented directly without surfacing a consent gate, correct per the decision-discipline contract. R6 numeric gate (lazy ≥2 across 3 consecutive retros) does NOT fire — trend lazy_first=0 lazy_last=0.
