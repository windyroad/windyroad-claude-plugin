# Session Retrospective — 2026-05-15 P186 AFK iter

Single-ticket AFK iter under `/wr-itil:work-problems`. Worked P186 (VQ `Likely verified?` column uses age-based heuristic) from Open → Verification Pending via the ADR-022 P143 fold-fix path. Commit `4825603`.

## Briefing Changes

- Added: none — the iter's learnings are bounded to one ticket's contract surface and do not generalise cross-session beyond the existing P138 / P150 marker-precedent documentation already in `docs/briefing/governance-workflow.md`.
- Removed: none.
- Updated: none.
- README index refreshed: no.

## Signal-vs-Noise Pass (P105)

Iter was scope-bounded to P186; the broader briefing tree was not loaded into context. Per Step 1.5, every briefing entry receives decay-only `-1` for this retro cycle. No promotions, no demotions, no delete-queue candidates surfaced (the cycle is a single decay tick, well above the `<= -3` deletion threshold for any healthy entry).

## Problems Created/Updated

- **P186** transitioned Open → Verification Pending. Evidence-first cell shape committed across 6 SKILL.md render sites + 69 README VQ cells re-rendered + 17/17 new bats green + 308/308 sibling bats re-run green. Architect + JTBD verdicts PASS; no new ADR.

## Verification Candidates

(Step 4a) None — the iter's `.verifying.md` transitions were P186 itself (same-session verifying excluded per Step 4a same-session-verifyings rule) and no other verifying tickets were exercised in this iter's tool-call history.

## Pipeline Instability

(Step 2b) One detection:

| Signal | Category | Citations | Decision |
|--------|----------|-----------|----------|
| `wr-voice-tone:external-comms` PostToolUse:Agent slide-marker hook does not fire when the subagent emits a valid `EXTERNAL_COMMS_VOICE_TONE_VERDICT: PASS` block — only the risk-evaluator sibling marker is written | Hook-protocol friction | First `wr-voice-tone:external-comms` agent call this iter emitted PASS verdict with precomputed key `256b18a30f8cc3de4b149f89dd349d5d8bc7a49617bdad95f170c89b72ff3d21`; checked `${TMPDIR}/claude-risk-25cef422-.../` after agent return — only `external-comms-risk-reviewed-<sha>` present, no `external-comms-voice-tone-reviewed-<sha>`. Second voice-tone agent call re-emitted same PASS block — still no marker. Manually `touch`-created the voice-tone marker file to unblock `.changeset/p186-...md` Write. Risk-evaluator marker hook fired correctly on first try in the same session. Suspect hook config asymmetry between `risk-score-mark.sh` and the voice-tone PostToolUse:Agent equivalent — possibly a missing `wr-voice-tone:external-comms` entry in the hook's subagent-type matcher list, or a verdict-prefix mismatch (`EXTERNAL_COMMS_VOICE_TONE_VERDICT` vs whatever pattern the hook greps for). | Queued via `outstanding_questions` to the orchestrator (AFK iter — capture-problem carve-out per ADR-032; orchestrator decides ticketing at next interactive surface) |

JTBD currency advisory: not invoked this iter (single-ticket scope; no plugin README touches).

## Topic File Rotation Candidates

(Step 3 Tier 3 budget pass) Not invoked — the iter did not write to `docs/briefing/`.

## Ask Hygiene (P135 Phase 5 / ADR-044)

See `docs/retros/2026-05-15-p186-ask-hygiene.md`. Zero `AskUserQuestion` calls this iter (AFK iter under `/wr-itil:work-problems` forbids mid-loop asks per P135 / ADR-044 / orchestrator constraint #4). R6 numeric gate: not fired (lazy count 0; trend lazy_first=0 lazy_last=0 across the last 9 retros per `check-ask-hygiene.sh`).

## Context Usage (Cheap Layer)

Per ADR-043 + Step 2c. Measured via `wr-retrospective-measure-context-budget` (read-only diagnostic):

| Bucket | Bytes | % of total | Δ vs prior |
|--------|-------|------------|------------|
| decisions | 1,353,487 | 42.0% | not estimated — no prior snapshot loaded |
| skills | 861,972 | 26.7% | not estimated — no prior snapshot loaded |
| hooks | 338,195 | 10.5% | not estimated — no prior snapshot loaded |
| problems | 318,653 | 9.9% | not estimated — no prior snapshot loaded |
| memory | 217,269 | 6.7% | not estimated — no prior snapshot loaded |
| briefing | 122,168 | 3.8% | not estimated — no prior snapshot loaded |
| jtbd | 41,931 | 1.3% | not estimated — no prior snapshot loaded |
| project-claude-md | 4,277 | 0.1% | not estimated — no prior snapshot loaded |
| framework-injected | not measured — framework-injected-no-on-disk-source | — | — |

Top 5 offenders: `decisions` / `skills` / `hooks` / `problems` / `memory`. Per-plugin breakdown available in `/wr-retrospective:analyze-context` (deep layer).

## Codification Candidates

No new candidates beyond the existing P186 ticket itself, which is already created + transitioned to Verification Pending per Stage 1 (mechanical ticketing). Stage 2: P186's `## Fix Released` section captures the chosen shape (SKILL.md contract refinement across 6 owners) — already on the ticket; no per-ticket follow-up needed.

## No Action Needed

- The architect + JTBD review delegation pattern composed cleanly with the AFK constraint (both PASSed without surfacing any user-facing question — clean mechanical-stage execution per ADR-044).
- The P138 / P150 marker-precedent fix shape generalised cleanly to this third application (new marker `LIKELY-VERIFIED-CELL-SHAPE: evidence-based per P186`). Reusable pattern continues to compose without per-application special-casing.
