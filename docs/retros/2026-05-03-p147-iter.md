# Session Retrospective — AFK iter 7, P147 fold-fix SIGTERM conditional caveat

**Date**: 2026-05-03
**Scope**: AFK `/wr-itil:work-problems` iter 7 — single ticket P147 (P121 SIGTERM-clean-flush guarantee conditional on prior ITERATION_SUMMARY emit; needs SKILL.md caveat for stuck-before-emit subclass).
**Outcome**: shipped fold-fix Open → Verifying in single commit per ADR-022 amendment (commit pending at retro time; same iter as this retro file).

## What Was Done

- Architect verdict APPROVE no required amendments — confirmed ADR-026 grounding (quantitative claims `0-byte JSON` and `exit 143` cited against the 2026-04-29 P146 incident in the prose); confirmed conditional-caveat is the right shape per ADR-029 (behavioural-change-to-detect-emission deferred to a sibling ticket per ADR-014 single-commit-per-batch); confirmed doc-lint assertions belong in the existing fixture (co-located with P121 prose-drift guards).
- JTBD verdict ALIGNED on JTBD-006 (Progress the Backlog While I'm Away) — halt-and-investigate shape aligns with persona "stops gracefully when it hits a blocker" outcome and "Decisions that would normally require my input are resolved using safe defaults" constraint; audit-trail outcome preserved via independent `git log` + `git status --porcelain` reconstruction.
- `packages/itil/skills/work-problems/SKILL.md` Step 5 gains a new **"SIGTERM exit-flush is conditional, not universal (P147)"** subsection conditioning the P121 clean-flush claim on prior `ITERATION_SUMMARY` emission, naming the stuck-before-emit subclass observed at P146, and documenting the metadata-loss-event handling shape (verify via `git log` + `git status --porcelain`, halt the AFK loop per exit-code semantics, reconstruct cost from the Anthropic billing dashboard).
- SKILL.md Related entry adds a P147 cross-reference paragraph naming the fix shape + the behavioural second-source extension.
- `docs/briefing/afk-subprocess.md` "P121 SIGTERM-clean-flush guarantee is conditional, not universal" entry gains the **Fixed 2026-05-03 (P147)** closing line cross-referencing the SKILL.md prose amendment + the new bats fixture; cites P146 as the remaining iteration-internal authoring class.
- `packages/itil/skills/work-problems/test/work-problems-step-5-idle-timeout-sigterm.bats` extends with 1 behavioural test (`P147: SIGTERM-before-emit produces 0-byte JSON (stuck-before-emit subclass)` — fake `claude_no_emit` shim that traps SIGTERM and exits 0 WITHOUT writing any stdout; orchestrator-shape harness fires SIGTERM and asserts `JSON_BYTES=0`, mirroring the 2026-04-29 P146 incident shape) plus 3 doc-lint contract assertions per ADR-037 Permitted Exception co-located with the existing P121 prose-drift guards (conditional-caveat phrase / metadata-loss-event handling shape / P147 citation).
- `@windyroad/itil` patch changeset queued: `.changeset/p147-sigterm-conditional-caveat.md`.
- Ticket P147 transitioned Open → Verification Pending (fold-fix per ADR-022 amendment); `## Fix Released` section added with release marker / one-sentence fix summary / exercise evidence / verification path / composes-with notes.
- `docs/problems/README.md`: P147 row removed from WSJF Rankings, inserted at top of Verification Queue; line-3 "Last reviewed" fragment updated to name P147 fold-fix; reconcile-readme.sh confirms zero drift post-edit.
- `docs/problems/README-history.md`: new `## 2026-05-03 (P147 fold-fix Open → Verifying — SIGTERM conditional caveat)` heading + body containing the displaced P145 fragment per P134 rotation contract.

## Test Evidence (TDD discipline preserved)

- RED → GREEN cycle confirmed: 3 of 4 new doc-lint contract assertions failed BEFORE the SKILL.md prose amendment (test 12 conditional-caveat phrase, test 13 metadata-loss-event handling shape, test 14 P147 citation); the behavioural stuck-before-emit fake-shim test 11 (`JSON_BYTES=0` after SIGTERM-before-emit) was self-contained against the existing harness and green from authoring (correct — the fixture exercises the failing operational shape, not the doc-lint contract).
- All 14 of 14 tests pass AFTER the SKILL.md edit.
- One test-design iteration: the initial `test 12` regex matched coincidental existing prose (the P118 "the JSON file stays 0 bytes" sentence under `JSON.{0,40}0.?byte`); tightened to require the new caveat's distinctive language (`stuck.?before.?emit`, `before ITERATION_SUMMARY`, conditional-on phrasing). Captured in retro as a behavioural-test design lesson — `grep` regexes that are too broad pass on coincidental matches.

## Reflection

### What was harder than it should have been

- (Same recurring observation as iter 6) **`/wr-itil:transition-problem` cached version 0.23.1 rejects direct Open → Verifying transition** — the skill's destination-reachability validation table only allows one-step-at-a-time per the SKILL.md (Open → Known Error, Known Error → Verifying, Verifying → Closed). But ADR-022 was amended to allow fold-fix Open → Verifying when the Open ticket already documents root cause + fix strategy + workaround inline. Workaround: do the fold-fix inline (rename + Status edit + Fix Released section + README + history rotation) without invoking the skill — same mechanic as the in-skill manage-problem Step 7 block per ADR-010 amended "copy, not move". Recurring across iters 5, 6, 7 — likely deserves a dedicated problem ticket in a future interactive session (NOT created mid-AFK-loop per the constraint, and not codify-worthy as a Stage 1 ticket here because the skill source already exists; the gap is plugin-cache lag vs the ADR-022 amendment that landed at P140).

### What surprised me (mildly)

- (None — iter shape was textbook fold-fix; architect + JTBD verdicts both PASS first-pass; TDD cycle clean; no friction.)

## Codification Candidates

(None this iter. Single-ticket fold-fix; no recurring-pattern observations or improvement-axis observations surfaced.)

## Verification Candidates (Step 4a)

(None this iter. P147 itself just transitioned to `.verifying.md` this session — same-session verifyings are excluded per Step 4a step 8 contract.)

## Pipeline Instability (Step 2b)

(None this iter. No hook TTL expiries, no marker-vs-file deadlocks, no release-path instability, no subagent-delegation friction, no repeat-workaround patterns observed. The recurring "transition-problem rejects Open → Verifying for fold-fix" friction noted in the Reflection section is a known declarative-vs-cache lag from prior iters, not a new instability signal — does not meet the "specific citation + new this iter" bar for Step 2b.)

## Topic File Rotation Candidates (Step 3 P099)

(Not run in this AFK iter — orchestrator constraint excludes mid-loop topic-file rotation; Tier 3 budget pass picked up by the user-interactive `/wr-retrospective:run-retro` invocation that follows AFK loop end.)

## Ask Hygiene (Step 2d / P135 Phase 5 / ADR-044)

| Call # | Header | Classification | Citation |
|--------|--------|----------------|----------|
| (none) | — | — | — |

**Lazy count: 0**
**Direction count: 0**
**Override count: 0**
**Silent-framework count: 0**
**Taste count: 0**
**Correction-followup count: 0**

The AFK orchestrator constraint forbids mid-loop `AskUserQuestion` per ADR-013 Rule 6 / ADR-044 framework-mediated surface; the iter contained zero AskUserQuestion calls.

## Tickets Deferred (Step 4b Stage 1)

(None this iter. No observations were deferred to the retro-summary table; the recurring transition-problem friction noted in Reflection is captured here as cross-session reflection text only, not as a deferred Stage 1 ticket — it is observation-of-known-state, not observation-of-new-state.)

## No Action Needed

- Iter shape was textbook fold-fix per ADR-022; gates all PASSed first-pass; TDD discipline preserved; README + history rotation per P134 contract; `@windyroad/itil` patch changeset queued; P147 transitioned to verifying.
