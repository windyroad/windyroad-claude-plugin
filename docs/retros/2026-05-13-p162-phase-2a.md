# Session Retrospective — 2026-05-13 P162 Phase 2a graduation evaluator (AFK iter)

AFK `/wr-itil:work-problems` iteration 1. Subprocess via `claude -p`. Per orchestrator constraint: no `AskUserQuestion`, no `capture-*` skills, no `ScheduleWakeup`, no push/release.

Iteration landed Phase 2a of P162 (dogfood-graduation evaluator): commit `afddda0`.

## Briefing Changes

No briefing changes this iter. The friction surfaces this iter encountered (external-comms gate marker KEY shape) and the patterns this iter applied (script + shim + bats triplet per ADR-049 + ADR-052) are already documented in the briefing tree (`agent-hook-gate-quirks.md`, `hooks-and-gates.md`).

## Signal-vs-Noise Pass (P105)

AFK iter — silent classification only. No briefing entries cited, paraphrased, or acted on directly. Per the decay-only rule (-1 per cycle), every entry decays by -1 this cycle. No entries cross the +/-3 thresholds based on this single iter's signal; cumulative trend handled across the broader retro cycle when interactive run-retro fires next.

No delete queue entries surfaced. No promotion candidates surfaced.

## Problems Created/Updated

- **P162** updated — Phase 2a Investigation Tasks marked done; Phase 2b deferral rationale documented (atomic-cohort Class 3b requires RFC cohort enumeration — distinct join surface); Change Log entry appended.

## Tickets Deferred

None — all friction surfaces this iter were handled inline OR surfaced in Pipeline Instability below.

## Verification Candidates

None this iter — orchestrator iter did not exercise any `.verifying.md` fix beyond the gates the iter itself ran through (architect / JTBD / risk-scorer-pipeline / risk-scorer-external-comms). Those gates' surfaces (P064 external-comms, P085 prose-ask, P159 JTBD-currency) are all `.verifying.md` but exercising them in-iter is not a sufficient signal per Step 4a "same-session verifyings excluded" — the iter-bounded exercise does not constitute multi-session dogfood evidence.

## Pipeline Instability

| Signal | Category | Citations | Decision |
|--------|----------|-----------|----------|
| `wr-risk-scorer:external-comms` agent emits a **placeholder SHA** in `EXTERNAL_COMMS_RISK_KEY` rather than computing the canonical key from `printf '%s\n%s' "$DRAFT" "$SURFACE" \| shasum -a 256`. First-pass review wrote a marker file at the placeholder key; gate re-fired because the hook computes its own KEY from DRAFT+SURFACE and the markers did not match. Required a second subagent invocation with the user-side-computed canonical KEY plus explicit `emit this exact KEY` instructions. | Subagent-delegation friction | `Agent(wr-risk-scorer:external-comms)` invocation 1 at retro turn N returned `EXTERNAL_COMMS_RISK_KEY: 8e0a3c5b...` (placeholder); Write `.changeset/wr-risk-scorer-p162-phase-2a-graduation-evaluator.md` denied by gate; observed marker `/var/folders/.../claude-risk-<sid>/external-comms-reviewed-8e0a3c5b...` written at non-matching key. `Agent(wr-risk-scorer:external-comms)` invocation 2 with the computed canonical KEY emitted matching key and gate cleared on retry. Fix path: the external-comms agent prompt at `packages/risk-scorer/agents/external-comms.md` should explicitly instruct compute-the-KEY-via-shasum and emit the canonical hex. | new ticket via `/wr-itil:manage-problem` (deferred — user/orchestrator picks up on next Step 1 scan per AFK fallback) |

**JTBD currency advisory: clean** (12 packages with JTBD anchors, 0 drift instances).

## Topic File Rotation Candidates

Tier 3 budget pass surfaced significant overflow. Per Step 3's MUST_SPLIT branch, three files require action this cycle (ratio ≥ 2× ceiling). AFK iter — per ADR-013 Rule 6, rotation is silent agent-picked but the scope is genuinely large; surfacing the full list for the next interactive retro to apply since landing 3 split-by-date or split-by-subtopic rotations in this AFK iter would balloon scope well beyond M and risk truncating the orchestrator's next iter dispatch budget.

| Topic file | Bytes | Threshold | Proposed rotation | Decision |
|------------|-------|-----------|-------------------|----------|
| `docs/briefing/governance-workflow.md` | 11623 | 5120 (2.27×) | split-by-date (archive oldest entries to `governance-workflow-archive.md` — note the archive already exists at 5274; route oldest entries to new `governance-workflow-archive-2026q1.md`) | flagged (non-interactive — full rotation requires foreground; orchestrator picks up on next interactive retro) |
| `docs/briefing/hooks-and-gates.md` | 19515 | 5120 (3.81×) | split-by-subtopic (extract a coherent sub-topic; gate-marker-mechanics + TTL discipline as one candidate split target) | flagged (non-interactive) |
| `docs/briefing/releases-and-ci.md` | 15522 | 5120 (3.03×) | split-by-subtopic (extract a coherent sub-topic; npm-publish-discipline + push-watch-mechanics as candidates) | flagged (non-interactive) |
| `docs/briefing/afk-subprocess-mechanics.md` | 9093 | 5120 (1.78×) | trim-noise / split-by-subtopic (under MUST_SPLIT ratio; defer permitted) | deferred (Branch B) |
| `docs/briefing/afk-subprocess-recovery.md` | 9397 | 5120 (1.84×) | split-by-subtopic (defer permitted) | deferred (Branch B) |
| `docs/briefing/agent-hook-gate-quirks.md` | 9434 | 5120 (1.84×) | trim-noise / split-by-subtopic (defer permitted) | deferred (Branch B) |
| `docs/briefing/agent-interaction-patterns.md` | 6684 | 5120 (1.31×) | leave-as-is | deferred (Branch B) |
| `docs/briefing/governance-workflow-archive.md` | 5274 | 5120 (1.03×) | leave-as-is | deferred (Branch B) |
| `docs/briefing/governance-workflow-surprises.md` | 8269 | 5120 (1.62×) | trim-noise (defer permitted) | deferred (Branch B) |
| `docs/briefing/plugin-distribution.md` | 8975 | 5120 (1.75×) | trim-noise / split-by-subtopic (defer permitted) | deferred (Branch B) |

**MUST_SPLIT files (≥ 2×)**: 3 (governance-workflow.md, hooks-and-gates.md, releases-and-ci.md). These are the load-bearing rotation candidates the next interactive retro must address per P145 anti-recurring-defer rule.

## Ask Hygiene (P135 Phase 5 / ADR-044)

See `docs/retros/2026-05-13-p162-phase-2a-ask-hygiene.md`.

**Lazy count: 0** (AFK iter — orchestrator constraint prohibits `AskUserQuestion`).

**Trend** (per `packages/retrospective/scripts/check-ask-hygiene.sh`): `lazy_first=0 lazy_last=0 delta=+0` across the trailing window. R6 numeric gate (lazy ≥ 2 across 3 consecutive retros) NOT firing. No deviation-candidate auto-queued.

## Codification Candidates

| Kind | Shape | Suggested name / Target file | Scope / Flaw | Triggers / Evidence | Decision |
|------|-------|-----------------------------|--------------|----------------------|----------|
| improve | agent | `packages/risk-scorer/agents/external-comms.md` | Agent emits a placeholder `EXTERNAL_COMMS_RISK_KEY` instead of computing the canonical SHA from `printf '%s\n%s' "$DRAFT" "$SURFACE" \| shasum -a 256`. First-pass review writes a marker at the placeholder key; gate re-fires because the hook computes the canonical key and finds no match. Costs ~1 extra subagent invocation + bash compute round-trip per changeset author surface. | First-pass review at iter turn N emitted `EXTERNAL_COMMS_RISK_KEY: 8e0a3c5b...` (placeholder); gate denied subsequent Write; second-pass review with user-computed canonical KEY succeeded. | flagged (non-interactive — recorded in Pipeline Instability above; orchestrator/user picks up on next interactive retro to file as `/wr-itil:manage-problem` ticket per Stage 1 mechanical ticketing; Stage 2 fix-strategy is Option 2 `Skill — improvement stub` on the agent prompt) |

## Context Usage (Cheap Layer)

| Bucket | Bytes | % of total | Δ vs prior |
|--------|-------|------------|-----------|
| decisions | 1297688 | 42.5% | no prior snapshot — first measurement this iter |
| skills | 806639 | 26.4% | no prior snapshot |
| hooks | 315525 | 10.3% | no prior snapshot |
| problems | 298481 | 9.8% | no prior snapshot |
| memory | 208469 | 6.8% | no prior snapshot |
| briefing | 114004 | 3.7% | no prior snapshot |
| jtbd | 40866 | 1.3% | no prior snapshot |
| project-claude-md | 4277 | 0.1% | no prior snapshot |
| framework-injected | not measured — framework-injected-no-on-disk-source | — | — |

**Top-5 offenders**: decisions (1.3 MB; measurement-method: `wc -c` over `docs/decisions/*.md` per script contract), skills (807 KB), hooks (316 KB), problems (298 KB), memory (208 KB).

Per-plugin breakdown available in `/wr-retrospective:analyze-context` (deep layer). No deep-analysis advisory triggered this iter (no prior snapshot to compute +20% delta against).

## No Action Needed

- Architect + JTBD gates approved the Phase 2a plan on first pass with one architect advisory (document Phase 2a/2b split explicitly in P162) — already applied.
- Bats coverage green on first run (19/19 tests pass).
- Real-project state verification clean (P165 held changeset correctly detected as vp-blocked).
- Commit gate passed at 4/4/4 (Low across all three layers); within appetite; no remediations required.
- No tooling regressions observed beyond the documented external-comms-KEY-shape friction above.
